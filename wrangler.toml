# Cloudflare Pages — مشروع نشر ثابت مع Pages Functions في functions/
# ملاحظة: لا نستخدم كتلة [site] (خاصة بـ Workers Sites) لأنها قد تستبعد مجلد
# functions/ من النشر. أمر النشر: wrangler pages deploy . يرفع كامل المجلد
# ويكتشف functions/ تلقائياً.
name = "aldeyabi-procurement"
compatibility_date = "2024-01-01"
