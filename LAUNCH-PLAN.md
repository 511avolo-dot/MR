# خطة جاهزية الإطلاق — بوابة الطلبات والموافقات (نظام 3)

> **مرجع دائم مجزّأ.** تُنفَّذ على مراحل بسبب حدّ الاستخدام. بعد كل مرحلة: تحديث المربّعات + دفع.
> نُكمل بالترتيب من أول مربّع غير مُنجز. لا نُغفل شيئاً. آخر تحديث: 2026-07-08.

## الحالة الحالية
- المراحل A–F + هجرة 012 + توحيد البريد: **منشورة على الإنتاج ومُختبَرة** (regress صفر إخفاقات).
- الرابط: https://aldeyabi-procurement.pages.dev/portal
- هذه الخطة = تصليب نهائي لعرض على الإدارة العليا والبدء الفعلي.

---

## المرحلة 1 — الفجوات الوظيفية المُبلَّغة (الأعلى أولوية) [debug]
- [x] **1a — تنبيه الدعوات الأحمر:** ✅ **مُشخَّص (إعداد لا كود).** كل نقاط portal-* الخادمية ترجع
      `{ok:false}` لأن **`PORTAL_SUPABASE_URL`/`PORTAL_SUPABASE_SERVICE_ROLE_KEY` مفقودان في بيئة
      Cloudflare Pages Production.** يفسّر: تنبيه الدعوات + توقّف إشعارات البريد + فشل إدارة المستخدمين خادمياً.
      **الإصلاح (المالك):** ضبط المتغيّرين (service_role = Secret) + Retry deployment. لا كود.
- [x] **1b — تخصيص اللجنة المصغّرة:** ✅ هجرة **013** `portal_set_committee` (أدمن فقط، تصفية
      لغير النشط/المكرر، تدقيق) + لوحة «اللجنة المصغّرة» أعلى تبويب مصفوفة الصلاحيات (اختيار
      الأعضاء بمربّعات + حفظ) + `pa_canApprovePo` يعترف بعضو القائمة + تحميل اللجنة مع كل loadAll.
      مُختبَرة: غير الأدمن ممنوع · التصفية تعمل · العضو عبر القائمة مخوَّل.
- [x] **1c — توافق مفاتيح الصلاحيات:** ✅ مربّعات PERM_DEFS كانت تعرض مفاتيح قديمة ولا تحفظ شيئاً.
      الآن: `togglePerm` يحفظ فعلياً عبر `/api/portal-users setProfile` مع ترجمة
      (l1→can_approve_stage · l2→can_manage_users · rfq→can_manage_procurement) + مرايا عرض
      للمفاتيح القديمة تُحدَّث مع كل loadAll وتُنظَّف قبل الحفظ (لا تلوّث القاعدة).

## المرحلة 2 — إصلاح الطباعة: صفحة A4 واحدة لكل مستند
- [x] **2a** ✅ السبب مقيس بـChromium PDF: baseline = طلب 2ص · مقارنة 2ص · أمر شراء 2ص.
      ثلاثة أسباب: أحجام المرجع تفيض عن A4 + جداول عريضة (min-width) تُرقّم أفقياً +
      margin-top على محضر المقارنة يفتح صفحة ثانية مع page-break-inside:avoid.
- [x] **2b** ✅ رقعة `pa-print-compact` تُحقن كـ<style> من المُحوِّل (منطقة التصميم لم تُمسّ،
      عرض الشاشة لم يتغيّر): هوامش A4 مضغوطة + خطوط/حشوات مصغّرة + جداول width:100%/fixed +
      margin:0 للمستندات + بنود/تواقيع/سجل اعتمادات مضغوطة.
- [x] **2c** ✅ حزام قياس دائم `print_measure.mjs` (vm يولّد المستندات الخمسة الحقيقية →
      Chromium PDF → عدّ الصفحات): **الخمسة كلها صفحة واحدة، حتى بـ12 بنداً.** لقطة بصرية مؤكَّدة.

## المرحلة 3 — فحص شامل لكل سيناريوهات الطلبات [workflows/agents]
> **تقدّم:** workflow `wf_c49c30ca-811` غطّى نطاقَي creation (10/12 full) + approval (11/14 full)
> ثم أوقفه حدّ الجلسة. **بقي:** procurement + payment + receipt_admin (أعد التشغيل بنفس الأبعاد).
> فجوات مؤكَّدة عولجت في م4 أدناه. فجوتا creation (مسودة دائمة = فرق مقصود عن المرجع؛
> إخفاء «رفع طلب» = أُصلح). فجوة بريد defer/resume/cancel بلا إشعار = متبقّية (منخفضة).
- [ ] **3a** حصر كل السيناريوهات (مصفوفة كاملة): المسار السعيد + كل تفرّع + الحالات الحدّية +
      حالات الخطأ، لكل مرحلة: إنشاء (عادي/وحيد المصدر/طارئ/تجزئة) · اعتماد/رفض/إرجاع مرن (لأي
      مرحلة سابقة) · تسعير (عرض واحد/عدّة) · تعميد + اعتماده · أمر شراء بكل شرائح DoA الخمس
      (لجنة/مالية/عام/مناقصة) · صرف (بنكي/عهدة/آجل) + اعتماد/رفض/إرجاع + إعادة إصدار · استلام
      (كامل/جزئي/متعدّد) · إلغاء · تأجيل مالي on_hold + استئناف · إعادة الطلب · التفويض عند الغياب ·
      تصعيد تعارض SoD · نطاق الرؤية (own/sector/all + المعتمِد).
- [ ] **3b** لكل سيناريو: تأكيد وجود الحلّ (RPC + UI + guard) وعمله. توثيق الناقص.

## المرحلة 4 — تنفيذ ما ينقص من السيناريوهات + إصلاحات
- [x] **setDelegate يحفظ فعلياً** (كان توضيحياً بالذاكرة — المفوَّض يضيع): تغليف في المُحوِّل يكتب delegate_to.
- [x] **إشعار الإرجاع المرن**: كان يُرسل returned فيُسقطه الخادم (الحالة in_review) — الآن stage عند seq>0.
- [x] **إخفاء «رفع طلب»** عمّن لا يملك can_create (تغليف navAllowed).
- [x] **هجرة 014**: تصعيد SLA لحاملي الدور + portal_sla_tick (نبضة كسولة، بلا pg_cron) — مُختبَرة.
- [x] **هجرة 015 (ثغرة مكتشفة)**: رؤية المعتمِد المنتظَر (مدير قسم own/مفوَّض/حامل دور/لجنة) — مُختبَرة
      + مثبَّتة في الحزمة 15 (خطوتا 9/10) وregress كله صفر إخفاقات.
- [ ] إشعار بريد عند defer/resume/cancel (منخفض — اختياري).
- [ ] بناء أي ناقص تكشفه بقية المرحلة 3 (بعد إعادة تشغيل الأبعاد الثلاثة).

## المرحلة 3+4 — فحص السيناريوهات المتبقية (تسعير/صرف/استلام-إدارة) — منجز
> **3 وكلاء عدائيين** دقّقوا ~38 سيناريو. **الفجوات المؤكَّدة وعلاجها:**
- [x] **[HIGH صحّة] تعميد مرفوض يعلّق الواجهة على طور 'award':** `pa_procPhase`/mapping كان يشتقّ
      طور 'award' من صف award مرفوض فتنكسر إعادة التسعير/التعميد (سيناريوهات رفض التعميد/أمر الشراء).
      الإصلاح: `activeAward` (يتجاهل status='rejected') — الواجهة تعود لطور comparison/pricing.
- [x] **[HIGH صحّة] إعادة إصدار الصرف بعد الرفض/الإرجاع غير قابلة للوصول:** `pa_procPhase` كان يعلّق
      على طور 'payment' من صف صرف مرفوض/مُعاد فلا يظهر زر «إصدار صرف». الإصلاح: `activePayments`
      (يتجاهل rejected/returned) — يعود لطور 'po' ويظهر الزر لإعادة الإصدار.
- [x] **[HIGH أمن] هجرة 016 — فصل مهام ثلاثي في الصرف:** كان الطالب يستطيع تنفيذ صرفه إن اعتمده غيره.
      الآن: `requested_by = v_me` ممنوع في disburse (الطالب ≠ المعتمِد ≠ المنفّذ). اختبار 01 مُحدَّث (أمين صندوق مستقل).
- [x] **[MEDIUM] توجيه مهمة الاستلام:** `myTasks` يُضيف «استلام منتظر» لحامل `can_verify_stock` (كان للمقدّم).
- [x] **[MEDIUM] إشعارات الصرف:** approve/reject/return يُخطران المشتريات (portal-notify + pa_notify).
- [ ] **[منخفض مؤجّل موثّق]** فرض عدد العروض حسب DoA خادمياً (UI يحذّر) · تفصيل الضريبة في سند الصرف ·
      setDoa award/po لا يُحفظ (شبه مقصود — الحوكمة ثابتة) · toggleSuspend يتجاوز ban_duration
      (الحجب الفعّال قائم عبر active في portal_username). كلها لا تمسّ الأمان/الحوكمة.

## المرحلة 5 — تحقّق E2E عدائي لكل سيناريو (رحلات كاملة بلا أخطاء)
- [ ] تشغيل كل سيناريو عبر الرحلة الكاملة حتى الإغلاق/الأرشفة، تأكيد لا أخطاء.

## المرحلة 6 — مراجعة نهائية [security-review] [code-review]
- [ ] security-review على كل التغييرات الجديدة.
- [ ] code-review على كل التغييرات الجديدة.
- [ ] تلميع UI + إصلاح ملاحظات المراجعة.

## المرحلة 7 — تقرير جاهزية الإطلاق للإدارة العليا [artifact-design] [dataviz]
- [ ] صفحة عرض احترافية (Artifact): ملخّص القدرات · مصفوفة السيناريوهات المُغطّاة · حالة الأمان ·
      رسوم بيانية للتغطية · توصية الإطلاق.

---
## مفاتيح الترجمة (مرجع سريع)
approveReq→can_approve_stage · approveAward→can_approve_award · issuePO→can_issue_po ·
manageRfq→can_manage_procurement · disburse→can_disburse · create→can_create · edit→can_edit ·
manageUsers→can_manage_users · see.finance→can_see_finance · warehouse→can_verify_stock ·
committee→can_approve_committee · GM→can_manage_users.

## مهارات مُطبَّقة (حسب طلب المالك) ولماذا
- **debug** (م1) · **workflows/agents** (م3 حصر السيناريوهات متعدّد الوكلاء) ·
  **security-review + code-review** (م6) · **artifact-design + dataviz** (م7 تقرير الإدارة).
- **غير منطبقة** (تُذكر للشفافية): design-consent/design-revoke (لا مشاركة تصميم خارجي) ·
  team-onboarding/session-start-hook (لا إعداد فريق/هوك جلسة مطلوب) — لا تُطبَّق قسراً.
