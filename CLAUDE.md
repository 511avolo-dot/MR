# ذاكرة المشروع — مجموعة الذيابي (Al-Deyabi Group)

> هذا الملف هو المرجع الدائم. **اقرأه أولاً في كل جلسة.** يمنع الخلط بين الأنظمة الثلاثة
> المنفصلة ويحفظ حالة العمل والقرارات. حدّثه كلما تغيّر شيء جوهري.

---

## ⚠️ القاعدة الذهبية: ثلاثة أنظمة منفصلة — لا تخلط بينها أبداً

| # | النظام | الواجهة | قاعدة البيانات | مشروع Supabase |
|---|--------|---------|----------------|----------------|
| 1 | **بوابة الموردين** (تسجيل الموردين) | `register.html` | `proc_supplier_registrations` (+ proc_*) | القديم `yofcaxvstjcrmbgciwym` |
| 2 | **نظام المشتريات** (النظام الرئيسي) | `index.html` (+ `requests.html` + `rfq.html`) | جداول `proc_*` / `pr_*` | القديم `yofcaxvstjcrmbgciwym` |
| 3 | **بوابة الطلبات والموافقات** (الجديدة، معزولة) | `purchase-portal.html` | جداول `portal_*` فقط | **الجديد `mwbjoysuybgbrvfrprex`** |

- النظامان 1 و2 يتشاركان **نفس مشروع Supabase القديم** وجداول `proc_*`/`pr_*`.
- النظام 3 **معزول فيزيائياً بالكامل**: مشروع Supabase مستقل، جداول `portal_*` فقط، لا يمسّ أي `proc_*`.
- **`requests.html` (نظام 2) ≠ `purchase-portal.html` (نظام 3).** كلاهما «طلبات شراء» لكنهما نظامان مختلفان تماماً. لا تخلط بينهما.

---

## 📜 تكليف المالك الحاكم (2026-08-07) — يُطبَّق على **كل** طلب لاحق
> أمر صريح: «احفظ هذه وتأكد أننا نطبقها مع باقي الطلبات». التفاصيل الكاملة وسجلّ التنفيذ في
> **`audit-output/OWNER_UX_PERMISSIONS_MANDATE_2026-08-07.md`** — اقرأه قبل أي عمل على البوابة.
1. **الوظائف والصلاحيات:** تغطية كاملة (لا وظيفة/مفتاح ناقص) · صلاحيات المستخدم **قابلة للتعديل** بقرار مدير البوابة.
2. **حجب المالية عن الطالبين:** التكاليف/العروض للمالية والمشتريات فقط، وتُمنَح **كوحدة (module)** لمستخدم بقرار المدير.
3. **تسليم دورة الشراء → دورة الصرف** يعمل صحيحاً، وسير موافقات الصرف (مباشر/عبر الشراء) **واضح وبائن**.
4. **الإرشاد لا الاستكشاف:** كل مستخدم يعرف فوراً **ما المتوقّف عليه وماذا يفعل**؛ النظام يقوده حتى الإتمام،
   بوضوح أماكن النقر والأفعال.
5. **تطوير الواجهات:** عرض سلسلة الموافقات/سير العمل أفضل بكثير · **لوحة الإعدادات** أوضح · **مصمّم سير العمل**
   يُطوَّر جوهريّاً (الإسناد والتصميم غير واضحين الآن) · تغطية الواجهات لكامل النظام.
6. **المستندات:** معاينة **ذكيّة بمستوى أنظمة إدارة المستندات (DMS)** بدل المعاينة الضعيفة + فحص/تحسين الطباعة.
7. **المستوى المطلوب:** عمل خبير UI/UX ونظم صلاحيات — مع **قابلية الضبط من الإعدادات** لكل سلوك حوكميّ،
   ودون إضعاف الحوكمة (فصل مهام · رفض افتراضي · بوّابات أدلّة · تدقيق).

---

## نظام 2 — القرار التشغيلي الحالي لموديل طلبات الشراء (2026-09-13)

هذا القرار **يلغي ويستبدل** قرار «متابعة فقط بلا موافقات إلكترونية» المؤرخ
2026-09-10 في هذا الموديل وحده. الموظفون المعنيون موظفو الصيانة والتشغيل في
المكتب مع فريق المشتريات، وليسوا موظفين ميدانيين.

- ينشئ الموظف الطلب داخل النظام؛ لا يلزم PDF موقع ليحل محل الاعتماد.
- البوابة الأولى: مدير الصيانة والتشغيل يعتمد الحاجة أو يعيدها أو يرفضها.
- البوابة الثانية: مدير المشتريات يسمح ببدء التسعير أو يعيد الطلب أو يرفضه.
- الطلب قد ينتج عدة أوامر شراء، وأمر الشراء قد يجمع عدة طلبات. التغطية تحفظ على
  مستوى بند الطلب والكمية، ولا يكفي رقم أمر واحد في رأس الطلب.
- كل مناقشة وقرار ومرفق وربط يبقى ضمن ملف الطلب وسجل التدقيق.
- الأسعار والمبالغ بصلاحية مستقلة، وتظهر مقنّعة عند غيابها.
- المشروع يختار من `proc_settings.projects_registry` ولا يكتب نصاً حراً.
- الحسابات بدعوة شخصية عبر Resend؛ الموظف يضبط كلمة المرور بنفسه. الدعوة تحمل
  الإدارة وملف الصلاحيات داخل رمز موقّع.
- التنفيذ المرجعي: `db/system2-purchase-request-workspace.sql`، والواجهة
  `requests.html`. هذا يخص النظام 2 (`proc_*`) ولا يمس النظام 3 (`portal_*`).

---

## النظام 3 — بوابة الطلبات والموافقات (عملنا الحالي، معزول تماماً)

**المبدأ الحاكم (توجيه المالك الصريح):** معزولة 100% عن `index.html` وكل `proc_*`.
لا ربط، لا اختلاط، لا تعديل على أي كائن قائم. «اشتغل على البوابة فقط.»

### الملفات (كلها جديدة — لم يُعدَّل أي ملف قائم)
- `db/portal-standalone.sql` — المخطّط الكامل: 17 جدول `portal_*`، دوال RPC ذرّية
  (SECURITY DEFINER)، محارس **رفض افتراضي (deny-by-default)**، RLS، تدقيق append-only،
  رموز بريد لمرة واحدة، بذور DoA، وكتلة تراجع (rollback) في نهايته.
- `purchase-portal.html` — الواجهة المستقلة. **مُنذ 2026-07-05: نسخة حرفية من النموذج المرجعي
  للمالك (`uploads/f22dd3af…123123123.html` = 2559 سطراً) — كل CSS/الشاشات/المستندات/الطباعة
  بايت-ببايت — + «مُحوِّل» مُلحَق يستبدل مقبس البيانات فقط:** عميل Supabase، شاشة دخول (Auth حقيقي
  بدل مبدّل المستخدم setMe)، `loadAll()` يجلب كل جداول portal_* ويحوّلها إلى أشكال كائنات المرجع
  (`chain[]`/`proc{offers,awardChain,po,payment,grn}`/`events[]`، camelCase، تواريخ ms)، ودوال
  الأفعال مربوطة بالـRPC. **قاعدة: أي تعديل واجهة = في المُحوِّل فقط (السطور 2556+)، مناطق التصميم
  1–2555 تبقى مطابقة للمرجع (تحقّق diff).** فروق مقصودة موثّقة: (أ) `portal_offers` يخزّن إجمالي
  عرض المورد فقط → أسعار خلايا المقارنة موزّعة تناسبياً (الإجماليات/الفائز/التواقيع دقيقة)؛
  (ب) نسخ/استيراد/تصفير localStorage معطّلة بأمان. إصلاحات ما بعد المعاينة: خروج في بطاقة المستخدم،
  التفاف أزرار الصرف، عرض كل أوراق المعاملة للطلب المُقفل (مقارنة+PO+صرف+استلام) + طباعة الملف الكامل.
- `functions/api/_portal-shared.js` — وحدة بريد مشتركة للبوابة (قوالب + رموز اعتماد).
- `functions/api/portal-notify.js` — إشعارات كل حدث (POST بأنواع kind: request/award/payment/receipt).
- `functions/api/portal-action.js` — الاعتماد بضغطة واحدة من البريد (portal_pr_transition_email).
- `functions/api/portal-users.js` — إدارة مستخدمي البوابة (admin فقط).

### متغيّرات البيئة (Cloudflare Pages) الخاصة بالبوابة
البوابة تقرأ **متغيّرات مستقلة** كي لا تلمس المشروع القديم أبداً:
- `PORTAL_SUPABASE_URL` = `https://mwbjoysuybgbrvfrprex.supabase.co`
- `PORTAL_SUPABASE_SERVICE_ROLE_KEY` = *(سرّي — المالك يضعه في Cloudflare، لا يُكتب هنا أبداً)*
- `RESEND_API_KEY` = *(مشترك للبريد — موجود مسبقاً)*
- الواجهة تستخدم anon key العام (مضمّن في `purchase-portal.html`، آمن).

### دورة الحياة (آلة الحالة)
`draft → in_review` (الدورة 1: سلسلة موافقات الحاجة) `→ pricing` (إدخال عروض)
`→ award_review` (الدورة 2: اعتماد التعميد حسب DoA بالقيمة) `→ awarded/payment`
`→ payment_pending` (طلب صرف) `→ receipt_pending` (بعد الصرف) `→ closed` (اكتمال الاستلام).

### قواعد الأمان المثبَّتة (لا تُضعِفها)
- كل انتقال حالة عبر RPC ذرّية ترفع علم `app.portal_transition` ثم تُصفّره.
- المحارس **رفض افتراضي**: أي كتابة مباشرة من العميل مرفوضة إلا عبر RPC/خادم/أدمن.
- جداول الأدلّة المقفلة: `portal_offers`, `portal_request_items`, `portal_receipts` (حارس `portal_locked_guard`).
- فصل المهام (SoD): الطالب ≠ المعتمِد؛ من يرسي التعميد ≠ من يعتمده؛ طالب الصرف ≠ معتمِده ≠ منفّذه؛ ومعتمِد مرحلة سابقة لا يعتمد لاحقة.
- `portal_email_tokens` بلا سياسة RLS (خادم فقط). `portal_audit` غير قابل للتعديل حتى بـ service_role.
- `portal_notifications` مقيّدة بالمستلِم. الهوية تُشتقّ من `session_user`/JWT لا `current_user` (خطأ SECURITY DEFINER معروف).

### الاختبار
بيئة PostgreSQL محلية تحاكي Supabase (`authenticator` + `SET ROLE` + `service_role BYPASSRLS`).
سكربتات الاختبار في مجلد scratchpad: `00_supabase_stub.sql`, `01_seed_and_test.sql` (E2E: 11 اختبار سلبي + دورة كاملة),
`02b.sql` (محارس), `03_rls_positive_path.sql`, `04_create_request.sql`, `06_reaward.sql`, `07_locked.sql`,
`08_jobs_model.sql` (المرحلة 1: بذور/وظائف/توجيه قطاعات — **يجب تشغيله كـauthenticator لا postgres**).
**آخر تشغيل: كل الاختبارات ناجحة.**

### مشروع «نقل التصميم المرجعي» (6 مراحل — الخطة في /root/.claude/plans/unified-cuddling-widget.md)
المرجع الأعلى: «الملف الشامل المتكامل» (uploads) — 18 وظيفة، 22 سيناريو، نظام تصميم بارد جديد.
- [x] **المرحلة 1:** بذور (4 أقسام OPS/CON/GA/LOG + 18 وظيفة + 4 سلاسل قطاعات بالأدوار) +
  RPCs الوظائف (portal_apply_job/save_job/delete_job بحمايات: آخر أدمن، gm، حاملون) +
  DoA الشريحتان الدنيتان → can_approve_award + فرض can_create + واجهة إنشاء موظف بوظيفة
  + نطاق رؤية own/sector/all. Migration: `db/portal-migrations/001-seeds-jobs-model.sql`.
- [x] **المرحلة 2:** نموذج الرفع (project/need_by/proc_type/justification + حدود MAXQ/MAXP
  + اشتقاق القسم + quotes حسب DoA). Migration: `db/portal-migrations/002-request-fields.sql`.
  اختبارها: `09_request_fields.sql` (7 سلبيات). portal_create_request توقيع 9 معاملات (حُذف القديم).
- [x] **المرحلة 3:** المستندات الأربعة (طلب/أمر شراء/سند صرف/استلام) + tafqit + طباعة —
  **واجهة فقط** (CSS المستندات موجود مسبقاً). دوال docReqHTML/docPoHTML/docPayHTML/docGrnHTML/
  documentsHTML/printEl/printDossier. تواقيع من المعتمِدين الفعليين.
- [x] **المرحلة 4:** on_hold (defer/resume — مستثنى من SLA) + منع التجزئة (split_flag) +
  تصعيد تعارض SoD (portal_qualified_approver) + سلامة الصرف (آيبان SA+22 + details jsonb) +
  مفاتيح SOD_RULES في portal_settings. Migration: `db/portal-migrations/003-governance.sql`.
  اختبارها: `10_governance.sql`. توقيعات جديدة: portal_pr_transition(+p_hold_until)،
  portal_payment_request(+p_details)، portal_resume_hold. E2E: صرف بنكي يتطلب آيبان صحيح الآن.
- [x] **المرحلة 5:** شاشات إدارة كاملة (وظائف/أقسام/موردون/DoA/مسارات/إعدادات/تدقيق+CSV) +
  `portal_suppliers` + RPCs (save/delete_department، delete_supplier). **إصلاحات أمان من الفحص
  العدائي:** منع تصعيد الصلاحية عبر portal_apply_job/save_job (gm أو مفاتيح إدارية = أدمن فقط +
  قائمة بيضاء)، منع انتحال القسم لموظف بلا قسم، DROP توقيعات قديمة، قفل استشاري لآخر أدمن.
  Migration: `db/portal-migrations/004-admin-suppliers.sql`. اختبارها: `11_admin_security.sql`.
- [x] **المرحلة 6:** التصميم البارد #f2f4f8 (استبدال الكريمي بالكامل) + خط IBM Plex Mono لأرقام
  دفتر القيد (كبسولات معرّفات الطلبات + قيم KPI) + شريط جانبي 238px بـ@media(min-width:921px)
  (CSS خالص، الشريط السفلي للجوال محفوظ) + مركز المهام «الجرس» (myTasks: اعتماد معلّق [عاجل عند
  تجاوز SLA]/مُعاد لي/مؤجَّل مالياً/صرف معلّق/استلام منتظر) بعدّاد حيّ + ورقة مهام قابلة للنقر +
  إعادة الطلب (نسخ بنود+مشروع لمسودة جديدة بمرجع). **واجهة فقط — بلا تغيير SQL.** إصلاحان:
  تلميح الاعتماد في القائمة مقيَّد بصلاحية اعتماد فعلية (لا نطاق رؤية واسع)، وloadPipeline
  لنطاق «قطاع» بلا قسم يرجع own-only. الاختبار: كامل الحزم صفر إخفاقات + دورة حياة كاملة حتى closed.
**ترجمة المفاتيح (نموذج→حيّ):** approveReq→can_approve_stage، approveAward→can_approve_award،
issuePO→can_issue_po، manageRfq→can_manage_procurement، disburse→can_disburse، create→can_create،
edit→can_edit، manageUsers→can_manage_users، see.finance→can_see_finance.

### التسعير/المقارنة بالبنود + مستندات الصرف/الاستلام (2026-07-14 — الفرع `claude/modest-goldberg-2p0gjn`، مُختبَر E2E محلياً)
**هجرتان جديدتان — شغّلهما في Supabase بعد 021 بالترتيب (022 ثم 023)، ومدمجتان في `portal-standalone.sql`:**
- **022 (`db/portal-migrations/022-offer-item-prices.sql`):** جدول `portal_offer_items(offer_id,item_seq,unit_price)`
  مقفل (portal_locked_guard) + سياسة SELECT برؤية الطلب. توسيع `portal_submit_offer` بمعامل تاسع `p_items jsonb`
  (`[{seq,price}]`) فيحسب الإجمالي = مجموع (كمية البند × سعره) ويخزّن الأسعار البندية. التوقيع النهائي 9 معاملات.
- **023 (`db/portal-migrations/023-disbursement-receipt-docs.sql`):** عمود `portal_receipts.doc_key` +
  `portal_payment_transition` اكتسب معاملاً خامساً `p_details jsonb` (يدمج `proof_key` محضر الصرف عند التنفيذ/الاعتماد) +
  `portal_record_receipt` اكتسب `p_doc_key` (مشهد/محضر الاستلام). فصل المهام الثلاثي والحوكمة كما هي.
**الخلفية:** `functions/api/portal-doc.js` — رفع/عرض PDF أو صورة في R2 (يعيد استخدام حاوية `QUOTES_BUCKET`)
بمفاتيح `docs/pay/…` (صلاحية can_disburse) و`docs/grn/…` (can_verify_stock) + تحقّق رؤية الطلب.
**الواجهة (المُحوِّل فقط):** `loadAll` يجلب `portal_offer_items` ويحقن أسعار البنود الحقيقية في `proc.offers[].items`
(بند غير مُسعّر = null)؛ إدخال العرض inline صار بالبنود (سعر لكل بند + إجمالي تلقائي + PDF إثباتاً)؛ **تغليف
`comparisonPanel`** بمصفوفة بنود × موردين (أرخص لكل بند + نقر خلية لاختيار مورد البند بصرياً + بطاقات وفر التجزئة)؛
نافذتا تنفيذ الصرف/الاستلام ترفعان المستند؛ أزرار عرض المستندات في لوحتَي الصرف والاستلام. أُزيل عارض iframe
الهشّ (استُعيد العارض المحلّي داخل #modal). اختبار محلي: PostgreSQL + stub Supabase — أسعار البنود/الإجمالي،
فصل مهام الصرف، تخزين مفاتيح المستندات، الإقفال بعد الاستلام الكامل، وسلبيات الصلاحية = **كلها نجحت**.
**التنفيذ المجزّأ الفعلي — بُنِيَ (النموذج «أ»، قرار المالك 2026-07-14): سلسلة صرف مستقلة لكل مورد بآيبانه.**
هجرتان جديدتان — شغّلهما بعد 023 بالترتيب (024 ثم 025):
- **024 (`024-award-lines-split.sql`):** جدول `portal_award_lines(request_id,item_seq,offer_id,supplier_name,qty,unit_price,line_total)`
  مقفل + سياسة رؤية. دالة `portal_award_split(p_request_id,p_lines,p_reason)` (p_lines=[{seq,offer_id}]):
  تتحقّق من تغطية كل البنود وأن كل مورد مُسعّر لبنده، تسجّل الترسية المجزّأة، وتبني اعتماد التعميد
  **بالقيمة الإجمالية** (أقوى حوكمة، يمنع التفتيت). المورد المهيمن يُحفظ winner_offer_id للتوافق.
- **025 (`025-split-per-supplier-payments.sql`):** عمود `portal_payments.award_offer_id`. `portal_payment_request`
  اكتسب معاملاً سادساً `p_offer_id`: عند التجزئة يتطلّب تحديد المورد، بسقف **نصيبه** شاملاً الضريبة +
  منع صرف مكرر للمورد + مجموع الدفعات ≤ التعميد الإجمالي؛ الطلب يبقى awarded حتى اكتمال كل الموردين.
  `portal_payment_transition` واعٍ بالتجزئة: فصل مهام ثلاثي لكل دفعة، وينتقل للاستلام **فقط بعد صرف كل الموردين**.
  **الترسية المفردة: سلوك 019 حرفياً محفوظ.** الحوكمة: اعتماد التعميد وأمر الشراء على الإجمالي؛ الاختلاف في الصرف فقط.
**الواجهة (المُحوِّل):** زرّ «تعميد مجزّأ» في مصفوفة المقارنة (portal_award_split)؛ لوحة صرف لكل مورد
(بطاقة بنصيبه وآيبانه ودورة إصدار/اعتماد/تنفيذ مستقلة) تحلّ محلّ الصرف المفرد عند التجزئة؛
loadAll يبني proc.isSplit/awardLines/slices. **مُختبَر E2E محلياً** (024/025): سقف النصيب، منع الصرف
المكرر، بقاء الطلب في الصرف حتى اكتمال الموردين ثم الاستلام، فصل المهام الثلاثي، والانحدار المفرد سليم —
كلها نجحت. الواجهة مُتحقَّقة منطقياً (Node). الاستلام يبقى بالبنود (يعمل عبر الموردين).
- **026 (`026-split-integrity-fixes.sql`) — سلامة من فحص السيناريوهات (شغّلها بعد 025):**
  (أ) [حرِج] `portal_award` و`portal_award_transition(reject)` يمسحان `award_lines` (المتغيّر الثابت:
  award_lines موجودة ⟺ الترسية النشطة مجزّأة — يمنع معاملة ترسية مفردة بعد مجزّأة مرفوضة كأنها مجزّأة).
  (ب) سقف مجموع الصرف المجزّأ = مجموع أسقف الأنصبة (يمنع رفض المورد الأخير بفرق تقريب).
  (ج) واجهة: proc.isSplit يشترط activeAward؛ لوحة اعتماد التعميد المجزّأ تعرض كل الموردين؛ عدّاد الصرف واعٍ بالتجزئة.
  **الترتيب الكامل للهجرات الجديدة: 022 → 023 → 024 → 025 → 026.** مُختبَر E2E: كل الحزم صفر إخفاقات.
**حقائق مؤكَّدة (سليمة): تعميد واحد لكل طلب + سلسلة اعتماد واحدة + أمر شراء واحد بالإجمالي؛ دفعات متعددة
(واحدة لكل مورد) بقصد النموذج أ — كلٌّ باعتماد/تنفيذ مستقلّ بآيبانه.**

### القسم د — الدفعات على مراحل (تعميد على دفعات) + متابعة مشتركة + تنبيهات (2026-07-15، مُختبَر E2E)
- **027 (`027-installment-payments.sql`) — شغّلها بعد 026:** عمود `portal_requests.pay_installments` +
  `portal_set_installments`. `portal_payment_request` اكتسب وضعاً ثالثاً «الدفعات» (عدّة دفعات على الإجمالي،
  كلٌّ ≤ المتبقّي، المجموع ≤ التعميد؛ الطلب يبقى awarded حتى سداد كامل القيمة). `portal_payment_transition`
  ينتقل للاستلام فقط بعد سداد كامل القيمة. **المفرد (019) والمجزّأ دون مساس.**
- **الواجهة:** لوحة «الصرف على دفعات» (إجمالي/مصروف/متبقٍّ + شريط تقدّم + رفع دفعة مستحقة بمرفق) + خيار
  تفعيل الوضع + `portal-doc.js` نوع `inst` (can_manage_procurement) لمرفق الدفعة (زيارة/محضر). لوحة متابعة
  مشتركة (تبويب «متابعة الصرف» للمالية والمشتريات معاً) بجدول كل طلبات الدفعات/المجزّأ + تذكير المالية.
- **الإشعارات/الإيميل:** `portal-notify` يقبل `awarded` (لا تُخنَق إشعارات المجزّأ/الدفعات) + حدث `reminder`
  (مشتريات→مالية) + `payment_disbursed_partial` (مالية→مشتريات لمتابعة المورد). قوالب في `_portal-shared`.
**الترتيب الكامل للهجرات الجديدة: 022 → 023 → 024 → 025 → 026 → 027.** كل الحزم (90–94) صفر إخفاقات على تحميل نظيف.

---

## 🏛️ تدقيق استشاري (تقني/مالي/مشتريات) + تصليب إنتاجي (2026-07-15، الفرع `claude/supabase-connection-check-t01so9`)

**تدقيق شامل بعدسة CTO + CFO + خبير مشتريات** كشف فجوات جاهزية مؤسسية (موثّقة كاملة في ردّ الجلسة).
**نقاط قوة مؤكَّدة بالكود:** 42 قفل `FOR UPDATE` (تزامن آمن) · رموز بريد مشفَّرة لمرة واحدة · تدقيق ثابت · SoD ثلاثي.
**فجوات P0 المتبقّية (لم تُنفَّذ بعد):** ضبط ميزانية · MFA/SSO + تفعيل حماية كلمات المرور المسرّبة · مطابقة ثلاثية/فاتورة
مورد · مسار تالف/مرتجع · تقارير/تحليلات · مراقبة/DR · ضبط تغيير آيبان المورد. **تصحيح المالك المثبَّت:** توقيت الصرف
يتبع شرط الدفع (كاش/آجل/دفعات — كلها ممثَّلة: `bank/custody/credit`+استحقاق، 027 دفعات، 024/025 مجزّأ) لا قاعدة
«استلام ثم صرف» — أُسقطت تلك الملاحظة.

**نُفِّذ في هذه الجلسة (مُختبَر E2E محلياً — PostgreSQL 16، 14 اختباراً كلها PASS):**
- **029 (`029-notification-outbox.sql`) — صندوق صادر معامَلاتي (Transactional Outbox، حجر الزاوية P0):** حلّ لكون
  الإشعارات fire-and-forget تضيع صمتاً عند سقوط Resend. جدول `portal_outbox` (server-only، RLS بلا سياسة) +
  **مُشغِّل `AFTER INSERT` على `portal_notifications`** يلتقط نيّة الإرسال في **نفس معاملة الانتقال** (ضمان معامَلاتي
  دون لمس أيٍّ من الـ80 دالة) + `portal_outbox_claim` (FOR UPDATE SKIP LOCKED، خادم فقط) + `portal_outbox_mark`
  (نجاح⇒sent، فشل⇒تراجع أُسّي 2ⁿ دقيقة بحدّ 60 ثم dead-letter عند 6 محاولات) + `portal_outbox_purge`.
  عامل التسليم: **`functions/api/portal-outbox-drain.js`** (يعيد استخدام `_portal-shared`، محميّ بـ`CRON_SECRET`).
  **يشغّل أيضاً `portal_run_sla` (تصعيد SLA مجدوَل)** فلم يعد التصعيد يعتمد على زيارة الواجهة؛ إشعارات
  التصعيد تُلتقط في الصادر وتُرسَل بنفس الدورة (بند P1 من التدقيق — SLA كسول → مجدوَل).
  **خامل حتى تفعيل المالك** (لا سلوك يتغيّر): يضيف `CRON_SECRET` + يربط Cron Trigger كل دقيقة.
  **⚠️ تصحيح مؤكَّد بالكود (2026-07-17) — لا تحذف `pa_notify`:** المُشغِّل يلتقط فقط الصفوف المُدرَجة في
  `portal_notifications`، وهذه **تُدرَج حصراً داخل `portal_run_sla()`** (`portal-standalone.sql:1609–1639`) — أي
  **تصعيد SLA للطلبات المتأخّرة فقط**. **لا دالة انتقال (submit/approve/award/payment/receipt) تُدرِج في
  `portal_notifications`.** لذا المساران **منفصلان تماماً بلا تقاطع**: بريد سير العمل كلّه عبر `pa_notify`→
  `portal-notify.js` (يحسب المستلمين من سلسلة الاعتماد)، وبريد تصعيد SLA فقط عبر الصادر. **لا يوجد ازدواج**،
  وحذف `pa_notify` **يُلغي كل إشعارات سير العمل** (كارثي). التحويل لتغطية الصادر الكاملة = بند P1 حقيقي (تعديل
  دوال الانتقال لتُدرِج `portal_notifications` لكل مستلِم + إثراء قالب/مستلمي الـdrain + اختبارات) — عندها فقط يُزال `pa_notify`.
- **030 (`030-execute-hardening.sql`) — تصليب صلاحيات التنفيذ + search_path** (من مدقّق Supabase الحيّ: Public Can
  Execute ×50، search_path Mutable ×4): سحب `EXECUTE` من `PUBLIC`/`anon` عن دوال الكتابة **بمبدأ أمان صارم** (فقط
  الدوال التي تملك منحاً صريحاً لـauthenticated/service_role — فلا تُكسر آخر صلاحية) + تثبيت `search_path` على كل
  دالة DEFINER تفتقده. **مُتحقَّق:** 25 دالة سُحب عنها العام؛ المجموعة الخادمية = 7 دوال مقصودة فقط
  (audit_write/create_token/pr_transition_email/run_sla + outbox×3)؛ **55 دالة بوابة تبقى للمستخدم المسجَّل** (الدورة سليمة).
**⚠️ هجرات جديدة يجب تطبيقها حيّاً بعد 028 بالترتيب: `029` → `030`.** مدمجتان في `portal-standalone.sql` (تنصيب نظيف).
**متغيّر Cloudflare جديد للتفعيل: `CRON_SECRET`** (قيمة عشوائية طويلة، Production).
**✅ مُفعَّل حيّاً (2026-07-17): آلية جدولة الصادر = Cloudflare Cron Trigger (Worker `portal-outbox-cron`).**
الإعداد القائم: `CRON_SECRET` على Pages Production + Worker بمعالج `scheduled` يطلب
`POST https://aldeyabi-procurement.pages.dev/api/portal-outbox-drain` بترويسة `Authorization: Bearer <CRON_SECRET>`
(المسار يقبل GET أيضاً بـ`?key=`) + سرّ الـWorker بنفس القيمة + Cron `* * * * *`. **مُتحقَّق حيّاً:** الاستدعاء
المُصادَق يعيد `{"ok":true,"processed":0,"sla":{"ok":true,"escalated":0}}` وبلا مصادقة `401`. مفاتيح الإنفاذ
الأربعة (`budget_enforce`/`three_way_enforce`/`iban_change_control`/`contract_enforce`) = 1 على القاعدة الحيّة.

- **حزمة اختبار دائمة + CI (P0 #3 — شبكة أمان آلية):** نُقلت الاختبارات من scratchpad المؤقت إلى المستودع في
  `db/portal-tests/` (`00_roles.sql` + `10_outbox.sql` + `11_security.sql` + `run.sh` + README) بصيغة **تأكيدات**
  (`RAISE EXCEPTION` + `ON_ERROR_STOP`) فتُفشِل البناء تلقائياً. workflow `.github/workflows/portal-tests.yml`
  يشغّلها بحاوية PostgreSQL 16 على كل PR/دفع يمسّ البوابة (يحمّل `portal-standalone.sql` كاملاً ثم 15 تأكيداً:
  8 صادر + 7 أمان). **مُتحقَّق محلياً: خروج 0.** أي هجرة لاحقة بمنطق حرِج ⇒ أضِف ملف تأكيدات هنا.
- **031 (`031-budget-control.sql`) — ضبط الميزانية (Commitment Control، P0):** جدول `portal_budgets`
  (قسم × سنة مالية × مبلغ معتمد، قراءة للمالية/الأدمن/المشتريات عبر RLS، كتابة عبر RPC فقط) +
  `portal_budget_set/delete/status` + `portal_budget_committed` (المرتبط = مجموع التعميدات النشطة شاملاً
  الضريبة؛ المجزّأ بمجموع بنوده لا المهيمن وحده) + **مُشغِّل قيد مؤجَّل** `trg_portal_budget_enforce` على
  `portal_award` يتحقّق عند تثبيت المعاملة (يلتقط الترسية المجزّأة كاملةً) دون لمس دوال الترسية. **خامل وآمن:**
  بلا ميزانية معرّفة ⇒ لا إنفاذ؛ ومع ميزانية: مفتاح `budget_enforce` (حقل في JSON `portal_settings`) = 0 تحذير
  غير مانع (افتراضي)، = 1 منع. اختبارها `12_budget.sql` (5 تأكيدات: المرتبط · المنع · التحذيري · بلا ميزانية · المجزّأ).
  **⚠️ تطبَّق حيّاً بعد 030. التفعيل: `portal_budget_set` لكل قسم/سنة ثم ضبط `budget_enforce=1` في الإعدادات.**
- **032 (`032-supplier-iban-control.sql`) — ضبط تغيير آيبان المورد (وقاية احتيال، P1):** كان تغيير الآيبان
  تعديلاً صامتاً مباشراً (ناقل احتيال). الآن: حارس `trg_portal_supplier_iban_guard` على `portal_suppliers`
  يمنع تغيير الآيبان المباشر عند التفعيل + سلسلة `portal_supplier_iban_request`/`approve`/`reject` بفصل مهام
  (الطالب ≠ المعتمِد؛ الاعتماد صلاحية مالية) + جدول `portal_supplier_iban_changes` (أثر تدقيق دائم) + تحقّق
  الصيغة `SA+22`. **خامل وآمن:** المفتاح `iban_change_control` (حقل JSON portal_settings) = 0 (السلوك الحالي)
  أو = 1 (يُفرض المسار). اختبارها `13_supplier_iban.sql` (5 تأكيدات: مُطفأ لا يكسر · مُفعَّل يمنع المباشر ·
  مسار الاعتماد يطبّق · الحقول غير البنكية حرّة · الصلاحيات). **الحزمة كاملة: 25/25 PASS.**
  **التفعيل: ضبط `iban_change_control=1` + شاشة طلب/اعتماد في الواجهة (متابعة لاحقة).**
- **033 (`033-three-way-match.sql`) — فاتورة المورد + المطابقة الثلاثية (P1):** جدول `portal_supplier_invoices`
  (+ `doc_key` لأصل الفاتورة PDF + `UNIQUE(request_id,invoice_no)` + كشف الفاتورة المكرّرة عبر الطلبات) +
  `portal_invoice_record` + `portal_three_way_status` (يقارن أمر الشراء ↔ المستلَم ↔ الفواتير) +
  دالتا حساب `portal_award_total`/`portal_invoiced_total` (خادميتان). **الإنفاذ عبر مُشغِّل
  `trg_portal_three_way_guard` على `portal_payments` — يُفرض على الصرف الآجل (`credit`) فقط؛ الكاش/العهدة
  (مقدَّم) مستثنى احتراماً لشرط الدفع.** خامل: مفتاح `three_way_enforce` (0/1) + `three_way_tolerance_pct`.
  **رفع أصل الفاتورة PDF:** `portal-doc.js` اكتسب نوع `inv` (صلاحية `can_manage_procurement` أو `can_see_finance`)
  → يُخزَّن في R2 (`docs/inv/…`) ويُفتح داخل النظام لكل من يرى الطلب. اختبارها `14_three_way.sql` (7 تأكيدات).
  **⚠️ تطبَّق حيّاً بعد 032. الترتيب الكامل الجديد: 029→030→031→032→033. الحزمة: 32/32 PASS.**
  **واجهة الفاتورة (المُحوِّل فقط، السطور 2556+):** `loadAll` يجلب `portal_supplier_invoices` (متسامح مع غيابه)
  ويضبط `window.__paInvOK`؛ لوحة `pa_invoicePanel` مُحقونة في مُغلِّف `paymentPreview` — تعرض المطابقة الثلاثية
  (أمر الشراء/الاستلام/الفواتير) + قائمة الفواتير بزرّ «عرض الفاتورة» (pa_docView على doc_key) + «تسجيل فاتورة +
  رفع الأصل» (pa_recordInvoice → pa_docUpload('inv') + portal_invoice_record). **لا تظهر اللوحة إلا بعد تطبيق 033
  حيّاً** (بوابة __paInvOK). السلامة النحوية مُتحقَّقة؛ **تحتاج معاينة بصرية على Cloudflare (لا يمكن اختبار المتصفّح محلياً).**
- **034 (`034-returns-debit-note.sql`) — المرتجعات/التالف + إشعار مدين (P1):** جدول `portal_returns`
  (بنود المرتجع + قيمة الخصم + رقم إشعار مدين تلقائي `DN-…` + مرفق محضر `doc_key`) + `portal_return_record`
  (صلاحية استلام/جودة أو مشتريات؛ يحسب الخصم ويولّد الإشعار) + `portal_returns_total`/`portal_return_status`.
  **دمج في المطابقة الثلاثية:** `portal_three_way_status` والمُشغِّل صارا يحترمان **صافي المستحق = أمر الشراء −
  المرتجعات**. مستند المحضر: `portal-doc.js` نوع `ret`. اختبارها `15_returns.sql` (4 تأكيدات).
  **⚠️ الترتيب: 029→030→031→032→033→034. الحزمة: 36/36 PASS.** (واجهة المرتجعات: متابعة لاحقة.)
- **035/036 (`035-multi-currency.sql` + `036-request-currency.sql`) — تعدّد العملات end-to-end:** جدول
  `portal_currencies` (سعر مقابل الأساس) + بذرة SAR=1 + `base_currency` + `portal_currency_rate/set/delete`.
  دوال القيمة (award_total/budget_committed/invoiced_total/returns_total) صارت **تحوّل بعملة الطلب لعملة
  الأساس** فكل التجميعات موحّدة. 036: `portal_set_request_currency` (قبل التعميد). **خامل: كل البيانات SAR بسعر
  1 = صفر تغيير.** الواجهة: تبويب إدارة «العملات» + مُنتقي عملة في تفاصيل الطلب. اختبار `16_currency.sql` (5).
- **037 (`037-framework-contracts.sql`) — العقود الإطارية / أوامر شراء ممتدّة بدفعات (البند الكبير الأخير):**
  جدول `portal_contracts` (سقف بعملة الأساس + مدة + حالة) + عمود `portal_requests.contract_id` (أمر سحب) +
  `portal_contract_consumed/status/set/close` + `portal_link_request_contract`. **مُشغِّل قيد مؤجَّل
  `trg_portal_contract_enforce`** يمنع تجاوز السقف/العقد المنتهي عند `contract_enforce=1` (وإلا تحذير). عدّة
  أوامر سحب على العقد وكلٌّ يُصرف بدفعات (027). الواجهة: تبويب إدارة «العقود» + مُنتقي «ربط بعقد» في التفاصيل.
  اختبار `17_contracts.sql` (5). **⚠️ الترتيب الكامل النهائي: 029→…→037. الحزمة: 46/46 PASS، خروج 0.**
  **مفاتيح التفعيل التدريجي (كلها في JSON `portal_settings`، افتراضي 0): `budget_enforce` · `iban_change_control`
  · `three_way_enforce`(+`three_way_tolerance_pct`) · `contract_enforce`.**
- **تبويب التقارير والتحليلات (واجهة فقط، المُحوِّل عبر تغليف):** تبويب «التقارير» يُضاف بـ`TABS.push` +
  تغليف `navAllowed`/`render` (لا لمس منطقة التصميم 1–2555). `pa_reportsHTML` يجمّع من `REQS` المحمّلة:
  KPIs (الالتزام/المصروف/المتبقّي/الفواتير/المعمَّدة/الصرف المعلّق/المتأخرات) + أشرطة (الالتزام حسب القسم/أعلى
  الموردين/الحالات). مُنطاق بالرؤية (visibleReqs)، مُتاح للمالية/الأدمن/المشتريات. سلامة نحوية مُتحقَّقة؛ يحتاج معاينة بصرية.
  **+ تصدير Excel/CSV** (`pa_exportCSV`: UTF-8+BOM، أعمدة عربية، بلا مكتبة خارجية = آمن CSP) **+ سند صرف مباشر
  قابل للطباعة/PDF** (`pa_expenseVoucher` في `pa_expenseDetail`: مبلغ+ضريبة+إجمالي، تواقيع سلسلة الاعتماد، `printEl`).

### 🔧 إصلاحات ما بعد الفحص العميق (2026-07-17، الفرع `claude/audit-fixes-round1` — PR مسودّة)
من جولتَي الفحص العميق، ثلاثة إصلاحات منخفضة المخاطر (مسودّة لمراجعة المالك + CI):
- **`functions/api/portal-doc.js` — fail-closed:** فحص رؤية الملف عند التنزيل (GET) صار يفشل **مغلقاً**
  (لا يُبَثّ الملف إلا بتأكيد `portal_can_see_request === true`؛ أي خطأ/تعذّر ⇒ 403). كان يفشل مفتوحاً.
- **`purchase-portal.html` (المُحوِّل، `setSupField`) — فخّ آيبان المورد:** عند `iban_change_control>=1` يُعترَض
  تعديل الآيبان المباشر برسالة توجّه لمسار «طلب تغيير الآيبان» بدل خطأ الخادم الخام (حقل 1828 في منطقة التصميم بقي كما هو).
- **038 (`038-split-award-nonlowest-flag.sql`) — رصد «غير-الأقل» في الترسية المجزّأة (غير مانع):**
  `portal_award_split` يحسب أرخص سعر مُسعَّر لكل بند ويعُدّ البنود المُرسَّاة لغير الأرخص (`non_lowest_items`)
  في التدقيق والاستجابة — أثر حوكمي دون منع (الواجهة تمرّر `p_reason=null`؛ المنع الفعلي يتطلّب حقل مبرّر لاحقاً).
  مُدمَجة في `portal-standalone.sql`. اختبار `18_split_justification.sql` (3 تأكيدات). **الحزمة: 49 تأكيداً.**
- **039 (`039-return-qty-invoice-supplier.sql`) — تكامل المرتجع + الفاتورة (شغّلها بعد 038):**
  (R2-2) `portal_return_record` يرفض كمية مرتجع تتجاوز المستلَم فعلاً (`received_qty`) شاملةً المرتجعات
  السابقة لنفس البند، ويتحقّق أنّ البند من بنود الطلب (يمنع إشعاراً مديناً مضخَّماً/إرجاع بضاعة غير مستلَمة).
  (L3) `portal_invoice_record` يُلزِم اسم المورد (كي يعمل كشف الفاتورة المكرّرة عبر الطلبات دائماً) + إلزامه
  في الواجهة (`pa_confirmInvoice`). مُدمَجة في `portal-standalone.sql`. اختبار `19_return_invoice_integrity.sql` (3).
  **الحزمة: 52 تأكيداً.**
- **إصلاح عاجل (شاشة خالية) — تكرار لا نهائي في `navAllowed`/`myTasks`:** مُغلِّفان لكلٍّ يتشاركان نفس المتغيّر
  العام (`_pa_refNavAllowed`/`_pa_refMyTasks`) فيستدعي الأول نفسه بلا نهاية ⇒ انهيار `render` ⇒ شاشة خالية.
  أُعيدت تسمية النسخة الثانية (`…2`). **خطأ كامن من تبويب التقارير (PR #37) ظهر مع إعادة النشر.** (PR #42، مدموج).
  **⚠️ قاعدة: أي مُغلِّف مُحوِّل يجب أن يستخدم اسم متغيّر ref فريداً — لا تُكرِّر `_pa_ref*`.**
- **040 (`040-advisor-hardening.sql`) — تصليب من مدقّق Supabase الحيّ (مُطبَّق حيّاً + مدمج):**
  (أ) `search_path=public` على 4 دوال يفتقدها (audit_immutable/gen_token/is_privileged/set_due)؛
  (ب) سحب تنفيذ `anon` عن دوال المُشغِّلات الأربعة (budget/contract/iban/three_way — تُستدعى عبر المُشغِّل فقط)؛
  (ج) 14 فهرس تغطية لمفاتيح أجنبية (أداء). **مُتحقَّق: security search_path 4→0 · anon guards 4→0.**
  **ملاحظات المدقّق المتبقّية كلّها مقصودة/حميدة** (جداول خادمية بلا سياسة · RPCs محميّة داخلياً · سياسات كتابة
  محروسة بمُشغِّلات · rls_auto_enable event trigger من Supabase · حماية كلمات مرور مسرّبة = قرار المالك).
- **✅ حالة القاعدة الحيّة (2026-07-18): الهجرات 022→041 كلها مطبَّقة ومُتحقَّقة** (038/039/040/040b/041 طُبِّقت هذه
  الجلسة عبر MCP). لم يبقَ فرق بين المستودع والقاعدة الحيّة.
- **متبقٍّ للمتابعة (لم يُنفَّذ — يحتاج جهداً مُختبَراً حيّاً):** تغطية الإشعارات المعامَلاتية الكاملة للأحداث
  (النتيجة العالية — تعديل كل دوال الانتقال لتُدرِج `portal_notifications` لكل مستلِم + إثراء الـdrain، بلا
  اختبار سلوكي محلي متاح فمخاطرته عالية) · تأكيد `PUBLIC_ORIGIN` (إعداد Cloudflare، لا كود) · بنود منخفضة
  مقبولة كما هي (كتابة الأدمن المباشرة المقصودة · سعر العملة بأثر رجعي الكامن · تسمية `portal-signup.js`).

## ✅ إثبات حيّ شامل للـworkflow (2026-07-18) — عبر RPC فعلي + rollback على القاعدة الحيّة
**الطريقة:** انتحال 6 مستخدمين بضبط `request.jwt.claims.email` (portal_username يطابق البريد)، استدعاء دوال الـRPC
بالتسلسل الفعلي داخل `DO$$` ينتهي بـ`RAISE EXCEPTION` (يُجهِض المعاملة → لا شيء يُحفَظ). **كله PASS:**
- **الدورة الكاملة ≤25K:** create→3 اعتمادات(مدير قطاع→مالية→مشتريات)→تسعير→تعميد→اعتماد تعميد→أمر شراء→
  طلب صرف→اعتماد→تنفيذ→استلام→**closed** — كل انتقال صحيح، تدقيق كامل.
- **المحارس (سلبي):** رُفِض: إنشاء بلا صلاحية · المقدّم يعتمد طلبه · غير-المعتمِد يعتمد · المُرسِّي يعتمد تعميده ·
  طالب الصرف يعتمده · صرف ثانٍ/تجاوز سقف. **فصل المهام الثلاثي محكوم.**
- **الإنفاذ (بعد ضبط المفاتيح=1):** صرف آجل بلا فاتورة **مُنِع** · صرف كاش **مسموح** · تعديل آيبان مباشر **مُنِع**.
- **التجزئة:** أرخص لكل بند، non_lowest_items=0، إجمالي صحيح. **الدفعات:** سقف مُنفَّذ، الطلب يبقى awarded،
  الزائد مُنِع. **الإرجاع (039):** يقبل ضمن المستلَم، يمنع تجاوزه/غير المستلَم، إشعار مدين صحيح.
- **⚠️ الشرائح >25K:** سلسلة أمر الشراء تبني «اعتماد اللجنة» — **0 مؤهَّلين** (لا `can_approve_committee`،
  `committee_members` فارغة). **ليس خطأ كود — إعداد لجنة ناقص** (تُضبط عند الإطلاق ببيانات حقيقية).
- **مفاتيح الإنفاذ ضُبِطت حيّاً =1** (`budget/three_way/iban/contract`+`three_way_tolerance_pct=5`) بعد اكتشاف
  غيابها الفعلي في `portal_settings`. **قابلة للتبديل من الإعدادات.**
- **حقيقة مؤكَّدة من المالك:** المستخدمون الحاليون كلّهم وهميون/تجريبيون؛ لا مستخدمون حقيقيون بعد (النظام قبل
  الإطلاق). فجوات الإعداد (لجنة/مدراء GA·LOG/وظائف) = **مهمّة إطلاق للمالك، لا إصلاح كود.**
**الخلاصة: منطق النظام كامل ومُثبَت حيّاً؛ لا إصلاح كود متبقٍّ. الجاهزية تعتمد على إعداد بيانات المؤسسة الحقيقية.**

### ✅ سيناريوهات موسَّعة + تثبيتها اختباراتٍ دائمة في CI (2026-07-18، الفرع `claude/advisor-hardening-040`)
تعميقاً للإثبات أعلاه، أُضيفت **35 تأكيد سيناريو تكاملي** يمرّ بالتسلسل الفعلي للدوال (لا حقن) بانتحال هوية
JWT (كعب `auth.jwt()` مطابق لدلالات Supabase + مطابقة `portal_username` بالبريد)، ونُقلت من scratchpad
المؤقّت إلى المستودع كأصول CI دائمة:
- **`db/portal-tests/20_scenarios_lifecycle.sql` (23):** تأجيل مالي→استئناف (والتأجيل مقيَّد بمرحلة المالية) ·
  تفويض عند الغياب (المفوَّض يعتمد، الغائب يُمنَع) · **ترسية مجزّأة + صرف لكل مورد بآيبانه** (منع صرف مكرّر ·
  الطلب يبقى payment حتى صرف كل الموردين ثم receipt ثم closed) · **استلام جزئي متعدّد** (لا يُقفَل إلا بالكامل ·
  لا استلام على مُقفَل) · **شريحة اللجنة في أمر الشراء** (`portal_set_committee` · غير العضو يُمنَع · العضو يعتمد) ·
  **إرجاع المشتريات للمقدّم** (عروض superseded · إعادة تقديم · مقيَّد بمرحلة التسعير).
- **`db/portal-tests/21_scenarios_advanced.sql` (12):** **سلسلة أمر شراء ثلاثية** (300K → لجنة→مالية→مدير عام)
  بفصل مهام كامل (منع اعتماد مرحلتين) · **الدفعات على مراحل** (سقف مُنفَّذ · الطلب يبقى awarded حتى سداد كامل
  القيمة ثم receipt) · **مرتجع + إشعار مدين** بعد الاستلام (قيمة الخصم صحيحة · منع تجاوز المستلَم).
- **مُدمَجة في `run.sh` + CI:** الحزمة الكاملة الآن **87 تأكيداً** (52 محارس وحدة + 35 سيناريو تكاملي) —
  مُتحقَّقة محلياً على PostgreSQL 16 **خروج 0**. تُشغَّل آلياً على كل PR/دفع يمسّ البوابة.

### 🧩 سدّ فجوة الوظائف الفارغة + تغطية الأدوار (2026-07-18، الهجرة 042)
**تدقيق تغطية الصلاحيات مقابل الوظائف (14 مفتاحاً × 18 وظيفة)** كشف أنّ **كل** مفاتيح سير العمل مُغطّاة
بوظيفة نشطة، **عدا فجوتين حقيقيتين:** وظيفة `gm` (المدير العام) و`qc` (مراقب جودة) كانتا بصلاحيات `{}` فارغة.
- **الأثر:** مرحلة «اعتماد المدير العام» في سلسلة أمر الشراء (الشرائح >250K) تتطلّب `can_manage_users`؛
  فمدير عام **غير-أدمن** بوظيفة gm كان بلا صلاحية ولا يعتمد مرحلته → تتعطّل الطلبات الكبيرة. ومراقب الجودة
  الفارغ لا يشارك في الاستلام/المرتجعات.
- **القرار (أقل امتياز + فصل مهام):** المدير العام **مشارك في سير العمل خاضع لـSoD، لا أدمن كامل** — مُنِح
  `can_manage_users` (هويّة مرحلة PO + إدارة مستخدمين محدودة بحُرّاس منع التصعيد الذاتي)، دون جعله admin
  (الذي يُعفيه من كل الضوابط). مراقب الجودة مُنِح `can_verify_stock`.
- **الهجرة `042-fill-empty-jobs.sql`** (UPDATE بدمج `||` idempotent، لا يمسّ أي تخصيص مالك) + تصحيح بذرة
  standalone + **اختبار تغطية دائم `22_jobs_coverage.sql` (5 تأكيدات):** كل مفتاح حرِج تمنحه وظيفة نشطة ≥1 ·
  gm/qc مُصلَحتان · لا وظيفة نشطة فارغة · **can_disburse تمنحه وظيفتان (فصل مهام الصرف: معتمِد≠منفّذ).**
- **⚠️ شرط تشغيلي مُوثَّق (ليس خطأ كود):** الصرف بفصل مهام ثلاثي يتطلّب **شخصين مختلفين على الأقل يحملان
  `can_disburse`** (مالي + محاسب) — لو واحداً فقط يتجمّد الصرف. منفّذ الصرف («أمين الصندوق») = حامل
  `can_disburse` (المحاسب/رئيس الحسابات)، لا وظيفة منفصلة.
- **✅ مُطبَّقة حيّاً (2026-07-18) على `mwbjoysuybgbrvfrprex`** عبر MCP apply_migration + تحقّق: gm=can_manage_users،
  qc=can_verify_stock، لا وظيفة نشطة فارغة، can_disburse بوظيفتين. الحزمة الكاملة الآن **92 تأكيداً**، خروج 0 محلياً.

### ✅✅ اختبار سير العمل الكامل على القاعدة الحيّة نفسها (2026-07-18) — لا محلياً فقط
بطلب المالك «شغّل الاختبار الكامل على القاعدة الحيّة للتأكّد». **الطريقة الآمنة على الإنتاج:** تشغيل السيناريوهات
عبر الـRPC الفعلية بانتحال هوية (`set_config('request.jwt.claims',…)` + `auth.jwt()` الأصلية في Supabase) داخل
**كتلة `DO` تنتهي بـ`RAISE EXCEPTION 'LIVE_ROLLBACK_OK>>'||v_log`** — الاستثناء **يُجهِض المعاملة (rollback)**
فلا يُحفَظ أيّ صفّ على الإنتاج، وسجلّ الـPASS يُعاد في نصّ الخطأ. الاتصال `session_user=postgres` (مميّز) فتُتجاوز
حُرّاس البذر فقط، بينما SoD/الصلاحيات تبقى مدفوعة بهوية JWT. **19 تأكيداً حيّاً — كلها PASS:**
- **تغطية حيّة:** كل صلاحية حرِجة تمنحها وظيفة نشطة · gm/qc مُصلَحتان · لا وظيفة نشطة فارغة.
- **دورة ≤25K كاملة:** إنشاء→3 اعتمادات→تسعير→تعميد→أمر شراء→صرف ثلاثي→استلام→**closed**.
- **محارس سلبية:** المقدّم لا يعتمد طلبه · غير المعتمِد مُنِع.
- **تجزئة:** أرخص لكل بند · صرف لكل مورد بآيبانه · منع صرف مكرّر · البقاء في payment حتى اكتمال الموردين→receipt.
- **شريحة اللجنة (100K):** po_review · منع غير العضو · اعتماد العضو→payment.
- **دفعات على مراحل:** سقف مُنفَّذ · سداد كامل→receipt · **مرتجع→إشعار مدين DN-…**.
- **تحقّق نظافة الإنتاج بعد الإلغاء:** 0 مستخدم مؤقّت · 0 طلب تجريبي · مدير OPS = `demo_ops_mgr` (لم يتغيّر) ·
  committee_members = `[]` (لم يتغيّر) · 0 سطر ترسية/مرتجع مُضاف. **المعاملات أُلغيت بالكامل — الإنتاج نظيف.**
**الخلاصة: المنطق مُثبَت على قاعدة الإنتاج نفسها (لا محلياً فقط)، بلا تلوّث بيانات. المستودع = القاعدة الحيّة تماماً.**

### 🔁 الإعادة الذكية للتصحيح — سدّ ثغرة «الإرجاع بلا تعديل» (2026-07-19، الهجرة 043)
**ثغرة من ملاحظة المالك (مؤكَّدة بالكود):** عند إرجاع طلب للمقدّم «للتعديل» كان زرّ «تعديل وإعادة التقديم»
يستدعي `portal_resubmit_request` التي تُعيد بناء السلسلة فقط بلا استقبال أي محتوى معدَّل — **ولا توجد أصلاً أي
دالة لتعديل بنود/كميّات/ملاحظة الطلب بعد إنشائه.** فالإرجاع للتعديل كان بلا معنى.
- **قرارات المالك:** المُعدِّل = المقدّم **+ حامل `can_edit`** (المشتريات نيابةً) · **يُمنَع الإرجاع الجذري بعد
  التعميد** (الطلب لا يبلغ `returned` إلا قبل التعميد؛ كل الإرجاعات بعده تذهب للمشتريات) · الحل الكامل.
- **الخلفية (`043-edit-returned-request.sql`):** `portal_update_request` — تعديل المحتوى (صلاحية: المقدّم/can_edit/
  أدمن · حالة `returned` فقط) · يعيد التحقّق والحساب كالإنشاء (الإجمالي/عدد العروض/علم التجزئة) · يستبدل البنود ·
  **يُصفّر كل اعتمادات الحاجة** (تغيّر المحتوى ⇒ إعادة اعتماد كاملة) · عمود `revision` + تدقيق `request_edited`.
  القيد على `returned` يضمن تلقائياً منع التعديل بعد التعميد. مُدمَجة في `portal-standalone.sql`.
- **الواجهة (المُحوِّل فقط، أسماء ref فريدة):** زرّ «تعديل وإعادة التقديم» يفتح نموذج الطلب **مُعبّأً** بمحتواه
  الحالي (`resubmitRequest` أُعيد تعريفه)؛ الحفظ يستدعي `portal_update_request`→`portal_resubmit_request` (فرع
  `NR_EDIT_ID` في `submitRequest`)؛ مُغلِّف `newRequestHTML` يُضيف لافتة تعديل ويُبدّل نصّ الزرّ؛ مُغلِّف `go`
  يُلغي وضع التعديل عند فتح «طلب جديد» من القائمة. **السلامة النحوية مُتحقَّقة (3 سكربتات، 0 أخطاء)؛ تحتاج معاينة
  بصرية على Cloudflare.**
- **الاختبار `23_rework_edit.sql` (7 تأكيدات):** إرجاع→تعديل فعلي (10000→31000، بنود مُستبدَلة، revision=1)→تصفير
  الاعتمادات→إعادة تقديم→إعادة اعتماد كاملة حتى pricing · حُرّاس: غير المخوَّل مُنِع · can_edit يعدّل نيابةً · لا
  تعديل إلا لـ`returned`. **الحزمة الكاملة الآن 99 تأكيداً، خروج 0.** ✅ مُطبَّقة حيّاً + مُثبَتة (6/6 rolled-back).

### 🔓 إعادة فتح التعميد من طور الصرف — تفعيل خيار كان ميّتاً (2026-07-19، الهجرة 044)
**ثغرة من ملاحظة المالك (مؤكَّدة بالكود):** نافذة إرجاع الصرف تعرض خيار «إعادة فتح التعميد» وتمرّر
`p_return_to='award'`، لكن `portal_payment_transition` كانت **تتجاهله تماماً** (تُسجّله بالتدقيق فقط وتعيد الطلب
دائماً إلى awarded = إعادة إصدار الصرف). فإن اكتشف المحاسب/المالية خطأً في سعر بند/عرض/اختيار المورد قبل التنفيذ،
لم يملك أي سبيل لتصحيح التسعير — فقط إعادة إصدار الصرف الخاطئ نفسه.
- **الحل (`044-reopen-award-from-payment.sql`):** عند `p_return_to='award'` يعود الطلب إلى `pricing` (يحاكي «رفض
  التعميد» لكن من طور الصرف): يُبطِل كل صرف غير منفَّذ + التعميد + سلسلتَي اعتماد التعميد وأمر الشراء، **مع إبقاء
  العروض** كي تُصحّح المشتريات سعر بند/تُرسي مورداً آخر/تُضيف عرضاً بديلاً — ثم إعادة اعتماد كاملة (تعميد→أمر
  شراء→صرف جديد). **حارس:** يُمنَع إن وُجد أي صرف منفَّذ فعلاً (المال خرج). متوافق مع «منع الإرجاع الجذري للمقدّم
  بعد التعميد» (هذا إرجاع للمشتريات لا للمقدّم). مُدمَجة في `portal-standalone.sql`.
- **الواجهة:** الخيار كان مُرسِلاً `p_return_to='award'` أصلاً؛ عُدِّل توست التأكيد ليعكس العودة للتسعير.
- **الاختبار `24_reopen_award.sql` (5 تأكيدات):** دورة كاملة (تعميد A→صرف→إعادة فتح→تسعير→إعادة تعميد B→payment) +
  حارس المنع بعد تنفيذ صرف (مجزّأ: A مصروف، B معلّق). **الحزمة الآن 104 تأكيداً، خروج 0.** ✅ مُطبَّقة حيّاً +
  مُثبَتة (3/3 rolled-back). ⚠️ تُطبَّق الهجرة 044 حيّاً بعد 043.

### 🧹 ربط حذف المستخدم بالخلفية — عنصر واجهة «ميّت» (2026-07-19، واجهة فقط)
**تدقيق «العناصر الميّتة» (أزرار تُرسل معاملات لكن الخلفية تتجاهلها، نمط 044):** كشف أنّ **كل** دوال إدارة
المستخدمين في المُحوِّل مربوطة بالخلفية (toggleAway/toggleSuspend/rejectUser/activateUser/togglePerm) **عدا
`confirmDeleteUser`** — كانت النسخة المرجعية الوحيدة (محلية فقط: `delete USERS[k]` + «نقل ملكية» بتعديل كائنات JS)
فلا يُحفَظ الحذف في القاعدة (يعود المستخدم عند إعادة التحميل)، و«نقل الملكية» وهميّ.
- **الإصلاح (المُحوِّل فقط، لا SQL):** `confirmDeleteUser` أُعيد تعريفه ليستدعي `POST /api/portal-users {action:'delete'}`
  (الخلفية `portal-users.js` قويّة أصلاً: منع الذات/آخر أدمن/**من له طلبات → توجيه للتعطيل** + فكّ المراجع + حذف
  حساب الدخول). و`askDeleteUser` أُعيد تعريفه لإزالة مُنتقي «نقل الملكية» المضلِّل (الخلفية لا تنقل الملكية بل تمنع
  الحذف لمن له طلبات) + إخفاء زرّ «حذف نهائي» لمن له طلبات قائمة (يبقى «إيقاف الدخول» الآمن). **بلا هجرة — يعيد
  استخدام خلفية قائمة.** السلامة النحوية مُتحقَّقة (3 سكربتات، 0 أخطاء)؛ تحتاج معاينة بصرية (لوحة الإدارة).

### 📧 عطل بريد تسجيل الموردين (نظام 1) — تشخيص نهائي + رصد يمنع تكراره (2026-07-21، الفرع `claude/supabase-connection-check-t01so9`)
**البلاغ:** بريد «تم استلام طلبك» للموردين المسجّلين عبر `register.html` توقّف منذ ~2026-07-05 (موردون حقيقيون
07-20 لم يصلهم شيء). **الجذر المؤكَّد (بفحوص للقراءة فقط):** `GET /api/notify` = `{"ok":false}` على الدومينات
الثلاثة ⇒ `emailConfigured(env)` خطأ (`notify.js:44` = `SUPABASE_URL && SUPABASE_SERVICE_ROLE_KEY && RESEND_API_KEY`).
سجلّات Resend تُظهر إرسالاً مُسلَّماً حتى 07-20 ⇒ `RESEND_API_KEY` سليم؛ و`SUPABASE_URL` مؤكَّد ⇒ **الفارغ حصراً
`SUPABASE_SERVICE_ROLE_KEY` في Cloudflare Pages Production** (يخصّ مشروع Supabase القديم `yofcaxvstjcrmbgciwym`).
**الأثر أوسع:** نفس المفتاح يخدم `admin-users.js` + `pr-action.js`/`_pr-shared.js` + `verify.js` — كلها متوقّفة بصمت.
البوابة (نظام 3) غير متأثّرة (تستخدم `PORTAL_SUPABASE_*`).
- **الإصلاح التشغيلي (المالك، لا كود):** Pages → Settings → Environment variables → **Production** → أعِد إدخال
  `SUPABASE_SERVICE_ROLE_KEY` + تحقّق `SUPABASE_URL` → **Redeploy** (المتغيّرات تسري على نشر جديد فقط). التحقّق:
  `curl -s https://suppliers.aldeyabi.com/api/notify` يجب أن ينقلب إلى `{"ok":true,"checks":{…all true}}`.
- **تصليب الرصد (كود، هذا الفرع — العطل بقي خفيّاً أسبوعين لأنّ فشل حدث `received` العام مُقنَّع بـ`{ok:true}`):**
  (أ) `notify.js`/`portal-notify.js`: `onRequestGet` صار يعيد `checks{}` **منطقيات وجود فقط، لا قيم** (يكشف *أيّ*
  متغيّر ناقص). (ب) `notify.js` مسار التخطّي يُصدِر `console.error('[notify] email_not_configured', {…})` (يظهر في
  Real-time Logs **حتى لو كان مفتاح الخدمة نفسه الناقص** — لا يعتمد على القاعدة). (ج) `index.html` محرّر قوالب البريد:
  مؤشّر صحّة (`etHealthCheck` → شارة حمراء بأسماء المتغيّرات الناقصة عند `ok:false`). **لا هجرة، لا سرّ يُطبع، التقنيع
  المضادّ للأوراكل باقٍ.** `node --check` نظيف للثلاثة. **توصية:** ربط `portal-outbox-cron` القائم ليستفتي `/api/notify` وينبّه.

### 📎 إلزام سند العرض + رفع المورّد لعرضه + حارس الملفات الخبيثة (2026-07-26، الهجرة 049)
**توجيه المالك:** «عند وضع عرض سعر يجب إرفاق مستند عرض المورد، ويجب أن تتيح للمورد رفع عرضه كذلك»
+ «أدوات حريصة على عدم رفع ملف خطير أو مسموم وملغوم».
- **049 (`049-quote-doc-required.sql`) — شغّلها بعد 048:** مفتاح `quote_doc_required` في `portal_settings`
  **افتراضي 1 (مُفعَّل)** خلافاً لبقية مفاتيح الإنفاذ الخاملة (طلب صريح) · `portal_submit_offer` يرفض عرضاً
  بلا مفتاح مستند · `portal_supplier_submit` **توقيع سادس** `p_quote_pdf_key` بنفس الإلزام مع **ترحيل مستند
  المراجعة السابقة** عند التنقيح + رفض مفتاح يخصّ طلباً آخر · `portal_supplier_rfq` يعيد `doc_required`
  و`previous.has_doc` · `portal_supplier_token_request` **خادمية بحتة (service_role فقط)** تتحقّق من الرمز
  وتعيد رقم الطلب + **سقف رفع 20 لكل رمز** (عمود `upload_count`). مدمجة في `portal-standalone.sql`.
  **⚠️ S7 في `11_security.sql` صار 13 دالة خادمية** (أُضيفت token_request).
- **`functions/api/portal-supplier-doc.js` (جديد):** رفع المورّد الخارجي — `POST ?t=<token>`؛ الرمز يُتحقَّق
  في القاعدة بمفتاح الخدمة، و**رقم الطلب يأتي من القاعدة لا من العميل** (فلا رفع تحت مجلّد طلب آخر)، والمفتاح
  عشوائي خادمي في نفس فضاء `quotes/<reqId>/…` فيظهر تلقائياً في لوحة ملفات العروض. **رفع فقط — لا قراءة.**
- **`functions/api/_file-guard.js` (جديد، مشترك للثلاثة: supplier-doc/quote/doc) — 5 طبقات:**
  (1) قائمة بيضاء بالتوقيع السحري (PDF/JPEG/PNG فقط، لا Content-Type) · (2) كشف متعدّد الصيغ polyglot
  (HTML/PHP/سكربت داخل صورة) · (3) سلامة بنيوية (%%EOF · FFD9 · IHDR/IEND + رفض البيانات الملحقة) ·
  (4) رفض المحتوى النشِط في PDF (`/JavaScript /OpenAction /Launch /EmbeddedFile /RichMedia /XFA
  /SubmitForm /ImportData /GoToR`) **بعد فكّ ترميز `#xx`** المُخفّي · (5) ترويسات تحييد عند العرض
  (`nosniff` + `CSP: sandbox; script-src 'none'` + `no-store` + CORP). **قرارات مقاسة:** كل علامة ≥5 محارف
  (القصيرة تتصادف عشوائياً في بايتات مضغوطة) · الفحص النصّي كامل للـPDF وأول 64KB للصور (الذيل يمسكه الفحص
  البنيوي) — قياس: PDF 5.7MB = 51ms، صورة 4.5MB = 2.5ms.
- **الواجهتان:** `supplier-quote.html` بطاقة «إرفاق عرض السعر الرسمي *» (PDF/صورة، تحقّق محلي، رفع قبل
  الإرسال) · `purchase-portal.html` (المُحوِّل) حقل «مستند العرض *» يقبل الصور ويمنع الحفظ بلا مرفق
  (`PA_DOC_REQ` من `PSET.quote_doc_required`) · بريد الدعوة يذكر الإلزام. `portal-quote.js` صار يقبل/يبثّ
  الصور أيضاً و**تحقّق الرؤية عنده فشل-مغلق** (كان مفتوحاً).
- **الاختبار:** `25_quote_doc.sql` (9 تأكيدات سلوكية عبر RPC بهوية مُنتحَلة) + `file-guard.test.mjs`
  (18 تأكيداً: 3 مستندات سليمة + 15 ناقل هجوم/تشوّه) مربوطان في `run.sh` و`portal-tests.yml`.
  **الحزمة الكاملة: 113 تأكيد SQL + 18 تأكيد حارس ملفات — خروج 0 محلياً (PostgreSQL 16).**

### 🚨 كشف حيّ: مخزن وثائق تسجيل الموردين (نظام 1) كان مفتوحاً للعموم (2026-07-26)
**بطلب المالك «نفس المحارس على بوابة تسجيل الموردين — لا يرفعون ملفات خطيرة ولا يصلون للقاعدة».**
فحوص **قراءة فقط** بالمفتاح العام (anon) المضمَّن في `register.html` أثبتت على `yofcaxvstjcrmbgciwym`:
- `POST /storage/v1/object/list/supplier-docs` ⇒ **200** و**23 مجلّد تسجيل** بأسمائها وأنواع وثائق كلٍّ.
- `GET /storage/v1/object/supplier-docs/DG-…/cr/….pdf` ⇒ **206** — أي **تنزيل سجلات تجارية وشهادات
  ورسائل بنكية لكل مورّد من الإنترنت بلا حساب**. و`/object/sign/…` ⇒ 200 (سكّ روابط موقّعة).
- الكتابة مفتوحة بالضرورة (الصفحة ترفع بـanon) ⇒ **أي شخص يرفع أي ملف**؛ وفحص التوقيع السحري في
  `register.html` يقع **في المتصفّح** فيُتجاوَز باستدعاء واجهة التخزين مباشرةً.
- ✅ **الجدول سليم:** `proc_supplier_registrations` يعيد لـanon `content-range: */0` — RLS تعمل.
  **الثغرة في المخزن لا في القاعدة.**
**العلاج (مُنفَّذ في الكود، والسياسات بانتظار المالك):**
- **`db/system1-storage-hardening.sql` — مرحلتان بترتيب مُلزِم:** (1) **فورية بلا اعتماد على شيء:**
  حذف سياسات `supplier-docs` + `SELECT` لـ`authenticated` فقط (الأدمن يدخل بحساب حقيقي فيبقى يعمل)
  + إبقاء `INSERT` لـanon **مؤقّتاً** كي لا يتعطّل التسجيل ⇒ **يُغلق التسريب كلّه**. لا سياسات
  UPDATE/DELETE ⇒ لا استبدال/حذف وثيقة. (2) **بعد ضبط `SUPABASE_SERVICE_ROLE_KEY` + نشر:** حذف
  سياسة الإدراج ⇒ لا صلاحية للعميل إطلاقاً. **لا تُشغَّل المرحلة 2 قبل أن يعيد `/api/reg-doc` `{"ok":true}`.**
- **`functions/api/reg-doc.js` (جديد):** رفع خادمي بمفتاح الخدمة + **نفس حارس `_file-guard.js` الطبقي**
  + قائمة بيضاء لصيغة `reg_id`/`doc` + اسم ملف مولَّد خادمياً + تنظيف النسخ القديمة بعد نجاح الرفع.
- **`register.html`:** `uploadDocViaServer` تجرّب الخادم أولاً؛ **رفض الملف (400) نهائي بلا سقوط**،
  و«سقوط مؤقّت» للمسار القديم فقط عند `503 not_configured` (مفتاح الخدمة الفارغ المعروف) — **يُزال فور
  ضبط المفتاح**. وأُزيل `storage.createBucket` من المتصفّح (عملية إدارية لا يجوز أن يحاولها العميل).
- **الاختبار:** `file-guard.test.mjs` اكتسب 5 تأكيدات لنقطة `/api/reg-doc` (أصل مختلف · بلا إعداد ·
  رقم تسجيل غير صالح · اجتياز مسار في نوع الوثيقة · PDF بمحتوى نشِط). **الحزمة: 113 SQL + 18 حارس + 5 نقطة.**
**⚠️ متابعة موصى بها:** تدوير anon key للمشروع القديم + مراجعة سجلّات التخزين لتقدير ما إذا استُغِلّ الكشف.

### 🏦 محرّك الصرف والموافقات المالية الموحّد — المرحلة 1 (2026-07-26، الفرع `claude/supabase-connection-check-t01so9`)
**دمج نظام طلبات الصرف المالية (لزميل، على Supabase) داخل البوابة كـ«محرّك صرف موحّد».** استُخرِج نظامهم
كاملاً (16 ملف تسليم من وكيلهم + تحقّق حيّ): نظام صغير (جدولان، دالة واحدة، صفر حوكمة، RLS متساهل، كل المنطق
في الواجهة). **القيمة في تدفّقه:** سلسلة موافقات مالية بأدوار مسمّاة متدرّجة. **قرار المالك:** محرّك صرف موحّد
بمدخلين، **بذكاء محرّك الوورك فلو القائم** (إرجاع/تفويض/تصعيد/معتمِد مؤهَّل/بريد)، قابل للتخصيص، تطوير كبير متدرّج.

**المبدأ:** تعميم `portal_approvals`+`portal_pr_transition` ليكونا **واعيَين بالدورة (cycle)** — `need` (القائمة بلا
تغيير) و`disbursement` (الجديدة) — فيرث محرّك الصرف الذكاء كلّه بلا ازدواج. عدم الانحدار مضمون: `cycle` افتراضه
`'need'`. **الدورة بُعد من الدرجة الأولى:** أي دورة/ميزة مستقبلية = بيانات (قالب `portal_workflows` بـ`cycle` جديد)
+ سطر تعيين، بلا إعادة كتابة المحرّك.

- **050 (`050-disbursement-core.sql`) — شغّلها بعد 049:** عمود `cycle` على `portal_approvals`(+فهرس فريد
  `(request_id,cycle,seq)`) و`portal_workflows`؛ `req_type`(purchase|direct_expense)+`beneficiary`+`expense_method`+
  `expense_details` على `portal_requests`. `portal_build_chain(request_id,cycle)` معمَّم (يخدم الدورتين). تعميم
  `portal_pr_transition`(معامل سادس `p_cycle`، أُسقط التوقيع القديم 5-معاملات)/`_email`(مُنطاق need)/`run_sla`/
  `resubmit`. **المدخل الأول** `portal_create_expense` (صرف مستقلّ خارج المشتريات → دورة `disbursement` → عند
  الاكتمال `portal_open_direct_payment` يفتح دفعة `approved_pay` → البنك ينفّذ → `closed`). **المدخل الثاني**
  `portal_po_transition` عند `disb_gate_purchase=1` يبني دورة صرف بدل الاعتماد المسطّح. `portal_payment_transition`:
  عند وجود سلسلة صرف يُنفَّذ مباشرةً (السلسلة = الاعتماد) بفصل مهام (المنفّذ ≠ الطالب ≠ أي معتمِد سلسلة)؛ الصرف
  المباشر → `closed` (لا استلام). **آليات المال محفوظة** (تجزئة/دفعات/آيبان/مطابقة ثلاثية/ميزانية/إعادة فتح تعميد 044).
  مفتاح `can_approve_disbursement` + 4 وظائف مالية (`fin_accountant`/`fin_accounts_mgr`/`fin_manager`/`bank_officer`)
  ⚠️ المدير المالي بلا `can_disburse` (فصل معتمِد الصرف عن منفّذه) + سلسلة صرف افتراضية `wf-disb-default` (3 مراحل،
  قابلة للتحرير في المصمّم). مدمجة في `portal-standalone.sql`.
- **`functions/api/portal-doc.js`:** نوع مستند `disb` (صلاحية `can_disburse`) عبر حارس `_file-guard.js` القائم.
- **الواجهة (المُحوِّل 2563+ فقط، أسماء `_pa_ref*` فريدة، الفرزن 1–2557 مطابق):** تبويب «صرف مباشر» + `pa_expenseForm`/
  `submitExpense`→`portal_create_expense` + `pa_disbChainPanel` (اعتماد/**إرجاع لمرحلة**/رفض) →
  `portal_pr_transition(cycle:'disbursement')` + `pa_expenseExecPanel` (تنفيذ البنك برفع سند `disb`) → `portal_payment_transition disburse`
  + `loadAll` يفصل `chain`(need)/`disbChain`(disbursement) ويبني `kind='expense'` + مركز مهام (اعتماد/تنفيذ صرف) +
  توسيع `pa_can`/`pa_permsFromCan`/`pa_acc` بـ`approveDisb`.
- **الاختبار `26_disbursement_core.sql` (11 تأكيداً عبر RPC بهوية مُنتحَلة):** دورة صرف مستقلّ كاملة (بلا تعميد) ·
  فصل مهام ثلاثي (المقدّم/غير المخوَّل/معتمِد السلسلة لا ينفّذ) · الإرجاع لمرحلة سابقة على دورة الصرف · قابلية الضبط
  (تحرير القالب يغيّر السلسلة) · بوّابة الشراء مُطفأة افتراضياً (لا انحدار). **الحزمة الكاملة: 124 تأكيد SQL + 18 حارس
  ملفات + 5 نقطة رفع = خروج 0 محلياً (PostgreSQL 16)، صفر انحدار على الـ113 القائمة.**
- **⚠️ تطبَّق 050 حيّاً بعد 049.** **مفاتيح تشغيلية:** `disb_gate_purchase` (0 افتراضي — يرفعه المالك لـ1 بعد ضبط
  سلسلة صرف الشراء وأدوارها لتفعيل بوّابة الصرف المتدرّجة على مسار الشراء بدل المسطّحة).
- **مؤجَّل بقرار المالك:** ترحيل الـ130 صفاً (النظام في يومه الأول) · إنشاء المستخدمين الخمسة وإسناد الوظائف
  (إعداد المالك) · تدوير مفاتيح مشروعهم بعد القطع.
- **المرحلة 2-أ (051 `051-idempotency-void.sql`) — حوكمة مالية متقدّمة (2026-07-26، مُختبَرة):**
  **(أ) idempotency (exactly-once):** جدول `portal_idempotency` (خادميّ، RLS بلا سياسة) + معامل سادس اختياري
  `p_idem_key` في `portal_payment_transition` — أي إعادة إرسال لنفس الدفعة+الفعل تُرجِع النتيجة المخزَّنة بلا
  تنفيذ مزدوج (يُخزَّن معامَلاتياً مع أثر الحالة نفسه فالضمان صحيح تحت التزامن؛ تعارض المفتاح لعملية أخرى = خطأ).
  الواجهة تمرّر مفتاحاً حتميّاً `pay:{id}:disburse`. **(ب) إبطال saga:** `portal_payment_void` — تعويض محكوم:
  صلاحية مالية + فصل مهام (المنفّذ/الطالب لا يُبطِل) + سبب إلزامي + قيد عكسي في التدقيق الثابت + لا إبطال بعد
  استلام بضاعة (يُستخدم مرتجع). الصرف المباشر يُقفَل مُلغىً؛ الشراء يعود لطور الدفع. أعمدة `voided_by/at/reason`.
  اختبار `27_idempotency_void.sql` (8 تأكيدات: I1–I4 + V1–V4). **الحزمة: 132 تأكيد SQL + 18 حارس + 5 نقطة، خروج 0.**
  ⚠️ تطبَّق 051 حيّاً بعد 050. عدم انحدار: `p_idem_key` اختياري (NULL = السلوك الحالي).
- **المرحلة 2-ب (052 `052-expense-budget-control.sql`) — ضبط ميزانية الصرف المستقلّ (2026-07-26، مُختبَرة):**
  ضبط الميزانية القائم (031) كان يحسب المرتبط من التعميدات (مسار الشراء) فقط، فالصرف المباشر (050) — بلا تعميد
  — كان يفلت من الضبط. **(أ)** `portal_budget_committed` أُعيد تعريفه ليجمع بندين: التزامات التعميد (كما هي،
  بعملة الأساس عبر `portal_currency_rate`) **+ التزامات الصرف المباشر النشط** (`req_type='direct_expense'` غير
  الملغى/المرفوض/المُعاد، `est_total` شاملاً الضريبة بعملة الأساس). **(ب)** `portal_create_expense` اكتسب فحص
  ميزانية بعد الإنشاء: مع ميزانية معرّفة و`budget_enforce=1` يُمنع تجاوز المرتبط للسقف (الدالة ذرّية فالمنع يُرجِع
  الطلب كأنه لم يُنشأ)؛ `=0` تحذير غير مانع. **خامل وآمن:** بلا ميزانية معرّفة ⇒ لا إنفاذ؛ عدم انحدار على مسار
  الشراء (تحويل العملة 035 محفوظ في كلا البندين). اختبار `28_expense_budget.sql` (4 تأكيدات: B1 المرتبط يشمل
  الصرف المباشر · B2 تحذيري · B3 إنفاذ يمنع · B4 بلا ميزانية = لا إنفاذ). **الحزمة: 136 تأكيد SQL + 18 حارس + 5
  نقطة، خروج 0.** ⚠️ تطبَّق 052 حيّاً بعد 051.
- **المرحلة 2-ج (053 `053-beneficiary-master.sql`) — سجلّ المستفيدين + ضبط آيبان المستفيد (2026-07-26، مُختبَرة):**
  إقفال المرحلة 2. الصرف المباشر (050) كان يخزّن اسم المستفيد نصّاً حرّاً وآيبانه في `expense_details` — بلا
  سجلّ موحَّد ولا ضبط بنكي (نفس ناقل احتيال الموردين المُعالَج في 032). **(أ)** جدول `portal_beneficiaries`
  (اسم/نوع/آيبان/حساب/رقم ضريبي/تواصل، RLS قراءة للمالية/المشتريات/الأدمن، كتابة عبر RPC) + `portal_beneficiary_save`
  /`_delete` (منع الحذف الصلب لمن له طلبات ⇒ تعطيل). **(ب)** ضبط تغيير الآيبان بنمط 032 حرفياً: حارس
  `trg_portal_beneficiary_iban_guard` + سلسلة `portal_beneficiary_iban_request`/`approve`/`reject` بفصل مهام
  (الطالب ≠ المعتمِد؛ اعتماد صلاحية مالية) + جدول تغييرات (أثر تدقيق) — محكوم بنفس مفتاح `iban_change_control`
  (ضبط موحَّد لكل البيانات البنكية). **(ج)** ربط الصرف: عمود `portal_requests.beneficiary_id` +
  `portal_create_expense` اكتسب معاملاً تاسعاً `p_beneficiary_id` — عند تحديده **يُفرَض آيبان/اسم السجلّ المُعتمَد**
  (يتجاوز قيم العميل، كسب وقاية الاحتيال). **خامل وآمن:** `p_beneficiary_id` اختياري (NULL = سلوك 050/052 بلا
  انحدار)؛ ضبط الآيبان لا يُفرَض إلا عند `iban_change_control=1`. **الواجهة (المُحوِّل فقط):** `loadAll` يجلب
  `portal_beneficiaries` (متسامح، `window.__paBenOK`) + مُنتقي مستفيد في نموذج الصرف يملأ ويقفل الآيبان/الاسم
  المُعتمَد (`pa_expPickBenef`) ويمرّر `p_beneficiary_id`. **سلامة نحوية مُتحقَّقة؛ تحتاج معاينة بصرية.** اختبار
  `29_beneficiary_master.sql` (7 تأكيدات: حفظ/صلاحية · حارس مطفأ/مفعَّل · مسار الطلب/الاعتماد بفصل مهام · فرض
  السجلّ في الصرف · مستفيد غير نشط مرفوض · منع الحذف الصلب). **⚠️ تطبَّق 053 حيّاً بعد 052. الترتيب الجديد:
  050→051→052→053. الحزمة: 143 تأكيد SQL + 18 حارس + 5 نقطة، خروج 0.** بهذا اكتملت المرحلة 2.

### 🚀 المرحلة 3 — الإنتاجية والكفاءة (بدأت 2026-07-26، الفرع `claude/supabase-connection-check-t01so9`)
- **المرحلة 3-أ (054 `054-bulk-approve.sql`) — الاعتماد الجماعي (2026-07-26، مُختبَرة):** معتمِد لديه عدّة طلبات
  معلّقة كان يفتح كلّاً على حدة. `portal_bulk_transition(p_request_ids text[], p_action, p_comment, p_cycle)` يعتمد/
  يرفض/يُرجِع مجموعةً دفعةً — **مع الحوكمة كاملة**: كل طلب يمرّ عبر `portal_pr_transition` نفسها (فصل مهام/معتمِد
  مؤهَّل/تفويض/إرجاع) داخل **معاملة فرعية معزولة** فطلبٌ يفشل حارسه (الطالب نفسه/ليس معتمِد المرحلة) يُسجَّل فاشلاً
  في نتيجته دون إجهاض الباقي. **واعٍ بالدورة** (need و disbursement). **بلا تغيير مخطّط** — دالة إنتاجية تعيد استخدام
  المحرّك المُختبَر (صفر انحدار). **الواجهة (المُحوِّل فقط، تغليف `openTasksSheet` المرجعي، اسم `_pa_refTasksSheet`
  فريد):** زرّ «اعتماد الكل (N)» في ورقة المهام يجمع طلبات الاعتماد المعلّقة (need + disbursement) ويستدعي
  `portal_bulk_transition` لكل دورة ثم يُلخّص النتيجة. اختبار `30_bulk_approve.sql` (BK1–BK4: اعتماد جماعي على
  دورة الصرف · فصل المهام لكل عنصر · سبب مطلوب للرفض · مصفوفة فارغة مرفوضة). **⚠️ تطبَّق 054 حيّاً بعد 053.
  الحزمة: 147 تأكيد SQL + 18 حارس + 5 نقطة، خروج 0.**
- **المرحلة 3-ب (055 `055-recurring-expenses.sql`) — الصرف المتكرّر/المجدوَل (2026-07-26، مُختبَرة):** الرواتب/
  الإيجارات صرف دوري ثابت. جدول `portal_recurring_expenses` (قالب: مستفيد/قسم/مبلغ/طريقة/تكرار/موعد تالٍ/صاحب) +
  `portal_recurring_save`/`set_active`/`delete` (يتحقّق كـ`portal_create_expense`: bank⇒آيبان+حساب، custody⇒مستخدم
  نشط، ربط مستفيد يفرض بياناته). **المولّد `portal_recurring_run()` خادميّ (service_role)** يُنشئ طلب صرف لكل قالب
  مستحقّ **بهوية صاحب القالب** ثم يبني دورة `disbursement` عبر `portal_build_chain` (يرث الحوكمة كاملة) — يُستدعى
  من عامل الكرون `portal-outbox-drain.js` كـ`portal_run_sla`. **حماية الفيضان:** طلب واحد لكل قالب في كل تشغيل +
  تقديم `next_run` لأوّل موعد مستقبلي (تُتخطّى الفائتة). **الواجهة (المُحوِّل فقط):** `loadAll` يجلب القوالب
  (متسامح، `window.__paRecOK`) + لوحة «الصرف المتكرّر» في تبويب الصرف (جدول + قالب جديد + تفعيل/إيقاف/حذف) للمالية/
  المشتريات/الأدمن. **S7 صار 16 دالة خادمية** (أُضيف `portal_recurring_run`). اختبار `31_recurring_expenses.sql`
  (RC1–RC7: حفظ/صلاحية · رفض طريقة غير مدعومة · توليد بهوية الصاحب + دورة صرف · لا تكرار نفس اليوم · المعطَّل لا
  يُولِّد · حماية الفيضان). **⚠️ تطبَّق 055 حيّاً بعد 054. الحزمة: 154 تأكيد SQL + 18 حارس + 5 نقطة، خروج 0.**
- **المرحلة 3-ج (056 `056-email-approval-disbursement.sql`) — الاعتماد بالبريد لدورة الصرف (2026-07-26، مُختبَرة +
  مُطبَّقة حيّاً):** الاعتماد بضغطة من البريد كان مُنطاقاً بدورة `need` فقط. الآن **واعٍ بالدورة:** عمود `cycle` على
  `portal_email_tokens` (افتراضي need) + `portal_create_token` معامل سادس `p_cycle` (أُسقط التوقيع القديم) +
  `portal_pr_transition_email` يقرأ دورة الرمز ويعمّم استعلاماته والاكتمال (need→pricing · disbursement→
  payment_pending + فتح دفعة الصرف المباشر) بنفس فصل المهام. **الكلاهما خادميّ (service_role) — مُتحقَّق حيّاً
  (authenticated=false).** **الخلفية:** `_portal-shared.js` `createToken(cycle)` + `notifyDisbPending` (بريد قرار
  بأزرار برموز disbursement) · `portal-notify.js` نوع `disbursement` جديد. **الواجهة (المُحوِّل):** إنشاء الصرف +
  كل مرحلة صرف وسطى يُرسلان `pa_notify('disbursement',id,'pending')`؛ الاكتمال يُرسل `payment` (للمنفّذين). اختبار
  `32_email_disbursement.sql` (EM0–EM4). **⚠️ طُبِّق 056 حيّاً بعد 055b. الحزمة: 160 تأكيد SQL + 18 حارس + 5 نقطة.**
- **متبقٍّ في المرحلة 3 (متابعة):** تفويض/تصعيد ظاهر في الواجهة (اختياري).
- **تبويب التقارير والتحليلات (واجهة فقط، المُحوِّل عبر تغليف):** تبويب «التقارير» يُضاف بـ`TABS.push` +
  تغليف `navAllowed`/`render` (لا لمس منطقة التصميم 1–2555). `pa_reportsHTML` يجمّع من `REQS` المحمّلة:
  KPIs (الالتزام/المصروف/المتبقّي/الفواتير/المعمَّدة/الصرف المعلّق/المتأخرات) + أشرطة (الالتزام حسب القسم/أعلى
  الموردين/الحالات). مُنطاق بالرؤية (visibleReqs)، مُتاح للمالية/الأدمن/المشتريات. سلامة نحوية مُتحقَّقة؛ يحتاج معاينة بصرية.
  **+ تصدير Excel/CSV** (`pa_exportCSV`: UTF-8+BOM، أعمدة عربية، بلا مكتبة خارجية = آمن CSP) **+ سند صرف مباشر
  قابل للطباعة/PDF** (`pa_expenseVoucher` في `pa_expenseDetail`: مبلغ+ضريبة+إجمالي، تواقيع سلسلة الاعتماد، `printEl`).

### 🏰 المرحلة 6 — التصليب المؤسسي (بدأت 2026-07-26)
- **المرحلة 6-أ (057 `057-audit-hash-chain.sql`) — تدقيق مقاوم للعبث (Hash-Chained WORM) — مُختبَرة + مُطبَّقة حيّاً:**
  سجلّ التدقيق كان append-only (حارس يمنع UPDATE/DELETE). الآن **مقاوم للعبث تشفيرياً**: عمودا `prev_hash`/`row_hash`
  + مُشغِّل `trg_portal_audit_chain` (BEFORE INSERT) يحسب `row_hash = SHA256(prev_hash | محتوى الصفّ)` بـ`sha256`
  المدمجة (لا امتداد) وقفل استشاري يُسلسِل الإلحاق (لا تفرّع) ويتجاهل أيّ قيمة يُمرّرها العميل. `portal_audit_verify()`
  (مالية/أدمن) تمشي السلسلة وتُرجِع أوّل صفّ مكسور. **النقطة الأولى genesis** (السلسلة تبدأ من 057؛ الصفوف القديمة
  التجريبية غير مُسلسَلة). اختبار `33_audit_hashchain.sql` (HC1–HC4: ترابط · تحقّق · **كشف عبث DBA عبر
  `session_replication_role=replica`** · صلاحية). **مُتحقَّق حيّاً:** عمودان + مُشغِّل + 3 دوال + `verify` للمستخدم
  المسجَّل + `chain` بلا anon. **⚠️ الترتيب: …→056→057. الحزمة: 165 تأكيد SQL + 18 حارس + 5 نقطة، خروج 0.**
- **المرحلة 6-ب (058 `058-transactional-notifications.sql`) — إشعارات معامَلاتية للاعتماد (2026-07-26، مُختبَرة +
  مُطبَّقة حيّاً خاملةً):** حلّ بند P1 (029): إشعارات سير العمل كانت fire-and-forget عبر `pa_notify` (تضيع لو سقط
  العميل بعد الالتزام). الآن تُلتقط نيّة إشعار «المعتمِد التالي» **داخل معاملة الانتقال** فيُسلّمها الصادر (029)
  بضمان دائم. **التصميم دون لمس أيٍّ من الـ80 دالة:** مُشغِّل `AFTER UPDATE` على `portal_requests`
  (`trg_portal_requests_notify`) يلتقط دخول/تقدّم مرحلة الاعتماد (status=in_review مع تغيّر seq)، فيستدعي
  `portal_enqueue_stage_notifications` (منطق مستلِمي `run_sla`: المعتمِد المُحلّ/حاملو role_key + مفوَّضوهم، باستثناء
  المُقدّم) → `portal_notifications` → مُشغِّل 029 → `portal_outbox` → drain. **خامل: مفتاح `txn_notifications`
  (0 افتراضي = لا سلوك جديد إطلاقاً، لا ازدواج مع pa_notify).** **التفعيل الكامل (خطوة مالك):** ضبط
  `txn_notifications=1` **مع إزالة نداءات `pa_notify` لمراحل الاعتماد** (تفادي بريد مزدوج) — النتيجة للمُقدّم تبقى
  عبر pa_notify. اختبار `34_txn_notifications.sql` (TN1–TN4: خامل · مُفعَّل يُدرِج · المُقدّم مُستثنى · الصادر يلتقط).
  **مُتحقَّق حيّاً: مُطبَّقة خاملةً (flag=0، مُشغِّل + دالتان + anon مسحوب). الحزمة: 169 تأكيد SQL + 18 حارس + 5 نقطة، خروج 0.**
- **المراحل التالية (روadmap معتمَد):** 2) حوكمة متقدّمة (idempotency/إبطال saga/ميزانية الصرف/سجلّ مستفيدين) ·
  3) إنتاجية (اعتماد جماعي/صرف متكرّر) · 4) تقارير ومستندات (لوحة تحليلات/سند صرف PDF/تصدير Excel) · 5) تميّز الجوال والإشعارات.

### 📎 المستندات الداعمة للصرف المباشر + جولة Codex الثالثة (2026-07-29، الفرع `audit/enterprise-certification-2026-07-27`، PR #74 مسودّة)
**متطلّب المالك الإلزامي:** رفع مستندات داعمة إجباري لطلبات الصرف المباشر (نموذج مُطبَّع + مسودّة→رفع→تقديم +
معاينة داخلية) عبر قاعدة/API/واجهة/اختبارات. **الهجرة 062 (`062-request-documents.sql`) — لم تُطبَّق على أي قاعدة
(قرار المالك: لا تطبيق قبل staging معزول + إذن صريح).** جدول `portal_request_documents` (مُطبَّع/ثابت/مُصدَّر) +
`portal_create_expense_draft`/`portal_attach_document`/`portal_remove_document`/`portal_replace_document`/
`portal_submit_expense` + إعداد `expense_docs_required` (افتراضي 1).
- **تصليب جولة Codex الثالثة (كله في 062 قبل أي تطبيق، مُختبَر):** تفويض مستند الدفعة (تطابق الدفعة/الطلب +
  صلاحية مالية) · ربط `storage_key` بمجال `docs/reqdoc/<request_id>/` (يمنع مفتاحاً مُلفَّقاً) · الاستبدال للطلب
  المُعاد فقط + مطالبة ذرّية (`WHERE id=? AND active`) · الحذف في المسودّة فقط · `portal_submit_expense` يقبل
  `returned` + بوّابة مستند في `portal_resubmit_request` (يسدّ حذف الأدلّة ثم إعادة التقديم) · استبعاد المسوّدات من
  `portal_budget_committed` · `portal_recurring_run` يُبقي المُولَّد مسودّةً بانتظار الأدلّة · `doc_max_bytes`=10MiB
  (مطابقة `_file-guard`) · أُسقِط `payment_docs_required` (كان بلا إنفاذ). اختبار `37_request_documents.sql` = **14 تأكيداً**.
- **`functions/api/portal-doc.js`:** نوع `reqdoc` (POST يتحقّق من الطلب الهدف: موجود/مرئي/مسودّة-مُعاد قبل الكتابة؛
  GET يشترط وجود صفّ فيصير المُزال غير قابل للعرض ويبقى المُستبدَل مرئيّاً) + أُزيل `target=_blank` من عارض `pa_docView`.
- **`functions/api/portal-config.js` (env-aware، fail-closed):** أزيل عنوان الإنتاج المُضمَّن من `purchase-portal.html`
  (تُجلب القيم من `/api/portal-config` وقت التشغيل). تصليب Codex round-3: تحليل URL قانونيّ (يرفض `user@host`/منفذ/مسار) ·
  تحديد البيئة بإيجاب (CF_PAGES_BRANCH/PORTAL_ENV وإلّا 503) · التحقّق أنّ المفتاح anon فعلاً (يرفض service_role/`sb_secret_`) ·
  جاهزية الخادم (مفتاح الخدمة + الحاوية). `scripts/env-guard.mjs`: تحليل hostname قانونيّ (يسدّ خداع `user@prod`).
- **واجهة المُحوِّل (draft→upload→submit):** `submitExpense` صار يُنشئ **مسودّة** (`portal_create_expense_draft`، بلا إشعار)
  ثم «مركز المستندات» (`pa_docCenter`: رفع مصنَّف/عنوان/وصف/معاينة blob داخلية/سجلّ إصدارات/حذف-مسودّة/استبدال-مُعاد) ثم
  زرّ تقديم مُعطَّل حتى وجود مستند نشط → `portal_submit_expense` (عندها فقط الإشعار). ضابط الآيبان اليدوي (حقل سبب
  إلزاميّ + شارة استثناء) + قفل القسم لغير الأدمن + تصحيح شارات مسودّة/مُعاد. `loadAll` يجلب `portal_request_documents`.
- **⚠️ ما لم يُطبَّق حيّاً/متبقٍّ (بترتيب المالك A→J):** تطبيق 062 على staging معزول (بانتظار إعداد المالك + إذن) ·
  معاينة/E2E متصفّح (لا تُدَّعى قبل staging) · الملف الموحّد لمعاملة الشراء عند الدفع (dossier، U4) · إنفاذ اكتمال مستند
  الدفعة (H) · `supplier-quote.html` env-config · حالات إقلاع وصولية (aria-busy/إعادة محاولة) · مندوبات مانداتة UX
  الكبرى (U0–U7: shell/tabs/decision workspace/dashboards/search…) كوثائق ثم تنفيذ متدرّج. **الحزمة: 192 SQL + 18 حارس
  + 7 نقطة، خروج 0. الكوميتات: `8cd7890` (تصليب backend/config) + `b3d949f` (واجهة). PR #74 مسودّة، لا دمج، لا main.**

### 🔐 P0-1 — أقلّ امتياز على `portal_users` + repo-parity (2026-08-02، الفرع `audit/enterprise-certification-2026-07-27`، PR #74)
**السياق:** أثبتت حزمة Auth/PostgREST الحيّة على staging `vpfnycxzqziltsnzxbpb` (تنفيذ المالك) أنّ مستخدماً عادياً
كان يقرأ **كل** صفوف `portal_users` بحقولها الإدارية (email/role/permissions). **إصلاح repo فقط — لا تطبيق حيّ
هنا (بانتظار قناة الأسرار + مراجعة Gate-1 المستقلّة). لا هجرة 063** (بأمر المالك): كل شيء مدمج في
`portal-standalone.sql` (المنطقة ما قبل 062) فينساب إلى `baseline_through_061` المُعاد توليدها.
- **RLS (قرار المالك 2026-08-02 عبر AskUserQuestion = «الأدمن يحتفظ بقراءة العميل»):** أُخرج `portal_users` من حلقة
  `auth_all` العامة؛ سياسة SELECT الجديدة `portal_users_self_or_admin_sel USING(username=portal_username() OR
  portal_is_admin())` (لا privileged — السوبر-يوزر/service_role يتجاوزان RLS أصلاً، وكل RPC يقرأ الجدول SECURITY
  DEFINER). الكتابة تبقى محكومة بمُشغِّل `portal_users_guard` (رفض افتراضي، لا تصعيد) — بلا تغيير.
- **دليل آمن:** عرض `portal_user_directory (username,display_name,department_id,active)` بامتياز المالك
  (`security_invoker=false`) لكل authenticated — أعمدة توجيه/عرض فقط، **بلا email/permissions/role/job/delegation/
  created_***. (definer-view مقصود — يُصنَّف في مدقّق Supabase كحارس مُختبَر لا ثغرة.)
- **الواجهة (المُحوِّل فقط، `loadAll`):** يجلب `portal_user_directory` (متسامح — يسقط إلى `uR.data` قبل التطبيق بلا
  انحدار) فيبني USERS الأساس (اسم/قسم/نشط لكل مستخدم)، ثم يُراكِب الصفوف الكاملة التي تسمح بها RLS (ذات المستخدم،
  أو الكل للأدمن) بحقولها الحسّاسة. إدارة الأدمن للمستخدمين تبقى تعمل (قراءة+كتابة عبر RLS+الحارس).
- **repo-parity (من مدقّق Supabase الحيّ):** سحب EXECUTE من PUBLIC/anon/authenticated عن 20 دالة مُشغِّل/حارس
  (تُستدعى عبر المُشغِّل فقط) → S7 في `11_security.sql` صار **36 دالة خادمية** (مُتحقَّق تجريبيّاً) · 4 فهارس تغطية FK
  (`portal_recurring_expenses(beneficiary_id/department_id)` · `portal_requests(beneficiary_id)` · `portal_request_documents(supersedes_id)` — الأخير في قسم/هجرة 062).
- **الاختبار الجديد `38_portal_users_least_privilege.sql` (PU1–PU5، RLS فعلية بدور `authenticated`):** البروفايل
  الذاتي يعمل · لا قراءة بريد/صلاحيات الغير · الدليل أعمدة آمنة فقط + يعرض الكل · الأدمن يقرأ الكل · تصعيد الدور
  المباشر محجوب. **بصمتا الأساس/062 المثبَّتتان في `supabase-push.mjs` حُدِّثتا** (baseline `e690edfe→db0aa6dc`).
- **التحقّق المحلّي (PostgreSQL 16، كـpostgres): كل الأخضر** — الحزمة **202 تأكيد SQL** (197+5) + 18 حارس + 7 نقطة ·
  `stage1-tests`=60 · drift-guard=مطابق · F1 `verify-baseline`=Phase A 28/Phase B 30 · `node --check` للكتلتين نظيف.
- **⚠️ مؤجَّل (execution، بانتظار قناة الأسرار + إذن + مراجعة Gate-1):** تطبيق التصليب على staging المعزول ثم إعادة
  حزمة Auth/PostgREST الديناميكية + advisors + Playwright E2E. **لا إنتاج، لا 063، PR مسودّة/غير مدموج.**

### 🚀 نقطة الاستئناف (2026-08-13) — الإطلاق الإنتاجيّ للمستندات + تصحيحات حوكمة (اقرأها أولاً)
**الفرع النشط ومصدر الحقيقة الآن: `main`** (PR #74 مُدمَج). كل عمل هذه الجلسة على `main` ومنشور على الإنتاج.
**⚠️ تحوّل جوهريّ: قاعدة الإنتاج `mwbjoysuybgbrvfrprex` صارت تحمل 062 + سلسلة تحصين P0 كاملة، ونظام المستندات حيّ.**

**١) دُمِج PR #74 إلى `main` ونُشِر** (كوميت الدمج `f65f9ba`) — واجهة المستندات + `functions/api` الجديدة حيّة.

**٢) طُبِّق حيّاً على الإنتاج (عبر MCP، بتفويض المالك «اجيزك/نفّذ الكل»):** الهجرة **062** (نموذج المستندات الداعمة)
+ **هجرة مواءمة إنتاجية** (`p0_1a`: سياسة SELECT للذات/الأدمن على `portal_users` — لولاها لكسر `p0_1b` الدخول لأن
الإنتاج مبنيّ تدريجيّاً لا من standalone) + **سلسلة P0 كاملة:** `p0_1b · d · e · f · g · h · i · j · k · l · m · n · o · p · r · s · v · t · w`
(استُثني `p0_1u` — مصمّم سير العمل يبقى fail-closed للقراءة). **مُتحقَّق حيّاً:** الدخول سليم للأدمن ولغير الأدمن
(محاكاة RLS)، صفر أخطاء أمنية، `txn_notifications=0` و`budget_enforce` (قيمة المالك السابقة) لم تُمَسّا.
**⚠️ درس مثبَّت: هجرات P0 تفترض baseline الـstandalone؛ تطبيقها على إنتاج تدريجيّ يتطلّب `p0_1a` أولاً وإلا يُكسَر الدخول.**

**٣) إعداد الدخول env-aware — احتياطيّ إنتاجيّ دائم:** الواجهة الجديدة تجلب url/anon من `/api/portal-config` وتفشل
مغلقةً إن غاب `PORTAL_SUPABASE_ANON_KEY`. تبيّن أنّه **غير مضبوط في نطاق Production بـ Cloudflare** (مضبوط في Preview
فقط — جُرِّب مرّتين وفشل). **الحلّ الدائم في `portal-config.js`:** احتياطيّ مفتاح anon **العامّ** لوضع الإنتاج فقط
(المعاينة معزولة، والمتغيّر إن ضُبِط له الأسبقية). **لا تُزِله** إلا بعد تأكيد أنّ متغيّر Production فعّال (فحص
`/api/portal-config`=`ok:true` **بعد** الإزالة). الحالة الحيّة: `ok:true`.

**٤) إصلاح الإشعارات للحسابات المُجازة/المحذوفة (طلب المالك):**
- `functions/api/_portal-shared.js` (`applyDelegation`): يُسقِط غير النشط/المحذوف، ويُسكِت المُجاز (يوجّهه لمفوَّضه النشط أو لا بريد).
- **`p0_2b`** (`portal_run_sla`): لا يُصعّد لأي مُجاز (أدمن/حامل صلاحية/معتمِد مقصود) — يحافظ على توجيه المفوَّض.

**٥) تصحيح حوكمة المدير العام (تكليف المالك — `p0_2a`):** «المدير العام ليس admin؛ دور سير عمل برؤية شاملة فقط،
قابل للحذف كأي مستخدم. السوبر-يوزر = مدير البوابة (عبدالله). ويُمنَح لمن يريد.»
- `portal_apply_job` **لم تعُد تصكّ admin لأي وظيفة** (بما فيها gm) — تُسند `role='user'`.
- **دالة جديدة `portal_set_admin(username,on)`** (أدمن فقط، حارس آخر أدمن) = قناة منح/سحب السوبر-يوزر.
- **مصالحة:** `maged` (وظيفة gm، كان admin، `is_away`) خُفِّض إلى `user` مع استعادة صلاحيات وظيفة gm
  (`can_manage_users` + رؤية `scope='all'`) — **صار قابلاً للحذف/الإيقاف**. **السوبر-يوزر الوحيد الآن = `admin` (عبدالله).**

**كل التغييرات مدمجة في `portal-standalone.sql`** (عدا مصالحة maged — incremental فقط). ملفات الهجرات:
`db/portal-migrations/p0_1a-…`(مدمجة في standalone فقط، بلا ملف) · `p0_2a-gm-role-decouple-set-admin.sql` · `p0_2b-sla-exclude-away.sql`.
**متبقٍّ اختياريّ:** حذف/إيقاف `maged` (متاح للمالك من اللوحة الآن) · ضبط `PORTAL_SUPABASE_ANON_KEY` في نطاق
Production الصحيح (اختياريّ — الاحتياطيّ يكفي) · إتمام parity الدقيق لحالة «المعتمِد المقصود المُجاز» في standalone's run_sla.

### 🔖 نقطة الاستئناف (2026-07-26، آخر جلسة) — اقرأها أولاً لأي جلسة جديدة
**الفرع النشط:** `claude/supabase-connection-check-t01so9` (PR #73، مسودّة). آخر كوميت `20d1cbe` (052). الشجرة
نظيفة، الدفع مكتمل، **CI أخضر** (portal-tests run #46 = success)، ومعاينة Cloudflare منشورة بنجاح.
**المُنجَز والمدفوع:** المرحلة 1 (050) + المرحلة 2 كاملةً (051/052/053) + **المرحلة 3-أ (054 الاعتماد الجماعي)
+ 3-ب (055 الصرف المتكرّر).** **الحزمة: 154 تأكيد SQL + 18 حارس ملفات + 5 نقطة رفع، خروج 0 محلياً وفي CI.**
**✅ الهجرات مطبَّقة حيّاً على `mwbjoysuybgbrvfrprex` (2026-07-26 عبر MCP):** `049→050→051→052→053→054→055→055b→056→057→058`
كلها مُسجَّلة (`list_migrations`) ومُتحقَّقة (سلسلة صرف افتراضية + 4 وظائف مالية نشطة + الدوال الخمس الجديدة + البوّابات
بقيمها الآمنة). **مدقّق الأمان بعد التطبيق:** لا جديد سوى `function_search_path_mutable` على `portal_recurring_next`
— **أُصلح فوراً (055b)**؛ بقيّة الملاحظات (91 دالة DEFINER للمستخدم المسجَّل · 36 سياسة كتابة مفتوحة محروسة بمُشغِّلات ·
5 جداول خادمية بلا سياسة · anon لبوابة المورّد · حماية كلمات المرور المسرّبة) **كلها مقصودة/حميدة** (موثّقة سابقاً في 040).
**⚠️ ما يجب عمله عند الاستئناف:**
1. **تفعيل الصرف المتكرّر (055):** `portal-outbox-drain` صار يستدعي `portal_recurring_run` تلقائياً — يعمل بمجرّد أن
   عامل الكرون القائم (`portal-outbox-cron`) يعمل؛ لا متغيّر جديد.
2. **معاينة بصرية على Cloudflare:** مُنتقي المستفيد (053) + زرّ «اعتماد الكل» في المهام (054) + لوحة «الصرف المتكرّر»
   في تبويب الصرف (055) — تحتاج تحقّقاً بصرياً. **✅ لوحة إدارة سجلّ المستفيدين + طلب/اعتماد تغيير الآيبان بُنِيَت**
   (`pa_beneficiaryPanel` في تبويب الصرف: قائمة/إضافة/تعديل/حذف + طلب تغيير الآيبان عند تفعيل الضبط + اعتماد/رفض
   المعلّق للمالية؛ `loadAll` يجلب `portal_beneficiary_iban_changes` بتسامح) — واجهة فقط، تحتاج معاينة بصرية.
3. **إكمال المرحلة 3 (بإشارة المالك لكل بند):** اعتماد بريد بضغطة لدورة الصرف (تعميم `portal_pr_transition_email`
   للـdisbursement) · تفويض/تصعيد ظاهر في الواجهة. ثم المراحل 4–7.
4. بنود تشغيلية للمالك (لا كود): تفعيل ضبط الميزانية (`portal_budget_set` + `budget_enforce=1`) وضبط الآيبان
   (`iban_change_control=1`) عند الرغبة · بند نظام 1 (`db/system1-storage-hardening.sql` مرحلة 1) ينتظر تطبيق المالك.

---

## ⏩ حالة الجلسة السابقة (2026-07-15) — نقطة الاستئناف لأي جلسة جديدة

**الفرع الحالي:** `claude/modest-goldberg-2p0gjn` (كل العمل مدفوع عليه، معاينة Cloudflare منشورة).
**المطبَّق حيّاً على Supabase `mwbjoysuybgbrvfrprex` (عبر MCP apply_migration، مُتحقَّق):** الهجرات **022→027**.
**⚠️ المتبقّي الوحيد — تطبيق الهجرة `028` على القاعدة الحيّة:** `db/portal-migrations/028-bounce-to-requester.sql`
(إرجاع المشتريات للمقدّم + علم `portal_offers.superseded`). مُختبَرة محلياً (كل الحزم 90–95 صفر إخفاقات) ومدمجة
في `portal-standalone.sql`، لكن لم تُطبَّق بعد لأن أداة Supabase انقطعت آخر الجلسة. **الخطوة التالية: apply_migration
لمحتوى ملف 028 على المشروع الجديد، ثم تحقّق `portal_bounce_to_requester` موجودة وعمود `superseded` مضاف.**
**قرار معلّق للمالك:** نشر واجهة هذا الفرع على `main` (الهجرات 022–027 حيّة فالنشر آمن ومتوافق خلفياً).

**ما أُنجز في هذه الجلسة (كله مُختبَر + مدفوع):** تسعير/مقارنة بالبنود (022) · مستندات صرف/استلام R2 (023 +
`portal-doc.js`) · تعميد مجزّأ + صرف لكل مورد بآيبانه — النموذج أ (024/025) · سلامة التجزئة (026) · دفعات على
مراحل + لوحة متابعة مشتركة + تذكيرات + إيميلات (027 + `portal-notify`/`_portal-shared`) · **محضر مقارنة عروض
الأسعار** مستند دائم بعد التعميد يوضّح البنود المعمَّدة (paComparisonDoc) · **نظام إرجاع ذكي** (لافتة «أُعيد إليك» +
مهام + خيارات وجهة الإرجاع + إرجاع المشتريات للمقدّم = هجرة 028). **الاختبار المحلي:** PostgreSQL 16 + stub في
`/tmp/pt/` (سكربتات 90–95) — يُعاد بناؤه بـ`00_stub.sql` عند فقده (بيئة scratchpad مؤقتة).

---

## حالة النشر الحالية (النظام 3) — حدّثها مع كل تقدّم

- [x] بناء المخطّط + الواجهة + دوال البريد + إدارة المستخدمين.
- [x] مراجعة أمنية عدائية + إصلاح كل الثغرات (SoD، deny-by-default، جداول مقفلة).
- [x] فصل الكود إلى مشروع Supabase مستقل (متغيّرات `PORTAL_SUPABASE_*`).
- [x] إنشاء مشروع Supabase الجديد `mwbjoysuybgbrvfrprex` + تشغيل `portal-standalone.sql` (نجح).
- [x] ربط الواجهة بعنوان + anon key الجديد (كوميت `11190bd`).
- [x] إضافة `PORTAL_SUPABASE_URL` + `PORTAL_SUPABASE_SERVICE_ROLE_KEY` في Cloudflare (Production).
- [x] إنشاء أول أدمن: `admin` / `abdullah@aldeyabi.com` / role='admin' (صف `portal_users` جاهز).
- [x] `NOTIFY_FROM` مضبوط: `Aldeyabi Group <noreply@suppliers.aldeyabi.com>`.
- [x] **الدفع إلى `main` — تمّ النشر** (2026-07-01). البوابة على `…/portal`.
- [x] **المراحل 1–6 كلها منشورة على `main`** (المرحلة 6 دُفعت 2026-07-04، cherry-pick `70761b7`).
- [x] **قاعدة البيانات مكتملة ومؤكَّدة (2026-07-04):** كل الهجرات 001–004 مطبَّقة في Supabase —
  18 جدولاً بينها `portal_suppliers` (تحقّق المالك: `information_schema.tables` = 1)؛ البذور موجودة
  (portal_jobs=18، departments=4، doa=4، workflows=4، settings=1). إصلاحات الأمان (004) حيّة.
- [x] **تحقّق ما بعد النشر (2026-07-11):** أول تسجيل دخول ناجح للأدمن `abdullah@aldeyabi.com`.
  **جذر عطل الدخول («تعذّر تحميل الحساب») كان ثلاثيّاً وحُلّ:** (1) سياسة RLS على `portal_users`
  كانت مُنطاقة تُخفي صف الأدمن → أُعيدت إلى `auth_all USING(true)` في Supabase؛ (2) مُغلِّف `loadAll`
  (السطر ~3384) كان ينفّذ `await _pa_loadAll_c()` **دون إرجاع نتيجته** فترجع الدالة undefined ويفشل
  الدخول دائماً → أُصلح بـ`var _ok=... ; return _ok;`؛ (3) فرع meRow كان يضبط `__paReason` قبل
  `signOut` الذي يُعيد دخول loadAll فيصفّره → أُعيد الترتيب (signOut ثم ضبط السبب). المُغلِّف الآن
  يُرجع القيمة الصحيحة. **⚠️ تذكير: أي مُغلِّف async لدالة تُستهلَك نتيجتها يجب أن يُرجع نتيجة الأصل.**
- [ ] **جولة اختبار تشغيلي كاملة** (دورة طلب: رفع→اعتماد→تسعير→تعميد→أمر شراء→صرف→استلام→إقفال).
- [ ] **تنظيف بنية تحتية (المالك):** فصل مشروع Cloudflare Workers الزائد `aldeyabi-pricing` عن ربط
  Git (يفشل في 0 ثانية على كل كوميت — لا إعداد له في المستودع، لا علاقة له بالكود). + تدوير anon key القديم.
- [ ] **قرار معلّق:** نطاق فرعي `portal.aldeyabi.com` (مسار `/portal` مُفعّل بالفعل). لم يُنفّذ النطاق الفرعي بعد.

---

## 🏗️ تصليب الإنتاج (جارٍ 2026-07-07) — نظام يعمل فعلياً لشركة كبيرة، ليس واجهات فقط

بعد فحص جاهزية إطلاق (workflow عدائي) كشف أن المُحوِّل لم يربط أفعالاً كانت في المرجع «عرضاً توضيحياً».
**المنجز على الفرع `claude/modest-goldberg-2p0gjn` (كله مُختبَر محلياً + مُنحدر نظيف):**
- **B1 إدخال العروض:** نموذج مُلحَق (تغليف procWorkspace) + `portal_submit_offer` — كان «إدخال عرض» نصّاً ميتاً.
- **B2 إعادة تقديم الطلب المُعاد:** RPC `portal_resubmit_request` (returned→in_review، يعيد بناء السلسلة). **هجرة 005**.
- **حُرّاس:** توسيع `portal_cancel_request` للمشتريات (**هجرة 006**) + `canReceive` يطابق `can_verify_stock`.
- **H2 الإشعارات:** `pa_notify` على 11 نقطة (submit/stage/approve/reject/return/award/payment/receipt) → `/api/portal-notify`.
- **A مصفوفة DoA الجديدة + سلسلة اعتماد أمر الشراء (هجرة 007):** التعميد دائماً مدير المشتريات؛ اعتماد **أمر الشراء** يكبر بالقيمة:
  `0–25K` مدير المشتريات مباشرة · `25–150K` +لجنة · `150–250K` +مالية · `250–500K` +مدير عام · `>500K` مناقصة+الكل.
  جدول `portal_po_approvals` + طور `po_review` + `portal_build_po_chain` + `portal_po_transition` (فصل مهام) +
  أعمدة DoA `po_committee/po_finance/po_gm`. **اللجنة:** `can_approve_committee` أو قائمة `committee_members` في portal_settings.
  **المدير العام:** `can_manage_users`. الواجهة: لوحة اعتماد أمر الشراء + تنبيه مركز المهام. **مُختبَر E2E لكل شريحة.**
- **B إعادة التوجيه المرنة (هجرة 008):** `portal_pr_transition` اكتسب معاملاً `p_return_to_seq` — الإرجاع لمرحلة
  اعتماد سابقة بعينها (تُصفَّر تلك المرحلة وكل ما بعدها إلى pending، الحالة in_review على الهدف)؛ `0`=إرجاع للمقدّم
  (السلوك القديم). من عاد إليه الطلب يعيده لمن سبقه (الآلية تراكمية). حارس: الهدف مرحلة سابقة فقط. دورة الصرف:
  `portal_payment_transition` اكتسب إجراء `return` مستقلاً (+`p_return_to`) — يعيد الطلب إلى awarded ليُعيد المشتريات
  إصدار الصرف، والوجهة تُحفظ في التدقيق. الواجهة: نافذة الإرجاع فيها قائمة «إلى مَن يعود؟» (المشاركون السابقون).
  اختبارها: `14_flexible_return.sql` (سلبي: منع الإرجاع للأمام). **مُختبَر محلياً، كل الحزم صفر إخفاقات.**

- **C (H1) فرض نطاق الرؤية على RLS (هجرة 009):** استبدال `auth_all` على الجداول المعامَلاتية بـ
  SELECT مُنطاق: `portal_my_scope()` (من وظيفة المستخدم: مشتريات/مالية/مستودع/جودة=all، مدراء القطاعات=sector،
  الموظف=own) + `portal_can_see_request()` (المالك دائماً، القطاع لـsector، **وأي معتمِد شارك فعلاً**). الجداول
  الفرعية مقيَّدة برؤية الطلب الأب؛ التدقيق للأدمن أو طلباته المرئية. **الكتابة تبقى عامة كي تظل محارس
  deny-by-default هي المدافع (لم نُضعف شيئاً)** — سياسات لكل أمر لأن FOR ALL تُلغي تقييد SELECT. جداول الإعداد
  (users/departments/jobs/doa/workflows/settings/suppliers) تبقى عامة القراءة. الدوال SECURITY DEFINER لتفادي
  تكرار RLS. الاختبار: `15_visibility_rls.sql` (own محجوب، sector يرى قطاعه، all يرى الكل، المعتمِد يبقى يرى،
  الجداول الفرعية محجوبة) + إسناد وظائف لمستخدمي الاختبار في `01_seed`. **كل الحزم صفر إخفاقات + محارس سليمة.**

### ⚠️ هجرات يجب تشغيلها في Supabase (المشروع الجديد) بالترتيب — إضافة فقط، لا حذف يدوي:
`005` → … → `018-save-job-committee-key` → `019-security-hardening` → `020-split-tier-aware`
**(من شغّل حتى 018: يكفيه `019`+`020`. ملف مجمّع: `RUN-ALL-005-to-020.sql`.)**
- **020 (كشف تفتيت شرائح DoA — البند المنخفض المتبقّي من 019):** `portal_create_request` يرفع علم
  التجزئة التحذيري إن كان مجموع عنقود الطلبات (نفس القسم/النافذة) يقع في شريحة اعتماد أعلى من الطلب
  منفرداً (تفتيت للتهرّب من اعتماد اللجنة/المالية/المدير العام). غير مانع. مُتحقَّق: 480K→2×240K يُعلَّم،
  300K منفرد لا يُعلَّم.
- **019 (تصليب أمني حرِج — من تدقيق عدائي بـ8 وكلاء + تحقّق تنفيذي؛ ثغرات مؤكَّدة بالاستغلال الفعلي):**
  **⚠️ يجب تشغيلها قبل أي استخدام إنتاجي حقيقي.** أغلقت (كلها مُعاد اختبار استغلالها → تُرفض الآن):
  - **الصرف (حرِج):** كان يُقبل **أي مبلغ** بلا ربط بالتعميد (900K لتعميد 20K)، و**صرف متعدّد** لنفس الطلب،
    وتنفيذ صرف **يُلغي إلغاء الطلب**. الآن: سقف = winner_total+الضريبة · صرف واحد قائم فقط · إعادة فحص حالة الطلب.
  - **دوال DEFINER مكشوفة لـPUBLIC (حرِج):** `portal_create_token`+`portal_pr_transition_email` (تزوير سلسلة اعتماد
    بلا حساب) و`portal_audit_write` (حقن تدقيق) → سُحبت إلى service_role.
  - **حارسا المستخدمين/الضبط (حرِج):** كانا يمرّران `can_manage_users`/`can_manage_company` → ترقية ذاتية إلى admin
    ومنح صلاحيات صرف/اعتماد وتخريب DoA بـPATCH مباشر. الآن **رفض افتراضي** (admin/privileged/علم الانتقال فقط) +
    حظر تصعيد في `portal_save_job`/`portal_apply_job` (لا صكّ/إسناد صلاحيات حسّاسة لغير الأدمن + منع إسناد ذاتي).
  - **حوكمة:** فرض عدد العروض عند التعميد (مصدر وحيد متنكّر) · رفض كمية استلام سالبة.
  - **RLS (كشف آيبان):** بوابة مالية على `portal_payments` و`portal_suppliers` (كان أمين مستودع/موظف يرى كل الآيبانات).
  **إصلاحات كود مرافقة:** `portal-action.js` يشتقّ origin من `PUBLIC_ORIGIN` لا من ترويسة المهاجم (تسريب رمز اعتماد)؛
  حُرّاس الأدمن `active !== true` (فشل مغلق). **متبقٍّ منخفض موثّق:** كشف تفتيت شرائح DoA (علم تحذيري غير مانع — تحسين لاحق).
- **018 (MEDIUM من مراجعة نهائية بثلاثة وكلاء):** مواءمة قائمة صلاحيات `portal_save_job` — كان
  `can_approve_committee` موجوداً في `portal-standalone.sql` فقط لا في سلسلة الهجرات (004)، فقاعدة
  الإنتاج (مبنية من الهجرات) ترفض إنشاء/تعديل وظيفة تمنح صلاحية اللجنة عبر محرّر الوظائف. 018 تعيد
  تعريف الدالة مطابقةً لـstandalone. (تعيين اللجنة عبر لوحة اللجنة كان يعمل — لهذا متوسط لا حرِج.)
  **إصلاحات مرافقة (واجهة خلفية، غير SQL):** `portal-users.js` حارس «آخر أدمن» صار **يفشل مغلقاً**
  (يرفض العملية عند تعذّر قراءة العدّ بدل تجاوز الحماية) في الحذف/التعطيل/التخفيض؛ `portal-register.js`
  يحذف حساب Auth المُنشأ حديثاً إن فشل إدراج الملف (لا حساب يتيم يمنع إعادة التسجيل).
- **017 (MEDIUM من المراجعة الأمنية النهائية):** سحب `portal_run_sla()` من PUBLIC — كانت قابلة للاستدعاء المباشر من anon عبر PostgREST متجاوزةً بوابة `portal_sla_tick` (أثر مخنوق: إشعارات/تدقيق تصعيد فقط). البوابة الوحيدة الآن هي `portal_sla_tick` (المحروسة). مُتحقَّق بـ`has_function_privilege`.
- **016 (ثغرة أمنية من فحص السيناريوهات):** فصل مهام ثلاثي في تنفيذ الصرف — كان يمنع المعتمِد فقط، الآن يمنع الطالب أيضاً (الطالب ≠ المعتمِد ≠ المنفّذ).
- **013:** RPC `portal_set_committee` + لوحة تعيين اللجنة في «مصفوفة الصلاحيات».
- **014:** تصعيد SLA يصل حاملي role_key (لا الأدمن فقط) + `portal_sla_tick` نبضة كسولة من الواجهة (مشتريات/مالية/أدمن، بخانق داخلي) — لا اعتماد على pg_cron.
- **015 (ثغرة مكتشفة من فحص السيناريوهات):** رؤية «المعتمِد المنتظَر»: مدير قسم/مفوَّض/حامل دور/عضو لجنة بمرحلة معلّقة **يرى** الطلب حتى لو نطاقه own — كان محجوباً فلا يستطيع الاعتماد.
(007 يستبدل بذور DoA داخلياً بأمان: يفكّ ارتباط التعميدات ثم يعيد الزرع — لا تحذف شيئاً يدوياً.)
**010 يتطلّب متغيّر Cloudflare جديد: `INVITE_*`؟ لا — يعيد استخدام `RESEND_API_KEY` + `PORTAL_SUPABASE_*` القائمة.**
**009 يتطلّب أن يكون لكل مستخدم `job_key` صحيح** (يحدّد نطاق رؤيته)؛ من بلا وظيفة = own (طلباته فقط).

- **D إدارة المستخدمين + بوابة الدعوات (هجرة 010):** التفويض (`delegate_to`/`is_away`) مربوط أصلاً في
  `portal_resolve_stage`/`portal_qualified_approver`. الجديد: جدول `portal_invitations` (رمز لمرة واحدة،
  صلاحية 7 أيام، **مقفل RLS = خادم فقط** كنمط email_tokens) + إعدادات `allowed_email_domain='aldeyabi.com'` +
  `email_whitelist=[]` + دالة `portal_email_allowed()` (نطاق الشركة أو قائمة بيضاء، ترفض الانتحال). الخلفية:
  `functions/api/portal-invite.js` (أدمن: send/list/revoke + whitelist_add/remove — البريد يجتاز السياسة، يُرسل
  رابط تفعيل Resend) + `portal-register.js` (عام محكوم بالرمز: يتحقّق ثم ينشئ Auth+ملف بصلاحيات الوظيفة) +
  تشديد `portal-users.js` create (نطاق/قائمة بيضاء خادمياً). الواجهة: صفحة `register-portal.html` المستقلة +
  تبويب «الدعوات» في لوحة الإدارة (مُحقَن عبر المُحوِّل: دعوة موظف + إدارة القائمة البيضاء + سجل الدعوات).
  الاختبار: `16_invitations.sql` (سياسة البريد + قفل الجدول + إدارة الخادم). **صفر إخفاقات.**
  ⚠️ الإعدادات محروسة بـ`portal_config_guard` (تتطلّب صلاحية إدارية) — الخادم يكتبها بمفتاح service_role (claims.role=service).

- **E1 حذف المستخدم (هجرة 011):** `portal_delete_user` (أدمن فقط، لا حذف للذات/آخر أدمن نشط بقفل
  استشاري، فك ارتباطات، تدقيق) + حماية «آخر أدمن نشط» في `portal-users.js` delete. اختبار `17_delete_user.sql`.
- **F تحقّق الأرشفة/الحركات:** أُكِّد أن التدقيق append-only يوثّق **كل الحركات** لدورة كاملة (13 حدثاً:
  submit→3×stage→offer→award→award_approved→payment req/approve/disburse→receipt→closed). الأرشفة كاملة.
- **إعادة إصدار الصرف بعد الإرجاع:** جاهز (payment `return`→awarded ثم `resubmitPayment`/toPayment يعيد الإصدار).
  توجيه الاستلام للمستودع: `myTasks` يُظهر «استلام منتظر» لحامل `can_verify_stock`.
- **متبقٍّ تجميلي اختياري (واجهة فقط):** H5 عرض اسم شريحة DoA مقروء في لوحات الاعتماد · لمسات تنسيق.
  (لا يؤثّر على الحوكمة/الأمان — كل المنطق مُختبَر.)

### 🔒 مراجعة أمنية + جودة نهائية (3 وكلاء عدائيين) بعد النشر — النتائج والإصلاحات (هجرة 012):
- **لا ثغرات حرجة/عالية أمنياً.** تأكيدات: الإرجاع المرن لا يُستغَل لتخطّي/اعتماد ذاتي · فصل المهام صامد ·
  كل DEFINER يثبّت search_path · الدعوات/رمز التسجيل/سياسة البريد سليمة (لا انتحال نطاق) · لا حقن PostgREST ·
  لا تسريب مفاتيح · التفويض على نقاط الأدمن محكم (Bearer JWT + admin نشط + same-origin).
- **إصلاحات مطبَّقة (هجرة 012 + `portal-users.js`):**
  - **(HIGH صحّة)** حذف مستخدم له طلبات كان يفشل بـFK (requester NOT NULL) والخادم يُبلّغ نجاحاً زوراً +
    يُتلف حساب الدخول. الآن: منع الحذف الصلب لمن له طلبات (سلامة التدقيق) → توجيه للتعطيل؛ فكّ المراجع؛
    حذف الملف قبل حساب الدخول مع فحص النتيجة.
  - **(RLS LOW)** مدير قطاع بلا قسم (department NULL) كان يتسرّب له طلبات الأقسام ذات القطاع NULL →
    قُيّد بـ`portal_my_sector() IS NOT NULL` (يسقط إلى own).
  - **(توفّر)** حماية «آخر أدمن نشط» أُضيفت إلى `setActive`(تعطيل) و`setProfile`(تخفيض الدور) لا الحذف فقط.
  - توحيد حدّ كلمة المرور (8) عبر كل المسارات.
  اختبارها: `15_visibility_rls` (مدير بلا قسم محجوب) + `17_delete_user` (منع حذف من له طلبات). **صفر إخفاقات.**
  ⚠️ **تأكيد بيئة:** اضبط `PUBLIC_ORIGIN` في Cloudflare (وإلا رابط تفعيل الدعوة يشتقّ من Host الطلب).

### قواعد التطوير المثبَّتة لهذا العمل:
- أي تعديل واجهة = في المُحوِّل فقط (السطور 2556+)؛ مناطق التصميم 1–2555 تبقى مطابقة للمرجع (تحقّق diff).
- كل schema change = هجرة idempotent في `db/portal-migrations/00X` **+ دمج في `portal-standalone.sql`** (تنصيب نظيف).
- اختبار محلي (PostgreSQL + authenticator + SET ROLE) قبل كل دفع؛ `regress.sh` صفر إخفاقات.
- لا نشر إلى `main` قبل موافقة المالك + تشغيله الهجرات.

### مصفوفة DoA (المرجع الحوكمي النهائي من المالك 2026-07-07):
| الشريحة ر.س | التعميد | اعتماد أمر الشراء |
|---|---|---|
| 0–25,000 | مدير المشتريات | مدير المشتريات (إصدار مباشر) |
| 25,001–150,000 | مدير المشتريات | + اللجنة المصغّرة |
| 150,001–250,000 | مدير المشتريات | + المدير المالي |
| 250,001–500,000 | مدير المشتريات | + المدير العام (أخيراً) |
| >500,000 | مدير المشتريات | مناقصة رسمية — الكل + المدير العام |

---

### إنشاء أول أدمن للبوابة (الطريقة)
1. Supabase (المشروع الجديد) → Authentication → Users → Add user (بريد + كلمة مرور + Auto Confirm).
2. SQL: `INSERT INTO portal_users (username,email,display_name,role,active) VALUES ('admin','<نفس البريد>','المدير العام','admin',true);`
   (البريد يجب أن يطابق حساب Auth تماماً — الربط عبر البريد.)

---

## سير العمل (Git) — التزم به
- التطوير على الفرع: **`claude/modest-goldberg-2p0gjn`**.
- commit + `git push -u origin <branch>`.
- النشر إلى `main` **فقط بعد إذن المالك**: `git checkout -B main origin/main` → `git cherry-pick <sha>` →
  تأكّد `git diff --stat <branch>` فارغ → push main → ارجع للفرع.
- الإصدار المباشر (النظام 2/1): `https://aldeyabi-procurement.pages.dev` (Cloudflare Pages).

## البريد (Resend)
- النطاق الموثّق: `suppliers.aldeyabi.com`. المُرسِل: `noreply@suppliers.aldeyabi.com`. الردّ: `supply@aldeyabi.com`.
- دوال البريد للنظام 2: `notify.js` + `_pr-shared.js` + `pr-action.js`. **لا تلمسها من أجل البوابة** — البوابة لها `portal-*` منفصلة.

---

## 🗺️ خريطة النظام 2 — نظام المشتريات الأساسي (`index.html`) — مرجع العرض والتقارير والمطبوعات (2026-08-31)
> أُنشئت بطلب المالك: «افهم وتذكر نظام المشتريات الأساسي لأننا سنجري عليه تحديثات في طريقة عرض المعلومات
> أو في التقارير والمطبوعات». **أرقام الأسطر تقريبية وتنزاح مع التعديل — استخدمها نقطة انطلاق ثم `grep` على اسم الدالة.**

### البنية العامة
ملف واحد ضخم `index.html` (~22,880 سطراً · 2.5MB) = HTML + CSS + JS مضمّن، بلا بناء (no build step)، RTL عربي.
- **1–2586:** كل الـCSS (بأقسام معلَّمة `/* ====== NAME ====== */`).
- **2587–5094:** HTML — الشريط الجانبي + الصفحات (`<section class="page" id="page-*">`) + كل النوافذ (modals).
- **5095–5143:** سكربت أول قصير. **5144:** فتح السكربت الرئيسي · **5146:** ثابت `DATA` = JSON ضخم في **سطر واحد**
  (بذور الأصناف/الموردين/السجل من الإكسل). ⚠️ لا تقرأ السطر 5146 كاملاً — يفجّر السياق؛ استخدم `awk` بقصّ الأسطر.
- **5148–5175:** `STATE` — مصدر الحالة الوحيد في الذاكرة (`items/suppliers/history/purchaseOrders` + المرشّحات
  والفرز والصفحات + `currentUser` + `poView/poSegment/poFilter/poSort/poPage`).
- **5177–5240:** كتالوج الصلاحيات `can_*` (`can_create_po`/`can_edit_po`/`can_approve_po`/`can_delete_po`/
  `can_export`/`can_import`/`can_view_audit`/`can_use_ai`/`can_manage_rfq`/`can_review_registrations`…).
  البوابات في الكود: `hasPermission(key)` · `requirePermission(key,label)` · `applyUserRoleToUI()` + سمة HTML `data-requires-perm`.

### التوجيه والعرض
`navigate(page)` (**~6044**) يبدّل `.page.active` ثم ينادي دالة العرض المطابقة:
`renderDashboard` 6066 · `renderItems` 6157 · `renderEntry` · `renderPricing` · `renderHistory` 6621 ·
`renderSuppliers` 6734 · `renderReference` · `renderAnalytics` 16866 · `renderAIPage` · `renderPurchaseOrders` 14107 ·
`renderReportsCenter` 12222 · `renderPRPortal` (بوابة طلبات الشراء الداخلية 10879+).
صفحات الشريط الجانبي (`data-page`): dashboard · items · entry · pricing · history · suppliers · registrations ·
reference · pr · purchase-orders · reports · analytics · ai (+ agents مخفيّة).
مُنسّقات: `fmtPrice` (en-US، حتى منزلتين) · `poFmtDate/poToISO` · `escapeHtml/escapeAttr` · `tafqitSAR` (تفقيط) 9739.
⚠️ **تعريف مكرّر:** `renderDashboardCharts` مُعرَّفة مرّتين (12729 و**17246** — الأخيرة هي الفعّالة). لا تُكرِّر الأسماء.

### وحدة أوامر الشراء (لبّ لقطة الشاشة) — 13823+
- **الثوابت 13827–13862:** `PO_ENUMS` (sector/officer/payment_method/priority/status/delay_reason/category) ·
  `PO_STATUS_META` (8 حالات + `step` + فئة لون) · `PO_BOARD_ORDER` · `PO_STEPPER` · `PO_TERMINAL` ·
  `PO_STATUS_ALIAS` (توافق خلفي) · `PO_SEGMENTS` (شرائح: الكل/طلباتي/متأخرة/متعثّرة/عاجلة/بانتظار الاعتماد/لدى المالية/غير مُسلَّمة).
- **الحساب المشتق 13879 `recomputePOderived`:** الإجمالي من البنود ⇒ `subtotal` ثم **ضريبة 15% ثابتة** ⇒ `vat`/`total`،
  و`days_delayed` و`lead_time_days`. **مصدر الحقيقة الوحيد للأرقام — أي تعديل حسابي يبدأ هنا.**
- **محرّك زمن المراحل 13898+:** `poStageTimeline` · `poCurrentStageAge` · `poStageStats` (للاختناقات وزمن الدورة).
- **العرض:** `renderPurchaseOrders` 14107 (منسّق) → `renderPOKPIs` 14128 (شريط الـ7 مؤشّرات في اللقطة، كلٌّ قابل للنقر
  فيصفّي) · `renderPOSegments` 14165 · `renderPOTable` 14186 (الجدول + الترقيم + الفرز بالنقر على `th[data-posort]`) ·
  `renderPOBoard` 14236 (كانبان بسحب وإفلات يغيّر الحالة) · `renderPODashboard` 14649 (لوحة التحكم التحليلية) ·
  `renderPOAlerts` (الإنذارات) · `poHealthScore` 14844 (مؤشّر صحة النظام).
  خلايا مساعدة: `poStatusChip` 14174 · `poDelayCell` 14180 · `poDashTable` 14645 · `poBarRows` 14640.
- **HTML الصفحة 3280–3347:** `#po-kpi-strip` · أزرار العروض `.po-view-btn[data-poview]` · `#po-segments` ·
  `#po-toolbar` (بحث/قطاع/حالة/أولوية) · `#po-table > #po-tbody` (10 أعمدة) · `#po-board` · `#po-dashboard` · `#po-alerts`.
- **الأفعال:** `changePOStatus` 14265 · `poNextStage` 14281 · الاستلام `poReceiveOpen/Save` 14316/14351 (يُغلق الأمر تلقائياً
  عند اكتمال الكميات) · الدرج `openPODrawer` 14384 · النموذج `openPOModal`/`poCollectForm` 14568/`savePO` 14592 ·
  `poFromAward` 14602 (إنشاء أمر من ترسية RFQ) · استيراد/تصدير Excel 15062+ (`poHandleImportFile`/`poConfirmImport`).
- **حقول أمر الشراء:** `po_number · issue_date · sector · project · supplier · subtotal(+vat/total مشتقّة) · officer ·
  payment_method · priority · expected_delivery · actual_delivery · status · delay_reason · category · notes ·
  items[{desc,unit,qty,price,received_qty}] · receipts[] · status_history[] · source · created_by/at`.

### نظام الطباعة الموحّد (كل المطبوعات تمرّ منه — نقطة التعديل الرئيسية للمطبوعات)
- **`printDocOpen(opts, bodyHtml)` ← 9728** هي **نقطة الدخول الموحّدة**: تحمّل بيانات الشركة ثم تبني وتعرض المعاينة.
  خيارات `opts`: `eyebrow · title · subtitle · docNumber · date · compactHeader · signature · note · watermark`.
- `buildPrintDoc` 9711 = `.print-document` [علامة مائية + ترويسة + eyebrow/عنوان/عنوان فرعي + المحتوى + تذييل].
  `buildPrintHeader` 9622 (ترويسة كاملة بمعلومات الشركة، أو `compactHeader` مختصرة للأوراق الداخلية) ·
  `buildPrintFooter` 9684 (طُبع بواسطة + التاريخ + خانة توقيع اختيارية).
- **معجم CSS للمطبوعات (1084–1440)** — استخدمه ولا تخترع أنماطاً: `.print-section/.print-section-title` ·
  `.print-table` (+`.num`/`.center`/`.price` + `thead`/`tfoot`) · `.print-grid-stats`+`.print-stat-card[.accent]` ·
  `.print-badge[.ok/.warn/.danger/.info/.neutral]` · `.print-note-box[.danger]` · `.print-parties/.print-party[.buyer]` ·
  `.print-quote-totals` · `.print-total-words` · `.print-terms` · `.print-signatures/.print-sign` · `.print-info-grid`.
- **المعاينة والإخراج:** `showPrintPreview` 10300 (يفتح `#modal-print-preview` ويضيف `body.print-preview-active`) ·
  `executePrint` 10322 (**طباعة عبر iframe مخفيّ** ينسخ كل الأنماط وينتظر جاهزية الخطوط — يحلّ مشاكل التشكيل العربي) ·
  تصدير PDF حقيقي 10402+ (html2canvas + jsPDF) · تصدير Word 10661+ (docx.js) · المكتبات من CDN بثلاثة مصادر بديلة.
- **مطبوعات جاهزة:** أمر شراء رسمي `printPO` 14902 (أطراف + بنود + إجماليات + تفقيط + شروط + 3 تواقيع) ·
  مذكرة استعجال مالية `printUrgentMemo` 14993 · طلب شراء `~11356` · تقارير الأصناف/الموردين/الكتالوج/المقارنة/
  عرض السعر الرسمي/السجل 10842–10878.

### مركز التقارير (11645–12275) — نقطة التعديل الرئيسية للتقارير
- **`reportsCatalog()` 12088** يُرجع مصفوفة تعريفات؛ كل تقرير كائن:
  `{group, perm, icon, title, desc, fields:[{type:'select|date', key, label, html, value, required}], run(values), ai?}`.
  **إضافة تقرير جديد = عنصر في هذه المصفوفة + دالة `buildXReport()` تنتهي بـ`printDocOpen`.** لا شيء آخر.
- `renderReportsCenter` 12222 يرسم البطاقات مجمَّعة بـ`group` ويقفل ما تنقص صلاحيته · `reportRun(idx)` 12261 يجمع
  قيم الحقول ويشغّل `run` (يدعم async + حالة تحميل على الزر).
- **المجموعات والتقارير القائمة (17):** *المشتريات:* لوحة المشتريات `poPrintDashboard` 14751 · التقرير الإداري الذكي
  (AI) `poGenerateAIReport` 15051 · سجل أوامر الشراء `buildPOListReport` 11670 · المتأخرات والتصعيد 11708 ·
  الأوامر لدى المالية 11739 · زمن الدورة والاختناقات 11774 · طلبات الشراء `buildPRReport` 11130.
  *الموردون:* تقرير مورد `printSupplierReport` · تقييم وأداء الموردين 11798 · دليل الموردين 11834.
  *الأصناف والأسعار:* كتالوج الأسعار · السجل التاريخي · فرص التوفير 11861 · تذبذب الأسعار 11891 · تغطية التسعير 11921 ·
  تحليل الفئات 11950. *طلبات التسعير:* حالة RFQ 11974. *التسجيلات:* تسجيل الموردين 12011. *التدقيق:* سجل التدقيق 12049.
- مُساعد مشترك للأصناف: `priceAnalytics()` 11650 (المُسعَّر/غير المُسعَّر/التذبذب/التوفير/الفئات).

### البيانات والمزامنة
- سحابة Supabase **المشروع القديم `yofcaxvstjcrmbgciwym`** بمتغيّرات `SUPABASE_*` (لا `PORTAL_*`).
  الجداول المستخدَمة: `proc_items · proc_history · proc_suppliers · proc_purchase_orders · proc_purchase_requests ·
  proc_pr_items · proc_pr_approvals · proc_rfqs · proc_rfq_quotes · proc_users · proc_departments · proc_settings ·
  proc_notifications · proc_item_aliases · proc_audit_log · proc_ai_usage`.
- وحدة المزامنة 12775–13657 (السحابة مصدر الحقيقة لأوامر الشراء مع دمج الإضافات المحلية غير المرفوعة 13574).
  مفاتيح `localStorage` بادئتها `proc_*` أيضاً (نسخة محليّة/تفضيلات) — لا تخلطها بأسماء الجداول.
- التدقيق `logAudit(action,type,id,payload)`؛ وحدات أخرى: الاستيراد الذكي بالـAI 17980+ · مطابقة الأصناف 19209+ ·
  صفحة AI المتقدّمة 17359+.

### 🏗 جهة / موقع المشروع بُعد أساسي (تحديث 2026-08-31)
بتوجيه المالك: «لا يصبح أمر شراء ومبلغ ومورد فقط — لازم تبين جهة أو موقع المشروع». حقل `po.project` كان
موجوداً في النموذج والاستيراد لكنه **لم يكن يُعرض** في القائمة/اللوحة ولا في أغلب المطبوعات. الآن:
- **مساعدات موحّدة:** `poProjectList()` (الجهات المستخدَمة فعلياً، مرتّبة عربياً) · `poProjectCell(po)` (خلية
  الجداول، تُبرز النقص بشارة «غير محدّدة») · `poProjectText(po)` (نصّ المستندات — لا يختفي عند الغياب).
  **استعملها ولا تقرأ `po.project` مباشرةً في أي عرض جديد.**
- **العرض:** عمود «المشروع / الجهة» في جدول القائمة (قابل للفرز `data-posort="project"`، الجدول 11 عموداً) ·
  سطر الجهة في بطاقة الكانبان · سطر بارز في رأس الدرج بزرّ «تحديد الجهة/تغيير» (`poSetProject`) ·
  مُنتقي «كل الجهات / المشاريع» في شريط الأدوات (+ خيار `__none__` = بلا جهة محدّدة، لمعالجة النقص) ·
  بطاقة «المشتريات حسب الجهة / المشروع» في لوحة التحكم.
- **المطبوعات:** أمر الشراء (عنوان فرعي + كتلة `.print-party-proj` بارزة) · سجل أوامر الشراء (عمود + مرشّح +
  قسم «التوزيع حسب الجهة») · المتأخرات · الأوامر لدى المالية · مذكرة الاستعجال · لوحة التحكم المطبوعة.
- **الإدخال:** الجهة **إلزامية للأوامر الجديدة** وتحذير قابل للتجاوز عند تعديل أمر قديم بلا جهة
  (`poCollectForm`) + `datalist` بالجهات القائمة لتوحيد التسمية.
- **تنقّل المراحل:** `changePOStatus(num,status,note)` صارت النقطة الموحّدة — الإرجاع لمرحلة سابقة أو الإلغاء
  **يتطلّب سبباً مكتوباً** يُحفظ في `status_history[].note` ويظهر في الخط الزمني وسجل التدقيق؛ `poStageJump`
  (نقر المرحلة في مسار الدرج) يؤكّد قبل تخطّي أكثر من مرحلة؛ ولافتة «المرحلة التالية» في الدرج.
- **الجولة الثانية (طلب المالك: «تظهر المشروع مع كل أمر في تقارير الحالة + البحث بالمشروع»):**
  **كل** مطبوعة تسرد أوامر/طلبات صار فيها عمود الجهة — أُضيفت: **تقرير طلبات الشراء** `buildPRReport`
  (للطلبات حقل `pr.project`، 9 أعمدة) و**زمن الدورة** `buildPOCycleReport` (جدول جديد «الأوامر الأطول بقاءً
  في مرحلتها الحالية»: أمر · مورد · **جهة** · المرحلة · عمرها · القيمة). **التقرير الإداري الذكي:**
  `poProjectStats()` + `snapshot.no_project` تُمرَّر للنموذج فصار التحليل يغطّي الجهات.
  **البحث:** لوحة الأوامر (Ctrl+K) صارت تشمل **أوامر الشراء** (بحث بالرقم/المورد/**المشروع**/القطاع/الحالة →
  تفتح درج الأمر)، و**بحث الشريط العلوي** صار يفتح اللوحة الموحّدة بدل القفز الأعمى للأصناف (Enter يُبقي
  السلوك القديم: قائمة الأصناف مُصفّاة). `openCmdPalette(prefill)` تقبل نصّاً — وتتجاهل Event عند استعمالها مستمعاً.
- **التحقّق:** حزمة تأكيدات في Node (32 سلوكية + 28 لهندسة جداول المطبوعات وإحصاء الجهات) + معاينة متصفّح
  حقيقية (Chromium/Playwright) للقائمة والدرج وأمر الشراء المطبوع والبحث بالمشروع. `node --check` نظيف.

### 🔢 تسلسل أرقام أوامر الشراء (تحديث 2026-08-31)
شكوى المالك: «تسلسل الأوامر حسب الأرقام مهم جدًا… تسبب لي بعض اللخبطة». **الجذر المؤكَّد بالكود والبيانات:**
(أ) الفرز الافتراضي كان `issue_date desc` فتظهر الأرقام مبعثرة؛ (ب) الفرز على `po_number` كان **نصّياً**
(`localeCompare`) فلا يرتّب رقمياً؛ (ج) **السجل يحمل صيغتين للسنة نفسها** — `P.O-DG2026-3101` (البذور)
و`P.O-DG26-3209` (الأحدث) — فالفرز النصّي يفصلهما لكتلتين وينكسر التسلسل؛ (د) `poNextNumber()` كانت تبني
البادئة من `getFullYear()` فتقترح `P.O-DG2026-####` على سجلّ يستخدم `DG26` = **بادئة ثالثة تكسر السلسلة**.
- **الأساس:** `poSeqParse(num)` (بادئة + digits + seq + pad، آخر مجموعة أرقام) · `poSeriesKey(prefix)`
  (يوحّد `20YY`→`YY` فتصير الصيغتان سلسلة واحدة) · `poNumCompare` (**التسلسل أولاً ثم البادئة** فلا تفرّقه
  صيغة السنة) · `poSeqSummary(list)` (نطاق/عدد/الأرقام المفقودة لكل سلسلة). **استعملها ولا تفرز `po_number` نصّياً.**
- **العرض:** الفرز الافتراضي صار `po_number desc` (السجل دفتر مرقّم) · `poNumCell` يعرض البادئة باهتة
  والتسلسل بارزاً · **صفوف فجوة** داخل الجدول تُظهر انقطاع التسلسل (تظهر فقط عند الفرز بالرقم وبلا أي
  تصفية، وإلا فالنقص متوقَّع) · **شريط التسلسل** `#po-seq-bar` (نطاق/عدد/عدّاد المفقود قابل للنقر →
  `poShowMissing` تسرد الأرقام) + دعوة للترتيب بالتسلسل إن كان الفرز على عمود آخر · مؤشّر `th.sorted`.
- **الإدخال:** `poNextNumber` تواصل بادئة **أعلى رقم قائم** بنفس التبطين (`padStart`) — لا تخترع بادئة.
- **الطباعة:** سجل أوامر الشراء صار مرتّباً بالتسلسل تنازلياً + كتلة «تسلسل الأرقام» (النطاق والمفقود).
- إصلاح مرافق: نقر مؤشّر KPI كان يمسح مفتاح `project` من `poFilter` ويترك المُنتقيات بقيمها القديمة.
- **تمييز الأوامر المُسلَّمة (قرار المالك عبر AskUserQuestion = «تمييزها بصرياً بوضوح» فقط — لا إخفاء ولا
  إنزال لأسفل):** صفّ `.po-row-done` (خلفية خضراء خفيفة + بقيّة الأعمدة بلون هادئ + تسلسل أخضر) + شارة
  `✓ مُسلَّم` بجانب الرقم + خلية التأخير تقول **«سُلّم في الموعد» / «سُلّم متأخراً …»** بصيغة الماضي بدل
  «في الموعد» الملتبسة (مع `title` بتاريخ الاستلام الفعلي). ومساعد `poDays(n)` لصيغة الأيام العربية
  الصحيحة (يوم واحد/يومان/3–10 أيام/11+ يوماً) مُطبَّق في خلية التأخير وتقرير المتأخرات.
- **الملغى (طلب المالك «الملغى أيضاً يجب أن يوضح»):** `po-row-cancelled` — خلفية رمادية مخطّطة + الرقم
  والقيمة **مشطوبان** + شارة `✕ ملغى` بتلميح يحمل **سبب الإلغاء ومن ألغاه ومتى** (`poCancelInfo` تقرأه من
  `status_history`، إذ صار السبب إلزامياً منذ تعديل `changePOStatus`) + لافتة في الدرج · وخلية التأخير
  للملغى تعرض «—» بدل «في الموعد» المضلّلة.
- **التسليم الجزئي (طلب المالك: «لون آخر + تنبيه للمتابعة والإقفال»):** أربع حالات صفّ متمايزة الآن —
  عادي (أبيض) · `po-row-partial` (**ذهبي**) · `po-row-done` (أخضر) · `po-row-cancelled` (رمادي مشطوب). شارة تقدّم `مستلَم/مطلوب` بشريط صغير
  بجانب الرقم (`poPartialInfo`) · شريحة «تسليم جزئي» في شريط الشرائح · **بطاقة إنذار ذهبية «📦 تسليم جزئي —
  بانتظار الإقفال»** + جدول متابعة في تبويب الإنذارات (المستلَم/المتبقّي/منذ آخر استلام مرتّباً بالأطول
  انتظاراً + زرّ «إكمال الاستلام» → `poReceiveOpen`) · لافتة في الدرج تشرح المتبقّي وأن إكمال الاستلام يُقفل
  الأمر تلقائياً. إصلاح مرافق: `poAlertJump` كان يمسح مفتاح `project` (نفس علّة KPI).
- الاختبار: 19 تأكيداً (قراءة الرقم · الفرز الطبيعي عبر الصيغتين · الفجوات · الرقم التالي · ترتيب التقرير)
  + معاينة متصفّح (الترتيب 3211→3199 مع فاصلَي فجوة، وشريط «3 رقم مفقود»، والمقترح `P.O-DG26-3212`،
  والحالات الثلاث للصفوف، وجدول متابعة التسليم الجزئي).

### 🚦 الترطيب (Hydration) + لوحة المهام عند الدخول (تحديث 2026-09-01)
**شكوى المالك:** «عند الدخول للنظام يأخذ عدة ثوانٍ إلى أن يتصل بالسحابة، فأول ما تدخل أرقام ومعلومات وبعد
الاتصال تتغيّر كل الأرقام». **الجذر:** `init()` يرسم من البذور/`localStorage` فوراً، ثم بعد **800ms مُتعمَّدة**
يُستدعى `tryAutoConnectCloud()` → `refreshFromCloud()` فيستبدل `STATE` ويعيد الرسم ⇒ قفزة أرقام أمام المستخدم.
- **الحل — قناع حتى وصول البيانات:** كائن `HYD` (`idle|pending|ready|offline`) + `hydBegin/hydSettle/hydIsPending`
  + `hydrateFromCloud()` تُستدعى **فوراً** بدل التأخير (عميل Supabase مُهيَّأ أصلاً في `bootstrap`).
  `body.data-loading` يُخفي القيم خلف **هيكل نابض** (CSS فقط، بلا لمس دوال الرسم): `.stat-value` · `.stat-meta` ·
  `.po-kpi-value` · `.po-hero .v` · `.nav-count` · `.seg-count` · `.tp-n`، ويُعتِّم `#kpi-section` و`.chart-card`.
  شريط علوي `#hyd-bar` يشرح «جارٍ تحميل أحدث البيانات…». **شبكة أمان 15 ثانية** ⇒ `offline` فتُكشف الأرقام
  المحلية مع تنبيه «تعذّر الاتصال — بيانات محلية». واتصال متأخر ناجح يرفع القناع من داخل `refreshFromCloud`.
  **⚠️ أي رقم جديد على لوحة التحكم: أضِف صنفه لقائمة القناع وإلا قفز عند الدخول.**
- **لوحة المهام `#tasks-panel`** (أعلى لوحة التحكم، تُرسَم من `renderDashboard`): تحيّة باسم المستخدم + بطاقات
  «ما يحتاج متابعتك» — متأخرة · تسليم جزئي بانتظار الإقفال · لدى المالية · بانتظار الاعتماد · **أوامر بلا جهة** ·
  متأخر بلا سبب · **فجوات التسلسل** · **تسجيلات موردين معلّقة** (صلاحية `can_review_registrations`) · **طلبات شراء
  بانتظار اعتمادك** (`prInbox`). كل بطاقة تنقل للشاشة المُصفّاة. العدّادات السحابية عبر `tasksLoadCloud()`
  (عدّ `proc_supplier_registrations` + `prLoadAll` المُخبَّأة) وتبقى `—` بهيكل نابض حتى تصل. صفر مهام ⇒ حالة
  «✓ لا يوجد ما يحتاج متابعتك». البطاقات ذات العدّ صفر تُخفى (قائمة مهام حقيقية لا لوحة إحصاءات).

### 🛡️ المرحلة 1 من خارطة التطوير — تثبيت الأرض (2026-09-01)
تشخيص تقني بالقياس والتشغيل الفعلي أنتج خارطة من 4 مراحل. **المرحلة 1 مُنفَّذة:**
- **(أ) دمج البذور كان معطّلاً صامتاً — حرِج:** `DATA` مُعرَّف بـ`const` فلا يوجد على `window`، و`refreshFromCloud`
  كان يقرأ `window.DATA?.items` ⇒ `undefined` ⇒ أساس الدمج **صفر دائماً**، فأي صنف/مورد/سجل محلي غير مرفوع
  للسحابة يختفي بلا إنذار. الآن ثابت `SEED` يشير لـ`DATA` مباشرةً (529 صنفاً · 42 مورداً · 808 سجلات مرئية للدمج).
  **⚠️ لا تقرأ البذور عبر `window.*` — يحرس ذلك اختبار بنيوي.**
- **(ب) 4 دوال بأسماء مكرّرة:** `saveUserItem`/`saveUserSupplier`/`saveUserEntry` كانت مُعرَّفة مرّتين والنسخة
  الأخيرة (في وحدة الاستيراد الذكي) تطغى: `push` بدل `unshift` (ينقلب ترتيب «الأحدث أولاً») و`catch{}` يبتلع
  فشل الحفظ. حُذفت المكرّرات وبقيت نسخة واحدة لكلٍّ. وحُذفت نسخة `renderDashboardCharts` الميتة (45 سطراً لا تُنفَّذ).
- **(ج) قفل متفائل على أوامر الشراء:** `poCloudUpsert` كانت آخر-كاتب-يفوز. الآن `po._sync` يحمل طابع النسخة
  السحابية (يُضبط في `refreshFromCloud` وبعد كل رفع ناجح)، وأي تعارض يُوقف الكتابة برسالة «عُدِّل الأمر… أعِد التحميل».
- **(د) `reportError(where, err, ctx, userMessage)`** — إبلاغ موحّد بديل عن كتل `catch` الفارغة (كانت 97):
  يسجّل دائماً في `ERR_LOG` والطرفية، ويُشعِر المستخدم عند مساس الخلل ببياناته. `procErrors()` للتشخيص.
  رُبِطت به المواضع الحرجة (حفظ محلي × 3 · حذف سحابي · تدقيق).
- **شبكة الأمان:** `scripts/system2-tests.mjs` — **75 تأكيداً بلا اعتماديات** (يستخرج دوال `index.html` ويشغّلها
  في صندوق بكعوب DOM): سلامة بنيوية (لا أسماء مكرّرة · لا `window.DATA`) · الحساب المشتقّ والضريبة والتأخير ·
  تسلسل الأرقام والفجوات · حالات الصفّ والجهة · صيغة الأيام · **هندسة كل جداول المطبوعات** (ترويسة/صفوف/تذييل) ·
  تجميع الجهات. + workflow `.github/workflows/system2-tests.yml` (فحص صياغة + الحزمة) على كل PR/دفع يمسّ
  `index.html`. `npm run test:system2`. **أي منطق حسابي أو عمود مطبوعة جديد ⇒ أضِف تأكيداً هنا.**
- **المتبقّي من الخارطة:** ③ تفكيك الملف تدريجياً + تزامن تفاضلي · ④ حوكمة ووصولية. الوثيقة منشورة كـArtifact.

### 💰 المرحلة 2 — إغلاق حلقة السعر (2026-09-03)
سعر بند أمر الشراء المعتمد أوثق سعر تملكه الشركة (نُفِّذ بمورد وتاريخ)، وكان **لا يعود أبداً** للسجل السعري
ولا يُحدِّث `last_price` (مُتحقَّق: صفر استدعاء داخل وحدة أوامر الشراء). الآن `poSyncPriceHistory(po)` يُستدعى
من `poSave` وينشئ ملاحظة سعرية لكل بند مؤهَّل.
- **القواعد:** لا تسجيل إلا من أمر بلغ «اعتماد مدير الشراء» فما بعد وغير ملغى (`poPriceEligible`) · المطابقة
  بالكتالوج **حتميّة ومحافِظة** عبر `siNormalize` القائم (تطابق تام ثم تساوي مجموعة الكلمات) · **الاسم الملتبس
  (يخصّ أكثر من صنف) لا يُطابَق تخميناً** — الكتالوج الحقيقي فيه 15 اسماً مكرّراً على 32 صنفاً · البند بلا مطابقة
  يُعَدّ ويُعرض في الدرج بسببه (`none` أو `ambiguous`) لا يُخمَّن.
- **التكرار والتراجع:** المفتاح `reference = 'PO:<رقم الأمر>#<ترتيب البند>'` — إعادة الحفظ تُحدِّث السجل نفسه،
  وحذف بند أو إلغاء الأمر أو حذفه **يسحب سجلاته** (سعر لم يُدفَع لا يبقى في الدفتر). السجلات اليدوية لا تُمَسّ.
- **بلا أي تغيير في المخطّط:** المرجع يُخزَّن في حقل `reference` القائم؛ و`recomputeItemStats` يشتقّ سعر الصنف
  من السجل أصلاً فتُحدَّث الأسعار والتقارير تلقائياً.
- **الواجهة:** لافتة في درج الأمر تُظهر كم سعراً غذّى السجل وما البنود التي لم تُسجَّل ولماذا.
- **الاختبار:** 24 تأكيداً جديداً في `scripts/system2-tests.mjs` (المجموع **99**) + تحقّق متصفّح على الكتالوج
  الحقيقي: السعر تحدَّث 1750→1805 والعروض 1→2، وإعادة الحفظ لا تُكرّر، والإلغاء يُعيد الحالة كما كانت.

### 🏗️ سجل المشاريع المعتمد — تعريف موحّد للجهة (2026-09-03)
**شكوى المالك:** «يجب أن يكون اختيار المشاريع اختياراً من متعدد أو أي حل يعالج الكتابة المختلفة لكل مشروع
والأخطاء الإملائية، ويصبح تعريف وتصنيف المشروع كمشروع آخر تماماً». **الجذر:** حقل «المشروع / جهة التنفيذ»
كان `<input list>` نصّاً حرّاً، و`poProjectList()` تشتقّ «المشاريع» من `DISTINCT po.project` — فكل كتابة
مختلفة («المحكمة الإدارية» / «محكمه الاداريه») أو خطأ إملائي («المحكمة الادارة») يصير مشروعاً مستقلاً في
المرشّح ولوحة التحكم وكل التقارير، ويستحيل معرفة قيمة مشروع واحد.
- **بلا أي تغيير في المخطّط (قرار مقصود):** `po.project` يبقى يحمل **الاسم المعتمد** فقط، فكل الجداول
  والمطبوعات والتقارير والتصدير القائمة تعمل كما هي دون تعديل. السجل نفسه في `proc_settings.projects_registry`
  (JSONB) + مرآة محلية `localStorage['proc_projects']` — لا جدول جديد ولا عمود جديد.
- **المحرّك (`index.html`، بعد `poProjectText`):** `prjNorm` تطبيع خاصّ بأسماء المشاريع (همزات/ياء/تاء مربوطة/
  أرقام هندية/تشكيل/أداة التعريف/كلمات عامّة كـ«مشروع»/رموز) — **لا يستعمل `siNormalize`** لأن مرادفاتها
  مخصّصة لوحدات الأصناف وتشوّه الأسماء · `prjSim` = الأعلى بين Dice على الثنائيات ونسبة Levenshtein
  (عتبتان: `PRJ_SIM_STRONG=0.86` اقتراح، `PRJ_SIM_WEAK=0.70` مرشّح) · **`prjResolve` بثلاث حالات:
  `ok` (اسم/مرادف/تطابق بعد التطبيع = قاطع يُطبَّق تلقائياً) · `suggest` (تشابه قويّ = **لا يُطبَّق أبداً
  بلا تأكيد بشريّ**) · `none`** · `prjAdd/prjAddAlias/prjRename/prjMergeProjects/prjDelete`
  (`prjRewriteOrders` هو الأساس الوحيد لتغيير `po.project`).
- **البذرة الأولى (`prjSeedFromOrders`, تُستدعى من `prjLoad` عند سجلّ فارغ):** تجمع الكتابات **المتطابقة بعد
  التطبيع** فقط (تطابق قاطع لا تخمين)؛ الاسم المعتمد = الأكثر وروداً، والبقيّة مرادفات. **لا تمسّ أي أمر شراء.**
- **شاشة «المشاريع» (زر في رأس أوامر الشراء) بتبويبين:** (1) *المشاريع* — قائمة/إضافة/إعادة تسمية (تُعيد كتابة
  كل الأوامر وتحفظ الاسم القديم مرادفاً)/مرادفات/دمج/تعطيل/حذف (ممنوع للمستعمَل) + عدد الأوامر والقيمة.
  (2) *توحيد الكتابات* — جدولان: **كتابات في الأوامر خارج السجل** (المؤكَّدة محدَّدة مسبقاً، المشتبهة تحتاج
  مراجعة) و**مشاريع متشابهة داخل السجل** (`prjDuplicatePairs` — يسدّ ثغرة أن الخطأ الإملائي المسجَّل مشروعاً
  يبدو معتمداً فلا يظهر في التوحيد؛ **لا شيء محدَّد مسبقاً**). التطبيق يعيد كتابة الأوامر ويحفظ المرادف.
- **الإدخال:** حقل النموذج صار `<select>` من السجل + «➕ مشروع جديد…» بتلميح حيّ (`prjTypeHint`) يكشف المشابه
  قبل الإنشاء، وحوار «هل تقصد…؟» عند الحفظ (`poReadProjectField`). ودرج الأمر يفتح مُنتقياً (`modal-po-project`)
  بدل `prompt` النصّي. **القيمة غير المعتمدة تبقى كخيار مؤقّت موسوم «(غير معتمد)» فلا يفقدها التعديل.**
- **الاستيراد (`poImportResolveProjects`):** المطابقة القاطعة تُطبَّق تلقائياً على كل صفّ مستورَد (يظهر عدّاد
  «جهة وُحّدت» في المعاينة)، والمشتبه/المجهول **تنبيه فقط** يُحسم من شاشة التوحيد — لا تخمين صامت.
- **العرض:** `poProjectCell` يسم الكتابة غير المعتمدة بـ⚠ · `poProjectList` = أسماء السجل ∪ المستعمل في الأوامر
  (فلا تختفي أوامر من المرشّح) · البحث واعٍ بالمرادفات (كتابة قديمة تجد أوامر المشروع باسمه المعتمد) ·
  بطاقة «كتابات مشاريع تحتاج توحيد» في لوحة المهام.
- **إصلاح الأوامر القائمة تلقائياً (`prjAutoUnify`, يُستدعى من `refreshFromCloud` بعد `prjLoad`):** كل كتابة
  تُحلّ **قطعاً** تُعاد كتابتها بالاسم المعتمد على الأوامر الموجودة بلا تدخّل (بصلاحية `can_edit_po`، مع تدقيق
  وتوست). المشتبه به والمجهول **لا يُمسّان**. إجرائيّاً مرّة واحدة: بعده لا يبقى ما يُعاد كتابته فلا كتابة
  سحابية متكرّرة (`prjRewriteOrders` يتخطّى ما قيمته الهدف أصلاً).
- **درسان من اختبار الإجهاد (كلاهما كان عيباً حقيقياً وأُصلح):**
  (أ) **المسافات الزائدة كانت تنجو:** `prjPendingUnify` كان يُشذّب القيمة قبل المقارنة فيَعُدّ `"  اسم  "` معتمداً
  بينما القيمة **المخزَّنة** تبقى مجموعة منفصلة في كل تجميع يقرأ `po.project`. الآن المفتاح هو القيمة كما هي
  مخزَّنة، و`prjRewriteOrders` يطابق القيمة الأصلية والمشذّبة معاً، و`prjClean` يُنظّف الاسم المعتمد عند الإنشاء/البذرة.
  (ب) **الأرقام كانت تُعامَل كخطأ إملائي:** «فرع الرياض 3» و«فرع الرياض 4» تشابههما 90% فظهرا مرشّحَي دمج.
  الآن `prjSim` يكسر التشابه عند اختلاف الأرقام: أرقام مختلفة على الجانبين ⇒ دون عتبة الترشيح؛ رقم على جانب
  واحد ⇒ لا يُقترح تلقائياً ويبقى مرشّحاً مرئيّاً.
- **الاختبار:** 67 تأكيداً جديداً في `scripts/system2-tests.mjs` (المجموع **166**) — تطبيع/تشابه/أرقام مميِّزة/
  حلّ ثلاثيّ/منع المكرّر/المرادفات المحجوزة/البذرة/التوحيد التلقائي/المسافات الزائدة/إعادة التسمية/الدمج/
  الأزواج المتشابهة/الاستيراد/العرض/البحث.
  + تحقّق متصفّح فعليّ (Chromium): البذرة، تبويب التوحيد بجدوليه، التطبيق (3 أوامر توحّدت وpending=0)،
  مُنتقي النموذج والتلميح الحيّ، ومُنتقي الدرج.
  + **اختبار إجهاد بأسماء واقعية:** 25 كتابة لـ7 مشاريع ⇒ **7 مشاريع و7 قيم مخزَّنة، 17 أمراً أُصلح تلقائياً،
  صفر متبقٍّ وصفر زوج كاذب**؛ وسلبيات صعبة (الرياض 3/4 · المعالجة/التحلية · جدة/الدمام · الشمال/الجنوب)
  **صفر خلط**. ⚠️ تعذّرت قراءة أوامر الإنتاج للتحقّق عليها: RLS يحجب المفتاح العام، والتطبيق يتصل بمفتاح
  يُدخله المالك في إعدادات السحابة ويُحفظ في متصفّحه.
- **⚠️ قاعدة:** لا تقرأ `po.project` مباشرةً في عرض جديد — استعمل `poProjectCell/poProjectText`، وأي تغيير
  لقيمة الجهة يمرّ عبر `prjRewriteOrders` أو `poReadProjectField` كي يبقى الاسم معتمداً.

#### 🎯 الكاسر الثاني — «الكلمة المميِّزة» (بلاغ كاذب حقيقيّ من الإنتاج، 2026-09-07)
**بلاغ المالك بلقطة شاشة:** تبويب «توحيد الكتابات» يعرض **8 «مشاريع متشابهة داخل السجل»** بنسب 87–91%،
وكلّها في الحقيقة **مطارات مختلفة** (الطائف · حائل · جدة · الجوف · تبوك · أبها · الرياض · الدمام · القصيم)
تشترك في السابقة الطويلة «المباني الجمركية بمطار» ولا تختلف إلا في الكلمة الأخيرة.
- **الجذر:** كان في `prjSim` كاسر **للأرقام** فقط (سابقة «فرع الرياض 3/4»)، ولا كاسر **للكلمات** — فاختلاف
  اسم مدينة كامل يُقرأ خطأً إملائيّاً لأن Dice/Levenshtein يقيسان النصّ كلّه لا الكلمة الفارقة.
- **الحلّ:** دالتان جديدتان `prjRawSim(x,y)` (تشابه خام بلا كواسر — تمنع التكرار اللانهائي عند مقارنة كلمة
  بكلمة) و`prjDistinctWords(x,y)` (فرق مجموعات يحترم التكرار)، ثم في `prjSim`: **إن تطابق الاسمان في كل
  كلماتهما عدا واحدة، وكانت الكلمتان الفارقتان غير متشابهتين ببعضهما (`prjRawSim < PRJ_SIM_WEAK`) ⇒ كيانان
  متمايزان** (يُخفَّض دون عتبة الترشيح). وكلمة زائدة على جانب واحد ⇒ لا تُقترح تلقائياً وتبقى مرشّحاً مرئيّاً.
- **القياس هو ما حدّد العتبة (لا تخمين):** مدن مختلفة **≤50%** (طاءف×حاءل=50 · جده×جوف=33 · ابها×تبوك=25 ·
  رياض×دمام=25 · معالجه×تحليه=33) مقابل **خطأ إملائي بحرف =75%** (دمام×دمم · شمال×شمل · رياض×ريان) —
  فـ`PRJ_SIM_WEAK=0.70` تفصلهما بهامش مريح.
- **⚠️ الشقّ المقابل محروس بالقوّة نفسها:** الخطأ الإملائي الحقيقي **يبقى مكشوفاً** (91%/88%/88%). أي تشديد
  لاحق للكاسر يجب أن يُبقي هذه الثلاثة فوق `PRJ_SIM_STRONG` — وإلا فشل البناء.
- **الاختبار:** 13 تأكيداً جديداً (المجموع **380**) — 5 أزواج مطارات + التحلية/المعالجة + 3 أخطاء إملائية
  تبقى مقترحة + الكلمة الزائدة + `prjDistinctWords` + `prjRawSim` + `prjDuplicatePairs` على سجلّ 9 مطارات.
  **مُتحقَّق أن الحرّاس تعضّ فعلاً:** إزالة الكاسر عمداً ⇒ **6 إخفاقات**. + تحقّق متصفّح على الشاشة نفسها:
  سجلّ 11 مشروعاً (9 مطارات + زوج بخطأ إملائي) ⇒ **الشارة 1 بدل 8**، وصفوف المطارات اختفت، وصفّ
  «مستودع الدمم × مستودع الدمام 91%» باقٍ.
- **ملاحظة تشغيلية:** لا شيء كان قد دُمِج — الجدول لا يحدّد شيئاً مسبقاً («القرار لك»)، فبيانات الإنتاج سليمة.

### 🖨️ تقارير بعدّة مشاريع/حالات + إصلاح بلاغ الفجوات الكاذب (2026-09-04)
**طلب المالك:** «في تقرير أوامر الشراء يمكن اختيار أكثر من مشروع لطباعة حالة الأوامر، وليس مشروعاً أو حالة
واحدة فقط». كانت حقول التقرير `<select>` مفردة، فطباعة حالة ثلاثة مشاريع = ثلاث مطبوعات منفصلة لا تُجمَع.
- **نوع حقل جديد `multiselect` في `reportsCatalog()`:** `{type:'multiselect', key, label, allLabel, opts:[{v,l}]}`
  يُرسَم بـ`repMultiHtml` (زرّ + لوحة مربّعات اختيار + «تحديد الكل»/«مسح»، ونصّ الزرّ يقول «الكل» أو الاسم أو
  «N مختارة»). `reportRun` يقرأ حقل `[data-rep-multi]` **كمصفوفة** وبقيّة الحقول كنصّ.
- **مرشّحات موحّدة تقبل الواحد والمتعدّد:** `repList(v)` (نصّ أو مصفوفة أو فارغ ⇒ قائمة) · `repFilterProjects(list,sel)`
  (يدعم عدّة مشاريع و`__none__` معاً) · `repDescList(label,sel)` (وصف مطبوع يسمّي حتى 4 ثم يختصر).
  **التوافق الخلفي محفوظ:** تمرير نصّ واحد يعمل كما كان.
- **المشمولة:** سجل أوامر الشراء التفصيلي (حالة + مشروع + قطاع، كلها متعدّدة) · تقرير المتأخرات والتصعيد
  (مشروع متعدّد — كان بلا مرشّحات) · الأوامر لدى الإدارة المالية (مشروع متعدّد).
- **🐛 عيب حقيقي أُصلح:** كتلة «تسلسل الأرقام» كانت تُحسب على القائمة **بعد** التصفية، فتُعلن أمراً استبعده
  المرشّح «غير مسجّل» وهو مسجَّل فعلاً — بلاغ كاذب يربك مراجعة الدفتر (قائم منذ إضافة الكتلة، ويطال التصفية
  بالحالة والقطاع والتاريخ لا المشروع فقط). الآن الفجوات **لا تُعلَن إلا على تقرير غير مُصفّى**؛ والمُصفّى يعرض
  النطاق والعدد مع «(التقرير مُصفّى — لا تُحتسب الفجوات على قائمة مُصفّاة)». نفس قاعدة صفوف الفجوة في القائمة.
- **الاختبار:** 27 تأكيداً جديداً (المجموع **193**) + تحقّق متصفّح: المُنتقي يفتح ويعرض المشاريع، الزرّ يقول
  «2 مختارة»، والمطبوعة تحمل «الجهة: برج الشمال + فرع الرياض 3» وتطبع أمرَيهما فقط بلا فجوات كاذبة.

### ⚡ المرحلة ③ — المزامنة التفاضلية + إنهاء تكرار الأصول (2026-09-04)
**المشكلة المقيسة (أ) — كل حدث لحظي كان يُعيد تحميل كل شيء:** `handleRealtimeChange` كانت تستدعي
`refreshFromCloud()` لأي حدث، وهي تجلب **كل صفوف أربعة جداول** (`proc_items`/`proc_suppliers`/
`proc_history`/`proc_purchase_orders`) بترقيم 1000 صفّ. حفظ زميلٍ لأمر واحد ⇒ إعادة تحميل كاملة في كل
متصفّح مفتوح — ومع حلقة السعر (المرحلة 2) صار كل بند معتمَد يولّد حدثاً.
- **الحل — تطبيق الصفّ من الحمولة نفسها بلا أي طلب شبكة:** `RT_MAP` (جدول → مصفوفة STATE + مفاتيح
  المطابقة + وسوم الصفّ) · `rtIndexOf` (يطابق بأول مفتاح تحمله الحمولة — المورد `id` ثم `name` فتغيير
  الاسم تعديلٌ لا صفّ جديد) · `rtApply(table,payload)` يُرجع `true` إن طُبِّق و`false` إن تعذّر.
  أوامر الشراء تمرّ بـ`recomputePOderived` ويُضبط `_sync` تماماً كما يفعل التحميل الكامل.
- **⚠️ الحذف يسقط للتحميل الكامل عمداً:** التحميل الكامل يُعيد إظهار سجلّ البذرة الذي كان الصفّ السحابي
  يحجبه ويُعيد تطبيق قوائم «المخفي»؛ تقليد ذلك تفاضليّاً تخمين. الحذف نادر والمكسب كلّه في الإضافة والتعديل.
  وكذلك: حدث مجهول · جدول غير معروف · حمولة بلا مفتاح ⇒ **كلها تسقط للسلوك القديم**، لا تخمين.
- **طابور الدفعة `RT_QUEUE`:** الخنق (debounce) كان يُسقِط ما قبل الحدث الأخير لأن التحميل الكامل يُغني
  عنه؛ مع التطبيق التفاضلي لا بدّ من تطبيقها كلّها — فتُجمَّع وتُطبَّق دفعةً في `rtDrain`. **أي حدث في
  الدفعة يتعذّر ⇒ الدفعة كلّها تسقط للتحميل الكامل** (لا حالة نصفية).
- `rtRerender()` مصدر واحد لإعادة رسم الشاشة الحالية — يستعمله التحميل الكامل والتفاضلي معاً.
- **تحقّق متصفّح فعليّ:** حدث واحد ⇒ صفّ مُطبَّق و**صفر تحميل كامل** والإجمالي 1150 صحيح · ثلاثة أحداث
  متلاحقة ⇒ الثلاثة مُطبَّقة (8001 حُدِّث إلى 5750، و8002/8003 أُضيفا) و**صفر تحميل كامل** · صدى الكتابة
  الذاتية مكبوح · **الحذف يسقط للتحميل الكامل (1)** · والصفّ يظهر فعلاً في الجدول.

**المشكلة المقيسة (ب) — أصول ضخمة مضمَّنة مرّتين:** قياس أكبر الأسطر كشف أنّ **شعار الشركة مضمَّن بالكامل
مرّتين** (شاشة الدخول + `DEFAULT_COMPANY_LOGO`) = 221KB × 2، **وعلامة الشريط الجانبي مرّتين** = 26KB × 2.
- **الحل — مصدر واحد:** كتلة مبكّرة تُعرّف `LOGO_B64` و`MARK_B64` مرّة واحدة، والوسوم تأخذ مصدرها منها
  بسكربت **مباشرةً بعد كل وسم فلا ومضة قبل الرسم**، و`DEFAULT_COMPANY_LOGO = LOGO_B64` (فمسار الطباعة/PDF
  يبقى data-URI مضمَّناً بلا أي تغيير في سلوكه). مُتحقَّق أنّ `const` في كتلة سكربت سابقة مرئيّ للاحقة.
- **الأثر: 2,658,651 ← 2,416,447 بايت = 242,204 بايت (9.1%) أقلّ في كل فتح للصفحة.**
- **حارس دائم في الحزمة:** «لا أصل base64 ضخم (≥5000 محرف) مضمَّن أكثر من مرّة» — وهو الذي كشف تكرار
  علامة الشريط الجانبي في أوّل تشغيل له. **أي أصل جديد تُضمّنه: عرّفه مرّة واحدة.**
- **تحقّق بصري:** شعار الدخول (1299×423) والعلامتان في الشريط الجانبي وصورة ترويسة المطبوعة — كلها محمَّلة.
- **الاختبار:** 24 تأكيداً جديداً (المجموع **217**) — تطبيق الإضافة/التعديل · الأرقام المشتقّة و`_sync` ·
  مطابقة الصنف بالكود والمورد بالمعرّف ثم الاسم · السجل برقمه · **كل حالات السقوط الآمن** · `rtIndexOf` ·
  تغطية الخريطة · وحارس تكرار الأصول.

### 🛡️ المرحلة ④ — حوكمة ووصولية (2026-09-04)
**⚠️ تصحيح رقم سبق أن ذُكر في هذا الملف/الجلسات:** «108 أزرار برموز بلا تسميات» **رقم خاطئ**. القياس الفعلي
على الـDOM الحيّ عبر ثماني شاشات: **زرّان اثنان فقط** (`#ai-close` و`#ai-send`). الفجوة الحقيقية كانت في
**الحقول والنوافذ** لا الأزرار.

**(أ) تقرير سجل التدقيق كان يعكس جهازاً واحداً — وهو أسوأ من غيابه:** `logAudit` يدفع كل إجراء إلى
`proc_audit_log` السحابي **وأيضاً** إلى `proc_audit` المحلي (سقف 500)، وعارض التدقيق فيه تبويب سحابي يعمل —
لكن **التقرير المطبوع** كان يقرأ المحلي وحده. أي دليل حوكميّ مطبوع يمثّل جهاز من طبعه فقط، ويوحي بالاكتمال.
- `auditFetchForReport(opts)` جديدة: السحابة مصدراً متى توفّرت (حتى 1000 إجراء، **والتصفية تُمرَّر للخادم**
  عبر `eq/gte/lte` لا بعد الجلب)، وتسقط للمحلي عند تعذّر الاتصال. `buildAuditReport` صارت `async`
  (و`reportRun` يدعم اللاتزامن أصلاً).
- **السقوط مُعلَن لا صامت:** كتلة `print-note-box` تقول «السجل السحابي — كل المستخدمين وكل الأجهزة»، أو
  بصيغة **danger**: «⚠️ تعذّر الوصول للسجل السحابي… لا يصلح دليلاً حوكميّاً شاملاً». والعنوان الفرعي يتبعها.

**(ب) الوصولية — القياس ثم الإصلاح بممرّ واحد:** على الـDOM الحيّ: **49 من 116 حقلاً** بلا اسم مقروء (أغلبها
`<select>` فلا `placeholder` لها) · **15 من 18 نافذة** بلا `role="dialog"`/`aria-labelledby` · زرّان.
- **الحلّ `a11yWire(root)`** — ممرّ واحد يشتقّ الاسم من **التسمية المرئية المجاورة** بدل 49 تعديلاً يدويّاً:
  حقل بلا اسم ⇒ نصّ `.form-label` داخل حاويته · **قائمة شريط أدوات بلا تسمية مرئية ⇒ نصّ خيارها الأوّل**
  («كل الفئات» · «الأحدث أولاً») وهو ما يراه المبصر نفسه · نافذة ⇒ `role/aria-modal/aria-labelledby` مربوط
  بـ`.modal-title` · زرّ أيقونة ⇒ `data-tooltip`. **لا يغيّر أي شيء مرئي.**
- **يُستدعى من ثلاث نقاط:** الإقلاع (`startApp`) · **بعد كل تنقّل** (الشاشات تُرسَم بـ`innerHTML` فتفقد
  السمات) · وعند `openModal` مُنطاقاً بالنافذة. **الميزة: يغطّي المولَّد وقت التشغيل لا الثابت وحده.**
- **النتيجة المقيسة: 49 ← 0 حقلاً · 15 ← 0 نافذة · 16 ← 0 زرّاً.** (`#pp-select` و`si-file-input` سُمّيا
  صراحةً في HTML لأنهما بلا تسمية مجاورة.)

**(ج) مراجعة الصلاحيات — نظيفة، فلا إصلاح:** 21 مفتاحاً في الكتالوج · **بلا تكرار** · **كلها مُنفَّذة** فعلاً
(20 في الكود + 10 في `data-requires-perm`) · **ولا بوّابة تستعمل مفتاحاً غير معرَّف**. النتيجة سُجِّلت حارساً
دائماً بدل ادّعاء عمل لم يلزم.

- **الحرّاس الجدد (بنيوية، المجموع 221 تأكيداً):** لا مفتاح صلاحية بلا إنفاذ · لا بوّابة بمفتاح غير معرَّف ·
  لا مفتاح مكرّر · **ممرّ الوصولية مربوط بالنقاط الثلاث** (إزالة أيّ منها تُعيد الحقول بلا اسم بصمت).
- **تحقّق متصفّح:** تقرير التدقيق السحابي يعرض ثلاثة مستخدمين من أجهزة مختلفة وعنوانه «كل المستخدمين»،
  والتصفية وصلت الخادم (`action`/`entity_type`/`gte_ts`/`lte_ts`)، والسقوط المحلي يحذّر صراحةً.

### 🔒 تقرير متابعة ميدانية بلا مبالغ — لموظفي الصيانة (2026-09-06)
**طلب المالك:** «اصنع نوعاً من التقارير لمتابعة أوامر الشراء نفس المرفق بدون مبالغ، فقط معلومات، لتسليمه
لموظفي الصيانة للمتابعة دون كشف المبالغ». تقارير أوامر الشراء كلها تحمل الإجماليات، فتسليم ورقة متابعة
لموظف ميداني كان يكشف أسعار الموردين وقيم العقود.
- **`buildPOFollowupReport(opts)`** (قبل `buildPOOverdueReport`) + مساعدَين: `poFollowDelivery(po)` (حالة
  التسليم بصيغة الماضي للمُسلَّم والمستقبل لغيره، والملغى يُوسم ملغى لا «في الموعد») و`poFollowReceived(po)`
  (**نسبة الاستلام بالكميّات لا بالقيم**؛ الأمر بلا بنود مسجَّلة يعرض «—» لا «0%» المضلّلة).
- **المحتوى:** 4 بطاقات بالعدد فقط · لافتة تُعلن أن التقرير بلا مبالغ · لافتة تنبيه للتسليم الجزئي بانتظار
  الإقفال · جدول الأوامر بـ8 أعمدة (رقم · الجهة · المورد · المرحلة · الإصدار · التسليم المتوقع · حالة
  التسليم · المستلَم/المطلوب) · **جدول «البنود المنتظَر استلامها»** (المطلوب/المستلَم/المتبقّي — جوهر
  المتابعة الميدانية؛ يستثني المُقفل والملغى، بسقف 120 بنداً) · التوزيع حسب الجهة **بالعدد** لا القيمة.
- **المرشّحات نفسها التي في «سجل أوامر الشراء التفصيلي»** (حالة/جهة/قطاع متعدّدة عبر `repList`/
  `repFilterProjects`/`repDescList` + من/إلى تاريخ)، والترتيب بالتسلسل الرقمي عبر `poNumCompare`.
- **⚠️ قاعدة مثبَّتة بالحزمة:** لا عمود ولا سطر في هذا التقرير يحمل مبلغاً. الحارس بشقّين — **حارس مصدر**
  (متن الدوال الثلاث خالٍ من `fmtPrice`/`.total`/`.subtotal`/`.vat`/`.price`/«ر.س») و**حارس مُخرَج**
  (يولّد التقرير من أوامر بمبالغ مميّزة ويؤكّد أن أيّاً منها — ولا الإجمالي المحسوب ولا وحدة العملة ولا
  خلية `class="price"` — لا يظهر في المستند). أي إضافة تُسرّب رقماً مالياً **تُفشِل البناء**.
- **الاختبار:** 25 تأكيداً جديداً (المجموع **246**) — السرّية · المحتوى المفيد · صيغ حالة التسليم ·
  المرشّحات الثلاثة · وجود البطاقة في الكتالوج بمرشّحاتها. + تحقّق متصفّح (Chromium): المعاينة تفتح،
  والجداول الثلاثة تُطبع، و**صفر تسريب** لأيّ من المبالغ الثلاثة ولا «ر.س».

### 🗂️ ملفات الموردين تبقى في النظام + عارض مستندات منبثق (2026-09-06)
**طلب المالك:** «بيانات وملفات الموردين الذين يقومون بالتسجيل تُحفظ وتبقى في النظام، ولا حاجة إلى حفظها في
الخارج بعد الآن؛ يمكن فتح المستندات كتبويب منبثق في النظام».
**الجذر (ثلاث علل حقيقية):**
1. وثائق التسجيل كانت تُفتح بـ`target="_blank"` على **رابط تخزين Supabase موقّت** — أي مغادرة النظام لتبويب
   متصفّح خارجي، فصار العُرف تنزيلها ZIP وحفظها على جهاز/محرك خارجي.
2. **`archiveAndClean()` كانت تُصدّر الطلبات المعالَجة إلى Excel ثمّ تحذف صفوفها** «لتحرير المساحة» — فتيتّم
   وثائقها في المخزن وتقطع رابط بطاقة المورد ببيانات تسجيله نهائياً. هذه هي «الحفظ في الخارج» حرفيّاً.
3. `proc_suppliers` لا يحمل `doc_paths`، فالوصول للوثائق بعد الاعتماد يمرّ حصراً عبر صفّ الطلب — وحذفه يفقدها.
- **عارض المستندات (`#modal-doc-viewer` + دوال `docv*`):** نافذة بشريط **تبويبات، تبويب لكل مستند** + عدّاد +
  تنقّل (أزرار و◀▶ بلوحة المفاتيح، وEsc للإغلاق) + طباعة + تنزيل اختياري + تنزيل الكل ZIP. الملف يُجلب برابط
  موقّع (5 دقائق) **ثم يُعرَض من `blob:` محلّي**: PDF في `<iframe>` وصورة في `<img>`، وصيغة أخرى ⇒ رسالة واضحة
  مع زرّ تنزيل. الـblob مُخبّأ لكل مسار فتبديل التبويبات فوريّ، ويُبطَل كلّه عند الإغلاق.
  **قيد CSP مُلزِم (`_headers`): `frame-src 'self' blob:` و`img-src … blob:` مسموحتان و`object-src 'none'` —
  فـPDF في `<iframe>` لا `<object>`/`<embed>`.**
- **نقطتا الدخول:** شاشة مراجعة الطلب (كل شارة وثيقة + زرّ «فتح المستندات في النظام») · **بطاقة المورد المعتمد**
  (زرّ «فتح المستندات (N) في النظام»، وبقي «عرض الطلب كاملاً»).
- **⚠️ درس مثبَّت بتأكيد:** النافذة **يجب أن تكون خارج أي `<section class="page">`** — القسم غير النشط
  `display:none` فيُخفي كل ما بداخله **حتى `position:fixed`**. أُصيب العارض بذلك فعلاً (وُضِع داخل
  `#page-registrations`) فلم يفتح من صفحة الموردين رغم `display:flex`. مُلتقَط بمعاينة المتصفّح ومحروس الآن.
- **حُذفت `archiveAndClean()` وزرّها** (زرّ «تصدير Excel» الاختياري باقٍ بلا أي حذف)، وحُذفت `downloadNamedDoc`
  التي صارت ميتة. **لا مسار في النظام يحذف صفّ طلب تسجيل ولا وثيقة من مخزنه.**
- **الاختبار:** 7 تأكيدات جديدة (المجموع **255**) — لا حذف لصفوف الطلبات · لا حذف لوثائقها من المخزن · لا فتح
  في تبويب خارجي على رابط التخزين · العارض موجود ومربوط بنقطتَي الدخول · يستعمل blob لا رابطاً خارجياً · لا
  object/embed · النافذة خارج أقسام الصفحات. + معاينة متصفّح: PDF وصورة من `blob:`، الصيغة غير المدعومة تسقط
  للتنزيل، التبويبات والعدّاد والتنقّل بلوحة المفاتيح، والفتح **من صفحة الموردين** (ما كان مستحيلاً قبل النقل).
- **📌 بند مرصود لم يُمَسّ (خارج نطاق الطلب):** `rfqDownloadQuoteFile` تُنزّل ملف عرض سعر المورد **ثمّ تحذفه من
  المخزن** «لتوفير المساحة» — نفس نمط «احفظه بالخارج» لكن في مسار طلبات التسعير لا التسجيل. يحتاج قرار المالك.

### 👥 الموردون: ضبط العارض + بحث عربي متسامح + سعة ألف مورد (2026-09-06)
**طلب المالك:** «تأكّد من ضبطه 100% كخبير تقني، وحسّن نظام عرض الموردين ومتابعتهم والبحث عنهم وتسجيلهم،
وتأكّد أنّ الحفظ داخل النظام يكفي لألف مورد».

**(أ) تدقيق عارض المستندات — ثلاثة إصلاحات حقيقية:**
- `docvFromReg` صار **بالفهرس لا بمفتاح نصّيّ**: مفتاح يحمل علامة اقتباس يكسر سمة `onclick`
  (`escapeHtml` يحوّل `'` إلى `&#39;` فيفكّها محلّل HTML قبل أن يقرأها JS).
- **سقف لذاكرة الـblob** (آخر 4 مستندات): 13 نوعاً × 10MB كانت تبقى كلّها حيّة حتى الإغلاق.
- `docvWho` مصدر واحد لاسم المورد بدل تكراره في نقطتَي الدخول.

**(ب) شاشة التسجيلات — كانت تنهار عند ألف مورد (ثلاث علل مقيسة):**
1. `select('*')` بسقف **100 صفّ** بلا بحث ولا مؤشّر للمجموع ⇒ صفوف قديمة تختفي **صامتةً** ولا سبيل
   للعثور على مورد بعينه. الآن: **بحث على الخادم** (`ilike` على 9 حقول: الاسم عربي/إنجليزي/تجاري ·
   السجل التجاري · الضريبي · الهاتف · الجوال · البريد · المدينة) + **ترقيم `range`** (50/صفحة) +
   **مجموع دقيق `count:'exact'`** + شريط «عرض 101–150 من 1000». `regSearchSafe` يُعقّم محارف
   PostgREST الخاصة (`, ( ) * \`) وإلّا انكسر تعبير `or(...)`.
2. **حقن JSON الصفّ كاملاً في سمة `onclick`** لكل سطر ⇒ DOM ضخم وكسر عند وجود اقتباسات في البيانات.
   الآن `openRegById(id)` يجلب الصفّ عند الحاجة، والقائمة تجلب **أعمدتها فقط** (`REG_LIST_COLS`)
   لا `*` (بلا `products_services`/`metrics`).
3. **عدّ الحالات كان بجلب 2000 صفّ** (يسقط صامتاً فوقها). الآن 5 استعلامات `count:'exact', head:true`
   بلا صفوف، ومنها تُضبط شارة القائمة الجانبية بالعدّ الحقيقي. ومؤشّرات الكفاءة من **عيّنة 300** حديثة.
4. `regFetchAll` (صفحات 500) للتصدير والتقرير — **سقف PostgREST الافتراضي 1000 صفّ** كان يبتر
   `exportRegistrations` صامتاً، و`buildRegistrationsReport` كان بسقف 200.
**⚠️ قاعدة: أي استعلام جديد على `proc_supplier_registrations` يمرّ بـ`range`/`regFetchAll` — لا `limit` عارٍ.**

**(ج) دليل الموردين — بحث ذكيّ وسعة:**
- **`arNorm`** تطبيع عربي عامّ (همزات/ياء/تاء مربوطة/تشكيل/تطويل/أرقام هندية) — «احمد» صار يجد «أحمد».
  **⚠️ لا تستعمل `siNormalize` هنا** (مرادفاتها لوحدات الأصناف فتشوّه الأسماء) **ولا `prjNorm`** (تُسقِط
  كلمات المشاريع). ثلاث دوال تطبيع لثلاثة نطاقات — مقصود.
- **`supPhoneKeys`/`supQueryDigits`:** الرقم السعودي يُكتب `+966 13…` و`013…` و`13…` — نخزّن الأرقام
  **وآخر تسعة** ونختزل الاستعلام إليها فتتطابق الصيغ الثلاث. و`supMatches` **يُسقِط المصطلح الرقميّ
  الخالص من مطابقة النصّ** (وإلّا لم يجد «0138576550» الرقم المخزَّن `+966 13-857-6550` — عيب حقيقي كشفه الاختبار).
- البحث يشمل الآن: الاسم · التخصص · جهة الاتصال · البريد · العنوان · المدينة · **السجل التجاري** ·
  **الرقم الضريبي** · الملاحظات · الهاتف · الجوال (كلمات متعدّدة بأي ترتيب).
- **عرض على دفعات** (`SUP_PAGE=60` + «عرض المزيد») — ألف بطاقة دفعةً كانت تُجمّد الصفحة · **عدّاد**
  «عرض 60 من 1000» · **فرز** (الأكثر عروضاً/الاسم/الأحدث) · **مُنتقي التخصص يُعاد بناؤه** مع تغيّر
  القائمة (كان يُبنى مرّة فيتقادم بعد المزامنة).
- **⚠️ فخّ مُصلَح:** `addEventListener('input', renderSuppliers)` كان يمرّر الحدث كمعامل `more`
  (نفس سابقة `openCmdPalette`). صار `()=>renderSuppliers()` ومحروساً بتأكيد.

**(د) متابعة الموردين — `supExpiryStrip`:** تواريخ انتهاء السجل التجاري وعضوية الغرفة كانت مخزَّنة في
بيانات التسجيل **ولا تُعرض في أي مكان**، فمورد بسجل منتهٍ يُتعامَل معه كأنه سارٍ. الآن شريط في بطاقة
المورد: **منتهٍ (أحمر) · ينتهي خلال ≤30 يوماً (أصفر) · سارٍ (أخضر)** بصيغة الأيام العربية عبر `poDays`.

**(هـ) سعة ألف مورد — قياس فعليّ لا تخمين (`regMeasureStorage` + لوحة «سعة التخزين»):**
تقيس **عيّنة حقيقية** من مجلّدات المخزن (`metadata.size` لكل ملف)، تحسب متوسّط المورد الواحد، وتُسقِطه
على ألف مورد وتقارنه بخطط Supabase. **النتيجة المقيسة على سيناريو واقعي (6 وثائق إلزامية × 1.5MB):**
**≈9 MB للمورد ⇒ ~8.8 GB لألف مورد.** ⇒ **خطة Free (1 GB) تتّسع لنحو 113 مورداً فقط — لا تكفي؛
وخطة Pro (100 GB) تتّسع لنحو 11,377 مورداً.** الحدّ الأقصى المسموح للملف 10MB و13 نوع وثيقة
(أسوأ حالة 130MB/مورد = 130 GB لألف). **قاعدة البيانات نفسها ليست مشكلة إطلاقاً** (ألف صفّ ≈ عشرات
الميجابايتات مقابل 500MB في الخطة المجانية). **قرار تشغيلي للمالك: الترقية إلى Pro قبل تجاوز ~110 مورد.**

- **الاختبار:** 45 تأكيداً جديداً (المجموع **300**) — التطبيع والمطابقة وصيغ الهاتف الثلاث والبحث وسط
  ألف مورد (<300ms) · الدفعات والعدّاد والمُنتقي والمستمعات · صلاحية الوثائق · ترقيم التسجيلات والمجموع
  والبحث والتعقيم وأعمدة القائمة والعدّ على الخادم وتجاوز سقف PostgREST · العارض بالفهرس وسقف الذاكرة
  ونوع المستند · تنسيق الأحجام ولوحة السعة. + **معاينة متصفّح فعلية:** ألف مورد (رسم 4ms · بحث 10ms ·
  «عرض 60 من 1000» · البحث بكتابة مختلفة وبالهاتف وبالسجل التجاري يصيب واحداً) وألف طلب تسجيل
  (صفحات 1/20 و3/20 · بحث بالاسم والسجل · الشارة 250 من العدّ الخادميّ) ولوحة السعة بأرقامها.

### ☁️ نقل تخزين وثائق الموردين إلى Cloudflare R2 + لوحة متابعة المستندات (2026-09-06)
**قراران من المالك:** «طلبات التسعير تم إلغاء العمل عليها» (فلا تُمسّ إطلاقاً — أُسقط بند
`rfqDownloadQuoteFile` نهائياً) · «أريد أكبر حجم تخزيني لها حتى لو على كلودفلير → **R2**» ·
والمطلوب على المستندات = **لوحة متابعة: من تنقصه وثيقة ومن انتهت وثائقه**.

**(أ) التخزين على R2 (`functions/api/reg-doc.js`):**
- **binding جديد `SUPPLIER_DOCS`** — حاوية R2 **مستقلّة تماماً عن حاوية البوابة `QUOTES_BUCKET`**
  (القاعدة الذهبية: نظام 3 معزول). محروس بتأكيد: `env.QUOTES_BUCKET` ممنوع في هذا الملف.
- **الرفع:** `bucket.put` أولاً؛ يسقط لمخزن Supabase فقط إن لم يُربَط R2 وكان مفتاح الخدمة مضبوطاً.
  **مكسب جوهريّ: الرفع على R2 لا يحتاج `SUPABASE_SERVICE_ROLE_KEY` إطلاقاً** — فيزول الاعتماد على
  المتغيّر المفقود الذي عطّل الرفع الخادميّ منذ 2026-07-21.
- **القراءة `GET /api/reg-doc?key=`:** **مصادقة إلزامية** برمز جلسة الموظّف (`/auth/v1/user`، فشل
  مغلق ⇒ 401) + `safeKey` يقيّد المسار بشكله المولَّد خادمياً (`DG-XXXX/<doc>/<اسم>`) فلا اجتياز
  مسار + بثّ بترويسات التحييد المشتركة `fileResponseHeaders`. **هذا يُغلق تسريب مخزن Supabase**
  (كانت الوثائق تُنزَّل بلا حساب — كشف 2026-07-26).
- **`?meta=1`** يعيد الحجم عبر `bucket.head` بلا بثّ البايتات (لقياس السعة).
- **قراءة مزدوجة بلا ترحيل ولا انقطاع:** `regDocFetch(path)` في الواجهة يسأل R2 أولاً ويسقط
  للرابط الموقّع القديم عند **404 (ملف قبل النقل)** أو **503 (R2 غير مربوط بعد)**؛ أي حالة أخرى
  خطأ صريح. **الملفات القديمة تبقى مكانها والجديد يُكتب على R2 وحده — لا نقل بيانات.**
  **كل مسارات الجلب توحّدت عليه** (العارض · تنزيل الكل ZIP) — محروس بتأكيد.
- **✅ مُفعَّل حيّاً (2026-09-06):** الحاوية `aldeyabi-supplier-docs` مربوطة باسم **`SUPPLIER_DOCS`**
  على **Production و Preview** معاً (وحاوية البوابة `QUOTES_BUCKET` لم تُمَسّ)، و**`SUPABASE_ANON_KEY`**
  مضبوط على البيئتين. الدمج إلى `main` = PR #94 (`1f84825`). **مُتحقَّق على النطاقين:**
  `/api/reg-doc` ⇒ `{"ok":true,"store":"r2","checks":{"r2_bucket":true,"supabase_url":true,"api_key":true,"service_key":true}}` ·
  `?key=…` بلا مصادقة أو برمز مزوَّر ⇒ **401** `{"error":"غير مصرّح"}` · `?key=../../etc/passwd` ⇒ **400**
  (حارس `safeKey`). الدليل التنفيذي: `docs/دليل-ربط-R2-لوثائق-الموردين.md`.
  **⚠️ درس مثبَّت:** إعداد Cloudflare نُفِّذ **قبل** دمج الكود، فأعادت نقطة الفحص استجابة النسخة القديمة
  على `main` (`{"ok":true,"checks":{"supabase_url","service_key"}}` بلا `store`/`r2_bucket`) وبدا الأمر عطلاً.
  **الترتيب الصحيح: دمج الكود أوّلاً ثم إعداد البنية التحتية** — أو اقرأ نسخة `main` قبل تشخيص أي فحص فاشل.
- **الاقتصاد المقيس:** ≈9 MB للمورد ⇒ 8.8 GB لألف. **R2: 10 GB مجاناً ثم ‎$0.015/GB شهرياً بلا
  رسوم إخراج** ⇒ ألف مورد ≈ **$0.13 شهرياً** مقابل **$25** لترقية Supabase Pro. لوحة السعة صارت
  تقارن بثلاث خطط (Supabase Free للملفات القديمة · R2 المجاني · R2 المدفوع) وتقرأ الأحجام من R2.

**(ب) لوحة متابعة مستندات الموردين (`loadSupDocs`/`renderSupDocs`/`supDocRow`):**
الوثائق الستّ إلزامية عند التسجيل، لكن **لا شيء كان يُظهر من نقصته وثيقة بعد الاعتماد ولا من
انتهت شهاداته** (التواريخ مخزَّنة وتُعرض في بطاقة المورد وحدها). الآن في شاشة التسجيلات:
خمسة مرشّحات (تحتاج متابعة · تنقصها وثيقة · منتهية · تقارب الانتهاء · كل المعتمدين) + جدول
يعرض `present/6` وشارات الوثائق (موجودة خضراء **قابلة للنقر تفتح العارض** · ناقصة حمراء متقطّعة)
وصلاحية الشهادات بصيغة الأيام العربية. `supDocRow` منطق خالص مُختبَر؛ **يجلب المعتمدين فقط
بالصفحات عبر `regFetchAll`** (لا سقف عارٍ). + **بطاقة في لوحة المهام** «موردون بوثائق ناقصة أو
منتهية» تنقل للمرشّح مباشرةً. عتبة القرب `SUP_EXPIRY_SOON_DAYS=30`.

- **الاختبار:** 22 تأكيداً جديداً (المجموع **322**) — صفّ المتابعة (مكتمل/ناقص/منتهٍ/مقارب/بلا
  تواريخ/الأسوأ حاكم/عتبة 30) · ربط اللوحة والمرشّحات والعارض وبطاقة المهام · R2: binding مستقلّ ·
  الرفع بلا مفتاح خدمة · القراءة مُصادَقة فشلها مغلق · `safeKey` ضدّ اجتياز المسار · ترويسات
  التحييد · `head` للحجم · القراءة المزدوجة وتوحيد مسارات الجلب · خطط R2 في لوحة السعة.
  + **معاينة متصفّح فعلية** (نقطة R2 مُقلَّدة، والمخزن القديم مُعطَّل عمداً لإثبات أن المسار المستعمل
  هو R2): المرشّحات 3/1/1/1/4 صحيحة · «منتهٍ منذ 15 يوماً» · فتح وثيقة من اللوحة يعرض PDF من
  `blob:` عبر R2 · ولوحة السعة تقرأ الأحجام بـ`meta=1` (7.5 MB/مورد ⇒ 7.3 GB لألف ⇒ R2 المجاني يكفي).

### 🪟 نافذة تعلق أسفل الشاشة + تسريع فتح الوثائق + إنهاء آخر «تنزيل ثم حذف» (2026-09-06)
**بلاغان من المالك بعد التشغيل الفعليّ:** «بطاقة المورد تبقى أسفل الشاشة ولا يمكنني رفعها» ·
«الشاشة تعلق والملفات تأخذ عدّة ثوانٍ لعرض المستند».

**(أ) الجذر المؤكَّد بالقياس على الـDOM الحيّ — ليس CSS للنافذة بل fill-mode لحركة الصفحة:**
`.page.active { animation: slideInUp .32s … both }` — و`both` يُبقي إطار `to` مطبَّقاً **بعد**
انتهاء الحركة، فتحمل `.page.active` **`transform` محسوباً دائماً** (`matrix(1,0,0,1,0,0)` = هوية).
وأي `transform` غير `none` على السلف يجعله **الحاوي (containing block)** لكل `position:fixed`
بداخله — فتُحسب `inset:0` من صندوق القسم الطويل المُمرَّر لا من الشاشة. القياس: النافذة كانت
`top:-983px` وارتفاعها 1593px على شاشة 700px. **إصلاحان معاً:** `backwards` بدل `both` (المظهر
نفسه، والـtransform يزول بعد الحركة) + **نقل `#modal-reg-detail` و`#modal-email-templates` خارج
`section.page`** (كنافذة العارض سابقاً). **⚠️ القاعدة صارت محروسة لكل النوافذ لا للعارض وحده:
لا `id="modal-*"` داخل `<section class="page">`، ولا `both/forwards` على حركة `.page.active`.**

**(ب) بطء عرض المستند — ثلاث رحلات متتابعة لكل وثيقة قديمة:** كل وثيقة مرفوعة قبل النقل كانت
تكلّف: طلب `/api/reg-doc` ⇒ 404 (**وفيه تحقّق هوية على الخادم = رحلة إلى `/auth/v1/user`**) ثمّ
سكّ رابط موقّع ثمّ التنزيل. وكل تبويب يعيدها. المعالجة (كلها في الذاكرة — لا شيء يُحفظ على الجهاز):
- `REGDOC_R2_MISS` — أوّل 404 يسم **رقم التسجيل كلّه**، فبقيّة وثائقه تذهب للمخزن القديم مباشرةً.
- `regDocSignBatch` — `createSignedUrls` لكل وثائق الطلب **بنداء واحد** عند فتح العارض.
- `docvPrefetchNeighbors` — تحميل مُسبَق للتبويب التالي/السابق **بعد** رسم المطلوب لا قبله،
  وإبطال الـblob لا يطال الوثيقة المعروضة (كان التحميل المُسبَق يزاحمها فتُفرَّغ شاشتها).
- `reg-doc.js`: ذاكرة تحقّق هوية **60 ثانية** داخل نسخة العامل (الناجح فقط؛ الفشل يبقى مغلقاً).
**مقيس بالمتصفّح على ثلاث وثائق: 9 طلبات ← 5، ورحلة R2 واحدة بدل ثلاث، وتبديل التبويبات فوريّ.**

**(ج) آخر مسار «احفظه بالخارج»:** `rfqDownloadQuoteFile` كانت تُنزّل ملف عرض سعر المورّد على
الجهاز **ثمّ تحذفه من المخزن** «لتوفير المساحة». صارت `rfqViewQuoteFile` تفتحه في العارض داخل
النظام بلا أي حذف. **لم يبقَ في النظام أي مسار يحذف ملف مورّد، ولا أي تنزيل إجباريّ:** كل أزرار
التنزيل الباقية اختيارية بحتة (وثيقة من العارض · ZIP · تصدير Excel/CSV · نسخة احتياطية JSON).
- **الاختبار:** 7 تأكيدات جديدة (المجموع **329**) — لا نافذة داخل قسم صفحة · لا fill مُبقٍ
  للـtransform · الكاش السالب لـR2 · السكّ الدفعيّ · ترتيب التحميل المُسبَق · حماية الوثيقة
  المعروضة من الإبطال · لا تنزيل-ثم-حذف. + **معاينة متصفّح** (1600×700 مع تمرير كامل):
  النافذتان تغطّيان الشاشة والبطاقة متوسّطة داخل الحدود، و`r2Calls=1`/`batchSign=1`/`singleSign=0`
  لثلاث وثائق، والعرض من `blob:`.

### 🔁 تجديد المستندات المنتهية ببريد ورابط رفع خاصّ (2026-09-07)
**قرار المالك:** «السجلات والأوراق التي ستنتهي — يُرسَل بريد من Resend برفع المستند الجديد بتاريخه
الجديد واستبدال القديم». اللوحة كانت تُظهر من انتهت وثائقه لكن **المتابعة كانت يدوية بالكامل**
(اتصال/واتساب/بريد خارج النظام)، والمورّد لا يملك أي طريق لتحديث وثيقته إلا إعادة التسجيل.
- **`functions/api/doc-renew.js` (جديد) — ثلاث نقاط في ملف واحد:** (1) `POST` من موظّف مُصادَق
  (same-origin + Bearer) يبني **رمزاً موقَّعاً HMAC** ويرسل بريد Resend برابط واحد يغطّي كل الوثائق
  المطلوبة · (2) `GET ?t=` يعيد اسم المنشأة والوثائق وتواريخها فقط (لا بريد ولا PII) · (3) `POST ?t=&doc=&expiry=`
  رفع المورّد: حارس `_file-guard` الطبقي ← R2 ← **استبدال المؤشّر** في `doc_paths` + كتابة تاريخ الانتهاء
  في عموده (`cr_expiry_date`/`chamber_expiry`).
- **⚠️ بلا أي تغيير في المخطّط:** الرمز يحمل حمولته موقَّعة (`base64url(payload).HMAC`) فيُتحقَّق منه
  حسابيّاً لا بجدول — لا عمود ولا جدول ولا هجرة. المفتاح: `DOC_RENEW_SECRET` ← `CRON_SECRET` ←
  مفتاح الخدمة (يعمل اليوم بلا أي إعداد جديد). الصلاحية 21 يوماً.
- **الاستبدال منطقيّ لا تدميريّ** (نفس قاعدة SEC-06): الملف القديم **يبقى** في R2، والصفّ يشير للجديد،
  ومسار القديم يُحفَظ في `proc_audit_log.old_value` فيبقى قابلاً للاسترجاع. **لا يزال: لا مسار في النظام
  يحذف وثيقة مورّد.**
- **`renew-doc.html` (جديد):** صفحة المورّد العامّة — بلا حساب، `noindex`، بلا أي مصدر خارجيّ (آمنة CSP)،
  ترفع للنقطة الخادمية لا للتخزين مباشرةً، وتفحص التوقيع السحريّ محليّاً (والخادم هو المرجع).
- **الواجهة (لوحة متابعة المستندات):** `supDocRow` صار يحسب `renew` = الوثائق الإلزامية الناقصة + الشهادات
  المنتهية/المقاربة (مفاتيح لا أسماء) و`email` (الأولوية لمسؤول التواصل) · زرّ **«✉ طلب تجديد»** لكل صفّ
  + **«✉ إرسال طلبات التجديد (N)»** للتصنيف المعروض (بالتسلسل، لا نُغرق مزوّد البريد) · `loadSupDocs`
  يجلب `contact_email,email`.
- **الاختبار:** 20 تأكيداً سلوكيّاً في `db/portal-tests/file-guard.test.mjs` (صلاحية الموظّف · توقيع الرمز
  ومقارنة ثابتة الزمن · رمز معبوث/بمفتاح آخر · نطاق الوثائق داخل الرمز · إلزام التاريخ ورفض الماضي ·
  PDF بمحتوى نشِط · الاستبدال يُبقي بقيّة الوثائق · العمود الصحيح · **لا حذف** · القديم في التدقيق)
  + 18 تأكيداً بنيويّاً في `scripts/system2-tests.mjs` (**المجموع 347**). + **معاينة متصفّح:** الزرّان
  يظهران للصفوف المحتاجة فقط والسليم مستثنى · الحمولة الصحيحة لكل حالة · كل الطلبات تحمل رمز الموظّف ·
  وصفحة المورّد (تنبيه الانتهاء · منع بلا ملف/تاريخ ماضٍ/صيغة مرفوضة · نجاح + قفل الحقول).
- **✅ التذكير المجدوَل (2026-09-07، بأمر المالك «نفّذ»):** `GET/POST /api/doc-renew?sweep=1` محميّة
  بـ`CRON_SECRET` (مقارنة ثابتة الزمن). تمرّ على المعتمدين وترسل في موعده: **d30 · d14 · d7** مرّة
  لكل مرحلة · **المنتهي كل أسبوعين** · **النقص شهريّاً**. **منع التكرار بلا جدول ولا عمود:** كل
  إرسال يُقيَّد في `proc_audit_log` **بوسم مرحلته** (`new_value.stage`)، فيُقرأ آخر 60 يوماً ويُتخطّى
  ما أُرسِل لنفس (تسجيل+مرحلة). ضوابط: **خانق 20 ساعة** (تشغيلة واحدة يوميّاً ولو نُودي كل دقيقة —
  `force=1` للتجاوز في التجربة) · **سقف 40 رسالة/تشغيلة** · مباعدة 300ms · من بلا بريد صالح يُتخطّى.
  كل تشغيلة تُقيَّد (`action=sweep`) بعدد المرشّحين والمُرسَل والفاشل. اختبارها: 10 تأكيدات (صلاحية
  الكرون · الاختيار الصحيح · وسم المرحلة · منع التكرار · الخانق).
- **✅ التشغيل التلقائيّ بلا أي إعداد — النبضة الكسولة (2026-09-07):** كان التذكير معلَّقاً على مُشغِّل كرون
  في Cloudflare (لا أملك واجهته ولا قيمة `CRON_SECRET`)، فبقي يدويّاً. الآن `sweepReminders` **بمسارَي
  تصريح:** (أ) الكرون بـ`CRON_SECRET` · (ب) **جلسة موظّف مُصادَقة same-origin** (`verifyStaff`) — وهو نفس
  النمط المُثبَت في البوابة (`portal_sla_tick`). الواجهة: `docRenewTick()` تُنادى من `tasksLoadCloud`
  لحاملي `can_review_registrations` فقط، **صامتة** (بلا توست/انتظار)، بخانق محلّي 12 ساعة
  (`localStorage['proc_doc_renew_tick']`) — **والضمانة الحقيقية هي خانق الخادم 20 ساعة** فتعدّد الأجهزة
  والدخولات بلا أثر. `force=1` **للكرون وحده** (موظّف لا يتجاوز الخانق). الاستجابة والتدقيق يحملان
  `by: 'cron'|'staff'`. المساران يتعايشان بلا ازدواج (الخانق نفسه يحكمهما).
  **⇒ لا إعداد بنية تحتية مطلوباً؛ الكرون صار تحسيناً اختياريّاً** (الدليل المحدَّث:
  `docs/تفعيل-التذكير-المجدوَل-لمستندات-الموردين.md`، وWorker مستقلّ عن `portal-outbox-cron` إن رُبِط).
  **إصلاح مرافق:** بطاقة «موردون بوثائق ناقصة» في لوحة المهام كانت تجلب أعمدة بلا `local_content_has`
  فيسقط رصد `lcGap` من عدّها (تناقض مع اللوحة نفسها) — أُضيف مع سقوط متسامح عن `local_content_expiry`.
  الاختبار: 6 تأكيدات نقطة جديدة (نبضة مُصادَقة تُشغّل · رمز غير صالح 401 · أصل مختلف 401 · الموظّف لا
  يتجاوز الخانق · الكرون يتجاوزه · لا بريد في حالات المنع) + 5 بنيوية + 6 سلوكية للنبضة في صندوق.

### 🧾 شهادة المحتوى المحلي: رفع مكسور + تاريخ انتهاء (2026-09-07)
**سؤال المالك «هل لها تاريخ نهاية ورفع؟» كشف عيباً حيّاً.** `local_content` مرفق **خارج**
`REQUIRED_DOCS`/`OPTIONAL_DOCS` (بطاقة مستقلّة في الخطوة ٢) فسقط من القائمة البيضاء في
`reg-doc.js` ⇒ الرفع يُرفض **400**، و`register.html` يعامل الـ400 **رفضاً نهائيّاً بلا سقوط**
⇒ **الشهادة تُفقد صامتةً** لكل مورّد يختار «نعم» (منذ ضبط مفتاح الخدمة؛ قبله كان يسقط للمسار
القديم فينجح — فالتسجيلات الأقدم سليمة). مُثبَت بالتشغيل: `cr`/`municipal` = 200 · `local_content` = 400.
- **الإصلاح:** أُضيف للقائمة (يُصلح الرفع **والعرض** عبر `safeKey` **والتجديد** معاً).
- **⚠️ حارس الجذر (الأهمّ):** تأكيد يستخرج **كل** معرّفات الوثائق من `register.html` — المصفوفتين
  **وأي مرفق مستقلّ `fi-<id>`** (وهو ما فات القائمة) — ويُلزِم وجودها في `reg-doc.js`. **مُتحقَّق أنه
  يفشل فعلاً** عند إزالة `local_content` عمداً (تأكيد لا يمكنه الفشل بلا قيمة).
- **تاريخ الانتهاء (لم يكن موجوداً رغم اشتراط النموذج «سارية»):** حقل إلزاميّ مستقبليّ عند «نعم»
  (+`min`=اليوم + التدقيق النهائي + التعبئة عند الاستئناف + استخلاص AI) · يدخل صفّ المتابعة
  (منتهية/تقارب) **وبريد التجديد** وشريط بطاقة المورد وتفاصيل الطلب والتقرير · `DOC_META.local_content`.
- **الترقية `db/system1-local-content-expiry.sql`** (عمود nullable) — **⚠️ تُشغَّل من المالك على
  `yofcaxvstjcrmbgciwym`** (أداة Supabase غير مُصرَّح لها في هذه الجلسة).
  **والنظام يعمل قبلها وبعدها:** `fetchReg`/`loadSupDocs` يسقطان للأعمدة الأساسية، والـPATCH يعيد
  المحاولة بلا التاريخ عند `42703` فيُحفَظ المستند ويُبلَّغ `expiry_saved:false` (لا خسارة رفع).
- **✅ الترقية مُطبَّقة حيّاً (2026-09-07) على `yofcaxvstjcrmbgciwym`** عبر MCP + تحقّق (العمود موجود).
  **⚠️ ملاحظة وصول: `list_projects` لا يُظهر المشروع القديم (منظّمة أخرى) لكنه متاح فعليّاً بمعرّفه** —
  لا تستنتج غياب الوصول من القائمة وحدها؛ جرّب استعلاماً للقراءة أولاً.
- **📊 أثر العيب مقيس على الإنتاج:** من 8 موردين أعلنوا امتلاك الشهادة، **واحد فقط** ملفه محفوظ
  (DG-46P3QM، 2026-08-13 = قبل الكسر) و**7 فُقدت شهاداتهم** (2026-08-26 → 2026-09-07، منهم 6 معتمدون).
  الفترة تطابق ضبط مفتاح الخدمة الذي فعّل المسار الخادميّ.
- **سدّ الحلقة (وإلا بقي المتضرّرون غير مرئيين):** الشهادة ليست في الوثائق الإلزامية فلا يلتقط
  نقصَها فحص النواقص. الآن `supDocRow` يحسب `lcGap` = **أعلن الشهادة ويَنقص مرفقها أو تاريخها** ⇒
  يدخل «تحتاج متابعة» ويظهر «المحتوى المحلي: بيانات ناقصة» **ويُدرَج في بريد التجديد** تلقائيّاً.
  مُتحقَّق بالمتصفّح على الحالة الحقيقية: الصفّ يظهر والحمولة `docs:['local_content']`.
- **إصلاح مرافق كشفه المتصفّح وحده:** `getVal` **محليّة** داخل دالة الإرسال، فاستعمالها في
  `validateLocalContent` كان يرمي `ReferenceError` **يُعطّل التحقّق كلّه** — لم يلتقطه أي فحص نصّي.
  **⚠️ قاعدة: لا تستعمل `getVal`/`setVal` خارج نطاقهما في `register.html`.**

### 🔀 المورّد صار **مختلط المخزنَين** — الوثيقة المُجدَّدة لا تُفتح (بلاغ إنتاج، 2026-09-07)
**بلاغ المالك بلقطة شاشة:** «الموردون الذين رفعوا مستنداً جديداً هكذا يظهر في النظام» — تبويب
«الغرفة التجارية» في العارض يعرض **«تعذّر فتح المستند: تعذّر إنشاء رابط الملف»**.
- **الجذر (مُعاد إنتاجه بالمتصفّح حرفيّاً قبل الإصلاح):** كاش التسريع `REGDOC_R2_MISS` كان يسم **رقم
  التسجيل كلّه** بأنه «قديم» عند أوّل 404 من R2. كان ذلك صحيحاً حين كان المورّد إمّا كلّه قبل النقل أو
  كلّه بعده — **لكن ميزة التجديد كسرت هذا الافتراض:** الوثائق القديمة تبقى على Supabase والمُجدَّدة
  تُكتب على R2، **فالمورّد الواحد صار مختلط المخزنَين**. فأوّل وثيقة قديمة تُطلَب تسم التسجيل، ثمّ
  تُوجَّه الوثيقة المُجدَّدة (وهي على R2 فعلاً) للمخزن القديم ⇒ لا ملف ⇒ فشل سكّ الرابط.
  **⚠️ مفاتيح المخزنَين متطابقة الشكل** (`DG-XXXX/<doc>/<uuid>.<ext>`) فلا سبيل للتمييز بالشكل.
- **الإصلاح — الصحّة أوّلاً مع بقاء التحسين:** `REGDOC_R2_MISS` صار **بالمسار** لا بالتسجيل؛ ورقم
  التسجيل يُعطي **تلميح ترتيب فقط** (`REGDOC_LEGACY_HINT` = جرّب القديم أوّلاً). و`regDocFetch` فُصلت
  إلى `regDocFromR2`/`regDocFromLegacy` (كلٌّ يُرجع blob أو `null`) **ويُجرَّب المخزنان دائماً قبل
  الإخفاق** — بما فيه **السقوط العكسيّ إلى R2** حين يوجّه التلميحُ الوثيقةَ المُجدَّدة للقديم خطأً.
  الخطأ الحقيقيّ (401/500…) يُرمى ولا يُبتلع، والرسالة عند الفقد صارت «تعذّر العثور على الملف في مخزن النظام».
- **التحسين محفوظ بالقياس:** تسجيل كلّه قديم بثلاث وثائق = **رحلة R2 واحدة** لا ثلاث · وتسجيل كلّه
  على R2 = **صفر سكّ روابط** للمخزن القديم.
- **⚠️ درس منهجيّ:** الحرّاس كانت **نصّية** (تتحقّق من وجود `REGDOC_R2_MISS.has/add`) فمرّت العلّة كاملةً.
  الآن **8 تأكيدات سلوكية** تُشغّل `regDocFetch` فعلاً على شبكة مُقلَّدة (السجلّ المختلط · التسجيل القديم ·
  التسجيل الجديد · خطأ 401 · المفقود في المخزنَين). **لا تُبدِّلها بفحص نصّيّ.** لزمها في الصندوق:
  `window.CLOUD` مربوطاً بمتغيّر `CLOUD` نفسه (الكود يقرأ الاثنين) + تصدير `window`.
  **مُتحقَّق أنّ الحرّاس تعضّ:** إعادة العلّة عمداً ⇒ **3 إخفاقات**. **المجموع 388.**
  + معاينة متصفّح على العارض الحقيقيّ: تبويب «الغرفة التجارية» يفتح PDF المُجدَّد من `blob:` عبر R2.

### 🇸🇦 حملة شهادة المحتوى المحلي + تحسين لوحة المتابعة (2026-09-07)
**تدقيق الإنتاج (SQL) قبل أي كود:** 35 معتمداً · 365 مؤشّر وثيقة · **صفر وثيقة يتيمة** (الـ40 مؤشّراً
«المفقودة من Supabase» = 31 لتسجيلات ما بعد R2 + **9 وثائق جدّدها الموردون فعلاً على R2**، مُطابَقة بسجلّ
التدقيق). النواقص الحقيقية: **شهادة الآيبان تنقص 5** · الغرفة منتهية لواحد · 8 بلا تاريخ سجل تجاري.
- **المحتوى المحلي بُعد ظاهر (طلب المالك):** 8 أعلنوها ورقم الشهادة والنسبة **محفوظان لهم جميعاً**، والمرفق
  لواحد فقط. `supDocRow` اكتسب `lcFile/lcPct/lcCert` + **ثلاث حالات لا اثنتان:** `lcNone` (أفاد بعدمها) ·
  `lcUnknown` (لم يُجب) · معلَن. عمود «المحتوى المحلي» + مرشّح + ملخّص + شارة في بطاقة المورد.
- **🐛 عيب أبلغ عنه المالك بلقطة:** قسم الشهادة في تفاصيل الطلب كان يحذف صفّ «المستند» عند غيابه (القيمة
  `null` تُسقِط الصفّ) فيبدو القسم مكتملاً — «نعم ✓ + رقم + نسبة» بلا إشارة لغياب المرفق. الآن **«⚠️ غير مرفق»**.
  **⚠️ قاعدة: لا تُمرِّر `null` لصفّ يُفترض أن يُعلن نقصاً — الصمت يُقرأ سلامةً.**
- **الحملة (`purpose:'local_content'`):** قالب بريد مستقلّ `localContentEmail` بنبرة **دعوة** لا مطالبة
  بمتأخّر، مختصر، وبزرَّين: «لدينا شهادة — رفعها» و**«لا توجد لدينا شهادة»** (`&a=none`).
  **⚠️ زرّ «لا يوجد» ليس تجميلاً:** بدونه يختلط الصمت بعدم الامتلاك فنُراسل من لا شهادة لديه بلا نهاية.
- **البيانات المطلوبة تُجمَع مع الملف** (لا الملف وحده): `DOC_META.local_content.extra` = رقم الشهادة +
  النسبة (0–100)، و`setTrue:'local_content_has'` فرفعها يُثبِت الامتلاك لمن لم يُعلِنها عند التسجيل.
  الخادم يرفض بلا الحقلين **قبل لمس التخزين**، والصفحة تتحقّق محليّاً لتوفير رحلة فاشلة.
- **الإفادة تُثبَت في السجلّ:** `declareNoLocalContent` ⇒ `local_content_has=false` + **العمود الجديد
  `local_content_none_at`** (ترقية `db/system1-local-content-none.sql`، **مُطبَّقة حيّاً**). تظهر في تفاصيل
  الطلب («أفاد بعدم وجود شهادة — التاريخ») وفي اللوحة («لا توجد — بإفادتهم»). **حارس: من رفع شهادة فعلاً
  لا يُلغيها بضغطة ⇒ 409** (الدليل المرفوع أقوى من الإفادة).
- **زرّ الحملة في اللوحة** يستهدف **من لم يحسم أمره فقط** (لا مُرفِق ولا مُفيد) بتأكيد صريح بالعدد ومباعدة 350ms.
- **🔍 فحص سلامة الوثائق:** المؤشّر قد يشير لملف غير موجود والعدّاد يقول «6/6». يسأل المخزنَين لكل مسار
  (R2 بـ`meta=1` والقديم بسكّ رابط) ولا يحكم بالفقد إلا بخيبتهما، بتزامن 6، ويعرض المكسور بزرّ تجديد مباشر.
- **ترتيب الإلحاح:** منتهٍ ← يقارب ← ناقص ← نقص محتوى محلي ← سليم.
- **الاختبار: 419 تأكيد نظام 2 · 52 تأكيد `/api/doc-renew` · 18 حارس ملفات · 7 نقطة رفع** — صفر إخفاق.
  + معاينة متصفّح: اللوحة بشكل بيانات الإنتاج · صفحة المورّد (منع بلا رقم/نسبة خارج المدى · حمولة كاملة ·
  زرّ الإفادة) · وقالب البريد.

### 🌓 عناوين أعمدة الجداول الداكنة غير مقروءة نهاراً (بلاغ المالك بلقطة، 2026-09-07)
**الجذر (مقيس في Chromium لا مُخمَّن):** القاعدتان العامّتان `th{color:var(--ink-2)}` و`td{color:var(--ink-2)}`
**تتغلّبان على اللون المورَّث من `<tr>`** — مُحدِّد العنصر يسبق الوراثة، **ولو كانت الوراثة من نمط سطريّ على
الأب**. فصفّ الرأس الكحليّ `background:#0B1B36;color:#fff` يُورّث أبيض ثمّ تكسره قاعدة العنصر ⇒ النصّ
`rgb(58,51,48)` على كحليّ. **ثلاثة مواضع كانت مصابة لا واحداً:** جدول التسجيلات · جدول متابعة مستندات
الموردين · رأس بنود **طلب الشراء المطبوع**؛ وصفّا التجميع الكريميّان (`#231F20`) مصابان عبر `td{}`.
- **العلاج `color:inherit`** (لا لون ثابت) فيعود اللون الذي وضعه الصفّ نفسه — يصحّ للأبيض والكريميّ معاً:
  `tr.thead-navy th, tr.thead-navy td, tr[style*="#0B1B36"] th/td, tr[style*="#0b1b36"] th/td, tr[style*="#231F20"] th/td{color:inherit}`.
  **⚠️ أي صفّ جدول داكن جديد: أعطِه `class="thead-navy"`** (والمُحدِّدات السطريّة تلتقط القائم والمولَّد وقت التشغيل).
- **الحارس (تأكيدان، المجموع 429): يحسب إضاءة كل صفّ داكن** في الملف ولا يعتمد قائمة ألوان مثبَّتة، فيُفشِل
  البناء على أي صفّ داكن بلا تغطية. **مُتحقَّق أنّه يعضّ** (إزالة تغطية لون + صفّ داكن جديد ⇒ إخفاق).
- **⚠️ درس أدوات:** `curl .../index.html` يعيد **308** (تحويل إلى `/`) فيبدو الفحص «صفر مطابقات» وكأنّ النشر
  فشل. **استعمل `curl -sL <domain>/`** ولا تستنتج فشل نشر من فحص بلا `-L`.

### 📱 الجوال: قشرة تطبيق + عرض المستندات + سدّ ثغرة «ادّعاء بلا دليل» (2026-09-07)
ثلاثة بلاغات من المالك في رسالة واحدة، وكلٌّ منها كان عيباً حقيقيّاً مُثبَتاً بالقياس.

**(أ) «النظام في الجوال سيء جدًا» — القياس أوّلاً على 393×852:** الصفحة **تنزلق أفقياً 220px في
كل شاشة** · لا شريط تبويبات · 10–70 هدف لمس دون 36px · 19 استعلام وسائط فقط في 25 ألف سطر.
- **🐛 الجذر ليس الشريط الجانبي (وهو أوّل ما يتبادر):** بإخفائه بقي `scrollWidth=613`. الجاني
  **`#po-drawer`**: درج مُغلق يُخفى بـ`transform:translateX(100%)` **وحده بلا `visibility`**،
  فيبقى صندوقه خارج الشاشة **يُوسّع عرض المستند القابل للتمرير**. (طبقة التعتيم
  `.po-drawer-overlay` كانت تفعل الصواب بـ`visibility:hidden` — الدرج نفسه نسيها.)
  **⚠️ قاعدة محروسة: أي لوحة off-canvas تُخفى بـtransform يجب أن تحمل `visibility:hidden` وهي
  مُغلقة.** + حزام أمان `html{overflow-x:clip}` (لا `hidden` — `clip` لا يُنشئ حاوية تمرير فلا يكسر `sticky`).
- **البناء (كلّه داخل `@media(max-width:900px)` — سطح المكتب بلا تغيير، مُتحقَّق):** شريط تبويبات
  سفليّ `.mnav` (5 وجهات، مُزامَن بـ`mnavSync`) · **مناطق آمنة** (`viewport-fit=cover` + `env(safe-area-inset-*)`
  في الشريطين) · أهداف لمس 44px · **حقل البحث 16px** (دونها يُقرّب iOS الشاشة تلقائياً) · نوافذ
  كأوراق سفليّة · `.toolbar` يلتفّ · **تطبيق قابل للتثبيت** (`manifest.webmanifest` + 4 أيقونات
  مولَّدة من علامة العلامة التجارية + `apple-mobile-web-app-*` + `theme-color`).
- **`navDrawer(open)` نقطة تحكّم واحدة** (استبدلت فتحاً/إغلاقاً مبعثراً) و`body.nav-open` تُجمّد
  التمرير خلف الدرج. **⚠️ كتلة الحساب `.sidebar-account` في الدرج ليست تجميلاً:** أخفينا
  الإعدادات/الخروج/البوابة/المزامنة من الشريط العلويّ على الهاتف، فبلا بديلها **لا يجد مستخدم
  الهاتف طريقاً لتسجيل الخروج**. أزرارها تنقر الأصليّة (`getElementById(...).click()`) فلا يتكرّر معالج.
- **`mobileTableWrap(root)`** يلفّ أي جدول عريض بحاوية `.table-scroll` وقت التشغيل (مربوط بنقاط
  `a11yWire` الثلاث). **⚠️ لا تُعالِج بـ`table{display:block;overflow:auto}`** — تحويل الجدول إلى
  block يُلغي خوارزمية الأعمدة فتنفكّ الرؤوس عن الخلايا.
- **النتيجة المقيسة: 220px ← 0 على الشاشات الستّ**، وشريط التبويبات 53px، وصفر خطأ صفحة.

**(ب) «المستندات تظهر بيضاء في الجوال»:** العارض كان يرسم PDF في `<iframe src="blob:">` —
و**iOS Safari لا يرسم PDF داخل إطار مضمّن إطلاقاً** (بياض كامل، وكذلك WebView أندرويد أحياناً).
- **الحلّ: pdf.js يرسم على canvas** — سلوك واحد على iOS/أندرويد/سطح المكتب.
  **المكتبة والعامل مُضمَّنان محليّاً في `/vendor/`** فيغطّيهما `script-src 'self'` و`worker-src`
  (يسقط إلى `default-src 'self'`) — **بلا أي تعديل على CSP وبلا اعتماد على CDN**.
  **التحميل كسول** (`_PDFJS` + `import()` عند أوّل PDF) فصفحة الدخول لا تحمل بايتاً زائداً.
  سقف الدقّة `min(devicePixelRatio,2)` (A4 عند DPR=3 يتجاوز 20 مليون بكسل فتنهار تبويبة iOS) وسقف 40 صفحة.
- `docvPrint` لم يعُد يبحث عن إطار (لم يبقَ). **مُتحقَّق بالمتصفّح: صفحتان مرسومتان، `iframes=0`.**

**(ج) «كيف سمحت أنهم ما يرفعون الشهادات ويكتبون المعلومات فقط؟» — العيب كان ما يزال مفتوحاً:**
`local_content` ليست في `REQUIRED_DOCS`، وفحص الإجهاض في `submitForm` يمرّ على تلك القائمة وحدها.
ففشل رفع الشهادة كان **تنبيهاً عابراً** ثمّ **يكتمل الإرسال** بـ`local_content_has=true` ورقم الشهادة
والنسبة **بلا أي مرفق** — وهو ما أنتج السبعة (حين كان الخادم يرفض النوع 400 قبل إضافته للقائمة البيضاء).
- **الإصلاح:** حارس بعد حلقة الرفع: أعلن «نعم» و`docPaths.local_content` غائب ⇒ **يُمنَع الإرسال**
  ويُعاد للخطوة ٢. **⚠️ القاعدة: الادّعاء بلا دليل أسوأ من غيابه** — أي حقل «لدينا X» يجب أن يكون
  إرساله مشروطاً بوصول مرفق X فعليّاً، لا بنيّة رفعه.

#### 🔍 تدقيق ما بعد التعديل (2026-09-08، بطلب المالك «تأكّد من سلامة كل شيء والثغرات الخاملة»)
التدقيق كشف **أربعة عيوب أدخلتُها أنا** في الجولة السابقة — كلّها بالقياس لا بالقراءة:
- **🐛 قواعد «النوافذ كأوراق» كانت على أسماء أصناف غير موجودة** (`.modal-box`/`.modal-content` = صفر
  مطابقة) فلم تُطبَّق أصلاً؛ **والقاعدة الوحيدة التي أصابت (`.modal{align-items:flex-end}`) ضربت
  الصندوق لا الغلاف** — والصندوق `flex-direction:column` فصارت المحاذاة تُقلّص أبناءه: **رأس كل نافذة
  260px داخل صندوق 345px.** الصحيح: `.modal-overlay` هو الغلاف الثابت و`.modal` هو الصندوق.
  **⚠️ حارس معمَّم:** كل صنف في كتلة الجوال يجب أن يوجد في الترميز أو يُسنَد وقت التشغيل
  (`className`/`classList.add`) — فأي قاعدة على اسم متوهَّم تُفشِل البناء بدل أن تمرّ صامتة.
- **🐛 تسريب pdf.js:** كل `getDocument` يفتح **عاملاً مستقلّاً** ومخازن صفحات، ولا شيء كان يُدمّره
  (قياس: 3 وثائق ⇒ 3 عمّال أحياء). الآن `docvDestroyPdf()` نقطة إغلاق واحدة تُنادى قبل كل فتح وعند
  الإغلاق — مُتحقَّق: بعد 3 فتحات **مستند حيّ واحد**، وبعد الإغلاق **صفر**. + `isEvalSupported:false`.
- **🐛 فرضتُ عموداً واحداً على `.stats-grid`** بـ`!important` وكانت عمودين **عمداً** على الجوال
  (أفضل استغلال لبطاقات المؤشّرات). أُلغي الفرض — عادت `177.5px × 2`.
- **🐛 روابط الاتصال 77×20px** — وهي **أكثر ما يُنقَر** في دليل الموردين على الهاتف. وُسِّعت بالحشو
  (لا بتكبير الخطّ) ⇒ **الأهداف الصغيرة في شاشة الموردين 62 ← 2**.
**فحوص نفت مخاوف (لا إصلاح لزم — مقيسة لا مفترَضة):** `html{overflow-x:clip}` **لم يكسر `sticky`**
(الشريط العلويّ يلتصق عند 0 والتمرير الرأسيّ 600 سليم، و`overflow-y` بقي `visible`) · النوافذ
`z-index:1500` **فوق** الشريط السفليّ 880 · **العامل حقيقيّ لا سقوط للخيط الرئيسي** (3 عمّال
`pdf.worker.min.mjs`) · `telephone=no` **آمن هنا** لأن هواتف الموردين روابط `tel:` صريحة، والمفتاح
يمنع iOS من تحويل السجل التجاري/الضريبي/أرقام الأوامر إلى روابط زائفة — **⚠️ لا تحذف الروابط
الصريحة اتّكالاً على الاكتشاف التلقائيّ** · `register.html` و`renew-doc.html` **سليمتان على الجوال**
(صفر انزلاق · صفر حقل دون 40px · صفر خطّ دون 16px) · أُزيل قفل `orientation` من المانيفست (جداول
البيانات تستفيد من الوضع الأفقيّ).
- **🐛 عبور نقطة الانكسار (دوران/طيّ/تغيير حجم) — عيبان كامنان مقيسان:** (أ) درج مفتوح ثم دوران
  لعرض كبير ⇒ `#overlay` يبقى `display:block` **يغطّي سطح المكتب** والتطبيق مشلول (قاعدته خارج
  `@media` فلا يُلغيها اتّساع الشاشة)؛ (ب) جدول رُسِم على عرض كبير ثم دوران للجوال ⇒ **12 جدولاً
  عارياً و173px انزلاقاً** (اللافّ يعمل وقت الرسم فقط). الحلّ `onBreakpointChange` مؤجَّلة على
  `resize` **و`orientationchange`** (⚠️ iOS قد لا يُطلق `resize` عند الدوران فوراً). مُتحقَّق:
  الغطاء `none` والانزلاق 0 و13 جدولاً ملفوفاً.
**الاختبار: 459 تأكيد نظام 2** (+30) + 52 `/api/doc-renew` + 18 حارس ملفات + 7 نقطة رفع.
**مُتحقَّق أنّ الحرّاس تعضّ مرّتين:** العلل الثلاث الأصلية ⇒ 6 إخفاقات · وعيوب التدقيق الأربعة ⇒
**5 إخفاقات** (والحارس يسمّي `modal-box،modal-content` بالاسم). + معاينة متصفّح: الورقة السفليّة
بعرض كامل وأبناؤها كاملون · PDF مرسوم فعليّاً · سطح المكتب 1440 بلا تغيير (`260px 1180px`).

### 🚨 ثلاثة بلاغات إنتاج من جهاز حقيقيّ — واختباراتي المحلّية مرّت (2026-09-08)
**الدرس الحاكم: اختبار «محلّي ناجح» بلا ترويسات الإنتاج ليس اختباراً.** الثلاثة تسلّلت من فجوات
في منهج تحقّقي أنا، لا من الكود القائم:

**(أ) «المستندات لا تزال لا تفتح» — `Unexpected server response (0) while retrieving PDF blob:…`**
🐛 **الجذر:** pdf.js يجلب أي `url` تُعطيه **عبر طبقة الشبكة**، و`connect-src` في CSP الإنتاج
(`'self' https://*.supabase.co https://generativelanguage.googleapis.com`) **لا تسمح بـ`blob:`**
فيُرفَض الطلب. مررتُ محلياً لأنّ خادمي لم يُرسل الترويسات.
- **العلاج الجذريّ: لا تُعطِه رابطاً إطلاقاً** — `getDocument({data: bytes})` بعد `blob.arrayBuffer()`
  ⇒ لا طلب شبكة فلا علاقة لـCSP بالأمر. **وCSP بقيت مشدّدة كما هي** (لم نُضِف `blob:` — توسيعها كان
  سيُخفي العيب بدل حلّه). `DOCV.blobs` ذاكرة بايتات بسقف 3.
- **⚠️ الحارس الأهمّ: خادم اختبار يُطبّق CSP من `_headers` حرفيّاً** — مُتحقَّق: صفحة كاملة + فتح PDF
  ⇒ لوحة مرسومة و**صفر انتهاك CSP**. أي اختبار متصفّح لمسار جديد يمرّ بهذا الخادم لا بخادم عارٍ.

**(ب) «يحتاج مزامنة أكثر من مرّة ليتصل»**
🐛 **الجذر (مؤكَّد بالكود):** `CLOUD.enabled = true` يُضبط **داخل `init()`** أي **قبل** وصول البيانات؛
وحارس إعادة الاتصال التلقائي شرطه `!CLOUD.enabled`. فلو فشل `refreshFromCloud()` على شبكة جوّال بقي
الاتصال «مُفعَّلاً بلا بيانات» و**لا تُعاد المحاولة أبداً** — والضغط اليدويّ هو المخرج الوحيد.
- **العلاج:** عَلَم `CLOUD.dataLoaded` يفصل «متّصل» عن «البيانات وصلت»؛ الدورة الدوريّة تُعيد
  **التحميل وحده** عند `enabled && !dataLoaded` · و`__cloudRetryNow()` بخانق 5 ثوانٍ على حدثَي
  **`online`** و**`visibilitychange`** (على الجوال تعود الشبكة أو يعود المستخدم من تطبيق آخر،
  فانتظار 10 ثوانٍ يبدو تعليقاً).

**(ج) «النظام صغير جدًا وغير محكم على الهاتف»**
**التخطيط سليم مقيساً** (انزلاق أفقيّ حقيقيّ = صفر على الشاشات الستّ بعد رفع قناع `overflow-x:clip`
— ⚠️ **درس: القناع يجعل `scrollWidth==clientWidth` فيقيس نفسه**؛ لا تقس الانزلاق وهو مُفعَّل).
ولا WebKit في بيئة العمل فلا يمكن تشغيل سفاري. السبب المرجَّح خارج سيطرة الصفحة: **تكبير سفاري
المحفوظ لكل موقع** (يتذكّره من حين كانت الصفحة فعلاً أعرض قبل الإصلاح) أو «طلب موقع سطح المكتب».
- **بدل التخمين عن بُعد: الصفحة تقيس نفسها وتقول الرقم** — `mobileViewportCheck()` تقارن
  `clientWidth` بـ`screen.width`، وعند نسبة > 1.3 على شاشة صغيرة تُظهر شريطاً: «معروضة بتصغير 50%
  تقريباً (تخطيط 786px على شاشة 393px) — اضبط aA إلى 100%». مُتحقَّق: **لا يظهر على 393/393**
  ويظهر بالأرقام الصحيحة على 786/393.

**(د) «بعض النوافذ بالجوال مضروبة» — جدول «سجلّ المشاريع» بلا عمود اسم المشروع**
🐛 **سببان متراكبان:** (١) **اصطلاح `scrollLeft` في حاوية RTL يختلف بين المحرّكات** — كروميوم
يجعل `0` = البداية (اليمين)، وWebKit تاريخيّاً `0` = أقصى اليسار = **نهاية** المحتوى؛ ففي كروميوم
يبدو الجدول سليماً وعلى الآيفون يفتح منزلقاً وقد خرج عمود الاسم. **⚠️ لهذا لا يكفي القياس على
محرّك واحد.** (٢) لا شيء كان يُبقي عمود الهوية ظاهراً عند التمرير الأفقيّ.
- **العلاج:** `scrollInlineStart()` **يضبط ثم يتحقّق** (لا يفترض اصطلاحاً): يجرّب `0` ثم
  `scrollWidth` ثم `-scrollWidth` ويتوقّف عند أوّل قيمة تُحاذي أوّل خليّة بالحافة الابتدائيّة —
  ويجري **على كل العروض** لا الجوال وحده. + **عمود أوّل لاصق** (`position:sticky;inset-inline-start:0`)
  فلا تبقى أرقام بلا صاحب. + على الهاتف تُخفى أعمدة «الرمز/المرادفات» الثانويّة.
- **⚠️ النافذة تُعيد رسم محتواها بعد فتحها** (تبديل تبويب/إجراء/بحث)، فاللفّة عند الفتح وحدها
  تفوت كل جدول لاحق. `modalRenderWatch` مُراقب مؤجَّل لكل نافذة **يراقب `childList` فقط** —
  مراقبة السمات تُعيد إطلاقه من `a11yWire` فيدور بلا نهاية.
- مُتحقَّق: الأعمدة المرئية «المشروع · أوامر · القيمة · إجراءات» والاسم لاصق، **وتبقى كذلك بعد
  إعادة الرسم**؛ ومسح الـ18 نافذة على 393px = صفر فيضان (⚠️ أكثرها فارغ محليّاً فالمسح أضعف ممّا يبدو).

**الاختبار: 469 تأكيداً** (+10) — ومُتحقَّق أنّ الجديدة تعضّ (إعادة العلل ⇒ 5 إخفاقات، منها
«CSP تبقى مشدّدة» الذي يمنع علاج العَرَض بتوسيع الترويسة).

### 👥 «كيف أصبح الموردون من 104 إلى 137؟» — تركيب القائمة صار ظاهراً (2026-09-08)
**سؤال المالك بلقطة الشريط الجانبي. الجواب مقيس على القاعدة الحيّة** (وصول Supabase مُنِح في الجلسة):
`select count(*) from proc_suppliers` = **104 بالضبط** (104 اسماً فريداً).
**عدّاد الموردين ليس عدّ جدول واحد** بل طول `STATE.suppliers` = دمج مصادر: صفوف السحابة +
**`PO_SUPPLIER_SEED`** (42 مورداً مستخرَجاً من أوامر الشراء في الإكسل) + `DATA.suppliers`.
- **مصدر الـ33 مقيس لا مُخمَّن:** من الـ42 في `PO_SUPPLIER_SEED` هناك **34** ليست في `DATA.suppliers`
  (فتُضاف عند الإقلاع)، و**33** منها ليس لها صفّ في السحابة بالاسم الخام ⇒ **104 + 33 = 137**.
  تتبّع في المتصفّح بالبيانات الحيّة أعاد إنتاج الرقم حرفيّاً (وبعد إصلاح اليوم = **135**، لأن
  المطابقة المطبَّعة طوت كتابتين مختلفتين لمورد واحد).
- **⚠️ تصحيح ما قيل قبل القياس في هذه الجلسة نفسها:** لا علاقة للقفزة بـ`DATA.suppliers` ولا بإصلاح
  `window.DATA` (المرحلة 1). **كل الـ42 اسماً في `DATA.suppliers` موجودة في السحابة بالاسم الخام**،
  فأساس دمجها **صفر قبل الإصلاح وبعده** — أي أنّ ذلك الإصلاح لم يغيّر عدّاد الموردين إطلاقاً.
  **الدرس: لا تُفسِّر فرقاً عدديّاً قبل قياس طرفيه؛ الحساب المعقول ليس قياساً.**
- **الـ33 موردون حقيقيون** (تعاملت معهم الشركة في أوامر شراء) لكنّهم **غير مرفوعين للسحابة**، فهم
  محليّون على جهاز كل مستخدم. العدد ليس خطأً — لكنه كان **بلا تفسير على الشاشة**، وهو ما أُصلح.
- **عيب حقيقي كشفه الفحص — بطاقات شبح:** بذرة الملف **42 صفّاً بـ35 اسماً فريداً**؛ سبعة موردين
  مقسومون على صفّين (صفّ نحيل يحمل السجل التجاري فقط، وصفّ غنيّ يحمل الهاتف والتخصص والعروض) —
  فيظهر المورد ببطاقتين إحداهما شبه فارغة، **وكلتاهما تفتح التفاصيل نفسها لأن الفتح بالاسم**.
- **العلاج (عرض فقط — لا حذف من السحابة):** `supKey` (مفتاح الاسم المطبَّع عبر `arNorm`) ·
  `supMergeRows` (يجمع الحقول: لا يُتلف مملوءاً، و«—» تُعامَل فراغاً) · `supDedupeByName` ·
  `supSourceBreakdown`. تُطبَّق في **موضعين**: `init()` بعد `mergePOSupplierSeed`، و`refreshFromCloud`
  على أساس البذرة — **والمطابقة مع السحابة صارت بالمفتاح المطبَّع أيضاً** («الأدوات»/«الادوات» مورد
  واحد لا اثنان). **⚠️ لا دمج تلقائي للسحابة:** التكرار العابر للمصادر يُعرَض ويُترك قراره للمالك.
- **الشاشة تجيب السؤال بنفسها:** سطر العدّاد في صفحة الموردين صار `عرض N من M مورد · X من السحابة ·
  Y من بذرة الإكسل (لم تُرفع للسحابة بعد)` + رابط `⚠ N اسماً مكرّراً` يفتح `supShowDupes` (نافذة
  مُلحَقة بـbody، تسرد الاسم وعدد بطاقاته ومصدرها).
- **الاختبار: 483 تأكيداً** (+14) — دمج الصفّين · جمع الحقول بلا إتلاف · «—» فراغاً · مفتاح مطبَّع
  واحد للكتابتين · **بذرة الملف الحقيقية 42→35** · التفكيك سحابة/بذرة · رصد التكرار عبر المصدرين ·
  الاسم الفارغ لا يُبتلع · ربط الموضعين · لا دمج تلقائي. **مُتحقَّق أنّها تعضّ** (إزالة الدمج من
  المسارين ⇒ إخفاقان). + **معاينة متصفّح تحت CSP الإنتاج**: 42→35، والدمج مع سحابة مُقلَّدة يستبعد
  المطابق، وسطر التركيب يظهر بأرقامه، وصفر خطأ صفحة.

### ⬆️ رفع موردي أوامر الشراء للسحابة + إنهاء بذرة الموردين المحلّية (2026-09-08)
**أمر المالك:** «ارفع الـ33 مورداً للسحابة وتأكّد من عدم وجود تكرار أو مورد بلا بيانات».
- **النتيجة على القاعدة الحيّة: 104 ← 119 مورداً** (17 مُدرَجاً + حذف صفّين بلا بيانات).
  **مُتحقَّق بعدها: صفر مجموعة مكرّرة · صفر مورد بلا بيانات · صفر اسم فارغ.**
- **⚠️ الدرس الأهمّ — المطابقة النصّية البسيطة كانت سترفع 14 نسخة مكرّرة:** من الـ31 مرشّحاً
  (بعد استبعاد ما يطابق السحابة بـ`arNorm`) تبيّن بالفحص أنّ **14 منها موردون موجودون أصلاً**
  بكتابة أخرى، على أربعة أنماط لا يلتقطها التطبيع وحده:
  (أ) **كلمة الكيان**: «اكسترا» ⇄ «شركة اكسترا» · «منزل الياقوت» ⇄ «مؤسسة منزل الياقوت» (6 حالات).
  (ب) **لاحقة في الاسم السحابيّ**: «الوكالة العربية للسيارات» ⇄ «… (Arabian Auto A» · «شركة العربية
  للشاحنات والمعدات» ⇄ «… الإنشائية (ATEC)» · «مؤسسة ارتباط التكنولوجيا» ⇄ «… لتقنية المعلومات».
  (ج) **خطأ إملائي/فرق حرف**: «سماء الحاسوب» ⇄ «سما الحاسوب» (0.91) · «للالات» ⇄ «لللت» (0.88).
  (د) **نقل حرفيّ عربيّ↔لاتينيّ**: «شركة اكس سي ام جي للتجارة» ⇄ «شركة XCMG للتجارة» —
  **لا يلتقطه أي قياس تشابه نصّي إطلاقاً؛ أُمسِك بالفحص البشريّ للقائمة.**
  الخوارزمية النهائية للاستبعاد: مفتاح قويّ (تطبيع + إسقاط كلمة الكيان + إسقاط كل المسافات) ثم
  **الاحتواء** ثم Dice ≥0.85 ثم بادئة مشتركة ≥10 محارف (تُعلَّق للمراجعة لا تُرفَع).
- **صفّان بلا بيانات حُذِفا** (اسم فقط، أنشأهما `Mostafa`): `أحمد` و**`اسم المورد بالعربية`**
  (نصّ العنصر النائب في النموذج حرفيّاً). **⚠️ سجلّاتهما السعرية لم تُمَسّ** (14 و6 سجلاً) وتبقى
  مسنَدة لاسمَيهما — تحتاج إعادة إسناد لمورد حقيقي من المالك.
- **⛔️ أُزيلت بذرة `PO_SUPPLIER_SEED` + `mergePOSupplierSeed` نهائياً (وهي مصدر قفزة 104←137):**
  بعد الرفع لم يبقَ منها إلا **8 كتابات مختلفة لموردين قائمين** ⇒ بطاقات شبح في كل جهاز.
  **مُتحقَّق بالمتصفّح على قائمة السحابة الحقيقية: العدّاد = 119 بالضبط · صفر محليّ · صفر تكرار ·
  صفر خطأ صفحة.** ⚠️ **قاعدة: أي مورد جديد يُضاف للسحابة لا لبذرة في الكود.**
- **`supKeyStrong` (مفتاح المقارنة القويّ)** أُضيف للكود ويُستعمَل في بوّابة دمج بذرة `DATA` مع
  السحابة — **لا في `supDedupeByName`** (دمج الصفوف يبقى على `arNorm` المحافظ حتى لا يُدمَج
  موردان مختلفان). 5 تأكيدات جديدة + حارس «لا إحياء للبذرة». **الحزمة: 500 تأكيد، صفر إخفاق.**
- **⚠️ مرصود ولم يُنفَّذ (قرار المالك):** مورّدان مُعلَّقان لتشابه قويّ غير قاطع —
  «عبد اللطيف جميل للاليات» ⇄ «شركة عبداللطيف جميل للمعدات (ALJ)» · «معرض القيادة واريادة» ⇄
  «معرض القيادة للسيارات». لم يُرفَعا ولم يُدمَجا.
- **⚠️ تأكيد أمنيّ متكرّر:** مخزن `supplier-docs` على المشروع القديم **ما يزال مقروءاً بالمفتاح
  العام** (نزّلتُ منه شهادة حقيقية بلا حساب أثناء تشخيص عطل العرض). `db/system1-storage-hardening.sql`
  المرحلة 1 **لم تُطبَّق بعد** — بند مفتوح على المالك.

### 🔗 دمج كتابات الموردين في الأوامر والسجل + تنظيف الأسماء المشوّهة (2026-09-08)
**قرار المالك «نعم نفس المورد ادمجها»** فُتِح منه تدقيق كامل لكل مرجع لاسم مورد في القاعدة.
- **الزوجان المطلوبان لا مرجع لهما أصلاً** (0 أمر و0 سجلّ) — الاسم الوحيد الباقي هو المعتمَد،
  فلم يكن هناك ما يُدمَج فيهما. لكن التدقيق كشف **الفئة نفسها بحجم أكبر بكثير**.
- **أوامر الشراء: 47 أمراً وُحِّدت.** (أ) 36 أمراً كانت تحمل كتابة مختلفة لمورد **موجود في
  الدليل** فصارت تشير إليه (ركن القهوة 6 · العوبثاني 6 · الراشد 3 · سما الحاسوب 5 …).
  (ب) 11 أمراً في **ثلاث مجموعات كتابات لمورد واحد غير مسجَّل** («ندى احمد بن ابراهيم» ×2 ·
  «المنديل الأبيض» ×4 · «روائع الهيل» ×3) وُحِّدت لأكمل كتابة.
- **السجل السعري: 80 سجلاً وُحِّدت** على 12 كتابة (ركن القهوة 29 · ارتباط التكنولوجيا 27 …).
- **🐛 عيب بيانات مكتشف: أسماء مبتورة عند 40 محرفاً** في السجل السعري («…لتقنية المعلوما» ·
  «(Zoomlion » · «(Light Star for T») — بتر عند الاستيراد. الأسماء المبتورة التي طابقت الدليل
  بالاحتواء صُحِّحت ضمن الـ80؛ **الباقي لم يُمَسّ** (بند مفتوح: أصل البتر في مسار الاستيراد).
- **🐛 ثمانية أسماء موردين كانت برموز العرض العربية** (U+FB50–FEFF، مخلّفات استخراج PDF):
  «ﺷﺮﻛﺔ ﺃﺩﻳﻢ ﺍﻟﺼﺤﺮﺍﺀ…» تبدو عربيّة للعين لكنها محارف أخرى ⇒ **البحث بالاسم الطبيعيّ لا يجدها
  إطلاقاً** والمقارنة تعُدّها موردين مختلفين. نُظِّفت كلّها بـ`normalize(NFKC)` في القاعدة،
  **و`arNorm` في الواجهة صار يبدأ بـ`normalize('NFKC')`** فلا تتكرّر الحالة.
- **⚠️ خطأ ارتكبتُه وأصلحته:** أوّل تشغيل للدمج وجّه أمراً بنصّ نظيف («شركة اديم الصحراء
  للتجارة») إلى **الاسم المشوّه** لأنه هو المسجَّل في الدليل — أي أفسدتُ الصحيح. العلاج كان
  تنظيف الدليل نفسه لا العكس. **القاعدة: عند التوحيد، تحقّق أن الوجهة أنظف من المصدر.**
- **⚠️ وخطأ كدتُ أرتكبه:** «سراكو شركة» و«شركة سراكو للموارد البشرية» بدَتا كتابتين لمورد واحد،
  وفحص الصفّين قبل الدمج أظهر **سجلّين تجاريّين ورقمين ضريبيّين وبريدين مختلفين** (sraco.com.sa
  مقابل sracohr.com) = **شركتان مستقلّتان**. سُمِّيتا بوضوح ولم تُدمَجا.
  **القاعدة: تشابه الاسم ليس دليلاً — السجل التجاري هو الفاصل.**
- **الحالة النهائية مُتحقَّقة:** 119 مورداً · **صفر مجموعة مكرّرة حتى بالمفتاح القويّ** · صفر
  اسم مشوّه · صفر مورد بلا بيانات. **متبقٍّ (تقرير لا خطأ): 21 اسم مورد في 34 أمر شراء و27 في
  السجل السعري غير مسجَّلين في الدليل** (منها «مورد غير معروف» و«مورد عروض الأسعار» المقصودان).
- الاختبار: تأكيدان جديدان (فكّ رموز العرض + البحث يجدها فعلاً) — **الحزمة 502، ومُتحقَّق أنّهما
  يعضّان** (إزالة NFKC ⇒ إخفاقان).

### 📇 استكمال دليل الموردين من ملاحظات الأوامر + إصلاح البتر عند 40 محرفاً (2026-09-08)
**توجيه المالك: «اصلحهم واضفهم — بيانات الموردين في الأوامر تُسجَّل في الملاحظات».** وهي كذلك فعلاً:
حقل `notes` في أمر الشراء يحمل مقطعاً مُطبَّعاً `بيانات المورد — العنوان: … سجل تجاري: … رقم ضريبي: …
جهة الاتصال: … جوال: …` فاستُخرِج بالتعبير النمطي مباشرةً في SQL (لا نقل يدويّ = لا خطأ نسخ).
- **الدليل: 104 ← 153 مورداً** (51 مُضافاً اليوم: 17 من بذرة أوامر الشراء · **21 من ملاحظات الأوامر** ·
  13 من السجل السعري). **و«أوامر بلا مورد مسجَّل» = 104 ← صفر.**
- **قواعد الاستخراج المقيسة:** رقم السجل والضريبة بـ`mode()` (الأغلبية تغلب الخطأ المطبعيّ)، وجهة
  الاتصال والجوال **من أحدث أمر** (الأشخاص يتغيّرون)، و«جهة الاتصال» التي تساوي اسم المنشأة تُسقَط،
  و«ابهاء» صُحِّحت «أبها». **بيانات مشبوهة لم أخترع لها أرقاماً:** ضريبيّ «ندى احمد» 14 رقماً (المعيار 15)
  وسجلّ «الوردة المشرقة» 9 (المعيار 10) — خُزِّنا كما في الأمر ويحتاجان تدقيقاً.
- **🐛 بتر عند 40 محرفاً بالضبط** في أسماء الموردين — **مصدره بذرة الإكسل الأصلية لا كود التطبيق**
  (مُتحقَّق: 19 اسماً بطول 40 داخل `DATA.history`، ولا موضع في الكود يقصّ قبل الحفظ). النتيجة كانت
  اسمين لشركة واحدة («…(Albariq) —» و«…(Albariq Sa») وأسماء لا يجدها البحث. **أُصلح: 206 سجلاً سعريّاً
  استعادت أسماءها** (بقصّ القوس اللاتينيّ المعلّق فقط — الأقواس العربية المشروعة كـ«(نجار)» لم تُمَسّ)،
  **وصفّان في الدليل** («XCMG» و«الوكالة العربية للسيارات») + تصحيح خطأ مطبعيّ («المحترفون العرب لللت»
  ← «للآلات») وتسمية صفّ `Albariq` بالعربية. **الآن: صفر اسم مبتور في الجداول الثلاثة.**
- **🐛 البذرة كانت تُعيد إدراج ما نُقِّي من السحابة:** دمج السجل السعري كان بالمطابقة **بالرقم وحده**،
  والسحابة نُقّيت من سجلات مكرّرة برقمين مختلفين ⇒ ستة سجلات تعود في كل جهاز فينتفخ عدّاد العروض.
  الآن يُسقَط أيضاً ما يطابق سجلاً سحابيّاً **في محتواه** (صنف+مورد مطبَّع+سعر+تاريخ). وسجلّ واحد كان
  ناقصاً من السحابة فعلاً (`num=531`) رُفِع باسمه النظيف.
- **⚠️ 34 مورداً مُضافاً بلا بيانات تواصل** (6 من الأوامر بلا مقطع بيانات + 13 من السجل + …): **مُوسَمون
  في `notes` بـ«يحتاج استكمال»** — أُضيفوا لأنهم موردون حقيقيون لهم أوامر/عروض، لا لتضخيم العدد.
- **5 أسماء تبقى بلا بطاقة عمداً** (ليست موردين): «مورد غير معروف» 103 · «مورد عروض الأسعار» 21 ·
  «أحمد» 14 · «اسم المورد بالعربية» 6 · «مجموعة الذيابي (طلب شراء داخلي)» 3.
- الاختبار: تأكيدان جديدان لبصمة السجل السعري — **الحزمة 504**.

#### 🔎 استكمال البيانات — استُقصيت **كل** مصادر النظام (2026-09-08)
بأمر المالك «أكمل بيانات الـ34 من السجل أو ملاحظات الأوامر». **الحصيلة: 55 بطاقة مورد اكتسبت بيانات.**
- **المصادر التي أعطت:** ملاحظات أوامر الشراء (المقطع المُطبَّع) · **بذرة `DATA.suppliers`** ·
  **بذرة `PO_SUPPLIER_SEED` المُزالة — استُعيدت من git** (`git show 8c28951:index.html`) فهي تحمل هواتف
  وتخصصات وعناوين لا توجد في `DATA`. الملء **لا يمسّ حقلاً مملوءاً** (`coalesce` على الفارغ فقط).
- **⚠️ الصفر البادئ كان ساقطاً في البذرة:** الأرقام مخزَّنة **أعداداً** في JSON فصار الجوّال
  «542057081» والهاتف «112410019». يُعاد الصفر حين يكون الشكل قاطعاً (9 أرقام تبدأ بـ5، أو أرضيّ 1x).
- **حارس هويّة:** السجل التجاري/الرقم الضريبي **لا يُنسَخ إن كان مستعمَلاً لمورد آخر** — يمنع خلق
  هويّة مكرّرة. عضّ فعلاً: منع نسخ ضريبيّ «قمة امال» إلى «الراشد للإطارات».
- **تنظيف القيم النائبة** («—» و«-»): كانت تُحسَب «مملوءة» فتمنع الملء وتُعرَض كأنها بيانات.
- **🐛 مورد مكرّر كشفه تصادم الهويّة لا الاسم:** «SmartHandlesLineCo» و«شركة خط المقابض الذكية»
  **نفس الرقم الضريبي والجوال وجهة الاتصال والتخصص** — والاسم اللاتينيّ ترجمة حرفيّة للعربيّ.
  دُمِج (152 مورداً الآن). **القاعدة: تصادم السجل/الضريبي دليل أقوى من تشابه الاسم — في الاتجاهين.**
- **التغطية بعد الاستكمال (من 152):** هاتف/جوال **124** · تخصص **112** · مدينة **86** · هويّة (سجل أو
  ضريبي) **84**. **و«أوامر شراء بلا مورد مسجَّل» = صفر.**
- **⚠️ 27 مورداً بلا أي قناة تواصل — ولا مصدر لهم في النظام إطلاقاً.** استُقصيت جميعها ولم تُعطِ شيئاً:
  ملاحظات الأوامر (فارغة لهم) · ملاحظات السجل السعري (**صفر ملاحظة** على 190 سجلاً لهم) ·
  `proc_supplier_registrations` (**صفر تطابق**) · `proc_rfq_quotes` (صفّان لموردين آخرين) · البذرتان ·
  **`proc_audit_log`** (نتيجة واحدة بلا بيانات). ⇒ إدخال يدويّ من المالك، لا استخراج.
- **⚠️ ثلاثة تصادمات هويّة قائمة (سابقة لا مُستحدَثة — الحارس منع الرابع):** سجل `1010587730` لـ«خط
  المقابض» و«اعمال اليسر» · سجل `7004942491` لـ«قمة امال» و«الراشد» · ضريبيّ `310079237600003`
  لـ«شبكة دبي» و«ارتباط التكنولوجيا». أحد الطرفين في كلٍّ خطأ — يحتاج قرار المالك.

#### 🧩 شاشة ضبط بيانات الموردين — «من ينقصه ماذا» بنقرة (2026-09-08، واجهة فقط)
بأمر المالك «ابنه ليتم تحديث وضبط معلومات الموردين». الإكمال إدخال بشريّ (لا مصدر باقٍ)، فدور
الشاشة أن تُظهر النقص وتفتح التعديل فوراً بدل البحث اليدويّ عمّن ينقصه ماذا.
- **`SUP_GAP_FIELDS` (أربعة حقول بترتيب الأهميّة):** وسيلة تواصل (هاتف **أو** جوال **أو** بريد —
  أيّها يكفي) · هويّة (سجل تجاري **أو** رقم ضريبي) · التخصص · المدينة. `supDataGaps(s)` تُرجع
  الناقص، و`supGapMatch(s,mode)` مرشّح الشاشة.
- **⚠️ `supVal` تُعامل «—» و«-» وسطور الشرطات فراغاً** — كانت مخزَّنة قيماً نائبة فتبدو بياناتٍ
  وتُخفي النقص (نُظِّفت في القاعدة، والحارس باقٍ في الكود لأن الإدخال قد يعيدها).
- **مُنتقي `#sup-gaps`** (كل الموردين · بيانات ناقصة · بلا وسيلة تواصل · بلا سجل/ضريبي · بلا تخصص ·
  بلا مدينة · مكتملة) + **ملخّص في سطر العدّاد** كل رقم فيه رابط يضبط المرشّح · **شارة على البطاقة**
  تسمّي الناقص (حمراء للحرِج = بلا تواصل، كهرمانية لغيره) و**نقرها يفتح نموذج التعديل مباشرةً**
  (`stopPropagation` فلا تفتح بطاقة المورد). الترتيب الافتراضي «الأكثر عروضاً» فيتصدّر الأهمّ
  (مورد بـ114 عرضاً بلا وسيلة تواصل).
- **⚠️ `supplierCard` مُستعمَلة في موضعين:** الدليل و**«أعلى الموردين» في لوحة التحكم** — فالثانية
  تمرّر `criticalGapsOnly=true` (نقص المدينة/التخصص ضجيج على لوحة التحكم). محروس بتأكيد.
- **مُتحقَّق بالمتصفّح (سطح مكتب 1280 وجوال 393):** المرشّح يُرجع الثلاثة الصحيحة · الشارة **28px**
  (هدف لمس صالح) · **صفر انزلاق أفقيّ · صفر خطأ صفحة** · ونقر الشارة يفتح نموذج التعديل بالاسم
  الصحيح (المودال يُفتح بعد 150ms عمداً بعد إغلاق بطاقة المورد — لا تقس قبلها).
  **12 تأكيداً جديداً — الحزمة 518، ومُتحقَّق أنّها تعضّ** (إزالة التصفية والحرِج ⇒ 3 إخفاقات).

### 🔤 نصّ المستندات مقلوب ومتداخل — pdf.js كان بلا أي موارد خطوط (2026-09-08)
**بلاغ المالك بلقطة:** شهادة المحتوى المحلي تُعرَض بحروف عربية **منفصلة ومقلوبة** وكلمات لاتينية
**متداخلة بمسافات داخل الكلمة** («The Loca l Con tent»).
- **الجذر (فجوتان موثَّقتان في pdf.js، كلتاهما كانت مفقودة في `getDocument`):**
  (١) **`standardFontDataUrl` غائب** — مستند يستعمل أحد الخطوط الأربعة عشر القياسية **بلا تضمين**،
  فيستبدله pdf.js بخطّ الجهاز؛ عروض المحارف تختلف ⇒ تداخل ومسافات داخل الكلمة.
  (٢) **`cMapUrl`/`cMapPacked` غائبان** — خطوط CID/Type0 (وكل مستند عربي مُنسَّق تقريباً منها) تحتاج
  جداول cMap لربط رموز الخطّ بالمحارف ⇒ بدونها تُرسَم الحروف العربية مفكّكة وبترتيب مقلوب.
- **العلاج:** تضمين `vendor/cmaps/` (169 ملفاً) و`vendor/standard_fonts/` (16) من **نفس إصدار المكتبة
  المثبَّت (4.6.82)** + تمرير الأربعة: `standardFontDataUrl` · `cMapUrl` · `cMapPacked:true` ·
  **`useSystemFonts:false`** (فلا يتبع العرضُ ما هو مُثبَّت على جهاز المستخدم — سلوك واحد على
  الآيفون والأندرويد وسطح المكتب). **وأيضاً `siExtractFromPDF`** (استخراج النصّ بالاستيراد الذكي)
  اكتسب جداول cMap — كان يستخرج نصّاً عربيّاً مفكّكاً للسبب نفسه.
- **⚠️ CSP:** الموارد تُجلب بـ`fetch` من **نفس الأصل** (`/vendor/…`) فتغطّيها `connect-src 'self'` —
  **بلا أي توسيع للترويسة** (تكرار درس `blob:`). و`_headers` يكاش `/vendor/*` أسبوعاً فالإصدار مثبَّت.
- **مُتحقَّق بالقياس تحت CSP الإنتاج** (لا استنتاج): قبل الإصلاح **صفر طلب** لموارد الخطوط؛ بعده
  مستند بخطّ قياسي غير مُضمَّن يجلب `standard_fonts/LiberationSans-Regular.ttf` (وتتغيّر بكسلات
  النصّ المرسومة فعلاً)، ومستند بخطّ CID يجلب `cmaps/UniGB-UCS2-H.bcmap` و`Adobe-GB1-UCS2.bcmap` —
  و**صفر انتهاك CSP** في الحالتين. **9 تأكيدات جديدة (المجموع 492)، مُتحقَّق أنّها تعضّ** (إزالة
  الخيارات ⇒ 4 إخفاقات). ⚠️ لم أُعِد إنتاج ملفّ المالك نفسه (غير متاح لي) — التحقّق على مستندَين
  مصنوعَين يستدعيان المسارين بالضبط.

### 🧊 «السكرولينق يعلق في بعض الشاشات أحياناً» — عيبان مختلفان تماماً (2026-09-09)
**بلاغ المالك.** القياس (لا القراءة) كشف سببين مستقلَّين، كلاهما يُنتج الإحساس نفسه:

**(أ) قفل التمرير يتسرّب فيبقى مقفلاً — سبب «التعليق» الكامل حتى إعادة التحميل.**
`document.body.style.overflow` كان يُضبَط في **ثلاثة** مواضع (`openModal` · `showPrintPreview` ·
`docvOpen`) ويُفَكّ في **أربعة**، **ونافذتان تُفتَحان بـ`style.display='flex'` بلا مرور بأيٍّ منها**
(`#modal-reg-detail` تفاصيل طلب التسجيل · `#modal-email-templates`). فمسار حقيقيّ مُعاد إنتاجه:
تفاصيل الطلب (لا تقفل) ← فتح وثيقة (`docvOpen` **يقفل**) ← إغلاق العارض (`docvClose` يرى التفاصيل
مفتوحة فيمتنع عن الفكّ) ← إغلاق التفاصيل (`closeRegDetail` **لا تفكّ**) ⇒ **الصفحة غير قابلة
للتمرير**. وهذا يفسّر «بعض الشاشات أحياناً»: شاشة التسجيلات، وفقط عند فتح وثيقة من داخل التفاصيل.
- **العلاج — مصدر واحد مشتقّ من الحالة:** `anyOverlayOpen()` + `syncScrollLock()`؛ **لا أحد يضبط
  `body.style.overflow` مباشرةً** (حارس: موضع ضبط واحد فقط في الشيفرة). الطبقات المحسوبة:
  `.modal-overlay.active` و`.print-preview-overlay.active` (معاينة الطباعة صنفها مختلف — سقطت من
  أوّل صياغة وأمسكها القياس) + الثلاث المفتوحة بـ`display:flex`.
- **ثلاثة عيوب مرافقة أُصلحت في الطريق (كلّها مقيسة):** إغلاق معاينة الطباعة كنافذة عادية (Esc/نقر
  الخلفية) كان يترك `body.print-preview-active` والمستند في `#pp-content` ⇒ صار `closeModal` يوجّهها
  إلى `closePrintPreview` · النوافذ المُلحَقة وقت التشغيل (`modal-sup-dupes`/`modal-po-missing`/
  `modal-po-receive`) لم تكن تقفل خلفها إطلاقاً ⇒ وُسِمت `dataset.ephemeral='1'` فتقفل **وتُزال** من
  DOM عند الإغلاق (كانت تبقى بمعرّف مكرَّر يلتقطه `getElementById` في الفتحة التالية) · إغلاق نافذة
  داخلية كان يفكّ القفل والخارجية ما تزال مفتوحة.

**(ب) انزلاق أفقيّ يخطف تمرير الإصبع — سبب «التعليق» المتقطّع على الهاتف.**
عنصر الشبكة لا يصغر دون **min-content** ابنه (`min-width:auto` الافتراضيّ)، فصفٌّ لا يلتفّ يدفع
العمود كلّه أعرض من الشاشة، وعلى الهاتف يلتقط التمرير الأفقيّ الحركة المائلة فيبدو الرأسيّ متوقّفاً:
- **شاشة الإدخال:** «مبدّل الوضع» (3 أزرار، `inline-flex` بلا التفاف) مقاسه الأدنى **367px** ⇒
  `.entry-layout` يصير **429px** على شاشة 393px = **50px انزلاقاً**.
- **لوحة التحكم عند 360px:** صفّ «توزيع الإنفاق حسب الفئة» (رسم دائريّ 100px + مفتاح ألوان) مقاسه
  الأدنى **300px** ⇒ بطاقة الرسم **346px** = **29px انزلاقاً**.
- **العلاج (كلّه داخل `@media(max-width:900px)` — سطح المكتب مُتحقَّق أنّه بلا تغيير):**
  `.entry-layout > *{min-width:0}` · `.mode-toggle{display:flex;flex-wrap:wrap;width:100%}` ·
  `.dash-charts > *{min-width:0}` · `#dash-cat-chart > div{flex-wrap:wrap}` ·
  `.chart-card{overflow-x:auto}` (شبكة أمان: ما يفيض يمرّر داخل بطاقته لا على الصفحة).
- **⚠️ درس قياس متكرّر:** `html{overflow-x:clip}` يجعل `scrollWidth==clientWidth` أحياناً — **ارفع
  القناع قبل القياس** (`documentElement.style.overflowX='visible'`). أُعيد القياس بعد رفعه: صفر.

**التحقّق:** مسح 11 شاشة × 4 مقاسات (360/393/768/1440) ⇒ **صفر انزلاق · صفر بقايا قفل · صفر
`nav-open` عالق · صفر خطأ صفحة**، والتمرير الرأسيّ يعمل فعليّاً (`scrollY` 0→400)، تحت **CSP الإنتاج**.
**الاختبار: 34 تأكيداً جديداً (المجموع 552)** — منها **5 سلوكية تُشغّل `syncScrollLock` على DOM
مُقلَّد** بالمسار المُبلَّغ عنه حرفيّاً. **مُتحقَّق أنّ الحرّاس تعضّ:** إعادة العلل الخمس عمداً ⇒
**6 إخفاقات**. ⚠️ لا تُبدِّل التأكيدات السلوكية بفحص نصّيّ — الفحص النصّيّ هو ما مرّ عليه العيب أصلاً.

### 📣 حملة تسجيل الموردين: دعوة واتساب برابط شخصيّ يُكمِل البطاقة القائمة (2026-09-10، **مدموج ومنشور** — PR #95، `bb830f2`)
**طلب المالك:** رسالة واتساب للموردين الذين أُدخِلوا يدويّاً أو من عروض الأسعار بلا تسجيل، وبعد
تسجيلهم **تُكمَل بياناتهم في بطاقتهم الحالية** — لا بطاقة جديدة ولا إدخال يدويّ، وتبقى البطاقة السابقة.
- **القياس قبل الكود (القاعدة الحيّة):** **117 من 155** بلا طلب تسجيل · **90** لديهم جوال · **73 فقط**
  لديهم سجل تجاري. و`approveRegistration` كانت تطابق **بالسجل التجاري وحده** ⇒ دعوتهم كما هي كانت
  **ستُنشئ بطاقة ثانية لأغلبهم** وتشقّ أوامر الشراء والسجل السعري بين بطاقتين.
- **الربط القاطع — رابط شخصيّ لكل مورد:** هجرة `db/system1-registration-link-supplier.sql` (عمود
  nullable `link_supplier_id`، **مطبَّقة حيّاً**) + `functions/api/supplier-invite-link.js`: رمز HMAC
  موقَّع بمعرّف البطاقة — `POST` للموظّف المُصادَق same-origin يسكّه، و`GET` العامّ يتحقّق **حسابيّاً**
  ويعيد بيانات التعبئة بقائمة بيضاء. **بلا جدول رموز** (نمط `doc-renew.js`).
  ⚠️ **لا يُبَثّ أي حقل بنكيّ** عبر الرمز (`iban`/`account_number`/`bank_name`/`account_holder`) — محروس بتأكيد.
- **`register.html` وضع `?s=`:** يُعبّئ ما نعرفه **بلا إتلاف ما كتبه المورّد** + لافتة تُطمئنه أن ملفّه
  الحالي يُحدَّث + يحفظ `link_supplier_id` مع الطلب. أي فشل (رمز منتهٍ/شبكة) ⇒ **تسجيل عاديّ صامت**.
- **الاعتماد — مطابقة رباعية بترتيب القطعيّة** (`regMatchExistingSupplier`): رابط الدعوة ← السجل
  التجاري ← الرقم الضريبي ← `supKeyStrong`. **⚠️ المرتبة الرابعة مقيَّدة عمداً:** تُرفَض عند تعارض
  الهويّة (درس «سراكو») وعند التباس الاسم على بطاقتين — لا تخمين.
- **`regSupplierFill` — الفارغ فقط:** كل الحقول لا البنكية فقط · لا يُتلف مملوءاً · «—» فراغاً ·
  **ولا يُبدّل اسم البطاقة أبداً** (مفتاح ربطها بأوامرها وسجلّها السعريّ) · ورقم الطلب يُلحَق بالملاحظات
  بلا تكرار. و`_loadSupplierRegLink` صار يبحث بـ`link_supplier_id` أوّلاً فتظهر الوثائق على البطاقة.
- **الحملة (`regCampBuild`/`renderRegCampaign` في شاشة التسجيلات):** من لم يُسجّل مرتَّبين بعدد العروض ·
  زرّ واتساب لكل مورد (`wa.me` بالنصّ مُعبّأً — لا Business API) · نسخ · تصدير CSV (سكّ دفعيّ 200/نداء).
  ⚠️ **مطابقة الحملة متساهلة عمداً** (عكس الاعتماد): دعوة مسجَّل = إزعاج، أهون من بطاقة مكرّرة.
- **الرسالة (`regCampMessage`):** بتنسيق واتساب. **⚠️ الرابط في سطر مستقلّ** وإلّا لم يصر قابلاً للنقر
  بجوار نصّ عربيّ. + بطاقة مصوَّرة `supplier-invitation-whatsapp.html` (1080×1350، هويّة الدعوة السابقة)
  تُولَّد بـ`npm run build:invite-card`. 🐛 **أوّل نسخة فاض محتواها فقُصّ التذييل** (1552 على بطاقة 1350)
  فصار المولّد **يُفشِل البناء على أي فيض** — الصورة تبدو سليمة حتى تنظر إلى أسفلها.
- **🐛 تنظيف مرافق:** أُزيل من `register.html` سكربت `<image-slot>` مضمَّن `data:` URI — **41KB يُنزَّل مع
  كل فتح وتحجبه CSP فلا يعمل أصلاً**، والحملة تسوق ~90 مورداً لهذه الصفحة. **انتهاكات CSP: 1 ← 0.**
- **الاختبار: 55 تأكيداً جديداً (المجموع 607)** منها سلوكية على **عميل مُقلَّد** تمشي المراتب الأربع.
  **مُتحقَّق أنّ الحرّاس تعضّ: ستّ علل ⇒ 6 إخفاقات.** + معاينة متصفّح تحت CSP الإنتاج.
- **⚠️ حالة البيئة المقيسة:** `SUPABASE_SERVICE_ROLE_KEY` **مضبوط في Production** (`suppliers.aldeyabi.com`
  و`aldeyabi-procurement.pages.dev` = `service_key:true`) و**غير مضبوط في Preview** — فنقاط الخدمة كلّها
  (هذه و`reg-doc`) تعيد `503 not_configured` على المعاينة. **لا تشخّص ذلك عيباً في الكود.**
- **⚠️ 27 مورداً بلا أي قناة تواصل** لا يمكن دعوتهم — يظهرون في اللوحة موسومين «بلا رقم» (إدخال يدويّ).

#### ✅ فحص «هل نخسر بيانات مورّد؟» قبل الدمج + النشر الحيّ (2026-09-10)
بأمر المالك «ادمج وانشر وتأكّد ألّا نخسر بيانات الموردين إذا رفعوها». **الفحص قبل الدمج:**
- **إرسال تسجيل كامل بعد التعديلات** (متصفّح فعليّ تحت CSP الإنتاج، بشبكة معترَضة فلا كتابة على
  الإنتاج): 6 وثائق رُفِعت · **الإدراج بـ61 حقلاً** · `link_supplier_id` وصل · كل `doc_paths` ·
  صفر خطأ صفحة · صفر انتهاك CSP.
  ⚠️ **درسان من الاختبار نفسه:** (أ) `let SB` **ارتباط معجميّ لا خاصيّة على `window`** — إسناد
  `window.SB` لا يُبدّله، الإسناد المجرّد `SB = …` هو ما يعمل داخل `page.evaluate`؛ (ب) فحص
  `!el?.classList.contains('hidden')` يُعطي **نجاحاً كاذباً** حين يغيب العنصر (`!undefined`=true).
- **قبول المخطّط للحمولة (استعلام قراءة على الإنتاج):** **61/61 حقلاً تطابق أعمدة حقيقية · صفر
  عمود مجهول · صفر عمود إلزاميّ ناقص.** ⚠️ قناة MCP للقراءة فقط (`read-only transaction`) فلا
  يمكن إثبات الإدراج بالكتابة — الفحص الساكن هو البديل الصحيح.
- **إثبات سلوكيّ أنّ الاعتماد لا يُتلف بطاقة قائمة** (عميل مُقلَّد): لا بطاقة ثانية · الاسم لم
  يُمَسّ · **لا حقل مملوء أُتلف** (المدينة بقيت «الرياض» رغم أن الطلب قال «جدة») · الفراغات فقط
  مُلئت (جوّال/عنوان/ضريبي/بنك/آيبان) · الطلب صار `approved` · والتدقيق كُتِب.
- **🐛 عيب وُجِد وأُصلح قبل الدمج:** `approveRegistration` كانت **بلا أي `logAudit`** — وهي الآن
  تُعدّل بطاقة قائمة، فبلا أثر لا سبيل لتتبّع أي بطاقة تأثّرت. صارت تكتب `reg_approve_merge`/
  `reg_approve_create` بمعرّف البطاقة ورقم الطلب و**المرتبة التي طابقت** (`invite_link`/
  `commercial_reg`/`tax_id`/`name_key`) وأسماء الحقول المملوءة — بعد نجاح كل الكتابات، وفشل
  التدقيق نفسه لا يُسقِط اعتماداً نجح. **الحزمة: 614 تأكيداً، صفر إخفاق.**
- **✅ النشر الحيّ مُتحقَّق على النطاقين** (`suppliers.aldeyabi.com` و`aldeyabi-procurement.pages.dev`):
  لوحة الحملة موجودة · `/api/supplier-invite-link` = `{ok:true, service_key:true, token_secret:true}` ·
  **سكّ بلا مصادقة = 401** · **رمز مزوَّر = 401** · صفحة التسجيل 200 بوضع الدعوة مدمجاً وبلا السكربت
  الميّت · `/api/reg-doc` = `{ok:true, store:"r2"}`. **ولم يُكتب أي صفّ اختبار على الإنتاج** (مُتحقَّق باستعلام).

### 👷 حسابات موظفي الصيانة والتشغيل — نطاق بالقطاع + استلام + رفع طلبات (2026-09-10)
**طلب المالك:** حسابات لموظفي الصيانة والتشغيل يتابعون **أوامر الشراء التي تخصّهم فقط** ويرون تحديثاتها،
**ويرفعون طلباتهم عبر النظام الأساسي نفسه**، فيصير كامل عملهم في شاشة واحدة.
**قرارات المالك (AskUserQuestion):** ① النطاق = **القطاع** ② المبالغ = **مفتاح صلاحية قابل للضبط مُطفأ
للموظّف** ③ الطلبات = **داخل `index.html`** ④ الصلاحيات = **تأكيد الاستلام + التعليق**.

**القياس قبل التصميم:** **لا نطاق رؤية في النظام إطلاقاً** — RLS على كل `proc_*` هو
`auth_all FOR ALL USING(true)`، فكل مستخدم يقرأ كل صفّ (أوامر بمبالغها · `proc_users` بهاشات كلمات السرّ).
115 أمراً: الصيانة والتشغيل 73 · الإدارة العامة 21 · الإنشاءات 16 · النقليات 4 · **بلا قطاع 1**.
`officer` = **شخصان** (مشتريان) لا الموظف الميدانيّ. `proc_purchase_requests` **فارغ تماماً**.

- **`db/system2-staff-scope.sql` (تُطبَّق من المالك في SQL Editor — لم تُطبَّق بعد):**
  `proc_users.scope_sectors` (فارغ ⇒ **غير مُنطَّق** ⇒ سلوك اليوم حرفيّاً — الأربعة القائمون بلا تغيير) +
  `proc_me`/`proc_scope_sectors`/`proc_is_scoped`/`proc_can_see_po`/`proc_can_view_amounts`/`proc_can_see_pr` +
  **سياسات لكل أمر** (⚠️ `FOR ALL` يُلغي تقييد SELECT — درس هجرة البوابة 009) وتُحذف كل السياسات القائمة
  أوّلاً فينتهي كل جدول إلى حالة معروفة + عمود `comments` + عرض `proc_po_visible` (`security_invoker`) +
  **كتلة تراجع** في نهاية الملف.
- **⚠️ ثغرة تصعيد سُدّت:** `proc_users_guard` يمرّر أي UPDATE لا يمسّ قائمته الحسّاسة كـ«تعديل حميد»،
  و`scope_sectors` عمود جديد ⇒ **موظّف يمنح نفسه كل القطاعات بـPATCH**. أُضيف العمود لفحص الحارس.
- **⚠️ `proc_can_view_amounts` افتراضيّها `true`** حين يغيب المفتاح — مطابقةً لـ`hasPermission`
  (`userDefault:true`). **لا تستعمل `pr_has_perm` القائمة هنا** (افتراضيّها `false`) وإلا فقد المستخدمون
  القائمون مبالغهم فجأة. حارسها `SC7`.
- **⚠️ الكتابة عبر RPC لا `poSave`:** صفّ المُنطَّق يصل من العرض بمبالغ **مُصفَّرة**، ورفعه كاملاً يمحوها.
  `po_record_receipt` (تقصّ الكميّة عند المتبقّي خادميّاً وتحسب الحالة وتكتب التدقيق) و`po_add_comment`.
- **⚠️ لا تُسحَب `SELECT` عن الجدول الأساس:** القفل المتفائل في `poCloudUpsert` يقرأ `updated_at/updated_by`
  منه — سحبها تكسره لكل المستخدمين.
- **حجب المبالغ من نقطة واحدة:** `fmtPrice` هي **المنسّق النقديّ الوحيد** (172 موضع نداء — مُتحقَّق أنّ كل
  وسائطها نقديّة) فحُجِبت عندها بدل 172 تعديلاً، ومعها `tafqitSAR` (وإلّا تسرّب المبلغ **حروفاً**).
  **⚠️ قاعدة: لا تُنشئ منسّقاً نقديّاً يلتفّ حول البوّابة** (محروس: `fmtPriceRaw`/`tafqitSARRaw` نداء واحد لكلٍّ).
- **مركز التقارير قائمة بيضاء لا سوداء:** لا يظهر لمن لا يرى المبالغ إلا ما وُسِم `noMoney:true` صراحةً —
  فتقريرٌ جديد بلا العلامة يُحجَب لا يتسرّب؛ و`reportRun` يُعيد الفحص (الفهرس يُستدعى من onclick).
- **الترطيب:** `loadAll` لا يطلب الكتالوج/الموردين/السجل للمُنطَّق ويقرأ من `proc_po_visible`؛
  و`refreshFromCloud` **لا يدمج البذرة** له — وهي في الملف نفسه (808 سجلات سعرية) فلولا ذلك وصلته الأسعار
  رغم إحكام القاعدة؛ ويتخطّى `prjAutoUnify` (يعيد كتابة الأوامر بلا صلاحية ⇒ فشل صامت وتوست كاذب).
- **الطلبات:** الصفحة كانت **مبنيّة بالكامل** والناقص أنّ **لا شيء ينادي `prGoView('create')`**. أُضيف
  التبويب + إشعار `/api/notify {kind:'pr'}` (نفس نقطة `requests.html` — البوابة الداخلية لم تكن ترسل شيئاً
  إطلاقاً) + قفل القطاع على نطاق الموظّف + إخفاء أعمدة السعر و«بالكلمات».
- **⚠️ بند إعداد للمالك لا خطأ كود:** `proc_departments` بلا بذرة وصفر مستخدم له `department_id`، ومرحلة
  «مدير المشروع/القطاع» تحلّ معتمِدها منها ⇒ يقف الطلب بلا معتمِد. **لافتة صريحة** في نموذج الطلب بدل الصمت.
- **🐛 كشفه المتصفّح لا الفحص النصّيّ:** `.nav-item[data-page="pr"]{display:none !important}` **لا يغلبها
  `style.display='flex'` سطريّاً** — فمدخل الطلبات بقي مخفيّاً عن صاحبه. العلاج **قاعدة CSS مضادّة**
  (`body.role-scoped …{display:flex !important}`). + وجهتا «طلباتي/المتابعة» في الشريط السفليّ (`data-scoped-only`)
  لأن موظّف الميدان يعمل من هاتفه.
- **الاختبار: 640 تأكيد نظام 2 + 16 تأكيد SQL** (`db/system2-tests/`: كعب Supabase + RLS **فعليّة** عبر
  `SET ROLE authenticated` وهوية JWT، والهجرة idempotent). **مُتحقَّق أنّ الحرّاس تعضّ: 4 علل SQL + 8 علل واجهة
  ⇒ 12 إخفاقاً، كلٌّ على تأكيده الصحيح، والنسخة السليمة تمرّ.**
  ⚠️ **درس تحقّق تكرّر مرّتين:** أوّل «إثبات عضّ» كان **إيجابيّة كاذبة** — `CREATE DATABASE sX` يُطوى إلى `sx`
  فكل تشغيل فشل على قاعدة مفقودة لا على التأكيد؛ ومرساة `!=` والكود `!==` فلم تُطبَّق العلّة أصلاً.
  **تحقّق دائماً أن العلّة وصلت، وأن النسخة السليمة تمرّ في نفس الأداة.**
  + معاينة متصفّح **تحت CSP الإنتاج** للموظّف (4 وجهات · صفر مبلغ · تقرير واحد · صفر انزلاق على 393px)
  وللأدمن ولمستخدم قائم غير أدمن **بلا المفتاح الجديد** (11 وجهة · المبالغ ظاهرة · 20 تقريراً) —
  صفر خطأ صفحة وصفر انتهاك CSP في الثلاثة.
- **⚠️ ما لا يفعله التصميم (بصراحة):** إخفاء المبالغ **ليس محكماً تشفيريّاً** — كل العملاء يتشاركون دور
  `authenticated` فامتيازات الأعمدة لا تختلف بين مستخدم وآخر؛ المبالغ تُصفَّر في البيانات المطلوبة وتُحجب في
  العرض، لكن مُنطَّقاً يجيد أدوات المطوّر يقرأ الجدول الأساس لصفوف **قطاعه**. **نطاق الصفوف — الحدّ الذي طلبه
  المالك — محكم في القاعدة.** إحكام المبالغ يتطلّب دور Postgres منفصلاً أو توسيط نقطة خادمية.
- **⚠️ متبقٍّ للمالك:** تطبيق `db/system2-staff-scope.sql` · ضبط قطاع الأمر الوحيد بلا قطاع · إنشاء حسابات
  الموظفين وإسناد قطاعاتهم · ضبط `proc_departments` ومدرائها قبل اعتماد الطلبات.
#### ✅ التطبيق الحيّ والنشر (2026-09-10) — بتفويض المالك «قم أنت بذلك، تم ربطك بالقاعدة»
**مُطبَّق على قاعدة الإنتاج `yofcaxvstjcrmbgciwym` ومنشور على `main`** (كوميت `8d483fb`، بصمة الإنتاج =
بصمة المستودع). الهجرات الست: `system2_staff_scope_1_columns_helpers` → `_2_users_guard` →
`_3_policies` → `system2_goods_receipt_column` → `_4_write_rpcs` → `_5_visible_view` (+ `system2_fix_receipt_date_type`).
- **الفحص الأخطر أوّلاً (قبل أي كتابة):** عمود `email` **فارغ لأربعة من الخمسة** — لكن `pr_username()`
  تطابق على **اسم المستخدم** لا البريد، فتُحلّ هويّة الأربعة النشطين جميعاً. **مُثبَت تجريبيّاً** بانتحال
  بريد الدخول الفعليّ لكلٍّ داخل معاملة مُلغاة، والمجهول يُرجع NULL. لولا هذا الفحص لقطعت الهجرة الوصول.
- **تحقّق ما بعد التطبيق على الإنتاج نفسه (معاملة مُلغاة، صفر صفّ مكتوب):** الأربعة القائمون **115 أمراً
  ومبالغهم مرئيّة** (صفر انحدار) · موظّف مُنطَّق **73 أمراً فقط** · صفّ واحد من `proc_users` · صفر كتالوج/سجل/تدقيق ·
  **صفر مبلغ وصفر سعر بند مسرَّب** · محاولة الكتابة المباشرة **0 صفّاً**. و`po_record_receipt` على أمر إنتاج
  حقيقيّ: كمية 999999 **قُصّت إلى 2**، الحالة «تسليم جزئي»، والاستلام/التعليق خارج النطاق **مرفوضان**.
- **🐛 عيب نوع كشفه الإنتاج وحده:** `actual_delivery` عمود `date` و`to_char` يُرجع `text` ⇒ «CASE types date
  and text cannot be matched». **الجذر كعب الاختبار:** كتبتُ أنواعه من منظور العميل (التواريخ نصوص في JS).
  صارت أنواع `00_stub.sql` مطابقة لـ`information_schema` الإنتاج حرفيّاً، **ومُتحقَّق أنّها تمسك العيب**
  (إعادته ⇒ الحزمة تفشل بنفس رسالة الإنتاج). **⚠️ قاعدة: كعبٌ لا يطابق أنواع الإنتاج ليس اختباراً.**
- **🐛 `db/goods-receipt.sql` لم يُطبَّق على الإنتاج قطّ:** عمود `receipts` غائب، و`poCloudUpsert` يتخطّاه
  بصمت عند غيابه ⇒ **سجلّ أحداث الاستلام (مَن ومتى وبأي ملاحظة) لم يصل السحابة أبداً** — بقي في متصفّح من
  سجّله. الكميّات نجت لأنها داخل `items`. أُضيف العمود (و`source` رفيقه، غائب كذلك). **قرار المالك: لا
  استرجاع للقديم — الجديد يكفي.**
- **البيانات المضبوطة:** الأمر الوحيد بلا قطاع (`P.O-DG26-3184`) ⇒ `الإنشاءات` — **استنباط قاطع** (16 أمراً
  لنفس المشروع وبنفس المسؤول كلّها الإنشاءات)، بأثر تدقيق. **صفر أمر بلا قطاع الآن.** + ثلاثة أقسام جديدة
  (`DEP-OPS`/`DEP-CON`/`DEP-LOG`) فصار لكل قطاع قسم بمدير وتُحلّ مرحلة «مدير القطاع».
  **⚠️ المدير الافتراضيّ = المالك** (لا مدراء قطاعات بين المستخدمين؛ الآخران مشتريان) — يُعاد إسناده من اللوحة.
- **⚠️ المتبقّي الوحيد — إنشاء حسابات الموظفين:** لا أستطيع فعله بنفسي (يتطلّب إنشاء حساب Auth بمفتاح الخدمة
  أو من اللوحة، وكلمة سرّ لا أملكها ولا يصحّ أن أخترعها). يُنشَأ من **الإعدادات ← المستخدمون ← مستخدم جديد**:
  اسم + كلمة سرّ + الدور «مستخدم عادي» + تحديد **القطاع** في «نطاق أوامر الشراء» + صلاحية «تسجيل استلام
  البضاعة»، وإلغاء تحديد «عرض المبالغ والأسعار».

### 📨 دورة الطلب الكاملة لموظّف الصيانة — قوالب متكررة · استفهام · مراحل محكومة (2026-09-10)
**طلب المالك (نصّاً):** «يرون حالة الطلب، **من استلمه ومن بدأ في تسعيره وكم له يوم** منذ أن بدأ العمل
به · **يحفظون الطلبات الشهرية المتكررة للمشاريع بكمّياتها ومسمّياتها** دون كتابتها كل مرّة ·
**المشتريات تُرجعه للتعديل أو تستفهم من خلال النظام** · وكل هذا **بتنبيهات بريد**، محكوم ولا نفقد
المعلومة». ثم أضاف: **«موظف الصيانة لا تظهر له كل الشاشات — فقط شاشة أوامر الشراء التي تخصّه وشاشة
رفع الطلبات ومتابعتها، باقي النظام ما يخصّه أبداً»** + **«الطلبات تظهر لموظفي المشتريات في النظام»**.

**⚠️ الترتيب: `db/system2-request-flow.sql` تُطبَّق بعد `db/system2-staff-scope.sql`** (تبني على
`proc_me`/`proc_is_scoped`/`proc_can_see_pr`/`proc_drop_all_policies`). **✅ مُطبَّقة حيّاً على
`yofcaxvstjcrmbgciwym`** باسم `system2_request_flow_templates_messages_stages`.

- **`proc_pr_templates`** — القالب الشهريّ للمشروع (اسم/قسم/قطاع/مشروع/أولوية/مبرّر + `items` jsonb +
  `use_count`/`last_used_at`). RLS: يراه صاحبه وزملاء قطاعه، ولا يكتبه إلا صاحبه؛ وغير المُنطَّق
  (المشتريات/الإدارة) يرى الكل. **القالب بيانات لا التزام:** استدعاؤه يملأ نموذج طلب جديد يمرّ
  بسلسلة الاعتماد كاملةً — لا اختصار حوكميّ (محروس بتأكيد).
- **`proc_pr_messages` + `pr_post_message(pr_id, body, kind)`** — قناة **استفهام** كانت غائبة تماماً:
  البديل قبلها إرجاع الطلب كلّه (يُصفّر السلسلة) أو مكالمة تضيع. **لا تُغيّر حالة الطلب ولا تمسّ سلسلة
  الاعتماد.** **ثلاثة أقفال على الكتابة المباشرة:** لا امتياز `INSERT` · لا سياسة `INSERT` · لا منح
  للتسلسل ⇒ الكتابة عبر الـRPC وحدها (لا انتحال مؤلِّف). القراءة بـ`proc_can_see_pr`.
- **`pr_proc_stage(pr_id, stage)`** — مراحل المشتريات كانت **UPDATE مباشراً من العميل**: بلا حارس
  صلاحية، ولا تحقّق أن الطلب مُعتمَد، ولا منع للرجوع لمرحلة سابقة (فيُمحى أثر من بدأ العمل). الآن
  `SECURITY DEFINER`: صلاحية مشتريات (أو أدمن) · `status='approved'` · **تقدّم للأمام فقط**.
- **⚠️ منح الجداول صريحة في الملف** ولا تُترك للامتيازات الافتراضية للمشروع (إعداد بيئة لا عقد) —
  كشفه الاختبار المحلّي بـ`permission denied` قبل أي تطبيق حيّ.
- **🐛 والسحب صريح كذلك — عيب كشفه الفحص الحيّ بعد التطبيق:** Supabase تضبط
  `ALTER DEFAULT PRIVILEGES` على `public` تمنح **ALL** لـanon/authenticated على **أي جدول جديد**،
  فـ`GRANT SELECT` وحده **لا يسحب** INSERT/UPDATE/DELETE. مُقاس على الإنتاج:
  `has_table_privilege('authenticated','proc_pr_messages','INSERT')` = **true** بعد المنح — فقفل
  الامتياز الذي وثّقتُه كان **غائباً** (الـRLS منعت فعلاً فلا ثغرة وقعت، لكن الطبقة ناقصة).
  العلاج `REVOKE ALL … FROM authenticated` ثم `GRANT SELECT` (هجرة `system2_request_flow_revoke_message_writes`،
  **مُطبَّقة ومُتحقَّقة حيّاً**: `ins/upd/del = false` و`sel = true`، وanon مسحوب من الجدولين).
  **⚠️ ولماذا مرّ محلّياً: الكعب لم يكن يحاكي تلك الامتيازات الافتراضية** — أُضيفت الآن إلى
  `00_stub.sql`، **ومُتحقَّق أنّ `FL16` تمسك العلّة الحقيقية** (إعادة `GRANT SELECT` وحده ⇒ إخفاق).
  **قاعدة: كل هجرة تُنشئ جدولاً تسحب صراحةً ما لا تريده — لا تفترض أن مجموعة المنح تبدأ فارغة.**

**الواجهة (`index.html`):** تبويب «⭐ القوالب» + «حفظ كقالب متكرّر» أسفل النموذج + شريط «ابدأ من قالب»
· `prJourneyHTML` (خمس مراحل بلغة الطالب + **«⏱ منذ بدء العمل عليه: N أيام»** و**من بدأ**؛ وقبل بدء
العمل يُحسب العمر من الإرسال لا صفراً مضلّلاً) · `prThreadHTML`/`prPostMessage` (المشتريات تسأل والطالب
يجيب) · `prProcStage` عبر الـRPC · **`prNotifyPR` قناة إشعار واحدة** و**الاتجاه يُحسَب على الخادم من
نوع الحدث** (`TO_REQUESTER`/`TO_PROCUREMENT` في `notify.js`) لا يُمرَّر من العميل.

**⚠️ قاعدتان مثبَّتتان بتأكيدات:**
1. **`prPermStrict` — مفتاح `userDefault:true` لا يمنح المُنطَّق دوراً.** `can_manage_rfq` افتراضه
   `true` (مفتاح موظّفي المكتب)، فصفّ موظّف صيانة لا يذكره كان يُقرأ «من المشتريات»: تبويب «الوارد»
   وأزرار «بدأت العمل عليه»، و`prCanSeeAll` تعرض له طلبات زملاء قطاعه بدل طلباته. **الخادم كان يرفض
   الفعل** (`pr_has_perm` افتراضه false) فالأثر واجهة وسعة قائمة لا تجاوز حوكميّ. العلاج: **للمُنطَّق
   المنح صريح أو لا شيء**؛ وغير المُنطَّق بلا أي تغيير. **⚠️ فلا تمنح موظّف الصيانة مفتاح RFQ صراحةً.**
2. **«الشاشتان فقط»:** `SCOPED_PAGES = ['purchase-orders','pr']` و`SCOPED_HOME='purchase-orders'` —
   حتى لوحة التحكم والتقارير خارج نطاقه (تقرير المتابعة الميدانيّ يُطبع من داخل شاشة الأوامر عبر
   `#po-followup-btn`). **لا تُضِف وجهة ثالثة في الشريط السفليّ** — محروس بتأكيد.

**الاختبار: 675 تأكيد نظام 2** (القسم ٣١ + تصحيح تأكيدَي الشاشتين) **+ `FL1–FL16`** في
`db/system2-tests/11_flow.sql` بـRLS فعليّة، **وخروج 0**. **مُتحقَّق أنّ الحرّاس تعضّ: 11 علّة واجهة
+ 8 علل SQL أُعيدت عمداً ⇒ كلّها أفشلت الحزمة**، والتشغيل الضابط للنسخة السليمة يمرّ في الأداة نفسها.
+ معاينة متصفّح تحت CSP الإنتاج: الموظّف **وجهتان وثلاثة تبويبات · صفر مبلغ · صفر انزلاق على 393px ·
صفر خطأ صفحة · صفر انتهاك CSP**، وموظّف المكتب **5 تبويبات و12 وجهة والمبالغ ظاهرة** (صفر انحدار).

**⚠️ حدّان معلومان:** (أ) `prProcStage` لها **سقوط للكتابة المباشرة مشروط حصراً بغياب الدالّة من
القاعدة** — أي حين لا يوجد الحارس أصلاً، فليس بوّابة خلفية حوله (والهجرة مُطبَّقة، فالمسار الفعّال هو
الـRPC). (ب) لم تُختبَر رحلة بريد فعليّة عبر Resend للأحداث الأربعة (لا صندوق وارد متاح) — القوالب
والاتجاه مُتحقَّقان بالكود والتأكيدات.

### 🔁 تصحيح النموذج: **متابعة لا وورك فلو** (2026-09-10 — قرار المالك)
**نصّ المالك:** «لا، هو نظام بسيط وليس وورك فلو، عشان نتابع بعض ولا نضيّع المعلومات أو ننسى أو
تتلخبط. فما في مواضيع اعتمادات وقصص طويلة — **إلا إذا مكّنتهم من رفع الطلب PDF موقَّع من المدير**
ويصيرون يتابعون الطلب بدون نجيهم أو يجونا. وإذا عملنا العروض والمقارنة (طبعاً **برّا السستم**)
ورفعنا أمر الشراء يظهر لهم، ونقدر **نربط الأمر بالطلب**: هذا الطلب صدر له أمر رقم كذا. وخلاص
متابعة وليس وورك فلو.»

**فالاعتماد يقع على الورق خارج النظام ودليله مرفق موقَّع؛ والنظام يحفظ الدليل، ويتابع الحالة،
ويربط الطلب بأمر الشراء.** ⚠️ هذا **يُلغي** سلسلة الاعتماد الإلكترونية التي بُنيت قبله في اليوم نفسه.
**قرارات المالك (AskUserQuestion):** المرفق **إلزاميّ للإرسال** · سلسلة الاعتماد **تُزال من الواجهة**
· الربط **باختيار الأمر من قائمة**.

**القياس قبل التصميم:** `po_number` **موجود أصلاً** في `proc_purchase_requests` (عمود الربط جاهز ولم
يُستعمل قطّ) · **صفر طلب في الإنتاج** (تغيير النموذج بلا مخاطرة) · ما صنع «الوورك فلو» = **قاعدتا
اعتماد مبذورتان + 4 أقسام**، وهما سبب مطالبة المالك بإسناد مدراء الأقسام — **البند سقط بالقرار**.

- **`db/system2-request-tracking.sql`** (تُطبَّق بعد `system2-request-flow.sql`؛ **✅ مُطبَّقة ومُتحقَّقة
  حيّاً** باسم `system2_request_tracking_not_workflow`): عمودا `doc_key`/`doc_name` · **تعطيل** قواعد
  الاعتماد (`active=false` — لا حذف، فتُحيا بلا هجرة عكسية) · `pr_proc_stage` بمراحل متابعة
  (`received → in_progress → quotes_collected → po_issued → closed`) وحالة `submitted` ·
  `pr_link_po`/`pr_unlink_po`/`pr_set_doc`.
- **⚠️ `proc_config_guard` يحرس مسار الموافقات:** UPDATE عليه يتطلّب هوية إدارية/خدمية — فالهجرة ترفع
  `request.jwt.claims` **محصورةً بالمعاملة** (`is_local=true`) فتُصفَّر تلقائياً. (فشلت أوّل محاولة تطبيق حيّ لهذا.)
- **`functions/api/pr-doc.js` (جديد):** رفع/عرض السند. يعيد استعمال binding **`SUPPLIER_DOCS`** القائم
  تحت بادئة `docs/pr/` — **بلا أي إعداد Cloudflare جديد** — ولا يلمس `QUOTES_BUCKET` (النظام 3 معزول).
  حارس `_file-guard` الطبقيّ · **المفتاح مولَّد خادميّاً والامتداد من التوقيع السحريّ** لا من اسم الملف ·
  **الرؤية تُفحص بهوية المتصل** عبر `proc_can_see_pr` (RLS هي الحكم) و**تفشل مغلقةً**.
- **الواجهة:** حقل «الطلب الموقَّع من المدير *» إلزاميّ (**والمسودّة تُحفظ بدونه**) · مسار الطالب صار
  خمس مراحل متابعة تنتهي بـ«**صدر أمر الشراء رقم …**» قابلاً للنقر يفتح درج الأمر · لوحة ربط للمشتريات
  (`<select>` من الأوامر القائمة) · **ربط عكسيّ**: درج الأمر يقول «صادر عن طلب الشراء …» · السند يُفتح
  داخل النظام بحقن blob في عارض المستندات القائم (`DOCV.blobs.set`) · حدث بريد `po_issued` للطالب.

**⚠️ ثلاث قواعد مثبَّتة بتأكيدات:**
1. **لا تبويب اعتماد ولا شاشة أقسام ولا لوحة قرار** — و`view==='inbox'|'depts'` تسقط إلى `list`.
2. **«صدر أمر الشراء» لا تُدَّعى بزرّ:** `pr_proc_stage` ترفض `po_issued` بلا `po_number`، والرقم يُتحقَّق
   من وجوده في `proc_purchase_orders` (رقم بكتابة مختلفة يكسر الربط بصمت — درس توحيد المشاريع).
3. **الادّعاء بلا دليل أسوأ من غيابه:** الإرسال مشروط بوصول المرفق فعلاً (نفس درس شهادة المحتوى المحلي).

**عيبان أمسكهما المتصفّح وحده (لا `node --check` ولا الفحص النصّيّ):**
(أ) بقي `const showInbox = inboxN > 0` بعد حذف `inboxN` ⇒ **ReferenceError يُفرِغ شاشة الطلبات كاملةً**؛
(ب) لافتة «لم تُضبَط الأقسام ومدراؤها» بقيت ⇒ **إنذار كاذب** (صحيح للوورك فلو، باطل للمتابعة). كلاهما
محروس الآن. **و«كعبٌ لا يطابق الإنتاج ليس اختباراً» تكرّر مرّتين:** غياب `po_number` من الكعب أخفى الربط،
و**قاعدة اعتماد غير مبذورة جعلت تأكيد الإطفاء فراغيّاً** (العدّ صفر بالإطفاء وبدونه) — الكعب صار مطابقاً
لمخطّط الإنتاج عموداً بعمود ويبذر قاعدة نشطة.

**الاختبار: 698 تأكيد واجهة + `TR1–TR10`** (بـRLS فعليّة) — خروج 0. **مُتحقَّق أنّ الحرّاس تعضّ: 13 علّة
⇒ 13 إخفاقاً** (بينها تأكيدان كانا **فراغيَّين** فأُصلحا: الربط العكسيّ كان يفحص وجود النصّ لا وصوله للعرض).
+ معاينة متصفّح تحت CSP الإنتاج: **ثلاثة تبويبات للموظّف · صفر ذِكر للاعتماد · منع الإرسال بلا سند ·
سطر أمر الشراء · الربط العكسيّ · صفر مبلغ · صفر انزلاق · صفر خطأ · صفر انتهاك CSP**.

**⚠️ ما سقط من مهام المالك بهذا القرار:** إسناد مدراء الأقسام (`DEP-OPS`/`DEP-CON`/`DEP-LOG`) — لم تعُد
تُستعمَل في أي مسار. يبقى: إنشاء حسابات الموظفين فقط.

### 🔎 فحص ما بعد التنفيذ — ثلاثة عيوب مانعة + تنظيف (2026-09-11)
**طلب المالك «افحص كل شيء وتأكّد أن كل شيء سليم تقنيًا وفنيًا وواجهات».** الفحص (تتبّع مسارات
الاستدعاء + فحوص **قراءة حيّة** على الإنتاج عبر PostgREST بالمفتاح العامّ) أثبت سلامة الأساس، وكشف
**ثلاثة عيوب تمنع أوّل استخدام حقيقيّ** — كامنة لأنّ الإنتاج فيه صفر طلب. **كلّها مرّت من فحوص نصّية**،
فالتأكيدات الجديدة سلوكية.

- **① [حرِج] الترقيم كان يُشتقّ من قائمة مُصفّاة ثمّ يُكتب بـ`upsert`.** `prNextNumber()` تأخذ أعلى رقم
  من `STATE.purchaseRequests` = **ما تسمح به RLS فقط** (طلباتي + طلبات قطاعي). موظّف قطاع يرى 0003
  يقترح 0004 وهو لقطاع آخر محجوب عنه ⇒ المُنطَّق: `pr_update` ترفض و**كل إعادة محاولة تنتج الرقم نفسه**
  فيعلق نهائيّاً؛ وغير المُنطَّق: السياسة متساهلة فـ`upsert` **تكتب فوق طلب قائم** وتحذف بنوده.
  **الحلّ (`db/system2-request-numbering.sql`):** `pr_next_number()` (DEFINER + `pg_advisory_xact_lock`
  فترى كل الصفوف ولا تتسابق) + الواجهة `insert` صريح لا `upsert` (التعارض يُكشف بـ23505).
  السقوط للحساب المحلّي **مشروط بغياب الدالّة وحده** (نمط `prFnMissing`).
- **② [عالٍ] المسودّة كانت طريقاً مسدوداً.** `prSaveCloud` لها مستدعٍ واحد يولّد **رقماً جديداً كل مرّة**،
  فلا مسار يفتح مسودّة ويُكملها — التوست يَعِد «إكمالها لاحقاً» والنتيجة صفٌّ ميّت. وهو يصيب بالضبط من
  لم تجهز ورقته الموقَّعة. **الحلّ:** `prEditDraft(id)` + `__prEditId` + `prSaveCloud(...,'update')` +
  مدخلان ظاهران (القائمة وشاشة المتابعة) + لافتة «تُكمل الآن المسودّة». **وعيب كامن مرافق:** سياسة
  `prc_delete` على `proc_pr_items` كانت `USING (NOT proc_is_scoped())` ⇒ المُنطَّق **لا يحذف بنود طلبه**
  و`prSaveCloud` تحذف ثمّ تُدرِج ⇒ **تتضاعف البنود** عند أوّل إعادة حفظ. صُحِّحت إلى `proc_can_see_pr(pr_id)`.
- **③ [عالٍ] كتابة جزئية عند الإرسال.** كان الحفظ بحالة `submitted` **قبل** رفع المرفق، فأي فشل رفع
  يترك الطلب «مُرسَلاً بلا دليله» والتوست يقول «تعذّر الحفظ» ولا يُرسَل إشعار. **الترتيب الآن: مسودّة →
  مرفق → تحويل → إشعار.** ⚠️ **قاعدة: لا يُكتب `submitted` في صفّ الإنشاء إطلاقاً.**
- **④–⑥ حوكمة:** `pr_set_doc` لم تعُد تسمح بمسح السند بعد الإرسال (والاستبدال بعده صلاحية مشتريات) ·
  `pr_link_po` تمنع ربط أمر الشراء نفسه بطلبين («صدر أمر رقم …» لا يجوز أن يكذب) · `loadPR` تختار
  `proc_started_by/quotes_collected_by` — بدونهما كان فرع «الأولوية لمن بدأ العمل» **ميّتاً دائماً**
  فردّ الطالب يذهب بريداً جماعيّاً للفريق كلّه.
- **⑦ تنظيف:** حُذفت كتلة سلسلة الاعتماد الميتة (~490 سطراً: `prBuildChain`/`prMatchRule`/
  `prResolveApprover`/`prCurrentStage`/`prCanActOn`/`prInbox`/`prAct`/`prActPrompt`/`prInboxHTML`
  + `stepper` غير المستعمَل) — كانت غير قابلة للوصول لكنّها **دوالّ عامّة** فـ`prAct` تبقى قابلة
  للاستدعاء من الطرفية وتكتب قرارات اعتماد في نموذجٍ بلا اعتماد. وأُسقِط جلب `proc_pr_approvals`
  و`proc_approval_rules` (رحلتا شبكة بلا قارئ) والاشتراك اللحظيّ عليهما (صار على `proc_pr_messages`).
  **`prDeptsHTML` أُبقيت عمداً** (الأقسام ما تزال حقلاً حيّاً في الطلب، فحذف محرّرها يفقد قدرة).
  **مصدر واحد لنصّ المرحلة:** `PROC_STAGE_LABEL` + `prProcStageText` (الشارة والمطبوعة معاً).
- **⑧–⑨ اتّساق:** بوّابة زرّ الدعوة صارت `role==='admin'` كشرط الخادم (كان `can_manage_users` وحده
  فيُرفَض بعد النقر) · سقف المعلّقين صار **مُنطاقاً بالقطاع فعلاً** (كان استعلاماً عالميّاً) · نصّ بريد
  الاستلام بلا «سلسلة الاعتماد» · مسارا `notify.js`/`_pr-shared.js` أُضيفا لتشغيل CI (الحزمة تقرأهما
  وكان تعديلهما لا يُشغّل شيئاً).
- **🐛 وثالث تكرار لدرس الكعب:** `proc_pr_items` في `00_stub.sql` كان **هيكلاً متخيَّلاً**
  (`name/qty/price`) لا يوجد منه عمود واحد في الإنتاج (`description/requested_qty/unit_price`…) —
  فأي تأكيد يلمس بنود الطلب كان يختبر جدولاً غير موجود. صار مطابقاً عموداً بعمود (مُتحقَّق حيّاً).
- **التحقّق:** **732 تأكيد واجهة** (+21) · `NM1–NM6` بـRLS فعليّة (المجموع SC+FL+TR+IV+NM، خروج 0) ·
  22 تأكيد نقطة الدعوة · **معاينة متصفّح تحت CSP الإنتاج: 21 فحصاً كلّها نجحت** (موظّفان في قطاعين
  يحصلان على رقمين صحيحين رغم اختلاف ما يريانه · فشل رفع مُقلَّد يُبقي الطلب مسودّة ولا يُرسل إشعاراً ·
  المسودّة تُفتح وتُكمَل بصفّ **واحد** بلا طلب رقم جديد · صفر خطأ صفحة وصفر انتهاك CSP).
  **مُتحقَّق أنّ الحرّاس تعضّ: 4 علل SQL + 7 علل واجهة + علّتان في المعاينة ⇒ كلّها أفشلت**، والضابط يمرّ.
#### ✅ مُطبَّق حيّاً ومنشور (2026-09-11) — PR #97 مدموج، كوميت الدمج `156ade1`
**الترتيب المُلزِم نُفِّذ كما خُطِّط: الهجرتان أوّلاً ثمّ الدمج.**
- **الهجرتان على `yofcaxvstjcrmbgciwym`:** `system2_request_numbering_and_item_delete_policy`
  ثمّ `system2_request_tracking_doc_and_link_guards` (الثانية **دالّتان فقط** — `pr_set_doc`/`pr_link_po` —
  لا الملف كلّه، فلا يُعاد لمس قواعد الاعتماد المُطفأة).
- **الحالة قبل التطبيق (مقيسة):** الدالّة غائبة · `prc_delete` = `(NOT proc_is_scoped())` أي **العيب حيّ** ·
  0 طلب · 0 بند · 0 موظّف مُنطَّق · 115 أمر شراء · 4 مستخدمين نشطين.
- **التحقّق بعد التطبيق:** الحرّاس الأربعة `t/t/t/t` · `authenticated` ينفّذ و`anon` **لا** ·
  **إثبات سلوكيّ حيّ:** بانتحال `abdullah@aldeyabi.com` ⇒ الهوية `Abdullah` و`pr_next_number()` = `PR-DG2026-0001`.
- **صفر انحدار (أهمّ فحص):** الأربعة القائمون كلّهم `scoped=false`, `sees_money=true`, **115 أمراً بمبالغها**.
- **النشر:** Cloudflare خلال ~30 ثانية · `/api/pr-doc` = `{ok:true, r2_bucket:true}` ·
  `/api/staff-invite` = `{ok:true, token_secret:true, resend:true}` · **الحرّاس حيّة:** قراءة مرفق بلا
  رمز ⇒ **401** · سكّ رابط دعوة بلا مصادقة ⇒ **403** «للمدير فقط» · رمز مزوَّر ⇒ **401** ·
  `?key=../../etc/passwd` ⇒ **400**. والكود الجديد وصل فعلاً (`pr_next_number`/`prEditDraft`/
  `PROC_STAGE_LABEL` في الصفحة المنشورة)، و**الكتلة الميتة صفر تعريف وصفر استدعاء**.
- **⚠️ درس أدوات تكرّر:** `curl` على `*.html` يعيد **308** — استعمل `-L` ولا تستنتج فشل نشر.
- **⚠️ درس تحقّق:** أوّل استعلام تحقّق كتبتُه كان يستدعي `pr_next_number()` مباشرةً — ومحرّر SQL يعمل
  **بلا JWT** فتردّ «غير مصرّح» (سلوك صحيح) فيبدو النشر فاشلاً وهو ناجح. **تحقّق من وجود الحارس لا من
  تشغيله** حين يكون التشغيل مشروطاً بهويّة. أمسكه تشغيل تجريبيّ على قاعدة نظيفة قبل تسليم السكربت.

### 🔒 أقلّ امتياز لموظّف الصيانة + تنظيف قشرته (2026-09-12، PR #98)
**سبب البدء:** طلب المالك «أرني الواجهات التي يطّلع عليها موظفو الصيانة» قبل إنشاء حساباتهم.
اللقطات الحقيقية (`scripts/e2e/scoped-staff-screens.mjs` — 16 لقطة + 43 فحصاً تحت CSP الإنتاج،
بكعب سحابة مُقلَّد بالكامل) كشفت أربعة عيوب، ثمّ كشف إصلاحُها ثلاثة أخرى كانت كامنة. **كلّها واجهة
+ افتراضيّ صلاحية — ولا واحد منها كان يُسرّب مبلغاً** (`proc_po_visible` يُفرِغ subtotal/vat/total
ويسحب price من البنود، فحتى تصدير Excel يخرج بأعمدة مالية فارغة — مُتحقَّق).

- **① [الأهمّ] `hasPermission` كانت تسقط لافتراضيّ الدور عند غياب المفتاح**، ورابط الدعوة يكتب
  مفتاحين فقط (`can_receive_po`/`can_view_amounts`) ⇒ الموظّف الميدانيّ يُمنَح ضمنياً `can_edit_po`
  و`can_create_po` و`can_import` و`can_export` و`can_use_ai`… فيرى أزرارها والخادم يرفضها
  (`po_update`/`po_insert` = `NOT proc_is_scoped()`). **الآن: للمُنطَّق المنح صريح أو لا شيء**
  (سطر واحد في `hasPermission`). ⚠️ **غير المُنطَّق بلا أي تغيير** — الافتراضيّ حارس عدم الانحدار.
  و`prPermStrict` صارت مطابِقة لها (لا بوّابتان تتفارقان).
- **نظيرتها على الخادم `db/system2-scoped-least-privilege.sql`:** `proc_can_view_amounts()` كان
  افتراضها `true` عند غياب المفتاح، فحسابٌ مُنطَّق يُنشئه المالك يدويّاً وينسى إلغاء «عرض المبالغ»
  **يستقبلها من الخادم**. الآن الافتراضيّ يتفرّع: true لموظّف المكتب، false للمُنطَّق.
  **🐛 فخّ منطق ثلاثيّ أمسكه LP1 قبل أي تطبيق حيّ:** `scope_sectors` عمودها **NULL** لكل موظفي
  المكتب، فبلا `coalesce` داخليّة يُنتج التعبير NULL لا true ⇒ **الأربعة القائمون يفقدون مبالغهم
  لحظة النشر**. و`CASE` لا `AND` (ترتيب تقييم AND غير مضمون، و`jsonb_array_length` على غير مصفوفة يرمي).
- **② ثلاث بوّابات في صفّ أزرار الدرج كشفتها الصرامة ولم تُحدِثها:** «📦 استلام» كان محكوماً بـ
  `can_edit_po` وحده بينما `poReceiveOpen` تقبل `can_edit_po` **أو** `can_receive_po` (بوّابة الزرّ
  أضيق من بوّابة الفعل ⇒ **موظّف الميدان لا يرى زرّ عمله الأساسيّ**) · و«▶ المرحلة التالية» و«إلغاء»
  كانا **بلا أي بوّابة صلاحية** بينما `changePOStatus` ترفضهما.
  **⚠️ قاعدة: بوّابة الزرّ = بوّابة الفعل حرفيّاً.**
- **③ إنذار فجوات كاذب:** قائمة المُنطَّق **مُصفّاة بالقطاع بطبيعتها**، فأرقام القطاعات الأخرى تبدو
  «مفقودة» ⇒ «⚠ 8 رقم مفقود في التسلسل» وهي مسجَّلة فعلاً. `renderPOSeqBar` تُخلي الشريط له
  (نفس قاعدة «لا تُحتسب الفجوات على قائمة مُصفّاة»).
- **④ ضجيج القشرة:** المهبط صار **«القائمة»** لا «لوحة التحكم» (`STATE.poView='list'`) · بطاقتا
  «قيمة المشتريات» (قيمتها «—» دائماً) و«مؤشر صحة النظام» تسقطان عمّن لا يعنيانه · الزرّان العائمان
  (`.ai-fab`/`#wf-bell`) يُخفَيان عنه (كانا يغطّيان أزرار «القائمة/اللوحة/الإنذارات» على 393px) ·
  `hideEmptyNavLabels()` تُخفي عنوان أي مجموعة بلا وجهة مرئيّة تحتها (الشريط مسطّح فلا حاوية تُخفى معها).
- **⑤ نصّ يَعِد بما أُلغي:** «تابع اعتماده عبر **سلسلة الموافقات**» أُزيل من شاشة الطلبات وتقريرها
  (قرار المالك «متابعة لا وورك فلو»).
- **⚠️ قاعدة تحقّق تكرّرت:** `CODE` في الحزمة **جافاسكربت بلا CSS ولا ترميز** — تأكيدٌ على CSS أو
  HTML يمرّ **فراغاً**. استعمل `HTML`. (وقع فعلاً في أوّل صياغة لهذا القسم.)
- **الاختبار:** **747 تأكيد واجهة** + `LP1–LP5` بـRLS فعليّة في `db/system2-tests/15_least_privilege.sql`
  + **43 فحص متصفّح** تحت CSP الإنتاج (منها قياس أداء: `isScopedUser()` داخل `fmtPrice` = **1ms/20k نداء**).
  **مُتحقَّق أنّ الحرّاس تعضّ: 10 علل أُعيدت عمداً ⇒ كلّها أفشلت**، والضابط يمرّ.
- **⚠️ يجب تطبيق `db/system2-scoped-least-privilege.sql` حيّاً** (بعد `system2-staff-scope.sql`).
  الترتيب غير حرِج اليوم (صفر حساب مُنطَّق على الإنتاج) لكن لا تُنشئ حساب موظّف قبلها.

### 🔔 تصليب `proc_notifications` + وثيقة تصميم المتابعة المباشرة (2026-09-12، PR #98)
**طلب المالك:** «أفضل طريقة للمتابعة والتحديث المباشر بين القسمين» — ومناقشتها مع Codex.
⚠️ **لا Codex في بيئة الجلسة** (لا CLI ولا مفتاح)، فأُنتِجت وثيقة مُعدّة لتشغيله عليها:
`docs/تصميم-المتابعة-والتحديث-المباشر-بين-القسمين.md`. **قرار المالك: تُنفَّذ الثغرة الأمنية
الآن فقط، وينتظر الباقي مراجعة Codex** · و`recipient` يبقى **اسم المستخدم** (لا معرّف ثابت).

**الثغرة المقيسة (كانت حيّة):** `proc_notifications` سياستها الوحيدة
`auth_all FOR ALL USING(true) WITH CHECK(true)` ⇒ أي مستخدم مسجَّل **يقرأ إشعارات كل الموظفين**
(قرارات اعتماد ومبالغ وأسماء موردين) بطلب PostgREST واحد، و**يُدرِج إشعاراً بانتحال أي مستلِم**
(«تم اعتماد طلبك ✓» في جرس المدير). و`wfNotify` تُدرِج **من العميل** فالمؤلِّف غير موثوق.
- **`db/system2-notifications-hardening.sql`:** عمود `created_by` · **سحب صريح** لكل الامتيازات
  ثمّ `GRANT SELECT` + **`GRANT UPDATE (read)` فقط** (تقييد عموديّ بالمنح — RLS لا تُقيّد الأعمدة) ·
  سياستا SELECT/UPDATE للمستلِم وحده · **لا سياسة INSERT ولا DELETE للعميل إطلاقاً** ·
  `proc_notify()` SECURITY DEFINER: المُرسِل من `proc_me()` · المستلِم يُحلّ من سجلّ المستخدمين
  (لا اسم ملفَّق ولا حساب موقوف) · **ولا يُشعَر الفاعل بفعل نفسه**.
- **`db/workflows.sql`** لم يعُد يُنشئ السياسة المتساهلة (إعادة تشغيل المُنصِّب كانت ستفتحها فوق التصليب).
- **الواجهة:** `wfNotify` تستدعي `proc_notify`؛ والسقوط للإدراج المباشر **مشروط حصراً بغياب الدالّة**
  (= الهجرة غير مطبَّقة ⇒ السياسة القديمة قائمة أصلاً) — نفس نمط `prProcStage`، وليس بوّابة خلفية.
- **⚠️ درس تكرّر ثالثةً:** Supabase تمنح ALL افتراضياً على جداول `public`، فـ`GRANT SELECT` وحده
  **لا يسحب** INSERT — لا بدّ من `REVOKE ALL` صريح.
- **الاختبار `db/system2-tests/16_notifications.sql` (NT1–NT8) سلوكيّ بدور `authenticated` فعليّ**
  (لا فحص نصّ سياسة): الإدراج والمُرسِل · منع إشعار الذات · المستلِم الملفَّق/الموقوف · العنوان الفارغ ·
  **أليس لا تقرأ إشعارات بوب** · بوب يقرأ إشعاره · **الإدراج المزوَّر مرفوض** · التقييد العموديّ.
  والكعب يبدأ من **حالة الإنتاج المتساهلة** فيُبرهن أنّ الهجرة هي التي أغلقتها.
  **مُتحقَّق أنّ الحرّاس تعضّ: 5 علل ⇒ كلّها أفشلت** (منها الإدراج المزوَّر وإعادة السياسة للمُنصِّب).
- **الحزمة: 751 تأكيد واجهة + NT1–NT8 + 43 فحص متصفّح.** ⚠️ تُطبَّق الهجرة حيّاً بعد `db/workflows.sql`.

**⚠️ ست فجوات موثّقة في وثيقة التصميم ولم تُنفَّذ بعد** (بانتظار Codex): معالِجا الطلبات والرسائل
مشروطان بـ`STATE.page==='pr'` فردّ المشتريات لا يصل الميدانيّ على شاشته · الجرس يستفتي كل 45 ثانية
ولا يستمع للحظيّ · وجهات الإشعار اثنتان فقط (`approvals`/`registrations`) فلا مسار لطلب ولا لأمر ·
`rtRerender` لا تُرقّع الدرج ولا المحادثة المفتوحة · البريد fire-and-forget.

### 🔐 فحص جاهزية الإطلاق — إقفال سبعة جداول فاتت هجرة النطاق (2026-09-13، PR #98)
**طلب المالك «تأكّد أن كل شيء سليم وجاهز للإطلاق 100%».** الفحص أثبت خضرة كل الطبقات المحلّية
والنقاط الحيّة، وكشف **بنداً مانعاً واحداً** وعيباً في هجرتي أنا:
- **🐛 [حرِج] عيبٌ في هجرة تصليب الإشعارات نفسها — أُمسِك قبل التطبيق:** كانت تُسقِط السياسة
  **بالاسم** (`auth_all` نقلاً عن `db/workflows.sql`)، وقياس الإنتاج أظهر أنّ الاسمين هناك
  **`auth_read`/`auth_write`** وكلتاهما `USING(true)`. وسياسات RLS المتساهلة **تُجمَع بـOR**،
  فكانت الهجرة «تنجح» و**تترك تسريب القراءة مفتوحاً**. العلاج: الحذف **بالتعداد** على
  `pg_policies` لا بالاسم + **NT9** يفحص **الأثر** (لا سياسة متساهلة ناجية) لا الاسم.
  والكعب صُحِّح لأسماء الإنتاج — **خامس تكرار لدرس «كعبٌ لا يطابق الإنتاج ليس اختباراً»**.
- **البند المانع:** `db/system2-staff-scope.sql` غطّت الأوامر والمستخدمين والكتالوج والطلبات
  والتدقيق، و**سبعة جداول لم تُغطَّ إطلاقاً** وما تزال `USING(true)` لكل `authenticated` بلا حارس
  مُشغِّل: `proc_supplier_registrations` (**53 صفّاً ببيانات الموردين الحسّاسة** — سجل تجاري ·
  ضريبيّ · آيبان · مؤشّرات وثائق؛ و`auth_update`/`auth_delete` مفتوحتان) · `proc_ai_usage` 658 ·
  `proc_item_aliases` 95 · `proc_rfq_quotes` 2 · `proc_rfqs` 1 · `proc_pr_attachments` 0 ·
  `proc_pr_audit` 0. **آمن اليوم لأن الأربعة القائمين موظفو مكتب موثوقون — و«الإطلاق» نفسه
  (إنشاء حسابات الميدان) هو ما يفتح الثغرة** بإدخال مستخدمين أقلّ ثقة لنفس الدور.
- **`db/system2-scoped-table-lockdown.sql` — سياسات RESTRICTIVE لا إعادة كتابة:** المتساهلة
  تُجمَع بـOR (فتقييدها يوجب حذف الكلّ وإعادة البناء)، وRESTRICTIVE **تُجمَع بـAND** فتُضاف فوق
  القائم ولا تُوسِّع وصولاً بحال. **⚠️ وهذا ما يحفظ سياستَي anon** `public_insert`/
  `public_insert_pending` اللتين يعتمد عليهما نموذج التسجيل العامّ (`register.html`) — السياسة
  `TO authenticated` فلا يمرّ بها anon. الشرط `NOT proc_is_scoped()` ⇒ **صفر انحدار** للأربعة
  (نطاقهم NULL). فحصُ اعتماد مبكّر يمنع جدولاً نصف مُقفَل لو غابت `proc_is_scoped()`.
- **⚠️ مُتحقَّق قبل الإقفال أنّ لا مسار في واجهة المُنطَّق يقرأ أيّاً منها** (تتبّع كل مستدعٍ):
  التسجيلات وراء `can_review_registrations` · بطاقات الأداء وراء `can_manage_suppliers` ·
  الأسماء البديلة والاستخدام في شاشة AI · RFQ شاشة مستقلّة — وكلّها خارج `SCOPED_PAGES`
  و`hasPermission` صارمة للمُنطَّق؛ و`proc_pr_attachments`/`proc_pr_audit` **بلا مرجع واحد** في `index.html`.
- **`proc_settings` لا يُقفَل بل قائمة مفاتيح بيضاء:** المُنطَّق يحتاج منه `projects_registry`
  (أسماء الجهات) و`company_info` (ترويسة مطبوعاته) فعلاً، لكن قراءته كانت مفتوحة على كل مفاتيحه —
  ومنها `ai_config.api_key` (فارغ اليوم، وأي مفتاح Gemini يضعه المالك لاحقاً يصير مقروءاً للجميع)
  و`portal_settings.invite_code`. فحُصِر المُنطَّق في المفتاحين. **⚠️ سياسة لكل أمر لا `FOR ALL`
  واحدة:** بـFOR ALL يسري `USING` على DELETE فيصير له حقّ **حذف** صفّ سمحنا له بقراءته (LK9).
- **الاختبار `17_scoped_lockdown.sql` (LK1–LK9) سلوكيّ بدور `authenticated` فعليّ:** **LK1 شرط لازم**
  (موظّف المكتب يقرأ السبعة — وإلّا كان LK2 فراغيّاً على جدول محجوب عن الجميع) · المُنطَّق صفر ·
  لا تعديل/حذف/إدراج · **anon سليم** · **كل سياسة RESTRICTIVE فعلاً** (لو أُنشئت متساهلة لوسّعت
  بـOR — انقلاب صامت) · الأدمن المُنطَّق محجوب (نطاق لا رتبة). **الكعب يبدأ من حالة الإنتاج
  المفتوحة** فيُبرهن أنّ الإقفال هو ما أغلقها. **مُتحقَّق أنّها تعضّ: 7 علل ⇒ كلٌّ أفشل تأكيده
  الصحيح** (المتساهلة⇒LK2 · إسقاط جدول⇒LK2 · عكس الشرط⇒LK1 · حذف سياسة anon⇒LK5 · إسقاط القائمة
  البيضاء⇒LK8-أ · توسيعها⇒LK8-ب · دمج سياسات الإعدادات في FOR ALL⇒LK9)، والضابط يمرّ.
  **وكعبٌ ناقص أُصلح في الطريق:** `proc_settings`/`proc_approval_rules` كانا بلا RLS ولا منح فيفشل
  قارئهما بدور `authenticated` بـ«permission denied» لا بحجب السياسة — صارا كحال الإنتاج.
- **الحزمة: 751 تأكيد واجهة + SC/FL/TR/IV/NM/LP/NT1–NT9/LK1–LK9 — خروج 0.**

#### ✅ مُطبَّق حيّاً ومنشور (2026-09-13) — PR #98 مدموج، كوميت الدمج `497bbde`
**⚠️ الترتيب هنا معكوس عمّا اعتدناه — النشر قبل الهجرات، وهو مقصود:** الكود المنشور آنذاك كان يُدرِج
في `proc_notifications` **مباشرةً**، وهجرة التصليب تسحب صلاحية الإدراج ⇒ تطبيقها أوّلاً يفتح نافذة
تنكسر فيها الإشعارات. والكود الجديد يستدعي `proc_notify` ويسقط للإدراج المباشر **فقط عند غياب
الدالّة** — فينجح قبل الهجرة وبعدها. **القاعدة المعمَّمة: الهجرة التي تسحب صلاحيةً يعتمد عليها
الكود المنشور تُطبَّق بعد النشر؛ والهجرة التي يعتمد عليها الكود الجديد تُطبَّق قبله (سابقة PR #97).**
- **الهجرات الثلاث على `yofcaxvstjcrmbgciwym`:** `system2_scoped_least_privilege_view_amounts` →
  `system2_notifications_hardening` → `system2_scoped_table_lockdown`.
- **🐛 تصحيح قياس ارتكبتُه:** قلتُ «الأربعة القائمون» مراراً في هذا الملف — **النشطون ثلاثة فقط**
  (`Abdullah` أدمن · `Mahmoud` · `Mostafa` أدمن)؛ و`bakar` و`abdullah` **موقوفان**. أخطأتُ قراءة
  `active` في أوّل لقطة ثمّ صحّحه عدّاد صريح. **الدرس: لا تُعِد رقماً من لقطة سابقة بلا إعادة عدّه.**
- **التحقّق البنيويّ بعد التطبيق:** `proc_notify` موجودة · `INSERT`/`DELETE` مسحوبتان و`SELECT` ممنوحة ·
  `UPDATE(read)`=true و`UPDATE(title)`=false · سياستان على الإشعارات و**صفر سياسة متساهلة ناجية** ·
  **7 سياسات إقفال RESTRICTIVE** وصفر متساهلة · **سياستا anon على التسجيلات سليمتان (2)** · 4 سياسات
  RESTRICTIVE على `proc_settings`.
- **التحقّق السلوكيّ الحيّ (بذر موظّف مُنطَّق مؤقّت داخل معاملة تنتهي بـ`RAISE EXCEPTION` فتُجهَض):**
  موظّف المكتب **809 صفّاً** من السبعة (53+1+2+95+658) و7 مفاتيح إعدادات و115 أمراً — **صفر انحدار**؛
  والمُنطَّق **صفر من السبعة** · مفتاح إعدادات واحد (`projects_registry`) · **صفر من المفاتيح المحظورة** ·
  **73 أمراً (قطاعه وحده)** · `amounts=false`. و`proc_notify`: يُدرِج · المُرسِل مشتقّ خادميّاً ·
  يرفض إشعار الذات والمستلِم الملفَّق والموقوف · **والإدراج المباشر المزوَّر مرفوض**. وanon: **يُسجّل**
  (نموذج `register.html` سليم) و**يقرأ صفر**.
- **إثبات إغلاق التسريب بالأرقام:** إجمالي `proc_notifications` = **261**، ومحمود يقرأ **65** (إشعاراته
  وحده). قبل الهجرة كان أي مستخدم مسجَّل يقرأ الـ261.
- **⚠️ `company_info` ليس له صفّ في `proc_settings` أصلاً** (7 مفاتيح لا تشمله)، فقراءة المُنطَّق
  مفتاحاً واحداً هي الصواب لا نقص — ترويسة المطبوعات تسقط لقيمها المحليّة.
- **نظافة الإنتاج بعد الإجهاض مُتحقَّقة:** صفر مستخدم مؤقّت · صفر إشعار اختبار · صفر تسجيل اختبار ·
  المستخدمون 5 والإشعارات 261 والتسجيلات 53 **كما كانت قبل الفحص بالضبط**.
- **النشر:** علامات #98 الخمس حاضرة على **النطاقين**، والنقاط الخمس `{"ok":true}`
  (`notify`/`pr-doc`/`reg-doc`/`staff-invite`/`supplier-invite-link`).
- **⚠️ المتبقّي:** إنشاء حسابات الموظفين (يتطلّب كلمة سرّ — من اللوحة أو رابط الدعوة) ·
  و`db/system1-storage-hardening.sql` المرحلة 1 (مخزن `supplier-docs` مقروء للعموم — **مُعاد قياسه
  2026-09-13: 60 مجلّد مورّد** يُسرَدون وتُنزَّل ملفاتهم بلا حساب).

### 🔗 رابط دعوة موظفي القطاع للتسجيل الذاتيّ (2026-09-10)
**طلب المالك:** «رابط لدعوة موظفين ومدير الصيانة والتشغيل للدخول وتسجيل بياناتهم لتوصلهم الإشعارات
والمتابعة». **قراراته (AskUserQuestion):** لهم **بريد شركة `@aldeyabi.com`** · **رابط واحد للقطاع**
يُنشَر · **الحساب ينتظر تفعيل المالك**.

**⚠️ عائق قِيس قبل البناء وحسم التصميم:** `_pr-shared.js` (`userEmail`) **تُسقِط أي بريد لا ينتهي
بـ`@aldeyabi.com`** وتستبدله بعنوان مُشتقّ (`<الاسم>@aldeyabi.com`) — فلو سجّل موظّف ببريد شخصيّ
**لم يصله إشعار أبداً وبصمت**. ولهذا التسجيل يشترط بريد الشركة، وهو شرط وصول الإشعار نفسه لا تشدّد شكليّ.

- **`functions/api/staff-invite.js` (جديد):** رمز **HMAC موقَّع يحمل حمولته** (نمط `doc-renew.js`) —
  **لا جدول رموز ولا هجرة له**. `POST {action:'mint'}` للأدمن same-origin يسكّ رابط القطاع (سقف 60 يوماً) ·
  `POST {action:'revoke'}` يبطل **كل** الروابط · `GET ?t=` يكشف القطاع وشرط النطاق فقط (بلا PII) ·
  `POST ?t=` التسجيل العامّ.
- **⚠️ المبدأ الحاكم — الرابط المشترك لا يمنح صلاحية:** الحساب يُنشأ **`active=false`**، والقطاع من
  **الرمز** لا من العميل، والدور `user` والصلاحيات الميدانية (`can_receive_po` فقط، بلا مبالغ) **مفروضة
  نصّاً في الخادم**. فرابطٌ مسرَّب أقصى أثره صفوف بانتظار المراجعة — **مسقوفة** بـ`MAX_PENDING_PER_SECTOR`.
- **الإبطال بلا جدول:** مفتاح `staff_invite.epoch` في `proc_settings`؛ أي رمز `iat < epoch` يسقط.
  ⚠️ الهجرة تبذره بـ`ON CONFLICT DO NOTHING` فإعادة تشغيلها **لا تُحيي روابط أُبطلت** (محروس بـIV3).
- **`db/system2-staff-invite.sql`** (**مُطبَّقة ومُتحقَّقة حيّاً**): عمود `proc_users.mobile` + بذرة المفتاح.
  **⚠️ `proc_config_guard` يحرس الإعدادات** — الهجرة ترفع هوية الخدمة **محصورةً بالمعاملة** (`is_local`).
- **`staff-register.html` (جديدة):** صفحة عامّة `noindex` · **بلا أي مصدر خارجيّ** (آمنة CSP) · حقول 16px
  (فلا يُقرّب iOS) · ترفع للنقطة الخادمية لا للقاعدة · تُغلَق حقولها بعد النجاح.
- **الواجهة الإدارية:** زرّ «🔗 رابط دعوة الموظفين» في الإعدادات ← المستخدمون (محكوم بـ`can_manage_users`):
  اختيار القطاع والمدّة → رابط + نسخ + مشاركة واتساب + **إبطال كل الروابط**. ونافذته **`ephemeral`**
  فتُزال من DOM عند الإغلاق (سابقة تسرّب قفل التمرير).
- **تنبيه المدراء:** كل تسجيل يُرسل بريداً للأدمن بالاسم والبريد والجوال والوظيفة والقطاع — **وإلّا انتظر
  الموظّف بلا أن يعلم أحد**. وفشل حفظ الملف يحذف حساب الدخول (لا حساب يتيم يمنع إعادة المحاولة).

**الاختبار: 22 تأكيداً سلوكيّاً** لنقطة `/api/staff-invite` في `db/portal-tests/file-guard.test.mjs`
(شبكة مُقلَّدة: أصل مختلف · موظّف عاديّ لا يسكّ · رمز معبوث/بمفتاح آخر · الإبطال · بريد خارج النطاق ·
**الحساب موقوف والقطاع من الرمز والصلاحيات مفروضة مهما أرسل العميل** · لا صفّ ثانٍ لمن له حساب · حذف
الحساب اليتيم · سقف المعلّقين) **+ 13 تأكيداً بنيويّاً** في `scripts/system2-tests.mjs` (**المجموع 711**)
**لما لا يُمسَك سلوكيّاً**: المقارنة ثابتة الزمن (خاصيّة **زمنية** لا سلوكية — `===` و`timingSafeEq`
يقبلان ويرفضان نفس المدخلات) وسلامة الصفحة العامّة. **مُتحقَّق أنّ الحرّاس تعضّ: 12 علّة ⇒ 12 إخفاقاً.**
+ معاينة متصفّح تحت CSP الإنتاج على 393px: شارة القطاع · رفض البريد الخارجيّ محليّاً · الحمولة الصحيحة ·
قفل الحقول بعد النجاح · **صفر انزلاق · صفر حقل دون 16px · صفر خطأ · صفر انتهاك CSP**.

**⚠️ ملاحظة تشغيلية:** في الإنتاج **صفّ واحد موقوف مسبقاً** (من المستخدمين الخمسة القدامى) — فلا تظنّه تسجيلاً جديداً.

#### 🐛 بنود الطلب: الجمع من DOM قديم — عيبٌ واحد أنتج عَرَضين (2026-09-13)
**بلاغ المالك بلقطة:** «عند رفع البنود بالقراءة الذكية البنود تسجل بعد الأعمدة المضافة حتى لو كانت
فارغة، وعند حذف الأعمدة الفارغة لا تُحذف — تُحذف البنود المضافة بالذكاء في الأسفل».
- **الجذر (مُعاد إنتاجه بالتشغيل قبل أي إصلاح):** `prRenderItems()` كانت تبدأ بـ`prCollectItems()`
  «لحفظ القيم قبل إعادة الرسم» — وهي تقرأ صفوف DOM وتكتبها في المصفوفة **بنفس الفهرس**. فمتى تغيّرت
  المصفوفة للتوّ (قراءة ذكية · حذف · قالب · فتح مسودّة) صار الـDOM قديماً والفهارس **مُزاحة**،
  فتُكتب القيم القديمة فوق الجديدة.
- **⚠️ والأثر أسوأ ممّا بدا في اللقطة:** البند الأوّل المستخرَج **يُفقَد صامتاً** (يُطمَس بالصفّ الفارغ
  القديم) لا أنّه «يأتي بعد صفّ فارغ». وحذف الصفّ i يُعيد كتابة الصفوف مُزاحةً فيبقى المحذوف
  ويختفي **آخر بند**. مُعاد إنتاجهما حرفيّاً: `(فارغ)|صابون سائل|منظف زجاج` ثمّ `(فارغ)|صابون سائل`.
- **⚠️ القاعدة المثبَّتة: من يُغيّر `__prDraftItems` يجمع *قبل* التغيير ثم يرسم.** `prRenderItems` لا
  تجمع إطلاقاً؛ و`prRemoveDraftItem` تجمع قبل `splice`؛ وزرّ ➕ صار `prAddItemRow()`
  (جمع → إضافة → رسم) بدل `prAddDraftItem();prRenderItems();` في `onclick`.
- **والقراءة الذكية صارت تُسقِط الصفوف الفارغة وتُلحِق بعد المكتوب يدويّاً** بدل الاستبدال الكامل —
  فالاستبدال كان يمحو ما كتبه المستخدم بلا إنذار لو قرأ النموذج بعد إدخال بنود.
- **الاختبار: 10 تأكيدات (المجموع 770)** منها **خمسة سلوكية تُشغّل الدوال على DOM مُقلَّد**
  + **`scripts/e2e/pr-items-rows.mjs` — 9 فحوص على الجدول الحقيقيّ في متصفّح تحت CSP الإنتاج**
  (قراءة ذكية · حذف من الوسط · ➕ يحفظ المكتوب · حذف الفارغ · قراءة ثانية تُلحِق · حذف الجميع يُبقي
  صفّاً واحداً). **مُتحقَّق أنّها تعضّ: 4 علل ⇒ كلٌّ أفشل تأكيده، وإعادة العلّة الأصلية تُعيد أعراض
  البلاغ حرفيّاً.**
- **🐛 وسابع تكرار لدرس الكعب:** أوّل كعب DOM كتبتُه طابق `data-f="description" value="…"` مُتلاصقين،
  والترميز الحقيقيّ يضع `list="pr-item-names"` **بينهما** — فضاعت كل الأوصاف وأنتج **إخفاقاً وهميّاً
  في الكعب لا في الكود**. **لا تفترض ترتيب السمات في كعب يحاكي ترميزاً.**

#### ✉️ التحوّل إلى دعوة شخصية بقالب Resend (2026-09-13)
**طلب المالك:** «اجعله يرسل بتمبلت احترافيّ عن طريق Resend — فقط أضع اسم الموظف وإيميله وأحدّد
الوظيفة ويتم الإرسال». **قراراته (AskUserQuestion):** «الوظيفة» = **القطاع + مسمّى نصّيّ** ·
الموظّف **يدخل مباشرةً** بعد ضبط كلمة مروره · **الرابط المشترك يُحذَف**.
**وقرار أمنيّ لم أسأل عنه: لا تُرسَل كلمة مرور في البريد إطلاقاً** — الرابط دعوة لا اعتماد جاهز.
- **انقلاب الاتجاه:** كان المدير يولّد رابط قطاع يُنشَر، والموظّف يكتب اسمه وبريده ومسمّاه بنفسه.
  الآن المدير يُدخلها، و`action:'invite'` يسكّ **رمزاً شخصيّاً** يحمل `{n,e,s,j,by}` ويرسله بـResend.
- **⚠️ المبدأ الحاكم الجديد — الرمز صار الاعتماد:** الهويّة كلّها **داخله**، والمسار العامّ لا يقرأ من
  جسم الطلب إلا **كلمة المرور والجوال**. فلا يستطيع حاملُ الرابط انتحال بريد غيره ولا تغيير قطاعه
  ولا منح نفسه صلاحية. وقوّة الضمان = أنّ الرابط سُلّم إلى صندوق بريد الشركة لذلك الموظّف وحده.
- **أحاديّة الاستعمال بلا جدول رموز:** بعد الإتمام يوجد صفّ بذلك البريد، فإعادة الضغط تسقط على فحص
  «موجود مسبقاً». والفحص نفسه يجري **عند الإصدار** كذلك، فيُبلَّغ المدير فوراً لا بعد أن يضغط الموظّف.
- **⚠️ `MAX_PENDING_PER_SECTOR` أُسقِط عمداً لا سهواً:** كان حزاماً ضدّ رابط **مشترك** مسرَّب، وبعد
  أن صارت الدعوة إداريّة شخصية لم يبقَ ما يحرسه — ومُسنَده (`active=false`) لا يطابق صفّاً جديداً
  أصلاً بعد التفعيل المباشر. **تركُه حارسٌ ميّت يوحي بحماية غير قائمة**، ومحروسٌ الآن بتأكيد.
- **قالب البريد (`inviteEmail`):** جداول وأنماط سطريّة لا flex/grid (Outlook)، preheader، ترويسة
  كحليّة بالعلامة، جدول القطاع/المسمّى/بريد الدخول، زرّ تفعيل بارز، مهلة الصلاحية، **ورابط نصّيّ
  احتياطيّ**، وسطر «لم تتوقّع هذه الدعوة؟». وكل قيمة تمرّ بـ`esc`.
- **⚠️ الرابط يعود للمدير حتى عند نجاح الإرسال:** بريدٌ يقع في المهملات يترك الموظّف بلا طريق.
  وعند فشل Resend لا تسقط الدعوة — تُعرَض بلافتة تحذير مع الرابط لينسخه.
- **`staff-register.html` صارت صفحة تفعيل:** تعرض الاسم والبريد والمسمّى **مقروءةً من الرمز
  بـ`textContent`** (لا innerHTML — قيم من الشبكة على صفحة عامّة)، ولا حقل لتعديلها؛ المدخلات:
  الجوال (اختياريّ) وكلمتا المرور. والنجاح يقول «يمكنك الدخول الآن».
- **إشعار الداعي** صار «الموظّف فعّل حسابه» لا «بانتظار التفعيل» (يذهب لمن دعاه عبر `p.by`).
- **⚠️ رمز v1 القديم (قطاع بلا بريد) يسقط** في `readToken` — فالروابط المشتركة الصادرة تموت مع الحذف.
- **الاختبار: 35 تأكيداً سلوكيّاً** لنقطة الدعوة (شبكة مُقلَّدة: الأصل/الصلاحية/التحقّق · البريد
  للمدعوّ وحده وبلا كلمة مرور · الرمز · **العميل يُرسل بريداً وقطاعاً ودوراً مخالفة فيُتجاهَل كلّه** ·
  فشل فحص الحسابات **يفشل مغلقاً** · لا حساب يتيم) **+ 760 تأكيداً بنيويّاً** (هندسة القالب،
  والمقارنة ثابتة الزمن، وسلامة الصفحة العامّة). **مُتحقَّق أنّها تعضّ: 7 علل ⇒ كلٌّ أفشل تأكيده.**
- **🐛 وسادس تكرار لدرس الكعب:** الشبكة المُقلَّدة كانت تطابق `username=ilike.abdullah` **نصّاً
  صغيراً**، والاستعلام الحقيقيّ يأتي بـ`ilike.Abdullah` (من صفّ الأدمن) — فسقط إشعار الداعي بلا سبب
  في الكود. صار الكعب يحاكي **دلالة `ilike`** (غير حسّاس لحالة الأحرف) ويحترم `active=eq.true`.
- **`scripts/e2e/staff-invite-screens.mjs` (جديد): 21 فحص متصفّح تحت CSP الإنتاج** + 7 لقطات
  (التفعيل سطح مكتب/جوال · بعد النجاح · **قالب البريد مولَّداً من الدالّة نفسها** سطح مكتب/جوال ·
  نموذج الدعوة · بعد الإرسال). صفر انزلاق · صفر خطأ · صفر انتهاك CSP.
  ⚠️ درسان: `hideLoginScreen()` لازمة وإلّا اعترضت بطاقة الدخول النقر رغم أنّ الزرّ «مرئيّ ومُمكَّن»؛
  وفحصُ «لا أثر للرابط المشترك» على `page.content()` كلّه **إيجابية كاذبة** (عبارة «توليد الرابط»
  ترد في وحدة RFQ) — نُطِّق بالنافذة وحدها.

#### 🐛 الرابط كان يرفض المالك نفسه بـ403 (2026-09-11، `be370f4`+`cb605b3` — أُمسِك قبل أوّل استعمال)
عند سؤال المالك «هل يمكنني إرسال الدعوات الآن؟» فُحِص المسار الحيّ **قبل** الجواب، فظهر مانع حقيقيّ:
`callerProfile` كانت تطابق `proc_users?username=eq.` — **حسّاسة لحالة الأحرف**. والإنتاج يحمل
**صفَّين يختلفان بالحالة فقط**: `Abdullah` (أدمن نشط) و`abdullah` (مستخدم **موقوف**)، و`emailToUsername`
تُعيد الاسم بحروف صغيرة ⇒ المطابقة تلتقط **الصفّ الموقوف** ⇒ المالك يُرفَض عند توليد الرابط.
- **`admin-users.js` كانت قد تعلّمت الدرس** بـ`ilike` وتعليق صريح يشرحه — **وهذا الملف فاته**.
  ⚠️ **قاعدة: أي نقطة جديدة تحلّ هوية أدمن ⇒ انسخ نمط `admin-users.js` لا تعيد اختراعه.**
- **الإصلاح أقوى من الأصل:** `ilike` بتهريب أحرف البدل (`% _ \`) + **اختيار الأدمن النشط صراحةً**
  لا أوّل صفّ يعود (ترتيب PostgREST ليس عقداً، فـ«الأوّل» يجعل الصلاحية رهن الحظّ).
- **🐛 ورابع تكرار لدرس الكعب:** الشبكة المُقلَّدة كانت تُرجع صفّاً واحداً متخيَّلاً (`abdullah` أدمن نشط)
  — **عكس الإنتاج تماماً** — فمرّ العيب عبر 22 تأكيداً. صارت تُرجع صفَّي الإنتاج حرفيّاً **بالموقوف أوّلاً**.
- **مُتحقَّق أنّ الحارسين يعضّان:** إعادة `eq.` ⇒ إخفاق · وأخذ أوّل مطابقة بلا اشتراط أدمن نشط ⇒ إخفاق
  (**وكان سيمنح موظّفاً عاديّاً حقّ السكّ**). الحزمة: **733 تأكيد واجهة · 23 تأكيد نقطة الدعوة**، خروج 0.

#### 🔑 اسم الدخول المشتقّ كان هويّة ثالثة — بلاغا Codex على PR #100 (2026-09-13)
مراجعة Codex على الـPR **بعد دمجه** رصدت عيبين في `functions/api/staff-invite.js`؛ **كلاهما حقيقيّ،
والأوّل أخطر ممّا وصفه Codex** (أُكِّد بالقياس على صفوف الإنتاج، وأُصلح قبل أوّل دعوة حقيقية).
- **الجذر المشترك:** الملفّ كان يشتقّ اسم الدخول **باشتقاق خاصّ به**
  (`local-part` بعد حذف كل ما ليس `[a-z0-9_]`) بدل `emailToUsername` التي يستعملها بقية النظام.
- **(أ) [الأخطر] `AUTH_EMAIL_MAP` مُتجاهَلة:** بريد `supply@aldeyabi.com` اسمه في النظام **`mostafa`**
  لا `supply`. وصفّ `Mostafa` في الإنتاج **بلا بريد** ⇒ لا فحصُ الاسم ولا فحصُ البريد يجده ⇒ **الدعوة
  تُرسَل**، ثمّ يقول Auth عند التفعيل «مسجَّل مسبقاً» — وكان ذلك يُقرَأ **نجاحاً** فيُكتَب صفّ `supply`
  ثانٍ **ببريد مصطفى مضبوطاً**. و`proc_me()` تُطابق **بالبريد أوّلاً** ⇒ **مصطفى (أدمن) ينقلب موظّف
  ميدان مُنطَّقاً** بلا أن تتغيّر كلمة مروره. **وكلمة مرور المدعوّ لم تُثبَّت أصلاً — فـ«النجاح» كاذب.**
- **(ب) النقطة تسقط:** `ali.salem@` كان يصير `alisalem`، بينما `usernameToEmail` في الدخول تبني البريد
  من `ali.salem` ⇒ الملفّ لا يُعثَر عليه بعد المصادقة فيدخل الموظّف بلا نطاق ولا صلاحيات.
- **وثالثة كشفها الفحص:** `existingUser` كانت `username.eq.` **حسّاسة للحالة** — و`Mostafa`/`Mahmoud`
  بحرف كبير ⇒ **دعوة بريدٍ له حساب تمرّ**. (نفس درس `be370f4` في `callerProfile`، **في الدالّة المجاورة**.)
- **العلاج (ثلاثة، بلا هجرة):** `usernameFrom = emailToUsername(...)` · `username.ilike.` بتهريب `% _ \` ·
  فشل Auth «مسجَّل مسبقاً» صار **409 بلا أي كتابة** بدل المضيّ.
- **🐛 وخامس تكرار لدرس الكعب:** الشبكة المُقلَّدة كانت تجهل `Mostafa`/`Mahmoud` وتُطابق نصّاً صغيراً
  فقط، فمرّ العيبان عبر 35 تأكيداً. صارت تحمل **صفوف الإنتاج حرفيّاً** (بحروفها الكبيرة و`email: null`)
  وتُحاكي دلالة `ilike`. ⚠️ و`saleh` **غائب عمداً** — هو المدعوّ في المسار السعيد.
- **الاختبار: 40 تأكيد نقطة `/api/staff-invite`** (+5: supply@ يُرفض · Mahmoud بحالة مختلفة يُرفض ·
  `ali.salem` يبقى بنقطته · البريد يُعلِم المدعوّ باسم دخوله · Auth-قائم ⇒ 409 بلا كتابة) **+ 770 تأكيد
  واجهة**، خروج 0. **مُتحقَّق أنّ الحرّاس تعضّ: العلل الثلاث أُعيدت عمداً ⇒ كلٌّ أفشل تأكيده الصحيح**
  (اشتقاق الاسم ⇒ supply@ و`ali.salem` · `eq.` ⇒ supply@ وMahmoud · تجاهل Auth ⇒ الملفّ الثاني)، والضابط يمرّ.
- **⚠️ قاعدة: لا تشتقّ اسم دخول من بريد إلا بـ`emailToUsername`** — أي اشتقاق ثانٍ يخلق هويّة ثالثة
  لا الواجهة تجدها ولا القاعدة. وأي مطابقة على `proc_users.username` **لحلّ هويّة (قراءة)** تكون
   `ilike` بتهريب أحرف البدل مع اختيار الأدمن النشط صراحةً. **⚠️ صُحِّحت 2026-09-16: القاعدة
   لا تسري على الكتابة** — `PATCH`/`DELETE` تبقى `eq.` لأنّ `ilike` تطال كل الصفوف المتشابهة
   بحالة الأحرف فتعدّلها معاً (الإنتاج يحمل `Abdullah` أدمن نشط و`abdullah` موقوفاً). محروس بتأكيدين.
- **✅ مدموج ومنشور (2026-09-13) — PR #101، كوميت الدمج `5eddcf3`.** **لا هجرة في هذه الدفعة** (كود
  فقط) فلا ترتيب مُلزِم بين الدمج والنشر. CI على `main`: الثلاثة خضراء. **مُتحقَّق حيّاً على النطاقين**
  (`suppliers.aldeyabi.com` و`aldeyabi-procurement.pages.dev`): `/api/staff-invite` = `{"ok":true}`
  بالمفاتيح الخمسة · سكّ بلا مصادقة **403** · رمز مزوَّر **401**.
  **⇒ إرسال الدعوات صار آمناً الآن** (قبل الدمج كانت دعوة `supply@`/`mahmoud@` تقلب هويّة الأدمن).

### 🎛️ صلاحيات الميدان + الرابط العميق من البريد — بلاغان من المالك (2026-09-14)
**بلاغه الأوّل:** «لا يوجد إدارة صلاحيات للمستخدمين الخاصين بالصيانة والتشغيل — مثل رفع مستند وغيره من
الأدوات لإدارتهم؛ لا يوجد صلاحيات ممكن إعطاؤها أو سحبها». **والثاني:** «عندما يصلهم إشعار بالإيميل وفيه
رابط لفتح النظام — الرابط لبوابة الطلبات وهو النظام الآخر، وليس النظام الحالي، ولا يفتح على الطلب مباشرة».
**كلاهما مؤكَّد بالقياس، والثاني أسوأ ممّا وصفه.**

#### ① الجذر: **النطاق ليس صلاحية**
حُرّاس الصفوف («هذا طلبك» · «هذا الأمر في قطاعك») يمنعون الوصول لبيانات غيره — لكنّهم **لا يمنحون المدير
أي وسيلة ليقول ماذا *يفعل* هذا الموظّف**. القياس: من **تسع** قدرات ميدانية حقيقية، **اثنتان فقط** لهما
مفاتيح (`can_receive_po` · `can_view_amounts`)، والسبع الباقية (رفع الطلب · تعديله · قوالبه · رفع السند ·
التعليق · الردّ على الاستفهام · طباعة تقرير المتابعة) **مشتقّة من كونه صاحب الصفّ** — فلا تُمنَح ولا تُسحَب.
- **قرارات المالك (AskUserQuestion):** **أربعة مفاتيح عملية** (لا مفتاح لكل زرّ) · **قوالب أدوار داخل
  نافذة المستخدم القائمة فقط** (لا شاشة جديدة) · **كل قدرات الميدان ممنوحة افتراضاً عدا المبالغ**.
- **المفاتيح الأربعة** (فئة «الميدان (الصيانة والتشغيل)»، وسمها `◈` في الشبكة): `can_create_pr`
  (رفع الطلبات وتعديلها وقوالبها) · `can_upload_docs` · `can_comment` · `can_print_followup`.
- **قوالب الأدوار `ROLE_PRESETS`:** «موظّف ميدانيّ» (الخمسة بلا مبالغ) · «مشرف ميدانيّ» (+المبالغ) ·
  «متابعة فقط» (لا شيء). **`applyUserFormPreset` تضبط *كل* مربّع** (تُطفئ ما ليس في القالب) وإلّا صار
  القالب إضافةً على حالة سابقة لا تعريفاً للدور، ويُعلن للمدير ما طُبِّق (`#uf-preset-hint`).
- **الافتراضيّ ثلاثيّ الحال (مطابق لسابقة LP1):** المفتاح موجود ⇒ يُحترَم · غائب ⇒ **true لموظّف المكتب
  و false للمُنطَّق**. مرآته على الخادم `proc_has_perm(p_key, p_office_default)` في
  **`db/system2-field-permissions.sql`** (سياسات `proc_purchase_requests`/`proc_pr_items`/
  `proc_pr_templates` + حُرّاس داخل `pr_set_doc`/`pr_post_message`/`po_add_comment`).
- **⚠️ فخّان أُمسِكا قبل أي تطبيق حيّ:** (أ) `pr_has_perm` القائمة افتراضها **false** — استعمالها للمفاتيح
  الجديدة كان **يسلب الثلاثة القائمين** رفعَ الطلبات ورفعَ السندات والتعليق **لحظة النشر**؛ (ب)
  `scope_sectors` عمودها **NULL** لموظفي المكتب، فبلا `coalesce` داخليّة يُنتج التعبير NULL لا true
  فيسقط الصفّ من `EXISTS` — و`CASE` لا `AND` (ترتيب تقييم AND غير مضمون، و`jsonb_array_length` على غير
  مصفوفة يرمي).
- **`pr_select` لم تُمَسّ عمداً:** سحب صلاحية الكتابة لا يعني إخفاء طلبات الموظّف السابقة.
- **`staff-invite.js`:** `FIELD_PERMISSIONS` من مفتاحين إلى **ستّة صريحة** — وإلّا وصلت الدعوة بحساب
  **مشلول** (المفاتيح الأربعة الجديدة غائبة ⇒ ممنوعة للمُنطَّق).
- **⚠️ قاعدة: لا تشتقّ قدرة ميدانية من ملكية الصفّ بلا مفتاح** — وإلّا لا يملك المدير سبيلاً لسحبها.

#### ② الرابط العميق: البريد كان يحيل إلى نظامٍ آخر
`_pr-shared.js` كانت تبني `${origin}/requests.html` بعنوان **«فتح بوابة الطلبات»** — أي صفحة الطلبات
المستقلّة القديمة لا `index.html`. **والأسوأ: `index.html` كان بلا أي معالجة للروابط العميقة إطلاقاً**،
فحتى لو صُحِّح العنوان لهبط المستخدم على المهبط العامّ لا على طلبه.
- **`requestUrl(origin, prId)`** مصدر واحد للرابط (`/?pr=<id>`) في المواضع الثلاث + تصحيح النصوص الخمسة.
- **`openDeepLink()`** تُنادى من `startApp`: تقرأ `?pr=` أو `?po=`، **تُنظّف المعامل بـ`history.replaceState`**
  (وإلّا أُعيد فتحه مع كل تحديث)، ثمّ `prGoView('track', id)` أو `openPODrawer(po)`.
- **⚠️ الرابط لا يمنح وصولاً:** RLS تبقى الحكم — رابطٌ لطلب خارج نطاق المستخدم يفتح شاشةً فارغة لا بياناتٍ.

#### التحقّق
**795 تأكيد واجهة** (+قسما ٣٤ صلاحيات الميدان و٣٥ الرابط العميق) · **`FP1–FP10`** في
`db/system2-tests/18_field_permissions.sql` بـRLS فعليّة بدور `authenticated` (عدم انحدار المكتب · منح/سحب ·
المُنطَّق بلا مفتاح ممنوع · الأدمن فوقها · السياسة تمنع الإدراج · الممنوح يُدرِج فعلاً بنوده · استقلال
`can_upload_docs` عن `can_create_pr` · حارسا التعليق · القوالب) · **18 فحص متصفّح** تحت CSP الإنتاج في
`scripts/e2e/field-permissions.mjs`. **مُتحقَّق أنّ الحرّاس تعضّ: 7 علل أُعيدت عمداً (3 SQL + 4 واجهة) ⇒
كلٌّ أفشل تأكيده الصحيح**، والضابط يمرّ في الأداة نفسها.
- **⚠️ الترتيب مُلزِم: `db/system2-field-permissions.sql` تُطبَّق *قبل* النشر** (الواجهة الجديدة تفترض إنفاذ
  المفاتيح خادميّاً) و**قبل إنشاء أي حساب ميدانيّ**، بعد `system2-staff-scope.sql` ثمّ `system2-request-flow.sql`
  ثمّ `system2-request-tracking.sql`. **لم تُطبَّق بعد** (أداة Supabase تحتاج إعادة مصادقة في هذه الجلسة).

### 🚀 إطلاق موديل طلبات الشراء — دمج #103 + عيبان حرِجان أُمسِكا بالتشغيل الحيّ (2026-09-14)
**قرارا المالك اللذان قلبا الاتجاه:** ① **وورك فلو باعتمادات** (يُلغى قرار 10-09 «متابعة لا وورك فلو»)
② **المقرّ `index.html`** لا `requests.html`. ③ ومبدأ حاكم: «**الرؤية المالية ليست للجميع — تُمنَح منّي**».

**المسار:** Codex بنى الموديل على فرع `codex/purchase-request-workspace`. راجعتُه ثلاث جولات؛ كل جولة
كشفت موانع أُغلِقت. **ثمّ دُمِج PR #103 (`5e23025`) ونُشِر** — والفحص الحيّ بعد النشر كشف عيبين ما كان
لأي حزمة أن تكشفهما.

#### ① [حرِج · أمنيّ] تصعيد صلاحية عبر أعمدة الموديل الجديدة — أُغلِق
سياسة `users_update` على `proc_users` هي `USING(true)`، فالمدافع الوحيد `proc_users_guard`، وهو يمرّر أي
UPDATE لا يمسّ قائمته. وأضاف الموديل ثلاثة أعمدة **تحمل صلاحيات خارجها**: `pr_permission_overrides` ·
`pr_profile_key` · `pr_department_ids`. **استغللتُه فعليّاً** بدور `authenticated` وهويّة موظّف صيانة:
PATCH واحد ⇒ `pr_view_financials`+`pr_manage_users`+`pr_view_all` = true، و`proc_can_view_amounts()`
انقلبت `f→t`. **وهي نفس ثغرة 10-09 حين أُضيف `scope_sectors` بلا حارس.**
**⚠️ القاعدة:** كل عمود جديد يحمل صلاحية يُضاف إلى قائمة `proc_users_guard` **في الهجرة نفسها التي
تُنشئه**. (أصلحها Codex داخل هجرته بأوسع ممّا اقترحتُ: شمل `department_id`/`manager_user`/`requested_role`.)

#### ② [عالٍ] دعوة v3 كانت تُلغي النطاق — أُغلِق
`staff-invite.js` كان يضع `scope_sectors: []` ⇒ `proc_is_scoped()=false` ⇒ **الإقفال والقشرة وأقلّ
الامتياز كلّها معلّقة على تلك الدالّة فتسقط معاً**. مُثبَت: المدعوّ قرأ صفّ تسجيل مورّد بسجله وآيبانه،
والمُنطَّق قرأ صفراً. وكلام المالك يحسمه: غير المُنطَّق يأخذ `can_view_amounts` ضمن **16 مفتاحاً تلقائيّاً**
من `DEFAULT_PERMISSIONS` — أي «للجميع» تماماً كما نفى. عاد `scope_sectors: [sector]`.

#### 🔴 ③ العيب القاتل: `make_interval(hours => numeric)` — لا تُكتشَف إلا بالتشغيل
```
ERROR: 42883: function make_interval(hours => numeric) does not exist
CONTEXT: PL/pgSQL function pr_set_due() line 8
```
`db/pr-portal.sql` يُعلن `v_h numeric := pr_sla_hours()` و`pr_sla_hours()` تُرجع **numeric**، بينما
`make_interval` معاملها **integer**. فلا توقيع مطابق ⇒ **يفشل المُشغِّل ⇒ يفشل كل INSERT/UPDATE يضع
الطلب في `in_review`** — أي **كل رفع طلب وكل انتقال مرحلة**. و**موضع ثانٍ بالعلّة نفسها** في
`pr_run_sla` (`v_sla numeric`) ⇒ **تصعيد SLA معطّل كذلك**.
- **عيب كامن منذ `pr-portal.sql`** لم يظهر لأن بوابة الطلبات الداخلية لم تُستعمل قطّ على الإنتاج
  (صفر طلب)، فظهر في **أوّل دقيقة** من تشغيل الموديل.
- **ولم تلتقطه أي حزمة**: الكعوب لا تبذر قيمة SLA فيختلف مسار النوع. **هذا هو حدّ الحزم — الفرق
  في البيانات لا في المخطّط.** الملف: `db/system2-fix-pr-set-due-interval.sql` (مُطبَّق حيّاً بهجرتين:
  `system2_fix_pr_set_due_interval` · `system2_fix_pr_run_sla_interval`) + تصحيح `db/pr-portal.sql`
  نفسه كي لا يعود العيب عند إعادة تشغيل المُنصِّب.
- **⚠️ قاعدة:** لا تُمرِّر `numeric` إلى `make_interval` — استعمل `(v * interval '1 hour')`، وهو أيضاً
  **يحفظ الكسور** (`hours=>int` كان سيبتر نصف الساعة).

#### 🔴 ④ ونافذة إنتاج مكسورة كشفها فحص الترتيب
الهجرة طُبِّقت على الإنتاج **قبل** الدمج، وهي تسحب `INSERT/UPDATE` عن `authenticated`، بينما `main` كان
ما يزال `5eddcf3` يكتب مباشرةً ⇒ **كل إنشاء/تعديل طلب كان يفشل**. أُغلِقت بالدمج والنشر فوراً.
**⚠️ القاعدة تتأكّد: الهجرة التي يعتمد عليها الكود الجديد تُطبَّق قبله — والنشر يتبعها فوراً لا لاحقاً.**

#### ⚙️ إعداد الإنتاج المُنفَّذ (بيانات لا كود)
- **كل المستخدمين كانوا بلا إدارة** (`department_id=NULL` · `pr_department_ids={}`)، و`pr_save_request`
  يرفض الإنشاء لإدارة خارج النطاق ⇒ **لا أحد غير الأدمن يرفع طلباً**. أُسنِد موظّفا الصيانة
  (`m.elsobky` · `mostafa.kishk`) إلى `DEP-OPS`.
- والهجرة كتبت لهما `{pr_create:false, pr_comment:false, pr_upload_attachments:false}` بحقّ (مفاتيحهما
  غائبة، و«المنح صريح أو لا شيء» للمُنطَّق) ⇒ أُزيلت التجاوزات الثلاثة فورثا ملفّ `requester`:
  **يرفعون · يرفقون · يعلّقون · يطبعون · يرون طلباتهم — والمبالغ محجوبة** (قرار المالك).
- **سلسلة الاعتماد لها مؤهَّلون** (لا تكرار لفجوة «صفر مؤهَّلين»): اعتماد الحاجة وإذن التسعير =
  `Abdullah`+`Mostafa` · ربط أوامر الشراء = +`Mahmoud`.
- **⚠️ ملاحظة حوكمة مرصودة:** `pr_decide` يمنع المقدّم من اعتماد طلبه، لكنه **لا يمنع الشخص نفسه من
  اعتماد المرحلتين**؛ واليوم المرحلتان بيد الأدمنَين نفسيهما. يُحَلّ بإسناد مدير صيانة حقيقيّ
  (ملفّ `maintenance_manager`) — إعداد لا كود.

#### ✅ الإثبات الحيّ الكامل (على الإنتاج، داخل معاملة أُلغيت — صفر أثر)
```
① موظّف الصيانة رفَع PR-DG2026-0002 ⇒ in_review / maintenance_review · استحقاق المرحلة مضبوط
② ✅ مُنِع المقدّم من اعتماد طلبه
③ اعتماد مدير الصيانة ⇒ in_review / procurement_review
④ إذن مدير المشتريات بالتسعير ⇒ approved / pricing / in_progress
⑤ رُبِط أمر الشراء P.O-DG26-3216 ⇒ partially_ordered · تغطية=0.33
⑥ ✅ مُنِع موظّف الميدان من فكّ الربط
⑦ المبالغ لموظّف الميدان: ✅ محجوبة   ⑧ يرى طلبه: ✅
```
**ونظافة الإنتاج بعده مُتحقَّقة:** صفر بقايا · 116 أمر شراء · 5 مستخدمين نشطين كما كانوا.

**وفحوص ما بعد الهجرة:** صفر انحدار على المبالغ (الثلاثة يرونها) · الحارس يغطّي الأعمدة الثلاثة ·
**7 سياسات إقفال RESTRICTIVE سليمة** · سياستا `anon` للتسجيل سليمتان · `authenticated` لا يكتب على
`proc_purchase_requests` · صفر دالّة DEFINER بلا `search_path` · النطاقان يخدمان الكود الجديد
(`pr_save_request`/`pr_decide`/`pr_link_purchase_order`/`openDeepLink`) بصفر كتابة مباشرة ·
النقاط الخمس `{"ok":true}`.

**⚠️ درس منهجيّ يتكرّر للمرّة الثامنة:** الحزم الخضراء لا تُغني عن **تشغيل الدورة على الإنتاج نفسه**.
العيبان ③ و④ مرّا من 7 فحوص GitHub و796 تأكيد واجهة و43 تأكيد مساحة عمل — وكشفهما استدعاءٌ واحد حقيقيّ.

### 🔚 دورة الطلب حتى نهايتها: إلغاء + أرشفة + إقفال تلقائيّ (2026-09-14، الفرع `claude/purchasing-system-review-n7x0v5`، PR #104 مسودّة)
**تكليف المالك:** «الغاء الطلب في حال تكنسل · ارشفة الطلبات بعد اقفالها وربطها بأمر ثم اقفاله باستلام كامل · في
حال اغلاق امر الشراء باستلام كامل يغلق الطلب بالاستلام · الواجهة بلا سلوب ذكاء واضح · اختبارات شاملة من الإنشاء
إلى الإقفال». مع تدقيق «رفع الطلب عبر الذكاء».
- **الخادم `db/system2-request-closure.sql`** (تُطبَّق بعد `system2-purchase-request-workspace.sql`): `pr_cancel_request`
  (سبب ≥3 · الطالب قبل صدور الأمر فقط · المشتريات أي حالة قبل الإقفال · **المُقفَل لا يُلغى** — مرتجع على الأمر ·
  يفكّ الروابط + يُصفّر الاعتمادات المعلّقة + تدقيق) · `pr_close_from_po` (يُقفِل فقط عند بلوغ **كل** أوامر الطلب
  النشطة «تسليم كامل» ويؤرشف تلقائيّاً) · مُشغِّل `trg_po_receipt_closes_pr` (`AFTER UPDATE OF status` — كل مسارات
  الاستلام تُقفِل الطلب المرتبط بلا لمس `po_record_receipt`) · `pr_set_archived` (للمشتريات · يرفض أرشفة طلب حيّ) ·
  أعمدة `archived_at`/`archived_by`/`cancel_reason`. اختبار `20_request_closure.sql` (CL1–CL12) بـRLS فعليّة.
- **الواجهة (المُحوِّل):** وضع «🗄 الأرشيف» يُخلي الطابور من المؤرشَف ويعرضه وحده بعدّاد حيّ · زرّ إلغاء (بوّابته =
  حارس الخادم) · زرّ أرشفة/إعادة (مشتريات + حالة نهائية) · شارة «مؤرشف» · **تنظيف من مظهر «الذكاء»** (علامة العلامة
  بدل الحرف · أزرار حقيقية بدل الرموز · إزالة البحث المكرّر). مسار `prParseForm` (رفع بالذكاء) دُقِّق وتُرِك سليماً.
- **الاختبار:** `system2-tests.mjs` قسم ٣٦ (سلوكيّ) — **المجموع 806** · `purchase-workspace-visual.test.mjs` موسَّع
  بلقطات (أمسك عيب `prFmtDate` غير المعرَّف الذي فات الفحصَ النصّيّ). كل الحزم خضراء.
- ~~**⚠️ لم تُطبَّق الهجرة على الإنتاج**~~ → **✅ طُبِّقت (2026-09-15)** باسم `system2_request_closure_cancel_archive`.
- **🩹 درس حرِج (كلّفني إعادة بناء كامل):** أثناء «بيت-بروف» شغّلتُ `git checkout index.html` **فمحا كل تعديلاتي
  غير المُلتزَمة** (الإغلاق كان uncommitted على `main`). التعافي: استخراج تعديلات `index.html` من نصّ الجلسة
  (`~/.claude/projects/.../<session>.jsonl`) وإعادة تشغيلها. **درسان:** (أ) **الْتزِم قبل أي `git checkout`/بيت-بروف
  مدمِّر** — لا تترك ساعات عمل غير مُلتزَمة. (ب) **إعادة التشغيل من نصّ الجلسة تلتقط أدوات `Edit` فقط لا تعديلات
  `Bash` (`sed`/`python`)** — فالإصلاحات التي أجريتُها عبر Bash (شارة CSS + إصلاح `prFmtDate`) فاتت أوّل إعادة بناء
  وأمسكها اختبار المتصفّح. تحقّقْ دائماً بمقارنة الحالة المُعاد بناؤها بقراءاتك المؤكَّدة + تشغيل المتصفّح.

### 🔑 حفظ المستخدمين كان مكسوراً بالكامل — قائمة بيضاء 7 مفاتيح والواجهة ترسل 27 (2026-09-15، PR #104 مدموج)
**بلاغ المالك:** «قمت بمحاولة اسناد الوظائف للموظف ومدير القطاع لكن النظام لا يحفظ الاسناد و لا اضافة او ازالة
صلاحيات، ايضاً صلاحية الرؤية المالية لأوامر الشراء لا يمكن اعطاؤها او سحبها للموظفين».
- **الجذر (مقيس لا مُخمَّن):** `cleanPermissionObject` في `functions/api/admin-users.js` يرفض **كائن الصلاحيات
  كلّه ⇒ 400** إن حمل مفتاحاً واحداً غير مسموح. `LEGACY_PERMISSIONS` كانت **7** مفاتيح و`PERMISSION_DEFS` ترسل
  **27** ⇒ **20 مفتاحاً مرفوضاً** في كل حفظ. (مُعاد إنتاجه: بالقائمة القديمة ⇒ `null`؛ بالجديدة ⇒ كائن 27 مفتاحاً.)
- **⚠️ ولماذا سقط كلّ شيء معاً (وهذا لبّ البلاغ):** في `index.html:6752` يُستدعى `setProfile` **أوّلاً**، وعند فشله
  `return` في **6759** — **قبل** كتابة `scope_sectors` المباشرة في **6780**. فالـ400 الواحد كان يُسقِط في ضربة
  واحدة: الصلاحيات · الوظيفة (`pr_profile_key`) · القسم · **ونطاق القطاعات**. و`can_view_amounts` **لم تكن
  مفقودة قطّ** — كانت تسقط مع بقيّة الحفظ، فبدت «غير موجودة».
- **الإصلاح:** توسيع القائمة لتُطابِق كل مفاتيح `PERMISSION_DEFS`. **بوّابات الأمان لم تُمَسّ** — منح
  `can_manage_users`/`pr_manage_users` ما زال يتطلّب `callerIsModuleAdmin`. التوسيع في **المفاتيح المقبولة**
  لا في **من يملك منحها**.
- **حارس بنيويّ دائم** في `system2-tests.mjs` يقرأ القائمة من `admin-users.js` ويؤكّد قبولها كل مفتاح واجهة.
  **مُتحقَّق أنّه يعضّ** (إزالة `can_create_pr` ⇒ إخفاق). **الحزمة: 807 تأكيداً، خروج 0.**
- **⚠️ قاعدة: أي مفتاح جديد في `PERMISSION_DEFS` يُضاف إلى `LEGACY_PERMISSIONS` في `admin-users.js`** — وإلّا
  فشل **كل** حفظ مستخدم (لا حفظ ذلك المفتاح وحده). نفس صنف الفخّ الذي أوجب `FIELD_PERMISSIONS` في `staff-invite.js`.
- **🩹 درس تكرّر (ثانيةً في الجلسة نفسها):** أنهيتُ أمر بيت-بروف بـ`git checkout functions/api/admin-users.js`
  **فمحا الإصلاح غير المُلتزَم**، بينما بقي حارسُه في الحزمة ⇒ حزمة حمراء بلا سبب ظاهر. **الْتزِم قبل أي
  بيت-بروف يستعيد ملفّاً.**

**✅ الدمج والنشر (PR #104، كوميت الدمج `5c8c176`):**
- **⚠️ الترتيب المقصود: الهجرة قبل النشر.** طُبِّقت `system2_request_closure_cancel_archive` على
  `yofcaxvstjcrmbgciwym` **قبل** الدمج، لأنّ الواجهة الجديدة تقرأ عمود `archived_at` وتستدعي دوال الإقفال —
  ونشرُها قبل الهجرة كان سيُرجِع **400 على `select`** ويكسر شاشة الطلبات للجميع (نفس صنف عطل `make_interval`).
  والهجرة إضافيّة بحتة فلا تمسّ الواجهة القديمة المنشورة.
- **تحقّق بنيويّ حيّ:** الأعمدة الثلاثة · المُشغِّل · الدوال الأربع · `pr_close_from_po` **محجوبة عن
  `authenticated`** · `anon` ممنوع · **صفر دالّة DEFINER بلا `search_path`**.
- **✅ إثبات سلوكيّ حيّ على الإنتاج** (داخل معاملة تنتهي بـ`RAISE EXCEPTION` فتُجهَض — صفر أثر): **6/6**
  `أ1` الربط بالـRPC تغطية=1⇒ordered · `أ2` استلام كامل ⇒ **closed + مؤرشَف تلقائيّاً** · `أ3` مُنع إلغاء
  المُقفَل · `ب1` ربط أمرين · **`ب2` أمرٌ واحد مستلَم فقط ⇒ لم يُقفَل** (جوهر الصحّة) · `ب3` وباكتمال الثاني أُقفِل.
  و**`T4` تحديث أمر الشراء نفسه نجح = لا انحدار من المُشغِّل الجديد** على 116 أمراً حيّاً.
- **نظافة الإنتاج بعد الإجهاض مُتحقَّقة:** صفر طلب/بند/رابط اختباريّ · 116 أمراً · حالتا الأمرين `مُسلَّم`
  كما كانتا · 5 مستخدمين نشطين.
- **⚠️ درس منهجيّ:** أوّل تشغيل للاختبار كان **فراغيّاً جزئيّاً** — فشل الربط (`pr_link_purchase_order` يتطلّب
  بنوداً وتوزيعاً) فلم يُقفَل الطلب، **فبدا تأكيدُ «مُنع إلغاء المُقفَل» إخفاقاً حوكميّاً وهو ليس كذلك**.
  **تحقّقْ دائماً أنّ الشرط المسبق تحقّق فعلاً قبل قراءة نتيجة التأكيد.**

### 🎨 إعادة تصميم مساحة الطلبات + محضر الشراء (2026-09-15، بلاغ المالك)
**نصّ المالك:** «شكل سير العمل وعرض الطلبات والشاشة سيء جدا… استعن بكلود ديزاين لتطويرها · محضر طلب
الشراء/التقرير الذي يصدر سيء جدا جدا من ناحية عرضه · الواجهة الحالية مشتّتة · اقتبس بعض المزايا من
واجهة بوابة الطلبات · اكتشف أي بقز أو إيرورز».

**(أ) الشاشة كانت مشتّتة — الجذر مقيس لا مُخمَّن: ثلاثة أشرطة داكنة متراكبة.**
`.pr-work-top` (#231f20) + `.pr-work-rail` (#312d2d) + **`.pr-work-summary` (#312d2d)** — والثالث
شريط إحصاءات داكن يقع مباشرةً تحت عنوان أبيض، فيصنع «ساندويتش» يقطع البصر. صار **سطحاً فاتحاً**
(القيمة بارزة والتسمية هادئة)، فبقي شريطٌ داكن واحد كما في البوابة.
- **اللوحة مقتبَسة من `purchase-portal.html`** (توجيه المالك): خلفية باردة `#f2f4f8` بدل الكريميّ +
  نظام ظلال `--prw-sh1/sh2` (كان مسطّحاً بلا ظلال) + **أزواج دلاليّة** لكل حالة (لون/خلفية/حدّ) +
  نصف قطر 14px. الذهبيّ بقي **لون علامة لا لون سطح**.

**(ب) سير العمل — أهمّ مكسب.** كان `.pr-work-steps` قائمة `<li>` مسطّحة بخطّ 9–10px **بلا مسؤول ولا
تاريخ ولا رابط بصريّ**، فلا يُقرأ منها «أين الطلب الآن ومن يُمسِك به». صار `.prw-flow` مقتبَساً من
`.fl-node` في البوابة: شارة مرقّمة بحالة (✓ مكتمل · نابضة للحالية · ✕ مرفوض · ↩ مُعاد) +
**اسم المقرِّر الفعليّ وتاريخ قراره من `pr.approvals`** + موصّلات.
⚠️ المرحلتان 1 و2 تُطابَقان **بالترتيب** لا بمفتاح نصّيّ، فلا تنكسران لو تغيّرت مفاتيح القالب.
و**القرار الفعليّ يغلب الموضع**: مرحلةٌ رُفِضت أو أُعيدت تُعرَض بحالتها لا بموقعها.

**(ج) المحضر — الجذر مخالفة القاعدة 3 نفسها.** `prPrint` كانت تبني `<table border="1">` بأنماط
سطريّة مخترَعة بدل معجم `.print-*`، فتخرج بحدود المتصفّح الافتراضية بلا ترويسة ولا إجماليات. الآن:
`.print-parties` (مقدّم الطلب · سياق الاحتياج) + `.print-table` بـ`tfoot` إجماليات + **جدول «سلسلة
القرارات المسجّلة»** (وبدونه الورقة طلبٌ لا محضر) + أوامر الشراء المرتبطة + `.print-note-box` حجّية +
**تواقيع من المعتمِدين الفعليّين** (`prSignatories`) لا مسمّيات ثابتة.
⚠️ **والمبالغ تمرّ بـ`canViewAmounts()`** فموظّف الميدان يطبع المحضر بلا أي قيمة.

**(د) عيوب وُجِدت وأُصلحت:**
1. **[واجهة]** `colspan="8"` ثابت في «لا توجد بنود» بينما الجدول 6 أعمدة لمن لا يرى المبالغ.
2. **[محضر]** كان يطبع الحرف **«ض»** مكان الشعار (`MARK_B64` موجود ويُستعمَل في الشريط العلويّ).
3. **[محضر]** عمود «الملاحظات» يُحشى بـ**حالة الطلب** عند غياب ملاحظة البند ⇒ بيانات ليست بيانات البند.
4. **[محضر]** إجماليّ «**0 ر.س**» مُلفَّق حين لا سعر لأي بند (صار «—»).
5. **[ارتباطات — حقيقيّ]** طلبٌ يحمل `po_number` بلا صفّ في `proc_pr_po_links` (شكل الصفوف السابقة
   للموديل) كان يُعرَض **«لم يُربط أمر شراء بعد» وهو مرتبط فعلاً**. الحلّ `prPoLinksOf(pr)` مصدراً
   واحداً يسقط إلى `po_number`. **⚠️ لا تقرأ `pr.po_links` مباشرةً في عرض جديد.**
6. **[CSS ميت]** قواعد `.pr-work-steps` في استعلام الوسائط بقيت بعد حذف الصنف.

**(هـ) خمسة فحوص ساقطة في `scoped-staff-screens.mjs` — والسبب ليس عيباً في النظام.**
⚠️ **مُثبَت أنّها تسبق تعديلاتي:** شُغِّلت على `HEAD~1` فسقطت **نفسها بالضبط (38/43)**.
- ثلاثة منها لأنّ الكعب يمنح الموظّف `can_receive_po` وحده، وهو **يسبق مفاتيح الميدان (2026-09-14)**؛
  و`hasPermission` صارمة للمُنطَّق (الغياب = منع) ⇒ إخفاء زرّ التقرير وخانة التعليق كان **القاعدة
  تعمل بحقّ**. صار الكعب يمنح حزمة قالب «👷 موظّف ميدانيّ» نفسها.
- واثنان لإعادة بناء شاشة الطلبات (PR #103): `.pr-tab` صارت `.pr-work-navbtn`، و`prGoView('track')`
  صار اختياراً من الطابور + تبويب الارتباطات. **الحزمة الآن 43/43.**
- **⚠️ السبب الجذريّ أنّ السكربت خارج CI فتعفّن بصمت.** حاولتُ ضمّه فسقط **في العدّاء وحده**
  (43/43 محليّاً · 31/43 ثمّ 25/43 في CI) بنمط «الهوية والبيانات غائبتان» — سباق إقلاع لم
  أُثبِت علاجه (محاولة ضبط الكعب قبل `startApp` وإعادة البذر **زادته سوءاً**). فأُبقي **خارج
  CI عمداً**: سكربتٌ متقلّب هناك أسوأ من غيابه لأنّه يُعلّم الفريق تجاهل الأحمر.
  **بند مفتوح:** يُجعَل حتميّاً ثمّ يُضاف. وإصلاحاته الحقيقية (مفاتيح الميدان + محدّدات
  مساحة العمل) محفوظة، فهو يمرّ 43/43 محليّاً بعد رفع خادم المعاينة.

**(و) درس تحقّق تكرّر: تأكيد فراغيّ.** أوّل تأكيد للتواقيع طابق `maint.manager` على **نصّ المستند
كلّه**، والاسم يرد في جدول القرارات أيضاً ⇒ مرّ حتى بعد إعادة التواقيع مسمّياتٍ ثابتة (أمسكه
البيت-بروف). نُطِّق بـ`.print-signatures` وحدها. **لا تُطابِق نصّاً على المستند كلّه لتأكيد جزءٍ منه.**

**التحقّق: 813 تأكيد واجهة** (+6 حرّاس: معجم المطبوعات · التواقيع الفعليّة · سلسلة القرارات ·
`printDocOpen` · مصدر الارتباطات) · 36+6+9 عقد مساحة العمل · **18/18 صلاحيات الميدان تحت CSP** ·
**43/43 شاشات الموظّف المُنطَّق** · الحزمة البصرية + لقطة المحضر المطبوع.
**مُتحقَّق أنّ الحرّاس تعضّ:** أربع علل أُعيدت عمداً (جدول خام · تواقيع ثابتة · قراءة `po_links`
مباشرةً · إسقاط السقوط لـ`po_number`) ⇒ كلٌّ أفشل تأكيده الصحيح، والضابط يمرّ في الأداة نفسها.

#### ✅ مدموج ومنشور (2026-09-15، PR #105، كوميت الدمج `c4c057e`)
**بلا هجرة في هذه الدفعة** (واجهة ومطبوعات واختبارات فقط)، فلا ترتيب مُلزِم بين الدمج والنشر —
خلافاً لـ#104 التي سبقتها هجرة الإقفال.
- **الفحوص السبعة خضراء على الرأس `3d4be6d`** قبل الدمج، وعلى `main` بعده (`system2-tests` رقم 100 ·
  `pages` رقم 323).
- **مُتحقَّق حيّاً على النطاقين** (`aldeyabi-procurement.pages.dev` و`suppliers.aldeyabi.com`،
  **بصمة متطابقة 2,746,675 بايت**): `prw-flow` · `prPoLinksOf` · `prSignatories` · `print-signatures` ·
  `prw-navy` حاضرة، و**`pr-work-steps` = صفر** (الـCSS الميت أُزيل فعلاً لا في المستودع وحده).
  و`border="1"` الوحيد الباقي **تعليقٌ يشرح القاعدة** لا جدولاً — هذا هو سبب مرور الحارس.
- **النقاط الخمس `{"ok":true}`** بعد النشر (`notify`/`pr-doc`/`reg-doc`/`staff-invite`/`supplier-invite-link`).
- ⚠️ **تصحيح وصف الـPR قبل الدمج:** كان قسمه ⑤ يقول إنّ `scoped-staff-screens` «أُضيف إلى CI»، وهو
  **ادّعاء بات كاذباً** بعد تراجعي في `3d4be6d`. صُحِّح في الوصف المدموج. **درس: وصفُ الـPR أثرٌ دائم
  يُقرأ بعد سنة — راجعه قبل الدمج كما تراجع الشيفرة.**
- **⚠️ متبقٍّ (تشغيليّ، على المالك):** `db/system2-field-permissions.sql` **لم تُطبَّق حيّاً بعد**
  (أداة Supabase تحتاج إعادة تفويض) — **تُطبَّق قبل إنشاء أي حساب ميدانيّ** · إنشاء حسابات الموظفين ·
  `db/system1-storage-hardening.sql` المرحلة 1.
- **بند مفتوح عليّ:** جعل `scoped-staff-screens.mjs` حتميّاً ثمّ ضمّه إلى CI.

### 👤 هويّة المعتمِدين · تاريخ الإعادة · نسخة المورّد (2026-09-15، ستّ ملاحظات من المالك)
**نصّ المالك:** «تحديد مدير القطاع يظهر بلا · حددت أن مدير القطاع م محمد السبكي ومع ذلك اعتماد
الحاجة يصلني أنا · مسميات الأشخاص يكتب الاسم وليس اسم المستخدم في سير العمل أو سلسلة الموافقات أو
المناقشات · تأكد أن الطلب يرجع للطالب فعلاً وتُحفظ كامل المعلومات عند إرجاعه وعند عودته · طباعة
واضحة للموردين بالطلبات والكميات بلا سعر وحدة أو كميات مخزنية · وتأكد أن كل شيء عربي ويبدأ من اليمين».
ثمّ ثلاث إضافات: **حروف حقل التاريخ معكوسة** (لقطة) · **تاريخ التوريد لا يقبل الماضي** · **اسم
المشروع في شاشة الطلب وبطاقته الجانبية**.

#### ① المُنتقي الفارغ كان **سبب** بلاغ الاعتماد — لا بلاغاً مستقلاً
`__prUsers` **مُعرَّفة في سطر وتُقرأ في سطر ولا تُسنَد في الملف كلّه**، فمُنتقي «المدير (المعتمِد)»
يعرض «— بلا —» وحدها دائماً — **ويُظهر حالةً مخالفة للمخزَّن**. والقاعدة الحيّة: `manager_user='Abdullah'`
للأقسام الأربعة، و`pr_resolve_stage_approver` تأخذ **مدير القسم أوّلاً** فلا تبلغ أبداً الاحتياطيّ
الذي كان سيختار `m.elsobky` (المضبوط فعلاً: `DEP-OPS` · ملفّ `maintenance_manager` ·
`pr_approve_maintenance=true`). ⇒ لم يكن بيد المالك إصلاحه لأنّ الشاشة الوحيدة لذلك معطوبة.
**مُصلَح:** القائمة تُجلب في `prLoadAll` بتسامح · و`DEP-OPS.manager_user='m.elsobky'` حيّاً بأثر تدقيق ·
والمرحلة المعلّقة على `PR-DG2026-0003` حُوِّلت إليه.
**⚠️ درس: واجهةٌ تعرض قيمةً لا تملك مصدرها تكذب على صاحب القرار** — والعطل يبدو عطلاً في الحوكمة.

#### ② الاسم يُخزَّن خادميّاً — حلّه في المتصفّح مستحيل
`proc_pr_approvals` **بلا عمود `approver_name`** فيسقط `a.approver_name||a.approver` حتماً إلى اسم
المستخدم. **ولا يصحّ حلّه بترجمة محلّية:** سياسة `users_select` هي
`(NOT proc_is_scoped()) OR username=proc_me()` — فالموظّف المُنطَّق يقرأ **صفّه وحده**، وهو أكثر من
يحتاج معرفة من يُمسك طلبه. فالعمود يُملأ عند الإسناد (`pr_display_name`) **ويُثبَّت مع القرار**
(المقرِّر قد لا يكون المسنَد إليه — تفويض/أدمن). و«المسؤول الحالي» صار يُسمّى بشخصه (`prPendingOwnerName`).
**المناقشات كانت سليمة أصلاً** (`author_name` يُملأ في `pr_post_message`).

#### ③ إعادة الإرسال كانت تمحو صفّ الإعادة نفسه
`pr_save_request` كانت تنفّذ `DELETE FROM proc_pr_approvals WHERE pr_id=v_id` ⇒ **يُمحى مَن أعاد ومتى
ولماذا**، و`return_reason` يُصفَّر — بينما المحضر المطبوع يَعِد حرفيّاً بـ«بقاء السجل السابق دون حذف».
**الحلّ:** عمود `revision` + المفتاح الفريد صار `(pr_id,revision,seq)` + **حذف المعلّق وحده**.
والواجهة تفصل `approvals` (السجل الكامل، للمحضر) عن `approvals_current` (الإصدار الجاري، للمسار).
**⚠️ وفخّ لازم:** بعد بقاء التاريخ صار `(pr_id,seq)` غير فريد، فاستعلام `current_owner` للمرحلة 2
**يجب** أن يُقيَّد بـ`decision='pending'` وإلّا أعاد أكثر من صفّ وفشل التحديث (في `pr_decide`
و`pr_transition_email` معاً).

#### ④ «نسخة المورّد» — مستند خارجيّ بلا بيانات داخلية
`prPrintSupplier`: أطراف + موقع وموعد + جدول (الصنف · المواصفات · الوحدة · الكمية) + تعليمات + توقيعان.
**لا سعر وحدة ولا إجمالي ولا رصيد مستودعيّ ولا كمية تعاقدية ولا سلسلة اعتماد.** بوّابته المشتريات.
محروس بحارسَي **مصدر** (متن الدالّتين خالٍ من `fmtPrice`/`unit_price`/`stock_balance`/«ر.س») و**مُخرَج**
(يُولَّد من بنود بأسعار وأرصدة مميّزة ويُتحقَّق أنّ أيّاً منها لا يظهر).

#### ⑤ تاريخ التوريد لا يقبل الماضي · وحروف الحقل كانت تنعكس
نصّ العنصر النائب في `<input type="date">` **يرسمه المتصفّح** لا الصفحة، وفي RTL بلا `dir` صريح تخرج
حروفه معكوسة («ةنس/رهش/موي» = «يوم/شهر/سنة» مقلوبة). `a11yWire` صار يضبط `dir="ltr"` و`lang="en-GB"`
على كل حقول التاريخ/الوقت وقت التشغيل (فيغطّي المولَّد أيضاً). والماضي مرفوض **في الواجهة (`min`)
وعلى الخادم** — سمة العنصر تُتجاوَز بأدوات المطوّر.

#### ⑥ الاتجاه: `.prw-flow` كانت خارج ضبط RTL
مسار سير العمل حاوية `overflow-x:auto`، ولم تكن في قائمة `scrollersToStart` ⇒ على WebKit تفتح
**منزلقةً إلى نهايتها** فتبدو المراحل بادئةً من اليسار. أُضيفت، و**مرساة `scrollInlineStart` عُمّمت**
(`querySelector('th,td') || firstElementChild`) فتصحّ للحاويات غير الجدولية. و`renderPRPortal` صار
يُعيد ربط `a11yWire`/`mobileTableWrap` بعد رسمه اللاتزامنيّ (النداء العامّ في `navigate` يسبق عناصرها).
+ اسم المشروع في البطاقة الجانبية وفي شريط الملخّص (6 بطاقات، والمشروع **ثانياً** فينجو من اختصار
الشاشات الضيّقة الذي يُخفي `nth-child(n+3)`).

#### 🔒 تصليب من مدقّق Supabase (بطلب «افحص كل شيء»)
أغلب الـ21 ملاحظة مقصودة وموثّقة، و**ثلاث لم تكن**، ولا مستدعي لأيٍّ منها في المستودع كلّه:
`pr_run_sla()` **DEFINER وتكتب** (تصعيد+إشعارات) وكانت مكشوفة لـ`anon` — أي زائر بلا حساب يُطلق دورة
تصعيد (سابقة مُغلَقة في البوابة بالهجرة 017 وفاتت هنا) · `pr_transition` القديمة · ودوال المُشغِّلات
الستّ · و`proc_drop_all_policies` بلا `search_path`. **⚠️ سحب EXECUTE عن دالّة مُشغِّل قد يُعطّلها —
فحُقِّق بالتشغيل:** الحُرّاس ما زالت تمنع، والطريق المشروع يمرّ، ومُشغِّل التدقيق ما زال يكتب.

#### ✅ الإثبات الحيّ (على الإنتاج، داخل معاملة أُجهِضت — صفر أثر)
```
① رُفِع PR-… · الاعتماد مُسنَد إلى m.elsobky / م.محمد السبكي (لا للمالك)
② ✅ رُفِض تاريخ توريد ماضٍ
③ أُعيد · returned / owner=mostafa.kishk · البنود محفوظة
④ أُعيد الإرسال · إصدار 2
⑤ ✅ السلسلة: 1:1=returned/م.محمد السبكي · 2:1=pending · 2:2=pending  ← التاريخ بقي
   ✅ التعديل وصل (5 ← 10)
⑥ اعتماد الحاجة → procurement_review ثمّ إذن التسعير → pricing
```
ونظافة الإنتاج بعده: 3 طلبات · 4 اعتمادات · 116 أمراً · 5 مستخدمين نشطين · صفر صفّ اختبار.

**التحقّق: 836 تأكيد واجهة** (قسم ٣٧) · 9+6 عقود مساحة العمل · **18/18 صلاحيات الميدان تحت CSP** ·
**43/43 شاشات الموظّف المُنطَّق** · الحزمة البصرية بلقطة «نسخة المورّد».
**مُتحقَّق أنّ الحرّاس تعضّ:** 5 علل في الحزمة + 2 في المتصفّح ⇒ كلٌّ أفشل تأكيده الصحيح، والضابط يمرّ.
**⚠️ وحارسٌ أمسك خطأً في تعبيري أنا** (`\w+` لا تطابق `v_pr.id`) لا في الكود — وهو ما يُفترض به.

**⚠️ الهجرات مُطبَّقة حيّاً بالترتيب:** `system2_approval_identity_schema` →
`system2_pr_save_request_keeps_decision_history` → `system2_pr_decide_stamps_approver_name` →
`system2_definer_exposure_hardening`. الملفّان: `db/system2-approval-identity-and-history.sql` ·
`db/system2-definer-exposure-hardening.sql`. **الأولى تُطبَّق قبل النشر** (الواجهة تفترض العمودين،
وهي متسامحة فتعمل قبلها وبعدها) والثانية لا تسحب شيئاً يستعمله كودٌ منشور.

### 📧 رمز الاعتماد البريديّ كان يعتمد محتوىً لم يره صاحبه (2026-09-15، PR #108 مسودّة)
**الثغرة مُعاد إنتاجها بالتشغيل على PostgreSQL 16 قبل أي إصلاح** (لا بالقراءة): رمز بريد مسكوك على
إصدار 1 (كمية 5) ⇒ إعادة الطلب للتعديل ⇒ الطالب يرفعها إلى 500 ويُعيد الإرسال (إصدار 2) ⇒ ضغطة
«اعتمد» في البريد القديم تُرجِع `{"ok": true, "workflow_state": "procurement_review"}`.
- **ليست ثغرة صلاحية** (الشخص مخوَّل، والصلاحية تُعاد فحصها حيّاً عند الاستبدال) بل **ثغرة رضا**:
  قرارٌ يُسجَّل على محتوى لم يره صاحبه — تُبطل معنى دورة «الإرجاع للتعديل» كلّها.
- **⚠️ ولماذا لم تكفِ طبقة البريد:** `createToken` يُبطِل الرموز السابقة لنفس (الطلب/المرحلة/المعتمِد)
  عند سكّ رمز جديد — لكنّ ذلك **مشروط بإعادة الإشعار لنفس الأشخاص**. فإن تغيّر المعتمِد المسنَد
  (كما حدث بنقل `DEP-OPS`)، أو فشل بريد أحدهم، أو لم يستدعِ العميل الإشعار — بقي الرمز حيّاً.
- **⚠️ ولماذا لم يُشدَّد على «حامل الرمز = المعتمِد المسنَد»:** `notifyPending` يُرسل رمزاً **لكل
  معتمِد مؤهَّل** لا للمسنَد وحده (مقصود)، فتشديده كان سيُعطّل اعتماد أيّ مدير مشتريات غير المسنَد.
  **قِيس قبل التصميم فلم يُشدَّد.**
- **`db/system2-email-token-revision.sql`:** عمود `revision` على `proc_email_tokens` + **ردم** يختم
  كل رمز حيّ بالإصدار الجاري (فلا تُبطِل الهجرةُ رمزاً في صندوق بريد الآن) + حارس `stale_revision`.
  الدالّة مطابقة للقائمة حرفيّاً عدا الحارس (diff مُتحقَّق).
- **`_pr-shared.js`:** `createToken` يختم · `notifyPending` يقرأ الإصدار مرّة واحدة لكل الرموز.
  **تسامح مقصود:** قاعدة قبل الهجرة ترفض العمود بـ400 ⇒ يُعاد السكّ بلا ختم (البريد أهمّ من الختم
  في تلك النافذة — إسقاط التسامح يُنتج `all_sends_failed` مُتحقَّقاً). و**إصدار مجهول لا يُختَم بـ1
  مُلفَّقة**: ختمٌ خاطئ كان سيرفض *كلّ* اعتماد بريديّ لطلبٍ بلغ إصداره 2.
- **🐛 وعيبٌ في هجرة سابقة أُمسِك في الطريق:** ردم `system2-approval-identity-and-history.sql` كان
  يطوي **كل** صفوف الاعتماد على الإصدار الجاري ⇒ إعادة تشغيلها على طلبٍ مرّ بدورة إعادة تخرق المفتاح
  الفريد ⇒ **هجرة غير idempotent**. صار الردم يُصيب غير المختوم فقط ولا يمسّ ما يتصادم.
- **🐛 وفجوة تغطية:** تلك الهجرة شُحنت ودخلت الإنتاج و**لم تكن في `run.sh`** — فكانت الحزمة المحلّية
  تختبر **التعريفات القديمة** التي حلّت محلّها. أُضيفت مع `21_approval_identity.sql` (AI1–AI9).
  **⚠️ قاعدة: أي هجرة تُشحَن تُضاف إلى `db/system2-tests/run.sh` في الدفعة نفسها.**
- **الاختبار: 840 تأكيد واجهة · AI1–AI9 + TK1–TK8 بـRLS فعليّة · 6 تأكيدات سلوكية للطبقة الخادمية**
  (شبكة مُقلَّدة تحاكي 400 قبل الهجرة). **مُتحقَّق أنّها تعضّ: 11 علّة ⇒ كلٌّ أفشل تأكيده الصحيح.**
  CI: أُضيفت الهجرات و`pr-action.js` إلى `system2-tests`، و`_pr-shared.js` إلى `portal-tests`.
- **⚠️ ترتيب التسليم مُلزِم: الكود أوّلاً ثمّ الهجرة** (التسامح يجعله يعمل قبلها وبعدها؛ أمّا الهجرة
  على كودٍ قديم فتختم كل رمز بـ1 وترفض اعتمادات أيّ طلب بلغ إصداره 2).

#### ✅ مدموج ومنشور ومُطبَّق حيّاً (2026-09-16، PR #108، كوميت الدمج `f9699d5`)
**نُفِّذ الترتيب المُلزِم حرفيّاً: الدمج والنشر أوّلاً، ثمّ الهجرة.**
- الحزمتان خضراوان على `main` (`system2-tests` #109 · `portal-tests` #402) والنشر مُتحقَّق على
  **النطاقين ببصمة مطابقة للمستودع (2,764,184 بايت)**: علامات #108 حاضرة (`pr-del-label` ·
  `data-label=` ×9) والنقاط الخمس `{"ok":true}`.
- **الهجرة `system2_email_token_revision_stamp`** على `yofcaxvstjcrmbgciwym` بعد تأكيد وصول الكود.
  **تحقّق بنيويّ:** العمود موجود · حارس `stale_revision` في متن الدالّة · `authenticated` و`anon`
  **لا ينفّذان** و`service_role` ينفّذ · **صفر دالّة DEFINER بلا `search_path`**.
- **الردم فعل ما وُعِد به:** الرمز الحيّ الوحيد (`PR-DG2026-0003` · seq 1 · `Abdullah`) خُتِم
  بالإصدار 1 المطابق لطلبه ⇒ **ما يزال صالحاً في صندوق البريد**، و`live_mismatched=0`.
- **✅ إثبات سلوكيّ حيّ على الإنتاج** (داخل معاملة تنتهي بـ`RAISE EXCEPTION` فتُجهَض — صفر أثر): **3/3**
  `TK-LIVE-1` رمز إصدار 1 على طلب إصداره 2 ⇒ `stale_revision/409` · `TK-LIVE-2` الرفض **لا يستهلك
  الرمز ولا يغيّر الحالة** (فالمعتمِد يعيد المحاولة بعد المراجعة) · `TK-LIVE-3` رمز مختوم بالإصدار
  الجاري يعتمد الطلب ⇒ `procurement_review` = **صفر انحدار**.
- **نظافة الإنتاج بعد الإجهاض مُتحقَّقة:** صفر طلب/اعتماد/رمز اختباريّ · 3 طلبات · رمزان ·
  116 أمر شراء · 5 مستخدمين نشطين — كما كانت قبل الفحص بالضبط.

### 🤖 «رفع النموذج وقراءته آلياً» كان محروساً ببوّابة التحليلات (2026-09-16، بلاغ المالك)
**نصّه:** «رفع النموذج والقراءة الآلية المفروض يتاح للجميع، لأن بعض الموظفين عند الرفع يأتيهم تنبيه
عدم وجود صلاحية».
- **الجذر:** `prParseForm` كان `requirePermission('can_use_ai')` — وهي بوّابة **تحليلات** (رؤى لوحة
  التحكم · التقرير الإداريّ الذكيّ · الاستيراد الذكيّ) تمسح الكتالوج والموردين كلّهم. لكنّ هذا الزرّ
  لا يقرأ إلا **النموذج الذي يرفعه صاحب الطلب لطلبه هو**، وهو **داخل شاشة الإنشاء** التي بوّابتها
  `can_create_pr`. و`can_use_ai` **غائبة عن مفاتيح الميدان الستّة** في `staff-invite.js`،
  و`hasPermission` **صارمة للمُنطَّق** (الغياب = منع، تصليب 2026-09-12) ⇒ **كل موظّف ميدانيّ مدعوّ
  يُمنَع داخل شاشة فتحها بحقّ**. الإصلاح: البوّابة صارت `can_create_pr` = **بوّابة الشاشة نفسها**.
- **⚠️ ولا مفتاح Gemini يصل متصفّحه:** `loadAIConfig` تقرأ `proc_settings.ai_config`، وهي **محجوبة
  عن المُنطَّق** بقائمة المفاتيح البيضاء (إقفال 2026-09-13) ⇒ يسقط تلقائياً إلى الوسيط الخادميّ
  `/api/ai` (**مُتحقَّق حيّاً أنّه يعمل**). فالسرّ يبقى على الخادم، وفتح البوّابة لا يُسرّبه.
  **لذا لا يصحّ «الحلّ» بمنح `can_use_ai` للميدان** — كان سيفتح لهم التحليلات كلّها بلا داعٍ.
- **القياس نفى تكراراً أوسع:** بقيّة بوّابات `can_use_ai` في شاشة التحليلات (خارج `SCOPED_PAGES`)
  وأزرارها تحمل `data-requires-perm` فتُخفى — فلا «زرٌّ يُعرَض وفعلُه مرفوض». `prParseForm` كان الوحيد.
- **الاختبار: تأكيد بنيويّ (المجموع 850)** + **`scripts/e2e/pr-parse-gate.mjs`** — فحص متصفّح تحت
  **CSP الإنتاج** بموظّف **مُنطَّق فعلاً وبلا `can_use_ai`**: مُنتقي الملفات يُفتح · صفر توست رفض ·
  صفر خطأ · صفر انتهاك CSP. **ومُعاد إنتاج البلاغ حرفيّاً على البوّابة القديمة**
  (`صلاحية مرفوضة | قراءة النموذج بالذكاء غير متاح لك.`) ثمّ اختفى بالإصلاح.
- **⚠️ درس كعب تكرّر (الثامن):** أوّل نسخة من الفحص ضبطت `scope_sectors` بالشرطة السفلية، و
  `myScopeSectors` تقرأ **`scopeSectors` بالجَمَل** ⇒ `scoped:false` فكان الاختبار **فراغيّاً**
  يفحص موظّف مكتب لا ميدان. أمسكه شرط الخروج لا العين. **لا تثق بفحصٍ لم تتحقّق أنّ مُدخَله وصل.**

### ✉️ بريد الاعتماد كان أعمى — «بناءً على ماذا يقرّر؟» (2026-09-16، بلاغ المالك بلقطة)
**نصّه:** «التمبلت يجب أن تكون واضحة فيه تفاصيل الطلب مثل المشروع أو الجهة وموعد التوريد المطلوب
وهكذا — بناءً على ماذا يقرّر وهو لا يعلم أي شيء عن الطلب؟»
- **الجذر في `loadPR` لا في القالب (مقيس):** كانت تجلب **13 عموداً** بلا `project` ولا `needed_by`
  ولا `priority` ولا `justification`، **ولا دالّة تجلب البنود إطلاقاً**. فالبريد يعرض رقم الطلب
  وسطراً واحداً (العنوان · القسم · الطالب) — ويُطلب من المعتمِد أن يقرّر على ذلك. الطلب الحقيقيّ
  `PR-DG2026-0003` مثلاً: مشروعه **جامعة الملك سعود**، موعده **22 سبتمبر**، أولويته **عاجل**،
  و**17 بنداً** بكمياتها — **لا شيء منها كان يصل**.
- **`prDetailsBlock(pr, items, {showMoney})`:** شبكة حقائق (المشروع/الجهة · القسم · مقدّم الطلب ·
  تاريخ الطلب · **موعد التوريد + «باقٍ N» أو «تجاوز الموعد بـN»** · الأولوية · السند المرفق) +
  مبرّر الحاجة + **سبب الإرجاع السابق عند `revision>1`** (سياق يلزم المعتمِد) + جدول البنود
  (الصنف · الوحدة · الكمية، وعمود الرصيد إن وُجد) **بسقف 12 صفّاً ثمّ إشارة صريحة «و5 بنود أخرى»**
  — البتر الصامت يُقرأ اكتمالاً. وأُضيفت الكتلة لبريد المشتريات أيضاً (كان أعمى بالعلّة نفسها).
- **⚠️ المبالغ مقنّعة افتراضاً، والبوّابة لكل مستلِم:** البريد **لا تحرسه RLS**، فطباعة المبلغ فيه
  بلا بوّابة تتجاوز نظام الصلاحيات كلّه وتخرق «الرؤية المالية تُمنَح منّي». `seesFinancials()` تقرأ
  `pr_effective_permissions(username)` — **نفس مصدر الواجهة والقاعدة فلا تتفارق بوّابتان** —
  و**تفشل مغلقةً** عند أي تعذّر. مُتحقَّق حيّاً أنّ الفرق حقيقيّ (`mostafa.kishk` = false).
  وإجماليّ صفر **لا يُطبَع** «0 ر.س» (درس المحضر: رقمٌ مُلفَّق أسوأ من غيابه).
- **🐛 وحارسٌ قائم أمسك انحداراً أحدثتُه:** تأكيد «إصدار الحمولة بلا استعلام إضافي» كان يشترط
  **صفر** استعلام على `/proc_purchase_requests` — وإضافتي لحقول القرار أوجبت واحداً. **الحارس كان
  يُرمّز «صفر» بديلاً عن الثابت الحقيقيّ «لا استعلام لكل معتمِد».** فدُمِج الاستعلامان في واحد
  (`loadPR` تخدم الإصدار والحقول معاً) و**حُذفت `prRevision`** (مسار قراءة ثانٍ مهجور يدعو للتفارق)،
  وشُدِّد التأكيد على الثابت الفعليّ: **قراءة طلب واحدة وقراءة بنود واحدة مهما كثر المعتمِدون**
  (حالة ثلاثة معتمِدين مضافة). ⚠️ **حارسٌ يُرمّز بديلاً عن الثابت ينكسر حين يتغيّر التصميم بحقّ —
  صحّح الحارس للثابت لا الكودَ للحارس.**
- **الاختبار: 17 تأكيداً سلوكيّاً** (تُشغّل بناء البريد وتقرأ مُخرَجه) + 7 لختم الرمز = 24.
  **مُتحقَّق أنّها تعضّ: 3 علل ⇒ كلٌّ أفشل تأكيده** (إسقاط بوّابة المبالغ · عودة الأعمدة القديمة ·
  البتر الصامت). + **معاينة متصفّح** على 390px و700px: صفر انزلاق، والبنود والحقائق ظاهرة، وصفر مبلغ.
- **⚠️ درس قياس:** `getClientRects().length` **ليس عدّاد أسطر** لنصّ ثنائيّ الاتجاه (عربي+أرقام)
  — يعيد جزءاً لكل اتجاه فيبدو السطر الواحد ثلاثة. القياس الصحيح: **تمايز `top`**.

### 🔒 إقفال كتابة التخزين العامّة — المرحلة 2 (2026-09-16، أمر المالك «ادمج الهجرات»)
**⚠️ تصحيح قياس كنتُ أُكرّره في هذا الملف وفي ردودي:** «مخزن `supplier-docs` ما يزال مقروءاً
للعموم» كان **خبراً متقادماً**. القياس قبل أي كتابة أظهر أنّ **المرحلة 1 مُطبَّقة فعلاً**
(`supplier_docs_read_staff [SELECT→authenticated]`)، وأنّ قراءة anon **مُغلقة**: `list` يعيد `[]`
(كان 23 مجلّداً)، وتنزيل/توقيع وثيقة **موجودة فعلاً** يعيد `NoSuchKey`. **الدرس يتكرّر: لا تُعِد
بنداً مفتوحاً من لقطة سابقة بلا إعادة قياسه** — كنتُ أُنذر المالك بثغرة مُغلقة.

- **المتبقّي الحقيقيّ كان الكتابة وحدها:** `supplier_docs_insert_public_TEMP [INSERT→anon]` — أي
  أنّ أي شخص يرفع أي ملف مباشرةً إلى المخزن متجاوزاً الحارس الطبقيّ. الشرط المسبق في الملفّ
  (`/api/reg-doc` = `{"ok":true}`) **متحقَّق** (`store:"r2"` على النطاقين، والرفع على R2 لا يحتاج
  مفتاح الخدمة أصلاً). **الهجرة `system1_storage_hardening_phase2_close_anon_write` مُطبَّقة حيّاً.**
- **التحقّق السلوكيّ (لا قراءة سياسة):** رفع anon لـPDF سليم ⇒ **403** «new row violates row-level
  security policy» · السياسات الباقية = `read_staff` وحدها · **صفر كائن فحص** في المخزن بعده
  و**657 وثيقة قائمة سليمة** · و`/api/reg-doc` ما يزال `{"ok":true,"store":"r2"}` على النطاقين.
- **وأُزيل «السقوط المؤقّت» في `register.html`** كما تنصّ الهجرة نفسها: كان `uploadDocViaServer`
  يسقط إلى `SB.storage.from(BUCKET).upload` بمفتاح anon عند 503 — **وهو يتجاوز `_file-guard`**
  (تدقيق SEC-06). بعد حذف السياسة صار ميّتاً، **ولو بقي لرمى خطأ RLS خاماً في وجه المورّد وسط
  تسجيله**. الآن كل فشل خطأٌ عربيّ مفهوم = فشل مغلق صحيح. وحُذف ثابت `BUCKET` كي لا يُغري بإحياء
  المسار. **مُتحقَّق: صفر `storage.from(...).upload` في الصفحة.**
- **⚠️ قراءات الموظّف لم تُمَسّ:** الأربع في `index.html` (`createSignedUrl`/`list`) بجلسة
  `authenticated` تغطّيها `read_staff`. و`rfq.html` يكتب على حاوية أخرى (`QFILE_BUCKET`) — خارج النطاق.
- **⚠️ `db/system2-field-permissions.sql` لا تُشغَّل كما هي — والتحقّق أنقذ انحداراً أمنيّاً:**
  القياس أظهر أنّ **كل ما يهمّ منها مُطبَّق حيّاً** (`proc_has_perm` موجودة بـ`search_path` ومسحوبة
  عن anon · حُرّاس `pr_set_doc`/`pr_post_message`/`po_add_comment` · سياسات `proc_pr_templates`).
  والجزء الوحيد غير المطبَّق هو **ما يجب ألّا يُطبَّق**: سياسات `pr_insert`/`pr_update`/`pr_delete`
  على `proc_purchase_requests` — فقد **حلّ محلّها موديل PR #103** بـ`*_deny (false)` وكتابةٍ عبر
  `pr_save_request` وحدها. تشغيل الملف كما هو **يُسقِط حُرّاس المنع ويُعيد فتح الكتابة المباشرة
  من العميل**. **قاعدة: هجرةٌ حلّ محلّها موديلٌ لاحق لا تُعاد — قِس الحالة الحيّة قبل التطبيق.**
- الاختبار: **849 تأكيداً** (+3 حرّاس: لا كتابة مباشرة · لا سقوط عند 503 · فشل الاتصال خطأ ظاهر).
  **مُتحقَّق أنّها تعضّ:** إعادة السقوط عمداً ⇒ إخفاقان، والضابط يمرّ في الأداة نفسها.
- **متبقٍّ موصى به (قرار المالك):** تدوير مفتاح anon للمشروع القديم — كان يمنح وصولاً واسعاً لفترة.

### 📱 واجهة الجوال + تدقيق البقز الخفيّة (2026-09-16، بلاغ المالك)
**نصّه:** «النظام لم يتطوّر إلى الآن في واجهة الجوال · افحص النظام والموديل وتأكّد ألّا يوجد
بقز خفيّة أو إيرورز تؤثّر على العمل الحالي والمستقبلي».

#### واجهة الجوال — خمسة عيوب مقيسة (393×852 و360×740 تحت CSP الإنتاج)
⚠️ **قواعد `@media` كانت موجودة في الملف** — ووجود القاعدة ليس دليل أنّها تُطبَّق ولا أنّ
النتيجة صالحة. القياس هو الحكم:
1. **[الأكبر أثراً] 118 من 119 حقل إدخال في النظام كلّه دون 16px** ⇒ سفاري يُقرّب الصفحة عند
   التركيز على **كل** حقل، فتقفز ويجب التصغير يدويّاً. الدرس كان مُطبَّقاً على **حقل البحث
   وحده**. القاعدة الآن شاملة داخل كتلة الجوال. بعد الإصلاح: الجوال 112/112 حقل نصّيّ ≥16px
   (الباقي مربّعات اختيار — لا إدخال نصّيّ فيها) · **سطح المكتب 1440/1024: 115 حقلاً بأحجامها
   الأصلية بلا أي تغيير**.
2. **الشريط الملاحيّ لم يلتصق قطّ:** `overflow:hidden` على سلف **يُبطِل `position:sticky`**
   لكل ما بداخله. مقيس عند scrollY=700: **−541px**، وبرفع القناع **+62px** — إثبات تجريبيّ.
   الأثر: مستخدم الهاتف يفقد التنقّل بمجرّد التمرير داخل الطلب.
3. **آخر 53px من كل شاشة محجوبة دائماً:** `margin-bottom:-84px` بلا حشو معوّض، فالمحتوى ينتهي
   عند حافة الشاشة تحت الشريط الثابت. مقيس: زرّ «فتح داخل النظام» لا يُرى **حتى بعد التمرير للنهاية**.
4. **جدول البنود 9 أعمدة/636px داخل نافذة 335px** ⇒ صار **بطاقة لكل بند** بتسمية من `data-label`.
   ⚠️ لا يخالف قاعدة «لا تُحوِّل الجدول إلى block» — تلك تخصّ إبقاء تخطيط جدوليّ مع تمرير.
5. **الطابور أظهر بطاقة واحدة من ستّ** (شريط أفقيّ 245px/بطاقة) · وأزرار الرأس 67px بنصّ ملتفّ.
- **وعيبان كشفتهما اللقطة وحدها:** نصّ زرّ الحذف عبر `::after` **لم يُرسَم** (زاحم الأيقونة داخل
  `overflow:hidden`) ⇒ نصّ حقيقيّ يُقرأ بقارئ الشاشة · و«البند رقم/1» انكسرا سطرين.
- **وتصحيح انطباع:** حقول التاريخ **سليمة** (`dir=ltr`/`lang=en-GB` مضبوطان) — و`mm/dd/yyyy`
  في اللقطة هو ما يرسمه كروميوم بلغة بيئة الاختبار لا عيب في الكود.
- `scripts/e2e/purchase-workspace-mobile.mjs` (جديد): **24/24** على مقاسين. **846 تأكيد واجهة**
  (+6 حرّاس). **مُتحقَّق أنّها تعضّ: 7 علل ⇒ كلٌّ أفشل تأكيده.**
- ⚠️ **تأكيدان قائمان صُحِّحا لأنهما كانا يُرمّزان تصميماً مُلغى أو يقيسان السكون لا الحجب
  الفعليّ** (عتبة طول الطابور · والحجب يُقاس بعد التمرير للنهاية لا عند السكون).

#### تدقيق البقز الخفيّة — النتيجة: **لا عطل قائم**، وهشاشتان كامنتان
**سلامة بيانات الإنتاج نظيفة تماماً** (مقيسة): صفر بند/اعتماد يتيم · صفر رابط لأمر غير موجود ·
صفر طلب `in_review` بلا مرحلة معلّقة · صفر مرحلة معلّقة لطلب غير `in_review` · صفر إصدار اعتماد
متعارض · كل مرحلة معلّقة لها معتمِد **موجود ونشط** · كل قسم نشط له مدير صالح. ولا عنصر واجهة ميّت
(صفر `onclick` لدالّة غير معرَّفة) ولا استدعاء RPC بلا فحص `error`.
- **الهشاشة ①: حلّ الهويّة غير حتميّ.** الإنتاج يحمل صفَّين يختلفان بحالة الأحرف فقط
  (`Abdullah` أدمن نشط · `abdullah` مستخدم **موقوف** بملفّ `requester`)، و**13 دالّة** تُحلّ الهويّة
  بـ`LIMIT 1` **بلا `ORDER BY`** (سبعٌ منها بلا فلتر نشاط). **مُتحقَّق بالتشغيل: الهويّة تُحلّ صحيحةً
  اليوم** (`pr_username()`=`Abdullah`, `is_admin=true`) **وحتى بمحاكاة تفعيل الصفّ المتصادم بقيت خمس
  محاولات كلّها صحيحة** — فالاستقرار مصادفة خطّة تنفيذ لا ضمان. ⚠️ **ليس عطلاً قائماً**؛ الخطر أنّ
  صفّاً ثانياً نشطاً بحالة مختلفة (سهل الإنشاء) يجعل الصلاحيات عشوائيّة **بصمت وتقطّع**.
  **التوصية (قرار المالك — فيها تغيير بيانات):** حسم الصفّ الموقوف المكرّر ثمّ منع التكرار
  بفهرس فريد على `lower(username)`، أو حارس يرفض التصادم الجديد ويُبقي القديم.
- **الهشاشة ②: قاعدة موثّقة عندنا مفرطة في العموم — وتطبيقها حرفيّاً عيب جسيم.** القاعدة كانت
  «أي مطابقة على `proc_users.username` تكون `ilike` لا `eq`». لكن **الكتابة** بـ`ilike` تطال
  **الصفّين معاً**: `PATCH proc_users?username=ilike.abdullah` يعدّل الأدمن والموقوف في ضربة واحدة.
  **الكود اليوم صحيح** (قراءة الهويّة `ilike` · الكتابة `eq.`) — والخطر أن «يُصلَحه» أحدهم اتّباعاً
  للقاعدة. صار الفارق محروساً بتأكيدين. **القاعدة المُصحَّحة في البند 14 أدناه.**

### 📎 مرفقات الطلب: لا تُفتَح · لا تظهر لغير المالك · واحد فقط (2026-09-16، بلاغ المالك)
**نصّه:** «المرفقات لا يمكن فتحها ولا تظهر لجميع المسجّلين في السستم أنا فقط · رُفعت مرفقات مع بعض
الطلبات وبعد الرفع ظهر أنه لا يوجد مرفقات · ولا يمكن إلا رفع مرفق واحد فقط» + «حتى المناقشات المفروض
أن يصبح هناك إمكانية إضافة مرفق فيها». **أربعة أعراض، وجذران مستقلّان، وفجوتان في القدرة.**

#### ① [الأخطر] «لا تظهر إلا لي» و«بعد الرفع لا يوجد مرفقات» = **علّة واحدة**
`db/system2-scoped-table-lockdown.sql` (13-09) أقفلت سبعة جداول بسياسة `no_scoped_access`
RESTRICTIVE، على حقيقة **مقيسة يومها**: «`proc_pr_attachments` و`proc_pr_audit` بلا مرجع واحد في
`index.html` وبصفر صفّ». ثمّ جعلهما موديل مساحة العمل (14-09) **قلبَ الموديل**. وRESTRICTIVE تُجمَع
بـ**AND** فألغت حارسهما `pr_can_view_request` ⇒ **كل موظّف مُنطَّق يرى صفر مرفق — حتى مرفق طلبه الذي
رفعه هو**؛ والرفع ينجح (`pr_register_attachment` هي DEFINER فتتجاوز RLS) فيقع بالضبط: «رفعتُ ثمّ لا
يوجد مرفقات»، والمالك غير المُنطَّق يراها وحده. مقيس حيّاً: `mostafa.kishk` و`m.elsobky` كلاهما
`scope_sectors=["الصيانة والتشغيل"]` — وهما كلّ من يرفع طلبات اليوم.
**`db/system2-pr-attachments-unlock.sql`:** حذف الإقفال عن **الجدولين وحدهما** (الخمسة الباقية كما هي)
+ حارس أثر يرفض بقاء أي RESTRICTIVE + تأكيد أنّ العميل ما زال بـ`SELECT` وحدها. وأُخرِج الجدولان من
قائمة المُنصِّب كي لا تُعيد إعادةُ تشغيله العطلَ.

#### ② HTTP 400 عند الفتح — `docvBlobUrl` يتجاهل الـblob المحقون
`docvShow` ينادي `docvBlobUrl` **قبل** تفرّع النوع (الرابط يلزم الصورة ولا يلزم الـPDF)، و`docvBlobUrl`
كان يذهب رأساً إلى `regDocFetch` = نقطة **وثائق الموردين** التي يرفض حارسُها `safeKey` أي مفتاح
`docs/pr/…` بـ**400**. والحقنُ في `DOCV.blobs` الذي اعتمد عليه `prViewDoc` لم يكن يُقرَأ إطلاقاً.
**العلاج جذريّان معاً:** `regDocFetch` يوجّه بادئة `docs/pr/` إلى `/api/pr-doc` (الفضاءان منفصلان
**بالبناء**: `pr_register_attachment` تفرض `^docs/pr/…` و`safeKey` لا تقبل إلا `DG-…`)، و`docvSourceBlob`
مصدرٌ واحد يخدم `docvBlobUrl` و`docvBytes` معاً. **⚠️ الحقن وحده لا يكفي:** ذاكرة العارض مسقوفة بثلاثة،
فطلبٌ بأربعة مرفقات يُخلي أقدمها ثمّ يعود ليطلبه من المخزن الخطأ.

#### ③ مرفق واحد — والاختيار الثاني يمحو الأوّل بصمت
الحقل بلا `multiple` و`__prDraftDoc` ملفّ واحد، و**لا مسار في النظام لإرفاق ملفّ بعد إنشاء الطلب
إطلاقاً** (فالمشتريات لا تُرفق عرض سعر، ومن نسي مواصفةً لا سبيل له إلا طلب جديد). الآن: `__prDraftDocs`
مصفوفة **تُضيف ولا تستبدل** (بسقف 10 وكشف تكرار بالاسم+الحجم ورفض مُعلَن للصيغة/الحجم) · لوحة مرفقات
في شاشة المتابعة بحقل إضافة فوريّ · `pr_remove_attachment` **حذف ناعم** (`deleted_at`) بصلاحية
الرافع/المشتريات/الأدمن ولا يمسّ طلباً منتهياً · و`kind` صار من قائمة الخادم (كان يُرسَل `supporting`
وهي ليست فيها فتُقصَر صامتةً) ويُعرَض بالعربية.

#### ④ فشل المرفق كان يُبلَّغ «تعذّر الحفظ» — وهو سبب الطلبات المكرّرة
الرفع كان داخل نفس الـ`try`، فأيّ إخفاق فيه يقفز للـ`catch` فيقول «تعذّر الحفظ» و**الطلب محفوظ ومُرسَل
فعلاً** والإشعار لا يُرسَل ⇒ يُنشئ المستخدم بديلاً (أنتج على الإنتاج `PR-0004` و`PR-0005` خلال ثماني
ثوانٍ). الآن الرفع خارج مسار فشل الحفظ، ورسالة تقول ما حدث بالضبط وأين يُعيد الإرفاق.

#### ⑤ مرفق مع رسالة المناقشة (طلب المالك)
قناة الاستفهام كانت **نصّاً فقط** — سؤالٌ عن مواصفة أو صورة عطل يُجاب خارج النظام فتضيع المعلومة، وهي
العلّة التي بُنيت القناة لعلاجها. عمود `proc_pr_messages.attachment_ids` + `pr_post_message` اكتسب
معاملاً رابعاً (**والتوقيع الثلاثيّ أُسقِط** كي لا يصير نداءٌ بثلاثة معاملات مرشَّحاً لاثنين؛ والإسقاط
متوافق خلفيّاً لأن العميل ينادي بمعاملات مسمّاة). **الترتيب: يُرفع المرفق أوّلاً فيصير له معرّف ثمّ
تُنشأ الرسالة مشيرةً إليه** — فلا رسالة تَعِد بمرفق لم يصل؛ ومعرّف لا يخصّ الطلب **يُرفَض لا يُسقَط
صامتاً**. والوسم «مرفق مناقشة» يُشتقّ من إشارة الرسالة إليه لا من تخمين نوعه. وأُعيد سطر التدقيق الذي
سقط سهواً حين أعادت `system2-field-permissions.sql` تعريف الدالّة.

#### ⚠️ الدرس المنهجيّ (التاسع) — تأكيدٌ فراغيّ لأن البيئة المحلّية ليست الإنتاج
بعد إخراج الجدولين من قائمة المُنصِّب، **لم تعُد السياسة تُنشأ محلّياً أصلاً**، فصارت PA1 تمرّ بلا أن
تختبر شيئاً: حذفتُ جدولاً من قائمة الهجرة عمداً و**مرّت الحزمة كلّها**. العلاج أنّ
`23_pr_attachments.sql` **يُعيد بناء حالة الإنتاج** (السياستان)، يُثبت أنّها تحجب فعلاً (PA0 = العَرَض
حرفيّاً)، ثمّ يُشغّل الهجرة بـ`\i` ويتحقّق أنّها هي التي رفعت الحجب. **⚠️ ولا تكتفِ بأنّ الحقن «جرى» —
تحقّق أنّه وصل**: أوّل جولة بيت-بروف لم تُطابق أنماطها فبدت العلل «لا تعضّ» وهي لم تُحقَن أصلاً.
**وحارسان قائمان كانا يُرمّزان العطل نفسه فصُحِّحا للثابت الحقيقيّ:** WS32 كان يشترط **بقاء** الإقفال
على الجدولين، وحارسُ العارض كان يفحص «حقن blob» بدل «المفتاح يُجلَب من نقطته».

**التحقّق: 877 تأكيد واجهة** (+26) · **PA0–PA12** بـRLS فعليّة بدور `authenticated` ·
**13 فحص متصفّح** تحت CSP الإنتاج (`scripts/e2e/pr-attachments.mjs` — يفتح PDF حتى رسم pdf.js فعليّاً
وصورةً من `blob:`، ويؤكّد **صفر تسرّب** لنقطة وثائق الموردين). **مُتحقَّق أنّ الحرّاس تعضّ: 9 علل
أُعيدت عمداً (5 SQL + 4 واجهة) ⇒ كلٌّ أفشل تأكيده الصحيح، وإعادةُ العلّتين معاً تُعيد رسالة البلاغ
حرفيّاً في المتصفّح**، والضابط يمرّ في الأداة نفسها. + إصلاح مرافق: حقل الملفّات الجديد كان يُفشِل
فحص «16px» على الجوال — والصواب أنّ **حقول الملفّات مستثناة** (لا إدخال نصّ فلا تقريب iOS) كما هي
مستثناة في CSS، فصُحِّح الفحص لا القاعدة.
**⚠️ الترتيب: `db/system2-pr-attachments-unlock.sql` تُطبَّق قبل النشر** (الواجهة تستدعي
`pr_remove_attachment` وتمرّر `p_attachment_ids`)، وهي إضافيّة فتعمل الواجهة القديمة قبلها وبعدها.

### 🧭 وضوح مساحة الطلبات: مَن يملك المرحلة · مَن قرّر · بأي لغة · بأي ترتيب (2026-09-16)
**بلاغ المالك بثلاث لقطات:** «تشتيت في ترتيب الطلبات وطريقة عرضهم» · «المراحل التي بعد اعتمادي تخصّ
مصطفى خليل ومحمود العامودي — لماذا مسجَّلة لدى عبدالله؟» · «سجلّ القرارات جميعه عبدالله والقرارات
بالإنجليزي وغير متّزنة» · «عند إنشاء طلب جديد يجب أن تظهر قائمة منسدلة بأسماء المشاريع المسجّلة».
**أربع نقاط، أربعة جذور مستقلّة — كلّها مقيسة على قاعدة الإنتاج قبل أي كود.**

#### ① [الأخطر] «إذن التسعير» كان يُسجَّل «بدأ العمل» باسم المعتمِد
`pr_decide` (ونظيرتها `pr_transition_email`) عند اعتماد بوّابة التسعير كانت تكتب
`proc_status='in_progress'` و`proc_started_by=v_me` — أي أنّ **من أذِن** يُسجَّل أنّه **بدأ العمل**.
ثلاثة آثار مقيسة: (أ) مرحلة «التسعير والمقارنة» تُنسَب إليه لا لفريق المشتريات — على الإنتاج
`proc_started_by='Abdullah'` على **كل** الطلبات الستّة المعتمَدة، و`proc_started_at =
pricing_authorized_at` إلى الميكروثانية؛ (ب) انتقال `received → in_progress` **يُستهلَك قبل أن يراه
أحد**، فزرّ «بدأت العمل عليه» لدى المشتريات لا يعمل أبداً و`prWorkspaceNeedsAction` لا تُعلِّم الطلب
لهم ⇒ **لا أحد يُسنَد إليه العمل**؛ (ج) صفّ تدقيق ثالث باسمه في الثانية نفسها فيبدو السجلّ «كلّه عبدالله».
**الإصلاح (`db/system2-request-workspace-clarity.sql` + المُنصِّب نفسه):** الاعتماد يُسلّم الطلب بحالة
`received` ويحفظ `pricing_authorized_by` وحده؛ والختم يقع في `pr_proc_stage` حين يبدأ العمل فعلاً.
**والمداواة قاطعة:** نفس الشخص **ونفس الطابع الزمنيّ** ⇒ ختمٌ آليّ يُعاد إلى `received` بأثر تدقيق
مُعلَن (`proc_reset_to_received`)؛ وما تجاوز المرحلة أو بدأه شخص آخر **أو بدأه المُجيز نفسه لاحقاً**
لا يُمَسّ. (خمسة صفوف حُوِّلت على الإنتاج.)

#### ② سجلّ القرارات: إنجليزيّ · بأسماء دخول · منفوخ · مائل
`prAuditLabel` كانت خريطةً **بمفاتيح لا يُصدرها النظام إطلاقاً** (`proc_started` · `po_linked` ·
`message`…)، والمفاتيح الحقيقية غيرها — فكل حدث يسقط إلى `replaceAll('_',' ')` فيظهر «stage approved».
و`proc_pr_audit` **بلا عمود اسم**، و`actor` اسم دخول؛ ولا يصحّ الترجمة في المتصفّح لأن `users_select`
تحجب عن الموظّف المُنطَّق صفوف غيره (القاعدة 18). والكُتّاب كُثر ⇒ **مُشغِّل واحد** `BEFORE INSERT`
يملأ `actor_name` مهما كان الكاتب، حاضراً ومستقبلاً، + ردم للصفوف القائمة. وفعلٌ واحد يكتب ثلاثة
صفوف في الثانية نفسها ⇒ `prAuditGroups` تدمج صفوف الفاعل نفسه في اللحظة نفسها. والاتّزان: الوقت رقم
لاتينيّ داخل نصّ عربيّ ⇒ عمود مستقلّ بـ`dir="ltr"` و`tabular-nums`.
**⚠️ والفاعل المجهول يسقط لاسم دخوله لا لفراغ**، والحدث المجهول يُعرَض «تحديث على الطلب» لا خاماً.

#### ③ الطابور بلا فرز إطلاقاً
`prWorkspaceQueueData` كانت تُرجع ترتيب الجلب كما هو. الآن `prQueueSort`: **يحتاج إجراءك ← متعثّر ←
قيد الاعتماد لدى غيرك ← جارٍ لدى المشتريات ← صدر الأمر ← منتهٍ**، وداخل كل مجموعة الأحدث أوّلاً
**برقم الطلب عدديّاً** (لا نصّيّاً — درس تسلسل أوامر الشراء)، + عنوان مجموعة لاصق بعدّاده.

#### ④ المشروع نصّ حرّ رغم وجود سجلّ معتمد كامل
حقل الطلب كان `<input>` حرّاً بينما وحدة أوامر الشراء لها سجلّ مشاريع بمرادفات وكشف تشابه — فتسرّبت
إلى الإنتاج «امانة الاحساء» و«الادارة تجربة». الآن `<select>` من السجلّ + «➕ مشروع جديد» بتلميح حيّ
وحوار «هل تقصد…؟»، **بإعادة استعمال المحرّك نفسه** (`prjNames`/`prjResolve`/`prjAdd`/`prjAddAlias`)
لا نسخة ثانية من المنطق؛ والقيمة غير المعتمدة تبقى خياراً مؤقّتاً فلا يفقدها التعديل.
**⚠️ المُنتقي يُملأ قبل التعبئة المسبقة** وإلّا لم تلتصق القيمة (`select.value` بلا خيار = لا شيء).

#### ⚠️ دروس التحقّق في هذه الدفعة
- **تأكيدٌ فراغيّ للمرّة العاشرة:** بعد تصحيح المُنصِّب لم تعُد البيئة المحلّية تُنتج الحالة المعطوبة،
  فكانت تأكيدات المداواة تمرّ بلا أن تختبر شيئاً. `24_workspace_clarity.sql` **يُعيد بناء حالة الإنتاج
  يدويّاً** ثمّ يُشغّل الهجرة بـ`\i` ويتحقّق أنّها هي التي داوتها.
- **وشرطٌ بدا زائداً وليس كذلك:** أوّل بيت-بروف لحذف شرط الطابع الزمنيّ **لم يعضّ** لأن بيانات
  الاختبار لم تحوِ الحالة التي يحميها — **مَن أذِن ثمّ بدأ العمل بنفسه لاحقاً**. أُضيف الصفّ
  `WC-SAME` فصار الشرط مُبرهَناً لا مُفترَضاً.
- **والفرز العدديّ لا يتميّز على ترقيم مبطَّن:** `localeCompare` يوافق العدديّ على `0009/0012`،
  فاستُعمِل `PR-9` و`PR-10` — وهناك يخطئ النصّيّ.
- **وحارسٌ بدا أنّه لا يعضّ وهو يعضّ:** أوّل حقن لتسمية إنجليزية لم يُفشِل المتصفّح لأن كل الأحداث
  المبذورة معروفة؛ أُضيف حدث **غير معروف** للبذرة (وهو المسار الذي سرّب الإنجليزية أصلاً).

**التحقّق: 903 تأكيد واجهة** (+26) · **WC1–WC10** بـRLS فعليّة · **14 فحص متصفّح** تحت CSP الإنتاج
(`scripts/e2e/pr-workspace-clarity.mjs` — يقيس **صناديق العناصر المرسومة**: ترتيب الطابور وعناوين
مجموعاته، وسطر السجلّ في سطر واحد والوقت في عموده، والمُنتقي بمشاريعه). **مُتحقَّق أنّ الحرّاس تعضّ:
11 علّة أُعيدت عمداً (5 SQL + 6 واجهة) ⇒ كلٌّ أفشل تأكيده الصحيح**، والضابط يمرّ في الأداة نفسها.
**⚠️ الترتيب: `db/system2-request-workspace-clarity.sql` تُطبَّق قبل النشر** (الواجهة تقرأ
`actor_name` وتتوقّع `received` بعد الإذن)، وهي إضافيّة فتعمل الواجهة القديمة قبلها وبعدها.

#### ✅ مدموج ومنشور (2026-09-16، PR #112، كوميت الدمج `a77119c`)
**نُفِّذ الترتيب المُلزِم:** الهجرة `system2_workspace_clarity_stage_ownership_and_audit_names` طُبِّقت
حيّاً **قبل** الدمج (وداوت خمسة طلبات كما تنبّأ القياس)، ثمّ الدمج والنشر.
- **الحزم الثلاث خضراء على `main`** (`system2-tests` #118 · `portal-tests` #411 · `pages` #330).
- **مُتحقَّق حيّاً على النطاقين ببصمة مطابقة للمستودع (2,793,066 بايت):** علامات #112 حاضرة
  (`prQueueSort` · `prAuditGroups` · `PR_AUDIT_LABEL` · `prPersonName` · `pr-work-eventrow` ·
  `prFillProjectSelect` · `pr-project-hint`) والنقاط الخمس `{"ok":true}`.
- **⚠️ درس نشر:** أوّل استفتاء بعد الدمج أعاد **بصمة النسخة السابقة** (2,780,289) لأن نشر Cloudflare
  لم يكتمل بعد — **لا تستنتج فشل نشر من استفتاء واحد؛ استفتِ حتى تطابق البصمة أو تنقضي مهلة معقولة.**

### ✉️ «مين محمود السيد اللي بتروح له إيميلات وهو مش معنا؟» — العنوان كان **مُخمَّناً** (2026-09-16)
**بلاغ المالك بلقطة:** سطر `To` في بريد «طلب معتمد جاهز للمشتريات» فيه **Mahmoud Elsayed** — وهو
ليس محمود العامودي ولا يعمل معنا.
- **الجذر مقيس:** عمود `email` **فارغ** لثلاثة حسابات (`Abdullah`/`Mostafa`/`Mahmoud`)، فالعنوان
  **يُشتقّ** من ثابت مكتوب في الكود منذ مايو (`AUTH_EMAIL_MAP`): `mahmoud → mahmoud@aldeyabi.com`.
  وذلك الصندوق مسجَّل في دليل الشركة باسم **شخص آخر**. وسجلّ Resend يؤكّد التسليم: كل بريد مشتريات
  اليوم (0003 · 0004 · 0007 · 0008 · 0009 · 0010) ذهب إليه — **ومعه المبالغ** (المستلِمون منتقَون
  بصلاحية التسعير فـ`showMoney:true`).
- **تصحيح المالك الحاسم:** «لا يوجد بريد مخصّص لموظّف بعينه — محمود يستخدم صندوق المشتريات العامّ
  `supply@aldeyabi.com` نفسه الذي يستخدمه مصطفى».

#### ⚠️ ولماذا لم يُحَلّ بملء `email` (قياسٌ أنقذ انحداراً أخطر من البلاغ)
`proc_me()` تُطابق **بالبريد أوّلاً**: `WHERE active AND lower(email)=jwt_email LIMIT 1`. فكتابة
`supply@` في صفّ `Mahmoud` كانت تجعل **دخول supply@ يُحلّ إلى Mahmoud** (الصفّ الوحيد الحامل له)
بدل `Mostafa` — أي **أدمن ينقلب موظّفاً عاديّاً في كل جلسة**. ولهذا:
**`email` هويّةُ دخول · `notify_email` عنوانُ مراسلة — والفصل مقصود ومحروس.**
- **⚠️ ولا تُمَسّ `AUTH_EMAIL_MAP`** في `notify.js`/`admin-users.js`: هناك هويّة لا مراسلة
  (`verifyStaff` تطابق بريد الجلسة بالمُشتقّ · واشتقاق حساب Auth وكلمة المرور) — تغييرها كان
  **يمنع محمود من `/api/notify`** ويوجّه عمليات حسابه لصندوق آخر. محروس بتأكيد.

#### التنفيذ
- **`db/system2-notify-email.sql`:** عمود `proc_users.notify_email` (+قيد شكل `@aldeyabi.com`)
  + **إضافته لقائمة `proc_users_guard` في الهجرة نفسها** (القاعدة 16 — وتوجيه البريد **صلاحية**:
  من يُبدّله لمعتمِد يستقبل **رموز الاعتماد بضغطة** في صندوقه) + بذرة `Mahmoud → supply@`.
  ⚠️ البذرة ترفع هويّة الخدمة **محصورةً بالمعاملة** وإلّا سقطت على الحارس (سابقة `proc_config_guard`).
- **`_pr-shared.js`:** `rowNotifyEmail` **مصدر واحد** بترتيب `notify_email ← email ← الاشتقاق`،
  و`procurementRecipients` وحّدت موضعَي الإرسال، و`userEmail` (مسار رموز الاعتماد) تقرأه.
  **تسامح مقصود:** قاعدة قبل الهجرة ترفض العمود بـ400 ⇒ سقوط للأعمدة القديمة فلا ينقطع البريد.
- **الواجهة + `admin-users`:** حقل «بريد المراسلة» في نافذة المستخدم (يمرّ بـ`setProfile` لا بكتابة
  مباشرة) فيملكه المالك بنفسه — **العلاج الجذريّ أن يُخزَّن العنوان لا أن يُخمَّن.**

#### ✅ إثبات حيّ (على الإنتاج، داخل معاملة أُجهِضت — صفر أثر): **6/6**
```
① دخول supply@ ⇒ Mostafa ✅ · mahmoud@ ⇒ Mahmoud ✅ · abdullah@ ⇒ Abdullah ✅   (الهويّة لم تتغيّر)
② بريد دخول مكرّر: 0 ✅
③ مستلِمو بريد المشتريات: abdullah@ · supply@   ✅ لا أثر لـmahmoud@
④ موظّف ميدانيّ يُبدّل توجيه بريد معتمِد: مُنِع ✅
```
ونظافة الإنتاج: 6 مستخدمين (5 نشطون) · 12 طلباً · 121 أمراً · صفّ `notify_email` واحد + سطر تدقيق
واحد (المقصودان) · صفر دالّة DEFINER بلا `search_path`.

**التحقّق: 914 تأكيد واجهة** (+11) · **12 تأكيداً سلوكيّاً** على شبكة مُقلَّدة (تُشغّل حلّ المستلِمين
فعلاً وتقرأ سطر `To`) · **NE1–NE7** بـRLS فعليّة. **مُتحقَّق أنّ الحرّاس تعضّ: 6 علل أُعيدت عمداً ⇒
كلٌّ أفشل تأكيده الصحيح**، و**إعادة العلّة الأصلية تُعيد سطر البلاغ حرفيّاً**
(`["abdullah@…","supply@…","mahmoud@…"]`).
- **⚠️ وتأكيدٌ فراغيّ للمرّة الحادية عشرة — أمسكه البيت-بروف لا العين:** كان NE5 يرمي فشله **داخل**
  كتلة `BEGIN…EXCEPTION` فيلتقطه معالِجُها فيطبع ✓ — **تأكيدٌ لا يمكنه الفشل**. صار بعَلَم + فحص
  على **الأثر** (RLS قد تُصفّر الصفوف بلا خطأ). **لا تضع `RAISE` الفشل داخل كتلة تلتقط `raise_exception`.**
- **⚠️ الترتيب: الهجرة قبل النشر** (الواجهة والخادم يقرآن العمود)، وهي إضافيّة فالكود المنشور يعمل قبلها وبعدها.

#### ✅ مدموج ومنشور (2026-09-16، PR #113، كوميت الدمج `fbe7253`)
**نُفِّذ الترتيب المُلزِم:** الهجرة `system2_notify_email_separate_from_login` طُبِّقت حيّاً **قبل**
الدمج (وضبطت صفّ `Mahmoud` بأثر تدقيق)، ثمّ الدمج والنشر.
- **الفحوص السبعة خضراء على رأس الـPR** (ومنها `NE1–NE7` داخل حزمة RLS)، والحزم على `main` خضراء.
- **مُتحقَّق حيّاً على النطاقين ببصمة مطابقة للمستودع (2,795,117 بايت):** `uf-notify-email` ·
  `notifyEmail` · «بريد المراسلة» حاضرة، والنقاط الخمس `{"ok":true}`.
- **⚠️ ويتكرّر درس النشر:** أوّل استفتاء بعد الدمج أعاد بصمة النسخة السابقة (2,793,066) — النشر لم
  يكن قد اكتمل. استفتِ حتى تطابق البصمة، ولا تستنتج فشلاً من استفتاء واحد.
- **متبقٍّ اختياريّ (سؤال مطروح على المالك):** تثبيت بريد المراسلة لبقيّة الحسابات
  (`Abdullah`/`Mostafa` يشتقّان عنوانَيهما — صحيحان اليوم لكنّهما **مُخمَّنان**) فينتهي التخمين نهائيّاً.

### 📌 تصحيح حالة: حسابات الصيانة **مُنشأة ومستعمَلة** — ولا بند تشغيليّ فيها (2026-09-16)
**تصحيح المالك:** «حسابات الصيانة تم الإنشاء بالفعل — مصطفى كشك موظف الصيانة، ومدير الصيانة المهندس
محمد السبكي، فهذه ليست بنداً تشغيليّاً». **مؤكَّد بالقياس على الإنتاج:**

| الحساب | الدور | القسم | الوظيفة | النطاق | الاستلام | المبالغ | آخر دخول |
|---|---|---|---|---|---|---|---|
| `mostafa.kishk` — مصطفى كشك | user نشط | DEP-OPS | `requester` | الصيانة والتشغيل | ✅ | **محجوبة** | 2026-09-16 |
| `m.elsobky` — م.محمد السبكي | user نشط | DEP-OPS | `maintenance_manager` | الصيانة والتشغيل | ✅ | **ممنوحة صراحةً** | 2026-09-16 |

حسابا Auth مُنشآن 2026-09-13 ومؤكَّدا البريد، و**كلاهما سجّل دخولاً فعليّاً**. فالمنظومة كلّها
(النطاق · إخفاء المبالغ · مفاتيح الميدان · سلسلة الاعتماد) **تعمل على مستخدمين حقيقيّين لا افتراضيّين**.
- **`m.elsobky` يرى المبالغ بمنح صريح** (`can_view_amounts=true` مكتوبة في صفّه، لا افتراضاً) — متّسق
  مع «الرؤية المالية تُمنَح من المالك». لا يُسحَب إلا بقراره.
- **⚠️ درس تكرّر للمرّة الثالثة (وهو الذي أوقع هذا الخطأ):** كنتُ أُعيد «إنشاء حسابات الموظفين» بنداً
  مفتوحاً في كل تقرير **نقلاً عن لقطة قديمة** بلا إعادة قياسه — كما وقع سابقاً مع «مخزن `supplier-docs`
  مقروء للعموم» (كان مُغلَقاً) ومع «الأربعة القائمون» (كانوا ثلاثة). **لا تُعِد بنداً مفتوحاً بلا
  استعلام يُثبته في اللحظة نفسها.** وكل ذكر أقدم في هذا الملف لـ«إنشاء حسابات الموظفين» بنداً متبقّياً
  **متقادم ومنسوخ بهذا القسم** (وكذلك ذكر `db/system2-field-permissions.sql` بنداً متبقّياً — القياس
  أثبت أنّ كل ما يهمّ منها مطبَّق، وأنّ تشغيلها كما هي **انحدار أمنيّ**؛ انظر قسم إقفال التخزين).

**البند التشغيليّ الوحيد الباقي على المالك:** تدوير مفتاح `anon` للمشروع القديم `yofcaxvstjcrmbgciwym`.

### 🧾 كتالوج بلا أسعار + سجلّ وحدات + موعد التوريد بُعداً في الطابور (2026-09-17، PR #114 مسودّة)
**تصحيح المالك لملاحظةٍ من تدقيقي، وأقبله:** «صفر ربط بأمر شراء» **ليست ملاحظة أصلاً** — الطلبات
استُلمت اليوم، ودورة العمل حتى تعميد مورد وإصدار أمر تأخذ أكثر من يوم. **وما أخطأتُه بالضبط:** قِستُ
عدد الروابط ولم أقس **عمر الطلبات مقابل زمن الدورة**، فالرقم صحيح والاستنتاج منه خاطئ. **⚠️ قاعدة:
عددٌ بلا مقياسٍ زمنيّ يُقارَن به ليس ملاحظة.** والباقي أُصلح، ومعه تطوير الواجهة كما طلب.

#### ① الكتالوج كان محجوباً عن موظّف الميدان — وسببُ الحجب ليس ما بدا بدهيّاً
سياسة `cat_select` على `proc_items` هي `((NOT proc_is_scoped()) OR proc_can_view_amounts())`، فموظّف
الميدان **يقرأ صفر صنف** وقائمة الإكمال في نموذج الطلب تصله فارغة. المقيس: **63 وصفاً مختلفاً** في
بنود الطلبات، **ولا واحد** يطابق كتالوجاً فيه **711 صنفاً** — أي يكتب الأسماء من رأسه.
**⚠️ وتصحيح فرضيّة بدت بدهيّة: `proc_items` لا يحمل عمود سعر إطلاقاً** (مقيس على
`information_schema`: تسعة أعمدة، صفر ماليّ؛ الأسعار في `proc_history`). فالسياسة تحجبه خلف صلاحية
المبالغ **رغم أنّه ليس ماليّاً**. ولماذا لم نُوسِّع السياسة وهو الأبسط؟ لأنّ توسيعها تمنح الجدول
**كما سيصير** لا كما هو — أي عمود ماليّ يُضاف لاحقاً يتسرّب صامتاً. فالحلّ عرض
`proc_items_lookup` بـ`security_invoker=false` = **قائمة سماح صريحة بأربعة أعمدة**
(`code · name · category · unit`، و`notes` مستبعَد عمداً) — نفس نمط `proc_po_visible`
و`portal_user_directory` وقائمة مفاتيح `proc_settings`. ولا يُمنَح لـ`anon` إطلاقاً.

#### ② سجلّ الوحدات: 39 كتابة لـ711 صنفاً، أكثرها الوحدة نفسها
`حبة 475 · حبه 13 · ﺣبة 4 (رموز عرض) · حيه · جبه` · `كرتون 66 · كرتونة · BOX` · `قطعة · قطمة` …
فلا تجميع كمية ولا مقارنة عرضين على وحدة واحدة. `proc_unit_canon()` (NFKC + تشكيل/تطويل + مسافات
مكرّرة + خريطة الكتابات القاطعة) و`PR_UNITS`/`unitNorm` نظيرتها في الواجهة مع `datalist` للوحدات.
**⚠️ حدّها المقصود: القاطع فقط.** الدمج الدلاليّ («عدد» ⇐ «حبة» · «علبة 300 مل») **لا تفعله** —
درس سجلّ المشاريع: `ok` يُطبَّق و`suggest` لا يُطبَّق بلا قرار بشريّ. **والتنظيف يمسّ `proc_items`
وحده**: بند طلبٍ مُرسَل سجلٌّ لقرار، ووحدته كما كتبها صاحبه جزء منه.
**مُطبَّق حيّاً + مُتحقَّق: 39 ← 26 وحدة · صفر متّسخ · 711 صنفاً سليمة · `عدد`/`العدد` 8 صفوف لم تُمَسّ.**

#### ③ «مَن يُمسك الطلب في التسعير؟» كان بلا جواب على الشاشة
بعد إصلاح 16-09 صار الطلب يصل `received` بلا مالك، **ولا شيء كان يقول ذلك**. الآن `prProcActionsHTML`
مصدرٌ واحد لأزرار المشتريات (استُنسخت قبلُ في موضعين) + بطاقة «بانتظار من يستلمه» + `prWorkspaceNext`
تسمّي الشخص أو تقول صراحةً إنّ **أحداً لم يسجّل أنّه بدأ**.

#### ④ تطوير الواجهة — بقياس لا بذوق
- **موعد التوريد صار بُعداً في الطابور لا شارةً:** كان **أربعة طلبات تستحقّ غداً** بلا أي إشارة في
  أي مكان. مجموعة «موعد التوريد فات أو قارب» **ثانيةً بعد «يحتاج إجراءك»**، وداخلها الفرز
  **بالأقرب استحقاقاً** لا بالرقم (فما فات موعده يسبق ما رقمه أصغر)، وشارة على بطاقة الطابور وعلى
  «تاريخ الاحتياج». **والبعيد (>7 أيام) بلا شارة** كي لا تتحوّل إلى ضجيج.
- **شريط «ما ينقص قبل الإرسال»:** النموذج **5756px على الجوال** وزرّ الإرسال في قاعه، فالمستخدم
  يمرّر كل ذلك ليُخبَر أنّ بنداً ينقصه. الشريط يسمّي **كل** ما ينقص لا أوّل عائق.
  **⚠️ وهو `fixed` على الجوال لا `sticky` — لسببٍ مقيس:** أقرب حاوية تمرير `.pr-work-editor` لها
  `overflow:auto` **لكنّ ارتفاعها = ارتفاع محتواها فلا تمرّر أبداً**، فيلتصق الشريط بصندوقها لا
  بالشاشة (مُقاس: `bottom` تجاوز الشاشة بـ1415px). ومعه حشوٌ يمنع حجب آخر المحتوى.
  **⚠️ قاعدة: `position:sticky` يلزمه سلفٌ يمرّر فعلاً — وجودُ `overflow:auto` وحده ليس دليلاً.**

#### ⑤ سباق إقلاع أفرغ شاشتَي قياس — وجذر تقلّب `scoped-staff-screens` وُجِد أخيراً
`bootstrap` يعمل على DOMContentLoaded وينادي `showLoginScreen()` فيُخفي `#app-root`. وكان سكربتا
المتصفّح يُظهِران الشاشة **قبل** ذلك — أحدهما بعد انتظار تعريف الدوالّ (وهي تُعرَّف وقت تحليل
السكربت أي **قبل** الإقلاع)، والآخر بعد **مدّة ثابتة 1200ms** — فيُنقَض إظهارُهما بعد لحظة وتُقاس
شجرةٌ ارتفاعها صفر. **مقيس لا مُخمَّن:** `hideLoginScreen()` يُرجِع `display:grid` فوراً ثمّ يعود
`display:none` بعد ~1s، واعتراض `showLoginScreen` سمّى المنادي (`HTMLDocument.bootstrap`).
**وهذا بعينه جذر تقلّب `scoped-staff-screens.mjs` في CI الذي بقي مجهولاً منذ 2026-09-15**
(43/43 محليّاً مقابل 31/43 ثمّ 25/43 على العدّاء، بنمط «الهويّة والبيانات غائبتان»): عدّاءٌ بطيء
يسبق فيه البذرُ الإقلاعَ. العلاج في الاثنين: **انتظار الشرط الحتميّ — أن يُظهِر الإقلاعُ بطاقةَ
الدخول فعلاً**. **⚠️ قاعدة: لا `waitForTimeout` ثابتاً قبل بذر الحالة — انتظر الشرط.**
**وضُمّ السكربت إلى CI**، وأُضيفت هجرتا `system2-notify-email` و`system2-item-lookup-and-units` إلى
مرشّحات مسارات الـworkflow (وإلّا لم يُشغّل تعديلُهما شيئاً).

#### ⑤-ب الجذر الحقيقيّ كشفه CI — **كل فحص متصفّح كان يقرأ قاعدة الإنتاج**
ضمُّ السكربت أفشل CI (30/43) بتوقيعٍ جديد، وتتبّعُه كشف ما فات الإصلاح أعلاه:
`index.html` ينادي `hydrateFromCloud()` **فور الإقلاع** → `tryAutoConnectCloud()` → `getCloudConfig()`
**التي تسقط إلى إعداد مُضمَّن في الصفحة**. فأي فحص متصفّح يفتح الصفحة يطلب من الإنتاج فعلاً —
**مقيس باعتراض الطلبات: طلبٌ واحد لكل جلسة.** وعلى عدّاءٍ شبكته تعمل يكتمل المسار فيجري
`refreshFromCloud()` **فيستبدل الحالة المبذورة**، ثمّ `setupRealtime()` يفتح `wss://…supabase.co`
فتُسجَّل انتهاكات CSP (`connect-src` تسمح بـhttps لا wss). ومحليّاً تفشل الشبكة فيسقط المسار مبكّراً —
**فالفحص يمرّ هنا ويسقط هناك.** وهذا وحده يفسّر كل تاريخ التقلّب.
- **وعيبٌ ثانٍ مستقلّ في المسار نفسه:** كعب `auth.getSession` كان يُرجِع `session:null`، و
  `verifyAuthSession` اللاتزامنيّة عندها **تُصفّر `STATE.currentUser` وتُظهر بطاقة الدخول**.
  **مُعاد إنتاجه بالتشغيل قبل الإصلاح:** بعد النداء `currentUser=null` و`#app-root display:none` —
  وهو بالضبط توقيع CI (كل الصلاحيات false والمبالغ محجوبة **حتى عن موظّف المكتب**).
- **`scripts/e2e/app-boot.mjs` (جديد) — علاجٌ واحد لكل فحوص النظام 2:**
  `blockSupabase(ctx|page)` يُجهِض كل طلب لـSupabase (**شرط حتميّة وسلامة إنتاج معاً: لا فحص متصفّح
  يلمس القاعدة الحيّة**) · `enterApp(page)` ينتظر **الأثر** (التطبيق مرئيّ **ويبقى**) لا لحظةً بعينها،
  فيُعيد `hideLoginScreen()` في كل دورة حتى يثبت — لأنّ مسارات إقلاع متعدّدة تُعيد `showLoginScreen()`
  في لحظات مختلفة. ثمّ **يُنهي الترطيب**: بقطع الشبكة يستقرّ على `offline` فيبقى الهيكل النابض
  يتحرّك بلا توقّف، وهو ما جعل `locator.screenshot` ينتظر «استقرار العنصر» حتى المهلة.
- **التحقّق:** الفحوص الستّة خضراء منفردةً، **والستّة معاً متوازية تحت مزاحمة المعالج** (محاكاة
  عدّاء بطيء). **✅ ومُؤكَّد على العدّاء نفسه (`7b6a0fd`): الفحوص السبعة كلّها خضراء،
  و`scoped-staff-screens` = 43/43 بعد 30/43** — والتأكيدات التي كانت حمراء بعينها صارت خضراء
  (مبالغ موظّف المكتب `3,600` · مفاتيحه السبعة `true` · السند الموقَّع · رقم أمر الشراء ·
  **صفر انتهاك CSP**). ⚠️ **ودرسٌ منهجيّ: «أُثبِت أنّه حتميّ» بعد ثلاث تشغيلات محليّة ادّعاءٌ سابق
  لأوانه** — البيئة المحلّية كانت تُخفي الجذر (شبكةٌ مقطوعة) لا تُظهره. CI هو من أثبت.

#### ⑥ أمان: سحب `anon` عن ثلاث دوال مُشغِّلات
من مدقّق Supabase الحيّ: `pr_guard_status` · `pr_set_due` · `proc_pr_audit_fill_actor_name` كانت
مكشوفة لـ`anon` (مُتحقَّق: `has_function_privilege`=true للثلاث) وهي تُستدعى **عبر المُشغِّل وحده**.
**⚠️ والتحقّق بالتشغيل لا بالقراءة** (سحب EXECUTE قد يُعطّل المُشغِّل): IL9 يسحب ثمّ **يكتب** صفّاً
ويؤكّد نجاحه.

#### التحقّق
**955 تأكيد واجهة** (+41: القسمان ٤١ و٤٢) · **IL0–IL9** بـRLS فعليّة بدور `authenticated`
(**IL0 شرط لازم** يُثبت العَرَض قبل الهجرة، والهجرة تُشغَّل بـ`\i` **بعد** بذر الكتابات المتّسخة
فيثبت أنّها هي التي داوتها + idempotency) · متصفّح تحت CSP الإنتاج: 19+43+30+18+13+9.
**✅ الإثبات السلوكيّ الحيّ على الإنتاج** (بهويّة منتحَلة داخل معاملة تنتهي بـ`RAISE EXCEPTION` فتُجهَض
— صفر أثر): **LV0–LV7 كلّها PASS.** `LV0` [شرط لازم] الميدان يقرأ **0** من `proc_items` (العَرَض قائم
فعلاً) · `LV1` ويقرأ **712** من `proc_items_lookup` (711 إنتاج + الصنف المؤقّت) · `LV2` الوحدة المتّسخة
«حبه» تُعاد موحَّدةً «حبة» · `LV3` موظّف المكتب يقرأ **712** من `proc_items` = **صفر انحدار** ·
`LV4` anon محجوب عن العرض وعن دوال المُشغِّلات الثلاث و`authenticated` يقرؤه · `LV5` الكتابة على
الكتالوج ما زالت تمرّ **بعد** سحب EXECUTE (التحقّق بالتشغيل لا بالقراءة) · `LV6` أعمدة العرض =
`code,name,category,unit` (صفر عمود ماليّ ولا `notes`) · `LV7` صفر وحدة متّسخة على الإنتاج والوحدات 26.
**ونظافة الإنتاج بعد الإجهاض مُتحقَّقة:** صفر مستخدم/صنف مؤقّت · 711 صنفاً · 26 وحدة · 711 صفّاً في
العرض · 6 مستخدمين (5 نشطون) · 121 أمر شراء · 13 طلباً — كما كانت قبل الفحص بالضبط.

#### ✅ مدموج ومنشور (2026-09-17، PR #114، كوميت الدمج `f5d7e48`)
**الترتيب المُلزِم كان مُستوفى قبل الدمج:** الهجرة `db/system2-item-lookup-and-units.sql` **مُطبَّقة حيّاً
قبله** (الواجهة تقرأ `proc_items_lookup`)، وهي إضافيّة ومتسامحة فالكود المنشور عمل قبلها وبعدها.
- **الفحوص السبعة خضراء على الرأس `e109f31`** قبل الدمج.
- **مُتحقَّق حيّاً على النطاقين ببصمة مطابقة للمستودع (2,812,063 بايت):** `proc_items_lookup` ·
  `prCatalogUnit` · `prItemPicked` · `PR_UNITS` · `unitNorm` · `prDraftGaps` · `prDueChip` · `prDueLive` ·
  `prProcActionsHTML` — كلّها حاضرة بالعدد نفسه على النطاقين. والنقاط الخمس `{"ok":true}`.
- **⚠️ ووصف الـPR صُحِّح قبل الدمج** (درس #105: الوصف أثرٌ دائم): أُزيل «لم يُدمَج ولم يُنشَر» و«تُطبَّق
  قبل النشر» — وكلاهما بات كاذباً — وأُضيف جدول LV0–LV7 وقسم الجذر الذي كشفه CI.
- **⚠️ درس قياس جديد:** أوّل فحص للبصمة بعد النشر أظهر **فرقاً 313KB بين النطاقين** فبدا عطلاً — والسبب
  أنّ **`$(curl …)` في الصدفة يحذف البايتات الصفرية** (`ignored null byte in input`) بقدر غير ثابت.
  **قِسْ حجم صفحة بـ`curl -o ملف` ثمّ `wc -c` — لا بتعويض أمر.** (وتكرّر معه درس `-L`.)

### ↩ الطلب المُعاد كان طريقاً مسدوداً · خانات ما بعد الاعتماد · تنبيه المناقشة (2026-09-17، PR #116 مسودّة)
**بلاغ المالك بأربعة بنود، وكلّها عيوب حقيقية مقيسة. ولا هجرة في هذه الدفعة — واجهة فقط.**

#### ① [الأخطر] «أُعيد للاستكمال» بلا أي مخرج غير إلغاء الطلب
نصّه: «ارجاع الطلبات لمقدم الطلب — عندما يعود له الطلب لا يمكنه اجراء اي تعديل عليه، فقط عاد اليه.
**اليوم اضطرينا الغاء طلب تم اعادته للاستكمال** بسبب عدم قدرتنا على تعديل الطلب واعادة رفعه.»
- **الجذر (مؤكَّد بالكود):** `prEditDraft` تفتح بـ`if(pr.status!=='draft'){ toast('غير قابل للتعديل') }`،
  و**لا زرّ تعديل يظهر لطلبٍ `returned` في أي شاشة** (لوحة النظرة العامة · جدول القائمة · شاشة المتابعة
  القديمة — ثلاثتها مشروطة بـ`status==='draft'`). **والخادم كان يسمح منذ بنائه:**
  `pr_save_request` تقبل `status IN ('draft','returned')` وتُنشئ إصداراً جديداً وتُصفّر الاعتمادات.
  **فبوّابة الزرّ كانت أضيق من بوّابة الفعل** — والقاعدة 14 تُلزم تطابقهما. مرّ العيب من كل الفحوص
  النصّية لأنّ الدالّة موجودة وتُستدعى؛ ما كان غائباً هو **حالةٌ في شرطها**.
- **`prCanEditRequest(pr)` مصدراً واحداً منسوخاً عن حارس الخادم حرفيّاً:** `can_create_pr` + (صاحب
  الطلب · أو `can_manage_users` · أو أدمن) + غير مؤرشَف. **⚠️ و`can_manage_rfq` ليست بوّابة تعديل**
  (الخادم يقبل `pr_manage_users` والأدمن وحدهما) — زرٌّ يعرضه للمشتريات يُرفَض بعد النقر.
- **ثلاثة مداخل ظاهرة:** لوحة «↩ أُعيد إليك للاستكمال» على النظرة العامة بسبب الإعادة واسم من أعادها
  وزرّ التعديل · شارة «اضغط للتعديل وإعادة الإرسال» على بطاقة الطابور · وزرّ في جدول القائمة وشاشة
  المتابعة القديمة. ولمن لا يملك التعديل **تقول اللوحة من يملكه** بدل زرٍّ يُرفَض.
- **⚠️ السبب يُعرَض قبل أن يُمحى:** `pr_save_request` تُصفّر `return_reason` عند الحفظ، فاللافتة داخل
  المُحرِّر تحمله أمام عين المُعدِّل، و`prLastReturnComment` احتياطيٌّ يقرؤه من **`approvals`** (السجل
  الكامل) لا `approvals_current` — الإصدار الجاري لا يحمل قرار الإعادة الواقع على سابقه.

#### ②③ خانات ما بعد الاعتماد: ثلاث بدل واحدة
«عندما يتم استلام الطلب المفروض يذهب لخانة **الطلبات المستلمة تحت التنفيذ** وتظهر للمستلم في
**طلباتي المستلمة** · والطلبات بعد التنفيذ تذهب لموظّف الصيانة في خانة **الطلبات اللي تبقى لها الاستلام
وتقفل** — شوف المسمّى الأفضل لها.» وكانت مجموعة واحدة «جارٍ لدى المشتريات» تبتلع ثلاثة أطوار بثلاثة
مسؤولين مختلفين.
- **المسمّى المختار:** «بانتظار استلام البضاعة والإقفال» للمجموعة، و«الاستلام والإقفال» لزرّ التنقّل.
- **المصدر واحد** (`prProcLive`/`prUnclaimed`/`prInExecution`/`prAwaitingReceipt`/`prClaimedByMe`)
  تستعمله المجموعات وأوضاع التنقّل واللوحات معاً. مجموعات الطابور صارت: يحتاج إجراءك ← الموعد ←
  متعثّر ← قيد الاعتماد ← **بانتظار استلام المشتريات ← مستلمة تحت التنفيذ ← بانتظار استلام البضاعة
  والإقفال** ← أخرى ← منتهٍ. + وضعا تنقّل «⚙️ تحت التنفيذ» و«📦 الاستلام والإقفال» بعدّاديهما،
  ومرشّح «طلباتي المستلمة» (`proc_started_by = أنا`) للمشتريات.
- **⚠️ صدور أمر الشراء يُقدَّم على طور التنفيذ:** طلبٌ مُغطّى جزئياً يظلّ لدى المشتريات، لكنّ ما صدر
  منه **ينتظر استلام الميدان الآن**. و`prAwaitingReceipt` تقرأ **`prPoLinksOf`** لا `po_links` مباشرةً
  (الطلبات السابقة للموديل تحمل `po_number` بلا صفّ روابط فكانت ستسقط من الخانة).
- **⚠️ والسقوط الأخير خانةٌ محايدة «أخرى»** لا «تحت التنفيذ»: مسودّةُ غيرك ليست قيد التنفيذ، ووصفٌ
  كاذب أسوأ من خانةٍ عامّة.
- **لوحة الاستلام تفتح أمر الشراء نفسه** وتشرح أنّ الإقفال **خادميّ تلقائيّ** (مُشغِّل
  `trg_po_receipt_closes_pr` يُقفل ويؤرشف متى بلغت كل الأوامر «تسليم كامل») — فالفعل على الأمر لا الطلب.

#### ④ المناقشة: تنبيه على ما ينتظر ردّاً، ونداءٌ يقتبس السؤال
«الطلبات اللي عليها نقاش يظهر تنبيه ومنشن للرد، **وليس ما نعرف الرد على ايش**.»
- **⚠️ لا «غير مقروء» هنا:** النظام لا يخزّن حالة قراءة، واختراعها يُنتج شارةً تكذب. المحكّ **حقيقةٌ في
  البيانات**: آخر رسالة استفهام لم يأتِ بعدها ردّ (`prOpenQuestion`). و`prDiscussionFlag` تُعطي
  «بانتظار ردّك» لمقدّم الطلب · «بانتظار ردّ مقدّم الطلب» للسائل نفسه · null لما أُجيب عنه.
- **بانتظار الردّ فعلٌ مطلوب** فيدخل «يحتاج إجراءك» · **وعدّاد المناقشات صار يعُدّ ما يحتاج ردّاً**
  لا ما فيه رسائل (رقمٌ لا يزول بفعلٍ زينةٌ لا تنبيه) · ولوحة نداء تقتبس **نصّ السؤال واسم سائله** ·
  واقتباسٌ فوق صندوق الكتابة نفسه.
- **⚠️ `.pr-work-tag` صنفٌ مستقلّ — لا تُعِد استعمال `.pr-work-due` لشارةٍ ليست موعداً:** إلحاق شارة
  الإعادة بصنف الموعد كسر حارساً قائماً («شارتا موعد فقط») — **صنفٌ مُحمَّل بمعنيين يُبطل كل قياسٍ عليه**.

#### التحقّق
**995 تأكيد واجهة** (+41: القسم ٤٣) · **18 فحص متصفّح** تحت CSP الإنتاج
(`scripts/e2e/pr-request-flow-buckets.mjs` — **نقرٌ حقيقيّ** يفتح المُحرِّر على الطلب المُعاد ويقرأ
محتواه المُعبَّأ، ويقيس أنّ الاقتباس **فوق** صندوق الكتابة) + 6 لقطات. وصفر انحدار على الحزم القائمة
(43/43 · 30/30 · 19/19 · 18/18 · 13/13 · 9/9 + العقود والبصرية).
**مُتحقَّق أنّ الحرّاس تعضّ: 8 علل أُعيدت عمداً ⇒ كلٌّ أفشل تأكيده الصحيح**، والضابط يمرّ في الأداة نفسها.
**⚠️ وحارسان قائمان صُحِّحا للثابت الحقيقيّ لا للكود:** عدّ أزرار التنقّل (7→9، ومعه تسمية الوجهتين
صراحةً فيسمّي الإخفاقُ الغائبَ منهما) · ونمط `[\s\S]*?` المفتوح في حارس لوحة الاستلام كان **يلتقط
تعريف الدالّة نفسها لاحقاً في الملف فيمرّ وإن حُذف الاستدعاء** — أُمسِك بالبيت-بروف وصار محدود النافذة
وبصيغة الإقحام `${…}`.
**⚠️ ودرس تكرّر:** `let __prEditId` ارتباطٌ معجميّ لا خاصيّة على `window` — `window.__prEditId` يعود
`undefined` فيبدو التأكيد فاشلاً وهو ناجح (نفس سابقة `let SB`).

### قواعد التعديل على هذا الملف
1. **لا سكربت بناء** — التعديل مباشر في `index.html`؛ تحقّق السلامة بـ`node --check` على كتلة السكربت المستخرَجة.
2. **العرض:** غيّر في دالة `render*` المعنيّة فقط؛ الأرقام المشتقّة من `recomputePOderived` لا تُحسب مرة ثانية في العرض.
3. **المطبوعات:** ابْنِ `bodyHtml` بأصناف `.print-*` القائمة ومرّره لـ`printDocOpen` — لا `window.open`
   ولا CSS خاص ولا `<table border="1">`. **ومستندُ قرارٍ (محضر/سند) يحمل سلسلة قراراته وتواقيع
   المقرِّرين الفعليّين** لا مسمّيات ثابتة — خانةُ توقيعٍ لدور لم يقرّر شيئاً وثيقةٌ مضلِّلة.
   (محروسة بخمسة تأكيدات بعد بلاغ 2026-09-15.)
4. **التقارير:** أضف تعريفاً في `reportsCatalog()` + دالة بناء؛ واحترم `perm` (صلاحية) لكل تقرير.
5. **لا تُكرّر أسماء الدوال العامة** (سابقة `renderDashboardCharts` المُعرَّفة مرّتين).
7. **أي نافذة جديدة تُوضَع خارج `<section class="page">`** — القسم يحمل حركة بـfill-mode فيبقى له
   transform محسوب، وهو يكسر `position:fixed` لكل ما بداخله (محروس بتأكيد).
8. **أي لوحة off-canvas جديدة:** `visibility:hidden` وهي مُغلقة (لا `transform` وحده) وإلّا انزلقت
   الصفحة أفقياً على الجوال. وأي صفّ داكن جديد: `class="thead-navy"`. وأي جدول عريض: `mobileTableWrap`
   يلفّه تلقائياً، فلا تُحوِّله إلى `display:block`. (الثلاثة محروسة بتأكيدات.)
6. النظام 2 منفصل تماماً عن البوابة (نظام 3): لا `portal_*` هنا، ولا لمس `index.html` من أجل البوابة.
11. **أي مطابقة تربط طلب تسجيل ببطاقة مورد** تمرّ بـ`regMatchExistingSupplier` (مراتب القطعيّة الأربع)،
   وأي ملء يمرّ بـ`regSupplierFill` (الفارغ فقط، والاسم لا يُمَسّ). لا تُطابِق بالاسم بلا فحص تعارض
   الهويّة والتباسه. محروس بتأكيدات سلوكية.
9. **لا تضبط `document.body.style.overflow` في أي مكان** — أي طبقة جديدة تُفتَح/تُغلَق تنادي
   `syncScrollLock()`، وإن كانت تُفتَح بـ`display:flex` أضِف مُحدِّدها إلى `SCROLL_LOCK_LAYERS`
   (وإن كانت بصنف `active` غير `modal-overlay` فإلى `SCROLL_LOCK_ACTIVE`). محروس بتأكيد.
10. **أي صفّ لا يلتفّ داخل عنصر شبكة** (`display:flex` بأزرار/رسوم) يجب أن يحمل
   `flex-wrap:wrap` على الجوال وأن يُسمَح لعنصر الشبكة بـ`min-width:0` — وإلّا دفع الصفحة
   أعرض من الشاشة، والانزلاق الأفقيّ يخطف تمرير الإصبع فيبدو التمرير «معلَّقاً».
13. **لا يُشتقّ معرّف صفٍّ جديد من قائمة يقيّدها RLS** — الترقيم عبر `pr_next_number()` الخادميّة،
   والإنشاء `insert` لا `upsert` (رقمٌ مكرّر مع upsert = كتابة فوق صفّ غيرك). وأي حفظ يرافقه رفع
   مرفق **إلزاميّ** يبدأ مسودّةً ولا يُعلن الحالة النهائية إلا بعد وصول المرفق. (محروسة بتأكيدات
   سلوكية + معاينة متصفّح.)
18. **الاسم المعروض يُخزَّن مع الصفّ لا يُترجَم في المتصفّح** — `users_select` تحجب عن الموظّف
   المُنطَّق صفوف غيره، فأي عرض لهويّة شخص (معتمِد · مؤلِّف · مالك حاليّ) يقرأ عموداً مخزَّناً
   (`approver_name`/`author_name`/`requester_name`) لا جدول المستخدمين. وأي عمود يحمل صلاحية أو
   هويّة يُملأ **خادميّاً** وقت الإسناد ويُثبَّت وقت القرار. (محروس بتأكيدات.)
22. **الرمز البريديّ يُختَم بإصدار الطلب** — أي رمز/رابط يحمل قراراً على محتوى متغيّر يُختَم بنسخة
   ذلك المحتوى ويُفحَص الختم **في القاعدة** (لا في طبقة البريد: إبطالُها مشروط بإعادة الإشعار
   لنفس الأشخاص فيسقط عند تغيير المعتمِد أو فشل إرسال). والختم المجهول **لا يُلفَّق** — ختمٌ
   خاطئ يرفض كلّ اعتماد. **وأي هجرة تُشحَن تُضاف إلى `db/system2-tests/run.sh` في الدفعة نفسها**
   (هجرة #106 دخلت الإنتاج وبقيت الحزمة تختبر ما حلّت محلّه). محروسة بتأكيدات سلوكية.
19. **لا يُحذَف صفّ حمل قراراً** — إعادة إرسال طلب مُعاد تحذف **المعلّق وحده**؛ وبعد بقاء التاريخ
   صار `(pr_id,seq)` غير فريد فكل استعلام على `seq` يُقيَّد بـ`decision='pending'` وإلّا أعاد أكثر
   من صفّ. والمسار يقرأ `approvals_current` والمحضر يقرأ `approvals`. (محروسة بتأكيدات + إثبات حيّ.)
20. **أي مستند يخرج للمورّد بلا بيانات داخلية** — لا سعر وحدة ولا إجمالي ولا رصيد مستودعيّ ولا كمية
   تعاقدية ولا سلسلة اعتماد. محروس بحارسَي مصدر ومُخرَج كتقرير المتابعة الميدانيّ.
21. **حقول التاريخ `dir="ltr"`** — نصّها النائب يرسمه المتصفّح، وفي RTL بلا `dir` تنعكس حروفه.
   و**أي حاوية تمرير أفقيّ جديدة تُضاف إلى `scrollersToStart`** (لا الجداول وحدها)، وأي شاشة تُرسَم
   لاتزامنيّاً تُعيد ربط `a11yWire`/`mobileTableWrap` بنفسها. (الثلاثة محروسة بتأكيدات + قياس متصفّح.)
14. **لا قدرة ميدانية بلا مفتاح** — أي فعل جديد لموظّف الصيانة (رفع · تعليق · طباعة · قالب) يحمل مفتاحاً
   في `PERMISSION_DEFS` (بـ`field:true`) **ونظيره خادميّاً** عبر `proc_has_perm(key, office_default)` —
   لا `pr_has_perm` (افتراضها false يسلب موظفي المكتب). **وبوّابة الزرّ = بوّابة الفعل حرفيّاً**: زرٌّ يُعرَض
   وفعلُه مرفوض (أو العكس) عيبٌ محروس بتأكيد. وأي مفتاح جديد يُضاف إلى `FIELD_PERMISSIONS` في
   `staff-invite.js` وإلّا وصلت الدعوة بحساب مشلول، **وإلى `LEGACY_PERMISSIONS` في `admin-users.js`
   وإلّا فشل *كل* حفظ مستخدم بـ400** (`cleanPermissionObject` يرفض الكائن كلّه لا المفتاح وحده — وهو ما
   أسقط الصلاحيات والوظيفة والقسم والنطاق معاً في بلاغ 2026-09-15). الثلاثة محروسة بتأكيدات.
15. **أي رابط في بريد نظام 2 يمرّ بـ`requestUrl()`** — لا `requests.html` ولا `purchase-portal.html`
   (نظام آخر)، والهبوط على الصفّ المقصود عبر `openDeepLink` لا على المهبط العامّ. والرابط لا يمنح
   وصولاً — RLS هي الحكم. (محروسان بتأكيدات.)
16. **لا تُمرِّر `numeric` إلى `make_interval`** — معاملاتها `integer` فيفشل الاستدعاء بـ42883 وقت
   التشغيل لا وقت الإنشاء. استعمل `(v * interval '1 hour')` (ويحفظ الكسور أيضاً). وأي عمود جديد
   يحمل صلاحية على `proc_users` يُضاف إلى قائمة `proc_users_guard` **في الهجرة نفسها** وإلّا صار
   منحاً ذاتيّاً صامتاً (سابقتان: `scope_sectors` ثم أعمدة الموديل الثلاثة).
17. **حزمة خضراء ليست إطلاقاً** — قبل تسليم أي موديل يمسّ الإنتاج، شغّل **دورة حقيقية على قاعدة
   الإنتاج** داخل معاملة تنتهي بـ`RAISE EXCEPTION` (فتُلغى بلا أثر). العيبان اللذان عطّلا رفع
   الطلبات وتصعيد SLA مرّا من 7 فحوص و796 تأكيداً، وكشفهما استدعاء واحد حقيقيّ.
24. **مَن أذِن ليس مَن عمل** — لا تختم حقل «مَن بدأ» في خطوة اعتماد؛ الختم يقع في فعل البدء نفسه،
   وإلّا استُهلِك الانتقال فتعذّر على صاحب العمل تسجيله. و**أي مفتاح حالة/حدث إنجليزيّ يُعرَض للمستخدم
   يلزمه قاموس عربيّ كامل** مبنيّ على `select distinct` من الإنتاج لا على التخمين، وبديلٌ عربيّ للمجهول.
   و**الطابور يُفرَز بالإلحاح** لا بترتيب الجلب. و**قيمة معجميّة لها سجلّ معتمد لا تُدخَل نصّاً حرّاً** —
   أعِد استعمال محرّك السجلّ القائم (`prj*`) لا نسخةً ثانية منه. (الأربع محروسة بتأكيدات سلوكية + متصفّح.)
23. **مرفق الطلب يُجلَب من `/api/pr-doc` لا من نقطة وثائق الموردين** — بادئة المفتاح هي هويّة مخزنه
   (`docs/pr/` مقابل `DG-`)، و`docvSourceBlob` مصدرٌ واحد لبايتات الوثيقة يخدم الرابط والـPDF معاً.
   و**أي جدول يدخل موديلاً له حارس رؤية خاصّ به يُخرَج من قائمة الإقفال العامّ**: RESTRICTIVE تُجمَع
   بـAND فتُلغي الحارس بلا أن تُخفِق أي سياسة. وأي رسالة/دليل يُشير إلى مرفق: **يُرفَع المرفق أوّلاً
   فيصير له معرّف ثمّ يُشار إليه**، ومعرّف لا يخصّ الطلب يُرفَض لا يُسقَط صامتاً. (محروسة بتأكيدات
   سلوكية + فحص متصفّح تحت CSP الإنتاج.)
25. **بريد الدخول ≠ بريد المراسلة** — `email` هويّة (`proc_me` تُطابق به أوّلاً فصندوقٌ مشترك فيه
   يقلب هويّة من يدخل به)، و`notify_email` عنوان تصل إليه الإشعارات. وكل مستلِم يمرّ بمصدر واحد
   (`rowNotifyEmail`: `notify_email ← email ← الاشتقاق`) — **والاشتقاق آخر مَلاذ لا أوّل طريق**:
   عنوانٌ مُخمَّن من ثابت في الكود هو ما أرسل بريد المشتريات بمبالغه إلى صندوق شخص خارج الشركة.
   ولا تُوسَّع `AUTH_EMAIL_MAP` بأسماء جديدة — خزِّن العنوان في القاعدة. (محروسة بتأكيدات سلوكية + NE1–NE7.)
12. **بوّابة صلاحية تحكم دوراً (مشتريات/اعتماد) تمرّ بـ`prPermStrict` لا `hasPermission`** —
   مفتاح `userDefault:true` يجب ألّا يمنح الموظّف المُنطَّق دوراً بمجرّد غيابه من صفّه. وأي مبلغ
   يُعرَض يمرّ بـ`fmtPrice`/`tafqitSAR` (لا منسّق نقديّ ثانٍ). ووجهات المُنطَّق **شاشتان فقط**
   (`SCOPED_PAGES`) — لا ثالثة في الشريط السفليّ. (الثلاثة محروسة بتأكيدات.)
26. **بيانات مرجعية ليست بيانات مالية** — جدولٌ حجبتُه سياسةٌ ماليّة لا يُفتَح بتوسيع السياسة (فيمنح
   الجدول **كما سيصير**) بل بعرض `security_invoker=false` = **قائمة سماح صريحة بالأعمدة**. وأي تطبيع
   لقيمة معجميّة (وحدة · مشروع · مورّد) **يُوحّد القاطع ولا يدمج الدلاليّ** — الدمج قرارٌ بشريّ —
   **ويمسّ الجدول المرجعيّ وحده لا سجلّات القرارات** (بند طلبٍ مُرسَل سجلٌّ، ووحدته جزء منه).
   و**عددٌ بلا مقياسٍ زمنيّ يُقارَن به ليس ملاحظة** («صفر ربط» على طلبات عمرها ساعات). (محروسة بـIL0–IL9.)
27. **كل فحص متصفّح للنظام 2 يبدأ بـ`blockSupabase` + `enterApp` من `app-boot.mjs`** — الصفحة
   تتّصل بقاعدة الإنتاج **من تلقاء نفسها** عند الإقلاع (إعداد سحابيّ مُضمَّن)، فبلا إجهاض الشبكة:
   يُقرأ الإنتاج فعلاً، وتُدهَس الحالة المبذورة، وتُفتَح `wss://` فتنتهك CSP — **ويختلف السلوك بين
   بيئة التطوير وCI** لأنّ الشبكة تعمل في أحدهما فقط. ولا `waitForTimeout` ثابتاً ولا انتظار لحظةٍ
   بعينها قبل البذر: **انتظر الأثر** (التطبيق مرئيّ **ويبقى**)، فمسارات إقلاع متعدّدة تُعيد
   `showLoginScreen()` في أوقات مختلفة. وكعب `auth.getSession` يُرجِع **جلسةً حاضرة** — بـ`null`
   تُصفّر `verifyAuthSession` هويّةَ المستخدم وتُظهر بطاقة الدخول. و**`position:sticky` يلزمه سلفٌ
   يمرّر فعلاً** (وجود `overflow:auto` وحده ليس دليلاً). **⚠️ ولا تُعلن سكربتاً «حتميّاً» بتشغيلات
   محلّية:** البيئة المحلّية أخفت هذا الجذر سنةً كاملة من التقلّب — CI هو من أثبته.
28. **حالةٌ يقبلها الخادم بلا زرٍّ يفتحها = طريق مسدود** — `pr_save_request` تقبل `returned` منذ
   بنائها والواجهة لم تعرض زرّاً قطّ، فالمخرج الوحيد كان **إلغاء الطلب** (وقع فعلاً على الإنتاج).
   أي حارس واجهة يُنسَخ عن حارس الخادم **بحالاته كلّها**، ويُوضَع في دالّة واحدة (`prCanEditRequest`)
   لا مكرَّراً في كل شاشة. و**السبب يُعرَض قبل أن يُمحى** (الحفظ يُصفّر `return_reason`).
   و**خانةٌ تصف الطلب بغير حقيقته أسوأ من خانةٍ عامّة**: السقوط الأخير محايد، وصدورُ أمر الشراء
   يُقدَّم على طور التنفيذ، والروابط تُقرأ بـ`prPoLinksOf` لا `po_links`. و**التنبيه حقيقةٌ في
   البيانات لا حالة قراءة مخترَعة**: «بانتظار ردّك» = آخر رسالة استفهام بلا ردّ، وعدّادُه يعُدّ ما
   يحتاج فعلاً. و**لا تُحمِّل صنفاً واحداً معنيين** (`.pr-work-due` للموعد، `.pr-work-tag` لغيره) —
   صنفٌ مُحمَّل يُبطل كل حارسٍ عليه. (محروسة بالقسم ٤٣ + `pr-request-flow-buckets.mjs`.)

---

## ملاحظات مهمة لتجنّب الأخطاء
- عند العمل على البوابة (نظام 3): استعمل `portal_*` و`PORTAL_SUPABASE_*` و`purchase-portal.html`/`functions/api/portal-*` فقط.
- عند العمل على النظام الرئيسي (نظام 2): `proc_*`/`pr_*` و`SUPABASE_*` و`index.html`/`requests.html`/`admin-users.js`/`notify.js`.
- لا تُعدّل `index.html` أو أي `proc_*` من أجل البوابة إطلاقاً (توجيه صريح ومتكرّر من المالك).
- عنوان الموديل المُهيَّأ: `claude-opus-4-8` — لا تُدرجه في أي كوميت/كود.
