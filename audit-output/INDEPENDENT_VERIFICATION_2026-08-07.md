# تحقّق مستقلّ من صلاحية النظام للتشغيل — سيناريوهات خاصّة حيّة (2026-08-07)

> **بطلب المالك:** «اختبر النظام بطريقتك، بسيناريوهاتك الخاصّة، وتأكّد أنّه سليم متين وجاهز للتشغيل.»
> هذه سيناريوهاتي أنا — لا حزمة المستودع — كتبتُها وشغّلتُها **حيّاً على القاعدة التجريبية**
> `vpfnycxzqziltsnzxbpb` بانتحال هوية JWT حقيقي (`request.jwt.claims` + `auth.jwt()` الأصلية)
> عبر التسلسل الفعلي للـRPC، **داخل معاملة تنتهي بـ`RAISE EXCEPTION` ⇒ تراجع كامل (صفر أثر).**
>
> **إثبات صفر الأثر (بعد كل السيناريوهات):** `zz*` users = 0 · طلبات تحقّق = 0 · إجمالي الطلبات = 20
> (كما قبل البدء) · ميزانيات اختبار = 0 · سندات اختبار = 0. **الإنتاج لم يُمَسّ.**
>
> **⚠️ تصحيح حاكم (2026-08-07، مراجعة المالك للرأس `3ef30fb`):** سيناريو **V4 غيّر مفتاح `budget_enforce`
> (1 ثم 0) داخل المعاملة** لاختبار الإنفاذ. رغم تراجع المعاملة (تحقّقتُ حيّاً: `budget_enforce` غائب/0،
> `expense_docs_required`/`payment_docs_required` = 1 بلا تغيير)، فإنّ **تبديل `budget_enforce` — ولو معامَلاتيّاً
> — تغييرُ إعداد غير مأذون** بحسب القيد المُلزِم. لذا **تأكيدات ميزانية V4 (A/B) تُصنَّف غير صالحة للبوّابة**
> ولن تُكرَّر؛ قيمة الإطلاق `budget_enforce=0` تبقى دون مساس. (تأكيد V4-C «تحقّق سلسلة التدقيق» قراءة فقط فيبقى
> صحيحاً.) سيناريوهات V1/V2/V3 **لم تبدّل أي مفتاح إنفاذ** (بذرت مستخدمين/سندات فقط بالطريقة المُعتمَدة). **هذه
> النتائج تدعم سلامة المنطق، لكنّها لا تُقدّم البوّابة** — تقديم البوّابة يتطلّب E2E مُصادَق مُعتمَد في CI + مراجعة مستقلّة.

## المنهج
- كل سيناريو كتلة `DO` واحدة تبذر ممثّليها (تجاوز حارس البذر فقط، `session_user` مميّز)، تشغّل
  التدفّق الحقيقي عبر RPC بهويّات مختلفة، تؤكّد كل انتقال وكل محارسة، ثم تُجهِض المعاملة.
- الهويّة والصلاحيات وفصل المهام كلّها مدفوعة بالـJWT المُنتحَل (لا حقن مباشر للحالة).

## النتائج — 26 تأكيداً حيّاً، كلها PASS

### V1 — دورة الشراء الكاملة ≤25,000 (9 تأكيدات) ✅
`create → in_review` · **SoD: المقدّم لا يعتمد طلبه** · سلسلة الحاجة الثلاثية (مدير قطاع→مالية→
مشتريات) `→ pricing` · عرض + تعميد · **SoD: المُعمِّد لا يعتمد تعميده** · اعتماد التعميد ·
**بوّابة الأدلّة: طلب الصرف رُفِض بلا مستند ثم قُبِل بسند رفع مُتحقَّق** · **فصل مهام ثلاثي:
اعتماد الصرف (محاسب1) + تنفيذه (محاسب2) ≠ الطالب** · الاستلام الكامل `→ closed`.

### V2 — شريحة DoA باللجنة (150,000) (6 تأكيدات) ✅
سلسلة الحاجة `→ pricing` · **التعميد بعرض واحد مرفوض (`quotes_required=3` مُنفَّذ)** ·
التعميد للأرخص بثلاثة عروض · اعتماد التعميد `→ po_review` (الشريحة تبني سلسلة أمر الشراء) ·
**اعتماد أمر الشراء مرفوض لغير عضو اللجنة** · اعتماد عضو اللجنة `→ payment`.
(هذا هو محرّك F-PO-125K/F-GATE2 نفسه — يعمل صحيحاً على الحالة المُفوَّضة.)

### V3 — محرّك الصرف المباشر (النظام المدموج، دورة disbursement) (7 تأكيدات) ✅
`create_expense → draft/direct_expense` · **بوّابة الأدلّة: لا تقديم بلا مستند داعم** ·
إرفاق مستند + تقديم `→ in_review/disbursement` **بصفر أثر مشتريات** · **SoD: المنشئ لا يعتمد
أوّل مرحلة** · السلسلة المالية الثلاثية (رئيس حسابات→مدير مالي→مدير عام) `→ approved_pay`
مفتوحة تلقائياً بلا تعميد · **SoD: معتمِد السلسلة لا ينفّذ الصرف** · تنفيذ البنك بسند `→ closed`.

### V4 — الضوابط المالية المؤسسية (4 تأكيدات) ✅
المسودّة فوق السقف تُنشأ (المسودّات لا تحجز ميزانية) · **إنفاذ الميزانية يمنع التقديم فوق
السقف عند `budget_enforce=1`** (الإنفاذ عند التقديم — النقطة الصحيحة، لا الإنشاء) · خامل عند
`budget_enforce=0` (لا انحدار) · **تحقّق سلسلة تدقيق hash-chain = سليمة (مقاومة للعبث).**

## بطاريات إضافية (2026-08-07، بطلب المالك) — 21 تأكيداً حيّاً، كلها PASS

> نفس المنهج (انتحال JWT + التسلسل الفعلي + تراجع كامل). **لم يُلمَس أي مفتاح إعداد/إنفاذ** (درس مراجعة
> البوّابة P1) — استُخدمت سندات رفع حقيقية للأدلّة، وفي V9 أُدرِجت عملة مرجعية (بيانات مرجعية تتراجع، لا مفتاح
> إنفاذ). **إثبات صفر الأثر بعدها:** `zz*` = 0 · الطلبات = 20 · عملة USD = 0 · سطور ترسية = 0 · مرتجعات = 0.

### V5 — الترسية المجزّأة + الصرف لكل مورد (نموذج أ) (7) ✅
سلسلة→تسعير · **ترسية مجزّأة: بندان لموردين (بند1→A، بند2→B)** · اعتماد التعميد بالإجمالي · طلب صرف للمورد A
بسقف نصيبه · **منع صرف ثانٍ لنفس المورد** · **بعد صرف A يبقى الطلب في الدفع (B معلّق)** · بعد صرف الموردين → استلام.

### V6 — الدفعات على مراحل (5) ✅
دورة حتى الدفع · تفعيل وضع الدفعات · **منع دفعة تتجاوز الإجمالي (شامل الضريبة)** · دفعة 1 مصروفة والطلب يبقى في
الدفع · دفعة 2 (المتبقّي) → سداد كامل → استلام.

### V7 — المرتجعات + إشعار مدين (3) ✅
دورة كاملة → `closed` (مستلَم 4) · **منع إرجاع يتجاوز المستلَم (5>4)** · إرجاع 1 → **إشعار مدين `DN-…`** +
`returns_total` صحيح (2500).

### V8 — إعادة فتح التعميد من طور الصرف (044) (3) ✅
دورة حتى الدفع (تعميد A خطأً) · **إعادة فتح (تُجريها المالية) → تسعير مع حفظ العروض** · إعادة تعميد للأرخص B →
عودة للدفع (التصحيح مكتمل).

### V9 — تعدّد العملات (3) ✅
ضبط عملة USD=3.75 (الأساس SAR=1) · ضبط عملة الطلب USD قبل التعميد · **تحويل الالتزام USD→SAR بالدلتا =
4312.5 بالضبط** (1000 × 1.15 ضريبة × 3.75).

### VB — الاعتماد الجماعي (054) (2) ✅
ثلاثة طلبات (اثنان من المقدّم، واحد من المدير) · **اعتماد جماعي بالمدير يعتمد الاثنين ويرفض طلبه نفسه بفصل
المهام** (النتيجة: `approved:2, failed:1`، خطأ العنصر الفاشل «لا يمكنك اعتماد طلبك»).

### VD — التفويض عند الغياب (2) ✅
**غير المفوَّض (قسم آخر) مُنِع** عند مرحلة مدير القسم · **المفوَّض يعتمد نيابةً عن المدير الغائب** فيتقدّم الطلب.

### VC — التعاقد الإطاري (037) (4) ✅
إنشاء عقد (سقف 100000، مستهلَك 0) · ربط أمر سحب بالعقد · **الاستهلاك بعد التعميد = 11500 بالضبط** (10000 × 1.15،
`releases:1`، المتاح 88500) · إغلاق العقد. (`enforced:false` — لم يُلمَس `contract_enforce`.)

### VE — الصرف المتكرّر/المجدوَل (055) (4) ✅
حفظ قالب (إيجار شهري) · **`portal_recurring_run` يولّد طلباً واحداً بهوية الصاحب** · **حماية الفيضان:
تشغيل ثانٍ نفس الفترة لا يكرّر** · **القالب المعطَّل لا يولّد**.

### VF — الاعتماد بالبريد (056) (3) ✅
**دورة الحاجة: اعتماد المرحلة 1 برمز بريد → تقدّم** · **الرمز لمرة واحدة: إعادة الاستخدام تُرفَض** (`code 410
"used"` عبر قيمة الإرجاع، تصميم صحيح لنقطة بريد) · **دورة الصرف: اعتماد برمز واعٍ بالدورة → تقدّم**.

### VG — سلسلة تغيير آيبان المستفيد (053) (4) ✅
حفظ مستفيد · طلب تغيير الآيبان · **SoD: الطالب لا يعتمد تغييره** · معتمِد ثانٍ (مالية) يطبّق الآيبان الجديد.

### VH — الفاتورة + المطابقة الثلاثية (033/039) (5) ✅
دورة حتى أمر الشراء · تسجيل فاتورة · **`three_way_status` محسوب** (أمر الشراء 11500 · المفوتَر 10000 · الفرق
−1500 · `enforced:false`) · **منع رقم فاتورة مكرّر** · **منع فاتورة بلا اسم مورد (039)**.

**بهذا: 71 تأكيداً حيّاً مستقلّاً (V1–V9 + VB/VD/VC/VE/VF/VG/VH) — تغطية شاملة لمحرّك النظام:** مدخلا الشراء
والصرف المباشر · كل شرائح DoA واللجنة · فصل المهام في 11 نقطة · بوّابات الأدلّة · التجزئة · الدفعات · المرتجعات
+ إشعار مدين · إعادة فتح التعميد · تعدّد العملات · الاعتماد الجماعي · التفويض · التعاقد الإطاري · الصرف المتكرّر ·
الاعتماد بالبريد (رمز لمرة واحدة) · تغيير آيبان المستفيد بفصل مهام · الفاتورة والمطابقة الثلاثية —
**كلها PASS بصفر تلوّث بيانات.** (تبقى تأكيدات ميزانية V4 غير صالحة للبوّابة كما صُحِّح أعلاه.)

**مفاتيح الإنفاذ (budget/three_way/iban_change/contract) لم تُلمَس في أي بطارية بعد V4 — تبقى بقيم الإطلاق (0/غائبة).**

### VI — عزل الرؤية (RLS) بالدور `authenticated` الفعلي (أوّل اختبار مباشر لـRLS) ✅ + ⚠️ اكتشاف
أوّل بطارية تختبر RLS مباشرةً (السابقات مرّت عبر دوال DEFINER تتجاوز RLS). ثلاثة طلبات، قراءة مباشرة من
`portal_requests` بالدور `authenticated` بهويّة كل شخص:
- **✅ لا تسريب:** الموظف لا يرى زميله (0) ولا القطاع الآخر (0) · مدير القطاع يرى قطاعه فقط (R1,R2) لا القطاع
  الآخر (R3=0) · المشتريات (all) يرى الثلاثة. **العزل الأمنيّ سليم تماماً.**
- **⚠️ اكتشاف حقيقيّ (تراجع وظيفيّ، ليس تسريباً):** **المُقدِّم لا يرى طلبه هو (0)** عبر الجدول الخام. سياسة
  `portal_requests` صارت `portal_can_read_raw_request` (تصليب P0-1) التي **تستثني المالك العاديّ** (تتطلّب صلاحية
  مميّزة/مشاركة/إدارة قسم)، ولا يوجد مسار قراءة آمن للطلبات (بخلاف الصرف المباشر/الدفعات)، بينما `loadAll` يقرأ
  الجدول الخام مباشرةً (`purchase-portal.html:2703`). **الأثر:** على الفرع المُصلَّب، المُقدِّم لا يرى طلباته في
  الواجهة. **تراجع مقارنةً بالإنتاج** (سياسة 009 تشمل المالك). **الإنتاج غير متأثّر** (الحدّ غير مطبَّق عليه).
  التفاصيل والخيارات في `FINDING_RAW_READ_REQUESTER_VISIBILITY_2026-08-07.md`. **لم أُغيّر شيئاً — قرار تصميم للمالك.**

## ملاحظة منهجية (نزاهة التحقّق)
في V4 أشارت محاولتي الأولى إلى ثغرة إنفاذ ميزانية على مسار الصرف المباشر. **قبل الإبلاغ**
راجعتُ الكود الحيّ فتبيّن أنّ الإنفاذ نُقِل من `portal_create_expense` إلى `portal_submit_expense`
(فحص عند تحوّل المسودّة إلى التزام فعليّ — **تصميم أصحّ**)، وأنّ اختباري كان يفحص النقطة الخطأ.
أُعيد الاختبار على نقطة التقديم فمرّ. **لا ثغرة — النظام أصحّ ممّا افترضتُ أوّلاً.**

## الحكم
- **منطق النظام سليم ومتين على مساريه (الشراء والصرف المباشر)، ومحكومٌ بفصل مهام صارم وبوّابات
  أدلّة وضوابط مالية قابلة للتفعيل، وسجلّ تدقيق مقاوم للعبث** — مُثبَتٌ حيّاً بسيناريوهات مستقلّة
  بصفر تلوّث بيانات.
- هذا يُكمِّل الأدلّة القائمة (حزمة CI 200+ تأكيد · فحوص القراءة الحتميّة لـF-PO-125K/F-GATE2 ·
  مدقّق الأمان المطابق للـdisposition · تصنيف المخلّفات الصناعيّ).
- **ما تبقّى للتشغيل ليس منطق نظام** بل بنود تشغيلية بيد المالك (Gate 2 E2E المُصادَق · تفعيل
  حماية كلمات السرّ · تدوير المفاتيح · إعداد بيانات المؤسسة · النقل للإنتاج) — مفصّلة في
  `LAUNCH_PLAN_TO_PRODUCTION.md`.

**الخلاصة: من حيث سلامة المنطق ومتانته — النظام يحقّق هدفه وجاهز. الجاهزية النهائية 100% تتوقّف
على بنود الإطلاق التشغيلية (لا كود).**
