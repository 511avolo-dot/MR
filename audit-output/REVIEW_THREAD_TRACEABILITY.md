# REVIEW-THREAD TRACEABILITY — GENERATED APPENDIX (G0-R6 · corrected G0-F3)

> Regenerated from GitHub `get_review_comments` (both pages) — **all 102 inline threads**. Canonical dispositions cross-ref `MASTER_DELIVERY_LEDGER.md` §4. **Reviewed commit is NOT returned by the review-comments API for these threads → shown as `not returned by API` (never blank).** GitHub 'state' = thread status on GitHub (outdated/resolved/open); the canonical **ledger ID + disposition** is authoritative for whether the code finding is open/fixed. Threads were deliberately not mass-resolved.

**Summary:** 102 threads · open-on-GitHub 57 · outdated 45 · resolved 0 · still needing Stage-2 canonical triage 0. Each thread carries **one stable canonical ID** (split only when genuinely two concerns). Owner-named open items — `S1-GUARD-COUPLE`, `BOOT-STATES/S10-STATES`, `PAY-DOCS-COMPLETE/S8-PAY-EVID`, `RECUR-BLOCKED/S8-RECUR-BLOCK`, `RET-EXPENSE-EDIT/S8-EXPENSE-EDIT`, `PAY-ROLES` — carry their exact IDs and remain **open** (never the generic "fixed" bucket). `fixed @<sha>` = fixing commit; open items point at their gated Stage.

| # | Thread (file:line) | Reviewed commit | Sev | Finding (preserved) | Canonical ledger ID | GitHub state | Disposition |
|--|--|--|--|--|--|--|--|

| 1 | `audit-output/EXECUTIVE_REPORT.md` | not returned by API | P2 | Disclose requester access to beneficiary IBANs — Revise this answer because a low-level requester with `can_create` can read every column of `portal_beneficiaries`, including `iban`, through the `portal_beneficiaries_rea | `SEC-IBAN-EXPOSE` | outdated | open |
| 2 | `db/portal-tests/35_anon_hardening.sql` | not returned by API | P2 | Exercise the revoked anon grants in CI — Seed the four Supabase-style `anon` SELECT grants before applying migration 059, or test the migration independently. In the clean PostgreSQL CI setup, `00_roles.sql` only creates | `SEC-06` | outdated | open (P0) |
| 3 | `db/portal-tests/run.sh` | not returned by API | P2 | Correct the per-suite assertion breakdown — Update the breakdown to match the actual PASS assertions: `11_security.sql` emits 8 rather than 7, while `26_disbursement_core.sql` emits 10 rather than 11. The errors happen t | `TEST-COVERAGE` | outdated | open (Stage-2 test) |
| 4 | `audit-output/TECHNICAL_REPORT.md` | not returned by API | P2 | Detect audit-log truncation before claiming WORM — Qualify this claim or add an externally anchored checkpoint: `portal_audit_verify()` only recomputes the rows that remain, so a DBA who bypasses the regular trigger and | `DOC-ACCURACY` | outdated | addressed via G0/G0-R/G0-F reconciliation |
| 5 | `audit-output/API_SECURITY_MATRIX.md` | not returned by API | P1 | Authenticate registration-document uploads — Do not describe `reg-doc.js` as server-key-authenticated: callers provide no credential, and `sameOrigin` is only a forgeable header check for non-browser clients. Anyone can | `SEC-06` | outdated | open (P0) |
| 6 | `audit-output/API_SECURITY_MATRIX.md` | not returned by API | P2 | Inventory every API handler in the security matrix — Extend the matrix before treating it as the endpoint inventory: it omits handlers including `admin-users.js`, `ai.js`, `invite-supplier.js`, `portal-signup.js`, `porta | `AI-PROXY-ABUSE` | outdated | open |
| 7 | `audit-output/TECHNICAL_REPORT.md` | not returned by API | P1 | Enforce SoD for administrator accounts — Do not certify triple separation as universal while both transition paths explicitly exempt administrators. `portal_pr_transition` lets an admin requester approve their own reques | `OWN-ADMIN (accepted-risk)` | outdated | accepted-risk (labeled + audit) |
| 8 | `audit-output/FINAL_CERTIFICATION.md` | not returned by API | P1 | Bind direct expenses to the requester's department — Account for the missing department authorization in `portal_create_expense`: unlike `portal_create_request`, it accepts any existing client-supplied `p_department_id` | `AUTHZ-EXPENSE-DEPT (060)` | outdated | implemented @135f5af (live) |
| 9 | `audit-output/FINDINGS.md` | not returned by API | P1 | Require approved beneficiaries for bank expenses — Correct the claim that the RPC enforces a vetted IBAN: `portal_create_expense` declares `p_beneficiary_id` optional, and when it is null it accepts any client-supplied I | `SEC-IBAN-EXPOSE` | outdated | open |
| 10 | `audit-output/TECHNICAL_REPORT.md` | not returned by API | P2 | Enforce budgets during recurring generation — Qualify the budget-control claim or apply the same check in `portal_recurring_run`. When a recurring template becomes due, that function inserts directly into `portal_request | `RECUR-BLOCKED / S8-RECUR-BLOCK` | outdated | open |
| 11 | `functions/api/reg-doc.js`:94 | not returned by API | P1 | Authenticate reg-doc before enabling service credentials — When the service-role key is configured, `sameOrigin()` is forgeable by any non-browser client and the endpoint validates neither a caller credential nor that `r | `SEC-06` | open-on-GitHub | open (P0) |
| 12 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:30 | not returned by API | P1 | Require a vetted beneficiary for bank expenses — When a caller passes `p_beneficiary_id = NULL`, this branch is skipped and the later bank validation accepts any client-supplied IBAN matching `SA` plus 22 digits. A `can_ | `SEC-IBAN-EXPOSE` | open-on-GitHub | open |
| 13 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:157 | not returned by API | P1 | Refresh beneficiary data when generating recurring expenses — For a bank template linked by `beneficiary_id`, the generator copies the beneficiary name and IBAN snapshot from `v_t.details` without re-reading `portal_bene | `SEC-IBAN-EXPOSE` | open-on-GitHub | open |
| 14 | `audit-output/FINAL_CERTIFICATION.md`:43 | not returned by API | P1 | Apply segregation of duties to administrators — In the inspected disbursement workflow, administrators remain exempt from requester/approver/executor separation while `portal_has_perm` grants them every permission, so on | `PAY-ROLES` | open-on-GitHub | open |
| 15 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:138 | not returned by API | P2 | Preserve exact precision in recurring budget checks — When an expense has fractional currency units, rounding only the prospective recurring amount can admit a request that the authoritative `portal_budget_committed` cal | `AUTHZ-EXPENSE-DEPT (060)` | open-on-GitHub | implemented @135f5af (live) |
| 16 | `db/portal-tests/35_anon_hardening.sql` | not returned by API | P2 | Execute migration 059 in its regression test — The test does not execute either `059-revoke-anon-sensitive-reads.sql` or the standalone migration block; it embeds a second copy of the expected `REVOKE` statements after s | `SEC-06` | outdated | open (P0) |
| 17 | `audit-output/CODEX_HANDOFF.md` | not returned by API | P2 | Remove the unsupported completed-review claim — The previous Codex review covered commit `3b1bfc4927`, while migration 060, the reg-doc remediation, and their tests were added afterward, so labeling the source review “CO | `SEC-06` | outdated | open (P0) |
| 18 | `functions/api/reg-doc.js` | not returned by API | P1 | Match the upload allowlist to the registration form — When the server upload path is configured, every supplier registration fails because the form's required document IDs include `chamber`, `natl_addr`, and `iban_cert`, | `SEC-06 (reg-doc allowlist)` | outdated | open — align reg-doc allowlist to registration form (Stage-2, SEC-06 area) |
| 19 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:138 | not returned by API | P1 | Serialize budget checks per department and fiscal year — When a recurring run races with an interactive `portal_create_expense` call for the same department, neither path locks the budget row before reading committed spe | `FISCAL-POLICY` | open-on-GitHub | open |
| 20 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:145 | not returned by API | P1 | Keep over-budget recurring obligations pending — When a recurring obligation is temporarily over budget, this branch advances `next_run` to the next future occurrence without creating a blocked request, an audit entry, o | `RECUR-BLOCKED / S8-RECUR-BLOCK` | open-on-GitHub | open |
| 21 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:57 | not returned by API | P2 | Reject direct expenses for inactive departments — When a user's assigned department has been deactivated, or an administrator selects a deactivated department, this existence-only check still permits a direct expense and | `AUTHZ-EXPENSE-DEPT (060)` | open-on-GitHub | implemented @135f5af (live) |
| 22 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:61 | not returned by API | P2 | Validate IBAN checksums instead of only their shape — For bank expenses, this regex accepts every `SA` followed by 22 digits, including mistyped or impossible IBANs whose ISO 13616 MOD-97 checksum fails. The same weak va | `TEST-COVERAGE` | open-on-GitHub | open (Stage-2 test) |
| 23 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:21 | not returned by API | P2 | Provide a safe rollback procedure for migration 060 — Migration 060 replaces two live financial function bodies but the migration and remediation register provide no rollback artifact or procedure. Restoring the previous | `AUTHZ-EXPENSE-DEPT (060)` | open-on-GitHub | implemented @135f5af (live) |
| 24 | `audit-output/PRODUCTION_BLOCKERS.md` | not returned by API | P2 | Reconcile migration 060's production deployment status — The production-blocker checklist still instructs operators to apply migration 060, while the same document later says it is already applied and verified; `FINDINGS | `DOC-ACCURACY` | outdated | addressed via G0/G0-R/G0-F reconciliation |
| 25 | `db/portal-tests/file-guard.test.mjs`:89 | not returned by API | P2 | Exercise the successful reg-doc upload path in tests — All endpoint cases in this suite expect rejection before the storage request, so none executes a successful upload or observes subsequent fetches. Reintroducing the | `SEC-06` | open-on-GitHub | open (P0) |
| 26 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Validate stored objects before counting documents — When migration 062 is applied, any authenticated requester can call the granted `portal_attach_document` RPC with an arbitrary nonempty `p_storage_key`, plausible MIME | `DOC-RECEIPT` | outdated | open (P0) |
| 27 | `db/portal-migrations/062-request-documents.sql`:331 | not returned by API | P1 | Gate recurring disbursements on supporting documents — When `expense_docs_required` is enabled, migration 062 only changes the interactive `portal_create_expense`/`portal_submit_expense` path. The service-role-only `port | `RECUR-BLOCKED / S8-RECUR-BLOCK` | open-on-GitHub | open |
| 28 | `db/portal-migrations/062-request-documents.sql`:387 | not returned by API | P1 | Add the draft upload flow before applying migration 062 — When 062 is applied with its default `expense_docs_required=1`, `portal_create_expense` now returns a draft with `needs_documents=true`, but `purchase-portal.html | `DOC-UI (draft→submit)` | open-on-GitHub | fixed @b3d949f |
| 29 | `db/portal-migrations/062-request-documents.sql`:131 | not returned by API | P1 | Collect the manual-IBAN reason before invoking the RPC — For a bank disbursement entered without a beneficiary-master record, this new check requires `p_iban_reason`, but the existing form neither collects that reason no | `SEC-IBAN-EXPOSE` | open-on-GitHub | open |
| 30 | `db/portal-migrations/062-request-documents.sql`:269 | not returned by API | P1 | Restrict document replacement to returned requests — For any request visible to its requester, an editor, or an admin, `portal_replace_document` performs no request-status check before deactivating the old evidence and i | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 31 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Authorize payment-specific document attachments — When callers provide any non-null `p_payment_id`, this conditional skips both the requester/editor authorization and the draft/returned state restriction. Any authenticat | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | outdated | open |
| 32 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Preserve returned documents until guarded resubmission — For a returned direct-expense request, this condition permits physical deletion of the previously reviewed document. The existing `portal_resubmit_request` in migr | `CDX4-SUBMIT-AUTHZ` | outdated | fixed @b43ae88 |
| 33 | `functions/api/portal-config.js` | not returned by API | P1 | Refuse requests without a verified deployment branch — If `CF_PAGES_BRANCH` is absent or unavailable as a runtime binding, `!!branch` is false and the function classifies the request as production. A preview with product | `PAGES-DEPLOY` | outdated | open |
| 34 | `functions/api/portal-config.js` | not returned by API | P2 | Parse the Supabase origin canonically — In a preview, a production URL such as `https://user@mwbjoysuybgbrvfrprex.supabase.co` still resolves to the production host, but this prefix regex returns `null`, so the productio | `AI-PROXY-ABUSE` | outdated | open |
| 35 | `purchase-portal.html`:2573 | not returned by API | P1 | Preserve a boot path on GitHub Pages — The repository's `.github/workflows/pages.yml` publishes the repository root as a static GitHub Pages artifact and does not deploy Pages Functions, so `/api/portal-config` returns a | `PAGES-DEPLOY` | open-on-GitHub | open |
| 36 | `functions/api/portal-config.js` | not returned by API | P1 | Move the supplier frontend onto environment-aware config — The new endpoint isolates only `purchase-portal.html`; `supplier-quote.html:121-122` still embeds the same production project URL and anon key. A staging preview | `SUPPLIER-ENV` | outdated | open |
| 37 | `scripts/env-guard.mjs` | not returned by API | P2 | Parse guard URLs canonically before comparing hosts — For the documented E2E `--url` mode, `https://user@mwbjoysuybgbrvfrprex.supabase.co` is a valid URL whose host is the production project, but `refOf` fails the anchor | `CFG-ENV` | outdated | fixed @8cd7890 |
| 38 | `db/portal-migrations/062-request-documents.sql`:343 | not returned by API | P1 | Exclude unsubmitted drafts from committed budget — After 062 introduces persistent drafts, `portal_budget_committed` still includes every active `direct_expense` except cancelled/rejected/returned requests, and this subm | `CDX3-BUDGET-DRAFT` | open-on-GitHub | fixed @8cd7890 |
| 39 | `functions/api/portal-doc.js`:34 | not returned by API | P2 | Authorize reqdoc uploads before writing to R2 — For `kind=reqdoc`, the upload endpoint only checks the caller's global `can_create` or `can_edit` permission; it never verifies that `request_id` exists, is a draft/returne | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 40 | `db/portal-migrations/062-request-documents.sql`:244 | not returned by API | P2 | Make removed document objects inaccessible — `portal_remove_document` deletes only the database row and does not remove or tombstone the corresponding R2 object. The GET endpoint authorizes a key solely from the request | `CDX3-REPLACE (remove/GET path)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 41 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Enforce the payment-document setting — Migration 062 adds `payment_docs_required` as a configurable completeness control, but it does not redefine any payment creation, approval, or disbursement transition to read this v | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | outdated | open |
| 42 | `db/portal-migrations/062-request-documents.sql`:267 | not returned by API | P2 | Prevent replacement forks from inactive versions — `portal_replace_document` does not require `v_old.active=true`, so after replacing a document once the caller can invoke the RPC repeatedly on the same inactive original | `AI-PROXY-ABUSE` | open-on-GitHub | open |
| 43 | `scripts/env-guard.mjs`:51 | not returned by API | P2 | Couple the environment guard to the protected command — The guard only validates arguments and exits; the staging plan then runs the migration or E2E command separately, and no workflow or wrapper binds that command to t | `S1-GUARD-COUPLE` | open-on-GitHub | open |
| 44 | `functions/api/portal-config.js`:67 | not returned by API | P1 | Reject privileged keys from the public config response — The endpoint trusts the variable name and returns whatever value is stored in `PORTAL_SUPABASE_ANON_KEY` without checking its JWT `role`/project claims or rejectin | `CFG-ENV` | open-on-GitHub | fixed @8cd7890 |
| 45 | `functions/api/portal-config.js`:94 | not returned by API | P2 | Include required server bindings in boot readiness — The configuration endpoint returns `ok:true` after checking only the browser URL and anon key, even when `PORTAL_SUPABASE_SERVICE_ROLE_KEY` or `QUOTES_BUCKET` is absen | `CFG-ENV` | open-on-GitHub | fixed @8cd7890 |
| 46 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P2 | Use one authoritative document size limit — Migration 062 advertises `doc_max_bytes` and defaults it to 15 MiB, while the only upload endpoint calls `_file-guard.js`, whose hardcoded maximum is 10 MiB and never reads thi | `DOC-SIZE-LIMIT` | outdated | fixed (doc_max_bytes=10MiB aligned) |
| 47 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Validate stored objects before counting documents — When migration 062 is applied, any authenticated requester can call the granted `portal_attach_document` RPC with an arbitrary nonempty `p_storage_key`, plausible MIME | `DOC-RECEIPT` | outdated | open (P0) |
| 48 | `db/portal-migrations/062-request-documents.sql`:331 | not returned by API | P1 | Gate recurring disbursements on supporting documents — When `expense_docs_required` is enabled, migration 062 only changes the interactive `portal_create_expense`/`portal_submit_expense` path. The service-role-only `port | `RECUR-BLOCKED / S8-RECUR-BLOCK` | open-on-GitHub | open |
| 49 | `db/portal-migrations/062-request-documents.sql`:387 | not returned by API | P1 | Add the draft upload flow before applying migration 062 — When 062 is applied with its default `expense_docs_required=1`, `portal_create_expense` now returns a draft with `needs_documents=true`, but `purchase-portal.html | `DOC-UI (draft→submit)` | open-on-GitHub | fixed @b3d949f |
| 50 | `db/portal-migrations/062-request-documents.sql`:131 | not returned by API | P1 | Collect the manual-IBAN reason before invoking the RPC — For a bank disbursement entered without a beneficiary-master record, this new check requires `p_iban_reason`, but the existing form neither collects that reason no | `SEC-IBAN-EXPOSE` | open-on-GitHub | open |
| 51 | `db/portal-migrations/062-request-documents.sql`:269 | not returned by API | P1 | Restrict document replacement to returned requests — For any request visible to its requester, an editor, or an admin, `portal_replace_document` performs no request-status check before deactivating the old evidence and i | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 52 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Authorize payment-specific document attachments — When callers provide any non-null `p_payment_id`, this conditional skips both the requester/editor authorization and the draft/returned state restriction. Any authenticat | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | outdated | open |
| 53 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Preserve returned documents until guarded resubmission — For a returned direct-expense request, this condition permits physical deletion of the previously reviewed document. The existing `portal_resubmit_request` in migr | `CDX4-SUBMIT-AUTHZ` | outdated | fixed @b43ae88 |
| 54 | `functions/api/portal-config.js` | not returned by API | P1 | Refuse requests without a verified deployment branch — If `CF_PAGES_BRANCH` is absent or unavailable as a runtime binding, `!!branch` is false and the function classifies the request as production. A preview with product | `PAGES-DEPLOY` | outdated | open |
| 55 | `functions/api/portal-config.js` | not returned by API | P2 | Parse the Supabase origin canonically — In a preview, a production URL such as `https://user@mwbjoysuybgbrvfrprex.supabase.co` still resolves to the production host, but this prefix regex returns `null`, so the productio | `AI-PROXY-ABUSE` | outdated | open |
| 56 | `purchase-portal.html`:2573 | not returned by API | P1 | Preserve a boot path on GitHub Pages — The repository's `.github/workflows/pages.yml` publishes the repository root as a static GitHub Pages artifact and does not deploy Pages Functions, so `/api/portal-config` returns a | `PAGES-DEPLOY` | open-on-GitHub | open |
| 57 | `functions/api/portal-config.js` | not returned by API | P1 | Move the supplier frontend onto environment-aware config — The new endpoint isolates only `purchase-portal.html`; `supplier-quote.html:121-122` still embeds the same production project URL and anon key. A staging preview | `SUPPLIER-ENV` | outdated | open |
| 58 | `scripts/env-guard.mjs` | not returned by API | P2 | Parse guard URLs canonically before comparing hosts — For the documented E2E `--url` mode, `https://user@mwbjoysuybgbrvfrprex.supabase.co` is a valid URL whose host is the production project, but `refOf` fails the anchor | `CFG-ENV` | outdated | fixed @8cd7890 |
| 59 | `db/portal-migrations/062-request-documents.sql`:343 | not returned by API | P1 | Exclude unsubmitted drafts from committed budget — After 062 introduces persistent drafts, `portal_budget_committed` still includes every active `direct_expense` except cancelled/rejected/returned requests, and this subm | `CDX3-BUDGET-DRAFT` | open-on-GitHub | fixed @8cd7890 |
| 60 | `functions/api/portal-doc.js`:34 | not returned by API | P2 | Authorize reqdoc uploads before writing to R2 — For `kind=reqdoc`, the upload endpoint only checks the caller's global `can_create` or `can_edit` permission; it never verifies that `request_id` exists, is a draft/returne | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 61 | `db/portal-migrations/062-request-documents.sql`:244 | not returned by API | P2 | Make removed document objects inaccessible — `portal_remove_document` deletes only the database row and does not remove or tombstone the corresponding R2 object. The GET endpoint authorizes a key solely from the request | `CDX3-REPLACE (remove/GET path)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 62 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Enforce the payment-document setting — Migration 062 adds `payment_docs_required` as a configurable completeness control, but it does not redefine any payment creation, approval, or disbursement transition to read this v | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | outdated | open |
| 63 | `db/portal-migrations/062-request-documents.sql`:267 | not returned by API | P2 | Prevent replacement forks from inactive versions — `portal_replace_document` does not require `v_old.active=true`, so after replacing a document once the caller can invoke the RPC repeatedly on the same inactive original | `AI-PROXY-ABUSE` | open-on-GitHub | open |
| 64 | `scripts/env-guard.mjs`:51 | not returned by API | P2 | Couple the environment guard to the protected command — The guard only validates arguments and exits; the staging plan then runs the migration or E2E command separately, and no workflow or wrapper binds that command to t | `S1-GUARD-COUPLE` | open-on-GitHub | open |
| 65 | `functions/api/portal-config.js`:67 | not returned by API | P1 | Reject privileged keys from the public config response — The endpoint trusts the variable name and returns whatever value is stored in `PORTAL_SUPABASE_ANON_KEY` without checking its JWT `role`/project claims or rejectin | `CFG-ENV` | open-on-GitHub | fixed @8cd7890 |
| 66 | `functions/api/portal-config.js`:94 | not returned by API | P2 | Include required server bindings in boot readiness — The configuration endpoint returns `ok:true` after checking only the browser URL and anon key, even when `PORTAL_SUPABASE_SERVICE_ROLE_KEY` or `QUOTES_BUCKET` is absen | `CFG-ENV` | open-on-GitHub | fixed @8cd7890 |
| 67 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P2 | Use one authoritative document size limit — Migration 062 advertises `doc_max_bytes` and defaults it to 15 MiB, while the only upload endpoint calls `_file-guard.js`, whose hardcoded maximum is 10 MiB and never reads thi | `DOC-SIZE-LIMIT` | outdated | fixed (doc_max_bytes=10MiB aligned) |
| 68 | `db/portal-migrations/062-request-documents.sql`:384 | not returned by API | P1 | Wire mandatory documents into direct-expense submission — When `expense_docs_required` uses its new default of `1`, this path returns a draft with `needs_documents` rather than submitting it. In the System-3 direct-disbu | `R1-CANONICAL` | open-on-GitHub | fixed @3861171 |
| 69 | `db/portal-migrations/062-request-documents.sql`:129 | not returned by API | P1 | Collect the required reason for manual bank details — For a bank payment entered without selecting a master beneficiary, the current form collects only the IBAN and account name and sends no `iban_manual_reason` (`purcha | `SEC-IBAN-EXPOSE` | open-on-GitHub | open |
| 70 | `functions/api/portal-doc.js`:33 | not returned by API | P2 | Authorize reqdoc uploads against the target draft — Any active staff member with `can_create` or `can_edit` can POST a `reqdoc` for any syntactically valid request ID; the POST path never verifies that the request exists | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 71 | `purchase-portal.html`:2571 | not returned by API | P2 | Gate the portal behind an accessible bootstrap state — When `/api/portal-config` is slow or stalls, this unbounded fetch leaves the pre-existing portal chrome visible and interactive even though `SB` is still `null`; no | `BOOT-STATES / S10-STATES` | open-on-GitHub | open |
| 72 | `db/portal-migrations/062-request-documents.sql`:268 | not returned by API | P1 | Restrict document replacement to returned requests — `portal_replace_document` is documented as the correction path for a returned request, but it checks only the caller's identity and never checks `v_req.status`. A requ | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | open-on-GitHub | open |
| 73 | `db/portal-migrations/062-request-documents.sql`:178 | not returned by API | P1 | Validate payment ownership and role before attaching documents — When `p_payment_id` is non-null, this conditional skips both the requester/editor authorization and the request-state restriction. Any authenticated user w | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | open-on-GitHub | open |
| 74 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P2 | Make document replacement resilient to duplicate requests — A double-click, network retry, or stale tab can call `portal_replace_document` twice with the same old document ID. This update has no `active` compare-and-set | `CDX3-REPLACE` | outdated | fixed @8cd7890 |
| 75 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Revalidate documents when returned expenses are resubmitted — This function permits every document to be removed while a direct-expense request is `returned`, but migration 062 does not update the existing `portal_resubm | `CDX4-SUBMIT-AUTHZ` | outdated | fixed @b43ae88 |
| 76 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Require a verified uploaded object before attaching metadata — `portal_attach_document` accepts any non-empty `p_storage_key`, and `portal_submit_expense` later treats the resulting active row as sufficient evidence with | `DOC-RECEIPT` | outdated | open (P0) |
| 77 | `db/portal-migrations/061-codex-round2-hardening.sql`:215 | not returned by API | P1 | Apply the document requirement to recurring expenses — After migration 062 enables `expense_docs_required` by default, `portal_recurring_run` still bypasses the new draft/upload/submit path and directly moves each genera | `CDX3-RECURRING-GATE` | open-on-GitHub | fixed @8cd7890 |
| 78 | `db/portal-migrations/061-codex-round2-hardening.sql`:190 | not returned by API | P1 | Surface skipped recurring obligations to finance — When a recurring expense exceeds budget, the job writes an audit event with `request_id = NULL`, advances `next_run`, and creates no payable occurrence. The portal's aud | `RECUR-BLOCKED / S8-RECUR-BLOCK` | open-on-GitHub | open |
| 79 | `db/portal-migrations/062-request-documents.sql`:116 | not returned by API | P2 | Lock the direct-expense department to the user's profile — The backend now rejects any non-admin `p_department_id` that differs from the user's profile, but the direct-expense form still renders every department as an en | `AUTHZ-EXPENSE-DEPT (060)` | open-on-GitHub | implemented @135f5af (live) |
| 80 | `db/portal-migrations/062-request-documents.sql`:384 | not returned by API | P1 | Complete document upload before reporting expense submission — When `expense_docs_required` has its new default value of 1, this branch returns a draft with `needs_documents:true` instead of submitting it. The existing ` | `CDX4-SUBMIT-AUTHZ` | open-on-GitHub | fixed @b43ae88 |
| 81 | `db/portal-migrations/062-request-documents.sql`:129 | not returned by API | P1 | Collect the required reason for manually entered IBANs — For a bank expense entered without selecting a master beneficiary, this new check always rejects the current direct-expense form: `submitExpense()` sends only `iba | `SEC-IBAN-EXPOSE` | open-on-GitHub | open |
| 82 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Authorize and validate payment document attachments — When an authenticated caller supplies any non-null `p_payment_id`, this condition skips the entire requester/editor/admin authorization and draft-state block. The sec | `PAY-ROLES` | outdated | open |
| 83 | `db/portal-migrations/062-request-documents.sql`:268 | not returned by API | P1 | Restrict document replacement to returned requests — The replacement RPC checks the caller but never checks `v_req.status`, despite being documented as the returned-request workflow. A requester, editor, or administrator | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 84 | `db/portal-migrations/062-request-documents.sql`:331 | not returned by API | P1 | Apply the document gate to recurring direct expenses — Enabling `expense_docs_required` here does not cover the scheduled route: the still-active `portal_recurring_run()` in `061-codex-round2-hardening.sql` directly buil | `SCHED-DECOUPLE` | open-on-GitHub | open |
| 85 | `purchase-portal.html`:2582 | not returned by API | P2 | Allow recovery from transient configuration fetch failures — Any one-off network error, malformed gateway response, or temporary 5xx from `/api/portal-config` reaches this catch block and permanently replaces the applica | `BOOT-STATES / S10-STATES` | open-on-GitHub | open |
| 86 | `db/portal-migrations/062-request-documents.sql`:204 | not returned by API | P1 | Verify the stored object before satisfying the evidence gate — The attachment RPC accepts any nonempty caller-supplied `p_storage_key` and trusts the supplied MIME type, size, and checksum without proving that an R2 obje | `DOC-RECEIPT` | open-on-GitHub | open (P0) |
| 87 | `functions/api/portal-doc.js`:33 | not returned by API | P2 | Bind request-document uploads to an authorized draft — Adding `can_create` as sufficient permission for `reqdoc` exposes the R2 upload to every ordinary requester, but the endpoint only checks that `request_id` matches a | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 88 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Enforce the evidence gate when resubmitting returned expenses — This submission RPC accepts only `draft`, even though the same migration explicitly permits document attachment and replacement while a request is `returned | `CDX3-REPLACE` | outdated | fixed @8cd7890 |
| 89 | `db/portal-migrations/062-request-documents.sql`:116 | not returned by API | P2 | Align the department picker with server-enforced ownership — For a non-admin requester, the backend now requires any supplied department to equal the department in the user's profile. The existing direct-expense form sti | `AUTHZ-EXPENSE-DEPT (060)` | open-on-GitHub | implemented @135f5af (live) |
| 90 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Restore the disbursement phase when resubmitting — When `portal_payment_transition` returns a direct-expense payment, it leaves the request in phase `payment`; this returned-request path rebuilds a `disbursement` approva | `CDX4-PHASE-CYCLE` | outdated | fixed @b43ae88 |
| 91 | `purchase-portal.html`:5876 | not returned by API | P1 | Let requesters correct returned expense details — When a direct expense is returned for an incorrect beneficiary, amount, method, IBAN, or purpose, the returned branch renders only the existing header and document center | `SEC-IBAN-EXPOSE` | open-on-GitHub | open |
| 92 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Separate edit permission from submission authority — For any visible draft or returned direct expense, a procurement user with `can_edit` can call `portal_submit_expense` and start a new financial approval cycle even whe | `CDX4-SUBMIT-AUTHZ` | outdated | fixed @b43ae88 |
| 93 | `functions/api/portal-doc.js`:33 | not returned by API | P2 | Authorize reqdoc uploads against the target owner — A user with `can_create` and sector/all visibility can upload `reqdoc` objects for another request in `draft` or `returned` state because the pre-upload check validates | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 94 | `db/portal-migrations/062-request-documents.sql`:329 | not returned by API | P1 | Verify uploaded objects before accepting document evidence — An authenticated requester can call `portal_attach_document` directly with any fabricated key matching `docs/reqdoc/<request_id>/...`; no R2 object, trusted up | `DOC-RECEIPT` | open-on-GitHub | open (P0) |
| 95 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Revalidate master beneficiary data at submission — The draft copies the selected beneficiary's active state, name, account, and IBAN only when it is created, but submission may occur much later and performs no beneficiar | `SEC-IBAN-EXPOSE` | outdated | open |
| 96 | `db/portal-migrations/062-request-documents.sql`:284 | not returned by API | P1 | Prevent requesters from replacing payment evidence — If a direct-expense payment has a document row and finance returns the request, the requester or any `can_edit` user can invoke `portal_replace_document` with that pay | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | open-on-GitHub | open |
| 97 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P1 | Keep finance-view permission read-only — The seeded `fin_officer` role has only `can_see_finance` and is explicitly described as read-only, yet this branch treats that permission as authority to append documents to any v | `PAY-ROLES` | outdated | open |
| 98 | `purchase-portal.html` | not returned by API | P2 | Honor the document-enforcement rollback setting — When operators set `expense_docs_required=0` as the migration's documented fail-safe rollback, the database permits submission without evidence, but the new browser path | `DOC-ROLLBACK-FLAG` | outdated | fixed (pa_expDocsRequired honors 0) |
| 99 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P2 | Charge cross-year drafts to one consistent fiscal year — For a draft created before year-end and submitted afterward, this function checks the current year's budget because `v_year` comes from `now()`, but `portal_budget | `FISCAL-POLICY` | outdated | open |
| 100 | `purchase-portal.html` | not returned by API | P1 | Send only the cycle-aware approval notification — Every direct-expense submission launches both `request/submitted` and `disbursement/pending` notifications. The former calls `notifyPending`, loads the disbursement appro | `CDX4-SUBMIT-AUTHZ` | outdated | fixed @b43ae88 |
| 101 | `db/portal-migrations/062-request-documents.sql`:353 | not returned by API | P1 | Invalidate old approval tokens during resubmission — When a disbursement stage is returned through the portal, its previously emailed token can remain unused. This resubmission deletes and rebuilds the approval chain at | `CDX4-TOKENS` | open-on-GitHub | fixed @b43ae88; disbursement-cycle parity → Stage-2 |
| 102 | `db/portal-migrations/062-request-documents.sql` | not returned by API | P2 | Make draft document removal concurrency-safe — Two clients can read the same document before either obtains the parent request lock; after the first call deletes it, the second call proceeds and this unqualified DELETE a | `CDX3-REPLACE (remove/GET path)` | outdated | fixed @8cd7890/b43ae88 |
