# REVIEW-THREAD TRACEABILITY — GENERATED APPENDIX (G0-R6 · corrected G0-F3/G0-F3A-C)

> Regenerated from GitHub `get_review_comments` (both pages) — **all 102 inline threads**. Each row carries the **GitHub thread ID (`PRRT_…`)** and the **comment URL** so a reviewer can trace it back. **Finding = full comment body** (badge image + "Useful?" footer stripped only); nothing is truncated (G0-F3B). **Reviewed commit is NOT returned by the review-comments API** → not shown as a column rather than left blank/fabricated. `GitHub state` = thread status on GitHub; `Canonical disposition` (+ ledger ID) is authoritative for open/fixed/accepted-risk.

## Generation / validation report (G0-F3 item 6)
- Threads: **102** · unique thread IDs: **102** · blank thread IDs: **0** · dispositions present: **102** · unknown canonical IDs: **0**.
- GitHub state: open-on-GitHub 57 · outdated 45 · resolved 0.
- **Owner accepted-risk mappings** (documentation acceptance only, not security clearance): `OWN-ADMIN` (administrator SoD exception), `OWN-IBAN-MANUAL` (manual-IBAN reason rule — reason UI/RPC implemented), `OWN-MOD97` (IBAN MOD-97 out of scope; SA+22 shape retained).
- **G0-F3C corrections applied:** 059-regression-test threads → `TEST-COVERAGE` (not SEC-06) · manual-IBAN threads → `OWN-IBAN-MANUAL` (not SEC-IBAN-EXPOSE) · recurring beneficiary refresh → `CDX-BENEF-RECUR (061)` (not SEC-IBAN-EXPOSE) · administrator SoD → `OWN-ADMIN` (not PAY-ROLES) · MOD-97 → `OWN-MOD97` (not test coverage) · Supabase URL parse → `CFG-ENV` (not AI-PROXY-ABUSE) · replacement-fork → `CDX3-REPLACE` (not AI-PROXY-ABUSE) · payment-doc authorization `CDX3-ATTACH-PAY` (fixed) kept distinct from payment-doc completeness `PAY-DOCS-COMPLETE` (open) · verified-branch detection → `CFG-ENV` (fixed) kept distinct from GitHub-Pages boot path → `PAGES-DEPLOY` (open).

| # | Thread ID | Comment URL | File:line | Sev | Finding (full body) | Canonical ledger ID | GitHub state | Canonical disposition |
|--|--|--|--|--|--|--|--|--|

| 1 | `PRRT_kwDOSUSj886UXK7A` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664012 | `audit-output/EXECUTIVE_REPORT.md` | P2 | Disclose requester access to beneficiary IBANs — Revise this answer because a low-level requester with `can_create` can read every column of `portal_beneficiaries`, including `iban`, through the `portal_beneficiaries_read` policy in `db/portal-standalone.sql`; this is also acknowledged as the open SEC-03 finding. Telling the non-technical owner that bank details are limited to finance/procurement hides a verified data-exposure condition and can lead to deployment without the documented names-only/server-side redesign. | `SEC-IBAN-EXPOSE` | outdated | open (P1) |
| 2 | `PRRT_kwDOSUSj886UXK7G` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664022 | `db/portal-tests/35_anon_hardening.sql` | P2 | Exercise the revoked anon grants in CI — Seed the four Supabase-style `anon` SELECT grants before applying migration 059, or test the migration independently. In the clean PostgreSQL CI setup, `00_roles.sql` only creates the roles and the standalone schema never grants `anon` table SELECT before the new revoke block, so this check is already false even if all four `REVOKE` statements are deleted; AH1 therefore cannot catch a regression in the hardening it claims to pin. | `TEST-COVERAGE (059 anon regression)` | outdated | open (Stage-2) |
| 3 | `PRRT_kwDOSUSj886UXK7L` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664028 | `db/portal-tests/run.sh` | P2 | Correct the per-suite assertion breakdown — Update the breakdown to match the actual PASS assertions: `11_security.sql` emits 8 rather than 7, while `26_disbursement_core.sql` emits 10 rather than 11. The errors happen to cancel, leaving the 171 headline unchanged, but the certification output misattributes coverage and will give reviewers the wrong evidence when they audit either security or disbursement tests. | `TEST-COVERAGE (suite doc)` | outdated | open (Stage-2) |
| 4 | `PRRT_kwDOSUSj886UXK7N` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664033 | `audit-output/TECHNICAL_REPORT.md` | P2 | Detect audit-log truncation before claiming WORM — Qualify this claim or add an externally anchored checkpoint: `portal_audit_verify()` only recomputes the rows that remain, so a DBA who bypasses the regular trigger and deletes a valid suffix—or the entire chain—gets `ok: true` rather than a detected break. HC3 covers mutation of a middle row only; it does not prove the stated WORM/tamper-detection guarantee for deletion or truncation. | `DOC-ACCURACY (WORM)` | outdated | addressed via 057 hash-chain; tail-truncation checkpoint = Stage-2 |
| 5 | `PRRT_kwDOSUSj886UXK7R` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664037 | `audit-output/API_SECURITY_MATRIX.md` | P1 | Authenticate registration-document uploads — Do not describe `reg-doc.js` as server-key-authenticated: callers provide no credential, and `sameOrigin` is only a forgeable header check for non-browser clients. Anyone can submit a syntactically valid `DG-*` identifier repeatedly, consuming service-role-backed storage; if a real registration ID and document type are known, each successful upload also causes the endpoint to delete the existing files under that prefix as “stale.” The claimed document allowlist is likewise only the broad regex `^[a-z][a-z0-9_]{1,24}$`, so the storage hardening does not make this public write path fail-safe. | `SEC-06 (reg-doc auth)` | outdated | open (P0) |
| 6 | `PRRT_kwDOSUSj886UXK7U` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664040 | `audit-output/API_SECURITY_MATRIX.md` | P2 | Inventory every API handler in the security matrix — Extend the matrix before treating it as the endpoint inventory: it omits handlers including `admin-users.js`, `ai.js`, `invite-supplier.js`, `portal-signup.js`, `portal-supplier-invite.js`, and `verify.js`, while even listing `_pr-shared.js`, which is a helper rather than a handler. These omitted routes include service-role user administration, a server-key-backed AI proxy, and public or invitation flows, so leaving them outside the auth/control review makes the certification's API coverage materially incomplete. | `DOC-ACCURACY (API matrix)` | outdated | addressed via ARTIFACT_INVENTORY_APPENDIX |
| 7 | `PRRT_kwDOSUSj886UXK7Y` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664044 | `audit-output/TECHNICAL_REPORT.md` | P1 | Enforce SoD for administrator accounts — Do not certify triple separation as universal while both transition paths explicitly exempt administrators. `portal_pr_transition` lets an admin requester approve their own request, approve multiple stages, and bypass stage eligibility, while `portal_payment_transition` exempts admins from the requester, prior-approver, and chain-approver execution checks; because `portal_has_perm` treats an admin as holding every permission, one admin can create a direct expense, approve its full chain, and execute the payment. | `OWN-ADMIN` | outdated | accepted-risk (labeled + immutable audit) |
| 8 | `PRRT_kwDOSUSj886UXK7f` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664052 | `audit-output/FINAL_CERTIFICATION.md` | P1 | Bind direct expenses to the requester's department — Account for the missing department authorization in `portal_create_expense`: unlike `portal_create_request`, it accepts any existing client-supplied `p_department_id` and never compares it with the caller's profile or scope. A basic `can_create` user can therefore create a direct expense against another department, route it through that department's data/workflow, and consume that department's budget, contradicting the conclusion that the authorization model is sound. | `AUTHZ-EXPENSE-DEPT (060)` | outdated | implemented @135f5af (live) |
| 9 | `PRRT_kwDOSUSj886UXK7h` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664056 | `audit-output/FINDINGS.md` | P1 | Require approved beneficiaries for bank expenses — Correct the claim that the RPC enforces a vetted IBAN: `portal_create_expense` declares `p_beneficiary_id` optional, and when it is null it accepts any client-supplied IBAN matching the format regex; the UI explicitly exposes this as “manual entry.” A requester can thus route a bank expense to an account that never entered the beneficiary master or its IBAN-change approval process, so the UI display is not the only beneficiary-control gap. | `BENEF-MASTER (053)` | outdated | implemented @053 (live) |
| 10 | `PRRT_kwDOSUSj886UXK7l` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3664664061 | `audit-output/TECHNICAL_REPORT.md` | P2 | Enforce budgets during recurring generation — Qualify the budget-control claim or apply the same check in `portal_recurring_run`. When a recurring template becomes due, that function inserts directly into `portal_requests` and builds the approval chain without invoking `portal_create_expense` or comparing `portal_budget_committed` with `portal_budgets`; consequently even with `budget_enforce=1`, an over-budget recurring expense is still generated. | `AUTHZ-EXPENSE-DEPT (060) recurring-budget` | outdated | implemented @135f5af |
| 11 | `PRRT_kwDOSUSj886UqRFG` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938557 | `functions/api/reg-doc.js`:94 | P1 | Authenticate reg-doc before enabling service credentials — When the service-role key is configured, `sameOrigin()` is forgeable by any non-browser client and the endpoint validates neither a caller credential nor that `regId` belongs to an existing registration. An attacker can therefore choose arbitrary valid `DG-*` prefixes and repeatedly store guard-passing 10 MB objects with service-role authority, causing storage abuse or planting files beneath another registration; removing deletion does not remediate the unauthenticated privileged write. Require a registration-bound signed token or authenticated session, plus upload-rate/quota enforcement, before enabling this path. | `SEC-06 (reg-doc auth)` | open-on-GitHub | open (P0) |
| 12 | `PRRT_kwDOSUSj886UqRFK` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938560 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:30 | P1 | Require a vetted beneficiary for bank expenses — When a caller passes `p_beneficiary_id = NULL`, this branch is skipped and the later bank validation accepts any client-supplied IBAN matching `SA` plus 22 digits. A `can_create` user can consequently bypass the beneficiary master and its two-person IBAN-change workflow, so the claimed approved-beneficiary/IBAN enforcement still depends entirely on approvers spotting the substitution. Bank expenses should require an active beneficiary record and derive the current IBAN exclusively from it. | `BENEF-MASTER (053)` | open-on-GitHub | implemented @053 (live) |
| 13 | `PRRT_kwDOSUSj886UqRFQ` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938563 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:157 | P1 | Refresh beneficiary data when generating recurring expenses — For a bank template linked by `beneficiary_id`, the generator copies the beneficiary name and IBAN snapshot from `v_t.details` without re-reading `portal_beneficiaries`. If that beneficiary is later deactivated or its IBAN is changed through the approved workflow, subsequent recurring requests continue targeting the old account while retaining the beneficiary ID, defeating both deactivation and approved-IBAN enforcement. Revalidate the active beneficiary and rebuild bank details from its current approved record at execution time. | `CDX-BENEF-RECUR (061)` | open-on-GitHub | implemented @061 (live) |
| 14 | `PRRT_kwDOSUSj886UqRFW` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938572 | `audit-output/FINAL_CERTIFICATION.md`:43 | P1 | Apply segregation of duties to administrators — In the inspected disbursement workflow, administrators remain exempt from requester/approver/executor separation while `portal_has_perm` grants them every permission, so one compromised or malicious admin can create a cross-department expense, approve it, and execute its payment alone. Operational recommendations such as keeping two admins do not enforce that another actor participates; require distinct principals for financial approval and execution regardless of the admin role before certifying the workflow as production-ready. | `OWN-ADMIN` | open-on-GitHub | accepted-risk (labeled + immutable audit) |
| 15 | `PRRT_kwDOSUSj886UqRFc` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938579 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:138 | P2 | Preserve exact precision in recurring budget checks — When an expense has fractional currency units, rounding only the prospective recurring amount can admit a request that the authoritative `portal_budget_committed` calculation places over budget. For example, with 15% VAT, no prior commitment, amount `100.34`, and budget `115.20`, this check compares `115` and accepts, but the inserted request contributes `115.391`. Use the same unrounded expression as `portal_budget_committed` and round only values displayed in messages. | `AUTHZ-EXPENSE-DEPT (060) precision` | open-on-GitHub | implemented @135f5af |
| 16 | `PRRT_kwDOSUSj886UqRFg` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938584 | `db/portal-tests/35_anon_hardening.sql` | P2 | Execute migration 059 in its regression test — The test does not execute either `059-revoke-anon-sensitive-reads.sql` or the standalone migration block; it embeds a second copy of the expected `REVOKE` statements after seeding grants. Deleting or breaking the shipped migration therefore leaves AH0–AH2 green because the test's private copy still performs the fix, contrary to the claim that this regression test fails without the remediation. Seed the grants before loading the actual migration artifact and assert its resulting privileges. | `TEST-COVERAGE (059 regression)` | outdated | open (Stage-2) |
| 17 | `PRRT_kwDOSUSj886UqRFl` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938589 | `audit-output/CODEX_HANDOFF.md` | P2 | Remove the unsupported completed-review claim — The previous Codex review covered commit `3b1bfc4927`, while migration 060, the reg-doc remediation, and their tests were added afterward, so labeling the source review “COMPLETE” and saying the new fixes were reviewed overstates the evidence behind the production verdict. This handoff should distinguish findings from the earlier review from unreviewed remediation and must not treat the current head as independently certified until that review actually occurs. | `DOC-ACCURACY (handoff claim)` | outdated | addressed |
| 18 | `PRRT_kwDOSUSj886UqRFu` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938600 | `functions/api/reg-doc.js` | P1 | Match the upload allowlist to the registration form — When the server upload path is configured, every supplier registration fails because the form's required document IDs include `chamber`, `natl_addr`, and `iban_cert`, while this allowlist instead contains nonexistent form IDs `iban` and `address`. Those required uploads receive HTTP 400, which deliberately has no client fallback, and the form then aborts because required documents are missing; all optional IDs are also rejected. Build the allowlist from the actual `REQUIRED_DOCS` and `OPTIONAL_DOCS` IDs and test every legitimate type. | `SEC-06 (reg-doc allowlist)` | outdated | open (P0) |
| 19 | `PRRT_kwDOSUSj886UqRF2` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938611 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:138 | P1 | Serialize budget checks per department and fiscal year — When a recurring run races with an interactive `portal_create_expense` call for the same department, neither path locks the budget row before reading committed spending. Both transactions can observe the same pre-transaction commitment, independently pass the cap, insert their requests, and then commit a combined total above the enforced budget. Acquire the same budget-row or transaction-scoped advisory lock in both creation paths before calculating `portal_budget_committed`; the current sequential tests do not exercise this race. | `AUTHZ-EXPENSE-DEPT (060) serialize` | open-on-GitHub | implemented @135f5af |
| 20 | `PRRT_kwDOSUSj886UqRF7` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938620 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:145 | P1 | Keep over-budget recurring obligations pending — When a recurring obligation is temporarily over budget, this branch advances `next_run` to the next future occurrence without creating a blocked request, an audit entry, or a durable notification identifying the skipped template. The current rent/payroll/subscription occurrence is therefore permanently discarded rather than held for budget approval, and the cron response exposes only an aggregate skipped count. Preserve the due occurrence in a blocked state and emit a durable auditable alert instead of consuming its schedule. | `RECUR-BLOCKED / S8-RECUR-BLOCK` | open-on-GitHub | open |
| 21 | `PRRT_kwDOSUSj886UqRGC` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938628 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:57 | P2 | Reject direct expenses for inactive departments — When a user's assigned department has been deactivated, or an administrator selects a deactivated department, this existence-only check still permits a direct expense and builds its approval chain against the closed department. The function it claims to mirror, `portal_create_request`, explicitly requires `portal_departments.active`; apply the same predicate here so closing a department actually prevents new financial commitments. | `AUTHZ-EXPENSE-DEPT (060)` | open-on-GitHub | implemented @135f5af |
| 22 | `PRRT_kwDOSUSj886UqRGK` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938635 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:61 | P2 | Validate IBAN checksums instead of only their shape — For bank expenses, this regex accepts every `SA` followed by 22 digits, including mistyped or impossible IBANs whose ISO 13616 MOD-97 checksum fails. The same weak values can consequently be treated as approved bank details and proceed through the financial workflow until the bank rejects them or an operator manually notices. Normalize the value and validate the Saudi IBAN checksum, with regression tests for valid and one-digit-corrupted accounts. | `OWN-MOD97` | open-on-GitHub | accepted-risk (SA+22 shape retained) |
| 23 | `PRRT_kwDOSUSj886UqRGR` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938642 | `db/portal-migrations/060-authz-expense-dept-recurring-budget.sql`:21 | P2 | Provide a safe rollback procedure for migration 060 — Migration 060 replaces two live financial function bodies but the migration and remediation register provide no rollback artifact or procedure. Restoring the previous definitions from migrations 053 and 055 would reopen the cross-department authorization defect and remove recurring-budget enforcement, so operators cannot safely revert if this deployment causes an unrelated production regression. Supply a reviewed rollback that preserves the security invariants, or an explicit fail-closed procedure that disables the affected RPCs while a forward fix is deployed. | `MIG-ROLLBACK-DOC` | open-on-GitHub | open (Stage-2 doc) |
| 24 | `PRRT_kwDOSUSj886UqRGV` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938647 | `audit-output/PRODUCTION_BLOCKERS.md` | P2 | Reconcile migration 060's production deployment status — The production-blocker checklist still instructs operators to apply migration 060, while the same document later says it is already applied and verified; `FINDINGS.md` likewise says both “applied live” and “live-apply pending.” This contradiction makes the release gate and audited database state indeterminate, especially because the live claims are not independently reproducible from the repository. Record one verified deployment state consistently across the certification package and move any already-completed action out of the pending checklist. | `DOC-ACCURACY (060 status)` | outdated | addressed via G0-01 (060 live-verified) |
| 25 | `PRRT_kwDOSUSj886UqRGc` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3671938657 | `db/portal-tests/file-guard.test.mjs`:89 | P2 | Exercise the successful reg-doc upload path in tests — All endpoint cases in this suite expect rejection before the storage request, so none executes a successful upload or observes subsequent fetches. Reintroducing the previous list-and-DELETE cleanup would therefore leave every test green even though removal of that destructive behavior is presented as the prior P1 remediation. Mock `fetch`, perform a valid successful upload, and assert exactly one randomized object POST with no list or DELETE request. | `TEST-COVERAGE (reg-doc path)` | open-on-GitHub | open (Stage-2) |
| 26 | `PRRT_kwDOSUSj886UrXNh` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364202 | `db/portal-migrations/062-request-documents.sql` | P1 | Validate stored objects before counting documents — When migration 062 is applied, any authenticated requester can call the granted `portal_attach_document` RPC with an arbitrary nonempty `p_storage_key`, plausible MIME type, and null size; the function neither validates the `docs/reqdoc/<request>/...` namespace nor verifies that an R2 object exists. `portal_submit_expense` then counts this row as supporting evidence, so a fabricated key satisfies the mandatory-document control without any uploaded document. Route attachment through the upload service or validate an independently issued/uploaded key before inserting the row. | `DOC-RECEIPT` | outdated | open (P0) |
| 27 | `PRRT_kwDOSUSj886UrXNo` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364208 | `db/portal-migrations/062-request-documents.sql`:331 | P1 | Gate recurring disbursements on supporting documents — When `expense_docs_required` is enabled, migration 062 only changes the interactive `portal_create_expense`/`portal_submit_expense` path. The service-role-only `portal_recurring_run` retained from migration 061 still inserts `direct_expense` requests, builds their chains, and changes them to `in_review` with no `portal_request_documents` row, so scheduled disbursements continue to bypass the new release-blocking evidence requirement. | `CDX3-RECURRING-GATE` | open-on-GitHub | fixed @8cd7890 |
| 28 | `PRRT_kwDOSUSj886UrXNr` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364213 | `db/portal-migrations/062-request-documents.sql`:387 | P1 | Add the draft upload flow before applying migration 062 — When 062 is applied with its default `expense_docs_required=1`, `portal_create_expense` now returns a draft with `needs_documents=true`, but `purchase-portal.html:5655-5661` still treats that response as a submitted request and contains no calls to `reqdoc`, `portal_attach_document`, or `portal_submit_expense`. Every successful UI submission therefore remains a draft with no approval chain, while the UI reports success and sends pending-approval notifications; the frontend must implement draft creation, upload/attach, and explicit submission before this migration can be deployed. | `DOC-UI (draft→submit)` | open-on-GitHub | fixed @b3d949f |
| 29 | `PRRT_kwDOSUSj886UrXNw` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364221 | `db/portal-migrations/062-request-documents.sql`:131 | P1 | Collect the manual-IBAN reason before invoking the RPC — For a bank disbursement entered without a beneficiary-master record, this new check requires `p_iban_reason`, but the existing form neither collects that reason nor adds `iban_manual_reason` to `p_details` (`purchase-portal.html:5644-5657`). Because the compatibility wrapper derives the argument solely from `p_details`, every manual bank-transfer request now fails after 062 is applied even though that input mode remains offered by the UI. | `OWN-IBAN-MANUAL` | open-on-GitHub | accepted-risk; reason UI/RPC implemented @b3d949f |
| 30 | `PRRT_kwDOSUSj886UrXNz` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364226 | `db/portal-migrations/062-request-documents.sql`:269 | P1 | Restrict document replacement to returned requests — For any request visible to its requester, an editor, or an admin, `portal_replace_document` performs no request-status check before deactivating the old evidence and inserting a replacement. Consequently the requester can replace supporting evidence while the request is under review or even after approval/payment, contradicting the migration's immutable-history invariant and changing the active dossier after decisions were made; require the parent request to be in the intended returned state. | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 31 | `PRRT_kwDOSUSj886UrXN4` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364231 | `db/portal-migrations/062-request-documents.sql` | P1 | Authorize payment-specific document attachments — When callers provide any non-null `p_payment_id`, this conditional skips both the requester/editor authorization and the draft/returned state restriction. Any authenticated user who can merely see `p_request_id` can therefore insert a document for any existing payment ID, including one belonging to another request, because the function also never verifies that the payment belongs to the supplied request or that the caller can manage payment evidence. | `CDX3-ATTACH-PAY` | outdated | fixed @8cd7890 |
| 32 | `PRRT_kwDOSUSj886UrXOK` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364263 | `db/portal-migrations/062-request-documents.sql` | P1 | Preserve returned documents until guarded resubmission — For a returned direct-expense request, this condition permits physical deletion of the previously reviewed document. The existing `portal_resubmit_request` in migration 050 then moves the request straight back to `in_review` without checking `expense_docs_required`, so the requester can delete the last supporting document and resubmit with zero evidence. Returned requests should retain prior versions, and the resubmission RPC must enforce at least one active valid document. | `DOC-RESUBMIT-GATE (062)` | outdated | implemented (062, not applied) |
| 33 | `PRRT_kwDOSUSj886UrXOS` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364272 | `functions/api/portal-config.js` | P1 | Refuse requests without a verified deployment branch — If `CF_PAGES_BRANCH` is absent or unavailable as a runtime binding, `!!branch` is false and the function classifies the request as production. A preview with production-scoped Supabase values can then receive `{ok:true}` for the production project, bypassing the exact preview refusal this endpoint is meant to enforce. Fail closed when the deployment environment cannot be positively identified rather than treating a missing marker as production. | `CFG-ENV (verified branch)` | outdated | fixed @8cd7890 |
| 34 | `PRRT_kwDOSUSj886UrXOW` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364280 | `functions/api/portal-config.js` | P2 | Parse the Supabase origin canonically — In a preview, a production URL such as `https://user@mwbjoysuybgbrvfrprex.supabase.co` still resolves to the production host, but this prefix regex returns `null`, so the production-ref check does not reject it and the frontend boots against production. Parse the URL with `new URL(...)`, reject credentials and unexpected paths/ports, and compare the canonical hostname to the production hostname. | `CFG-ENV (config URL parse)` | outdated | fixed @8cd7890 |
| 35 | `PRRT_kwDOSUSj886UrXOZ` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364282 | `purchase-portal.html`:2573 | P1 | Preserve a boot path on GitHub Pages — The repository's `.github/workflows/pages.yml` publishes the repository root as a static GitHub Pages artifact and does not deploy Pages Functions, so `/api/portal-config` returns a static 404 there. Previously this page could initialize its direct Supabase client; after this change every GitHub Pages deployment deterministically enters the configuration-error screen and never boots. Either stop publishing this portal on that target or provide configuration through a mechanism available on static hosting. | `PAGES-DEPLOY` | open-on-GitHub | open |
| 36 | `PRRT_kwDOSUSj886UrXOf` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364290 | `functions/api/portal-config.js` | P1 | Move the supplier frontend onto environment-aware config — The new endpoint isolates only `purchase-portal.html`; `supplier-quote.html:121-122` still embeds the same production project URL and anon key. A staging preview's `portal-supplier-invite` generates a same-origin preview link, but opening that link makes the supplier page query production directly, so supplier-flow E2E still touches production and staging tokens fail or can collide with production data. Route every System-3 frontend, including the supplier page, through the environment-aware configuration. | `SUPPLIER-ENV` | outdated | open |
| 37 | `PRRT_kwDOSUSj886UrXOl` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364297 | `scripts/env-guard.mjs` | P2 | Parse guard URLs canonically before comparing hosts — For the documented E2E `--url` mode, `https://user@mwbjoysuybgbrvfrprex.supabase.co` is a valid URL whose host is the production project, but `refOf` fails the anchored regex, compares the entire lowercased string to `PROD_REF`, and exits successfully with `--confirm STAGING`. This makes the production guard bypassable by URL syntax; use `new URL(s).hostname` and require an exact canonical Supabase project hostname. | `CFG-ENV (env-guard)` | outdated | fixed @8cd7890 |
| 38 | `PRRT_kwDOSUSj886UrXOp` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364301 | `db/portal-migrations/062-request-documents.sql`:343 | P1 | Exclude unsubmitted drafts from committed budget — After 062 introduces persistent drafts, `portal_budget_committed` still includes every active `direct_expense` except cancelled/rejected/returned requests, and this submission check explicitly observes the current draft in the committed total. A user with `can_create` can therefore create abandoned drafts that reserve budget indefinitely—even without uploading or submitting—and eventually prevent legitimate expenses when `budget_enforce=1`; exclude unsubmitted drafts from commitment or provide an expiry/cancellation mechanism with an intentional reservation model. | `CDX3-BUDGET-DRAFT` | open-on-GitHub | fixed @8cd7890 |
| 39 | `PRRT_kwDOSUSj886UrXOu` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364306 | `functions/api/portal-doc.js`:34 | P2 | Authorize reqdoc uploads before writing to R2 — For `kind=reqdoc`, the upload endpoint only checks the caller's global `can_create` or `can_edit` permission; it never verifies that `request_id` exists, is a draft/returned request, is visible to the caller, or belongs to the requester. Any ordinary creator can consequently write unlimited 10 MB orphan objects under arbitrary request IDs even though the later attachment RPC rejects them, consuming storage without an associated record or cleanup path. | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 40 | `PRRT_kwDOSUSj886UrXOx` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364310 | `db/portal-migrations/062-request-documents.sql`:244 | P2 | Make removed document objects inaccessible — `portal_remove_document` deletes only the database row and does not remove or tombstone the corresponding R2 object. The GET endpoint authorizes a key solely from the request ID embedded in its path and never checks for an active `portal_request_documents` row, so anyone retaining the returned key can continue previewing a supposedly removed sensitive upload, and the object persists indefinitely in storage. | `DOC-API (reqdoc GET row-exists)` | open-on-GitHub | fixed @b43ae88 |
| 41 | `PRRT_kwDOSUSj886UrXO1` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364315 | `db/portal-migrations/062-request-documents.sql` | P1 | Enforce the payment-document setting — Migration 062 adds `payment_docs_required` as a configurable completeness control, but it does not redefine any payment creation, approval, or disbursement transition to read this value or require a payment-linked document. Setting it to `1` therefore has no effect, allowing procurement-linked payments to proceed without evidence while operators reasonably believe the new control is enabled; either implement the server-side transition check or omit the nonfunctional setting until the enforcing migration exists. | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | outdated | open |
| 42 | `PRRT_kwDOSUSj886UrXO6` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364322 | `db/portal-migrations/062-request-documents.sql`:267 | P2 | Prevent replacement forks from inactive versions — `portal_replace_document` does not require `v_old.active=true`, so after replacing a document once the caller can invoke the RPC repeatedly on the same inactive original. Each call inserts another active row with the same `version` and `supersedes_id`, producing forked histories with multiple active successor documents instead of the claimed linear version chain; lock and reject inactive source versions or enforce uniqueness for each superseded version. | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 43 | `PRRT_kwDOSUSj886UrXO-` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364326 | `scripts/env-guard.mjs`:51 | P2 | Couple the environment guard to the protected command — The guard only validates arguments and exits; the staging plan then runs the migration or E2E command separately, and no workflow or wrapper binds that command to the checked target. A changed variable, copied command, or different CLI target after the successful check can therefore apply 062 to production despite the displayed guard success. Execute the protected operation from the guard/wrapper using the already validated canonical target, rather than treating a prior independent process as an enforcement boundary. | `S1-GUARD-COUPLE` | open-on-GitHub | open |
| 44 | `PRRT_kwDOSUSj886UrXPB` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364329 | `functions/api/portal-config.js`:67 | P1 | Reject privileged keys from the public config response — The endpoint trusts the variable name and returns whatever value is stored in `PORTAL_SUPABASE_ANON_KEY` without checking its JWT `role`/project claims or rejecting Supabase secret-key formats. If an operator accidentally pastes the service-role or another privileged key into this similarly named setting, `/api/portal-config` publishes it to every unauthenticated visitor despite the stated guarantee that service-role material is never returned; validate that the key is explicitly an anon/publishable key for the configured project before responding. | `CFG-ENV` | open-on-GitHub | fixed @8cd7890 |
| 45 | `PRRT_kwDOSUSj886UrXPF` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364334 | `functions/api/portal-config.js`:94 | P2 | Include required server bindings in boot readiness — The configuration endpoint returns `ok:true` after checking only the browser URL and anon key, even when `PORTAL_SUPABASE_SERVICE_ROLE_KEY` or `QUOTES_BUCKET` is absent. In that environment the portal boots and permits login, but the new mandatory supporting-document endpoint immediately returns 503, making it impossible to complete the draft-to-submit flow; include boolean readiness checks for the required server-side bindings and fail boot without exposing their values. | `CFG-ENV` | open-on-GitHub | fixed @8cd7890 |
| 46 | `PRRT_kwDOSUSj886UrXPJ` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364340 | `db/portal-migrations/062-request-documents.sql` | P2 | Use one authoritative document size limit — Migration 062 advertises `doc_max_bytes` and defaults it to 15 MiB, while the only upload endpoint calls `_file-guard.js`, whose hardcoded maximum is 10 MiB and never reads this setting. Files between 10 and 15 MiB are therefore rejected despite being permitted by the database configuration, and lowering the database value lets the endpoint store a now-disallowed object before attachment fails; enforce the same environment/database-derived limit before writing R2 and when inserting metadata. | `DOC-SIZE-LIMIT` | outdated | fixed (doc_max_bytes=10MiB) |
| 47 | `PRRT_kwDOSUSj886UrXRu` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364584 | `db/portal-migrations/062-request-documents.sql` | P1 | Validate stored objects before counting documents — When migration 062 is applied, any authenticated requester can call the granted `portal_attach_document` RPC with an arbitrary nonempty `p_storage_key`, plausible MIME type, and null size; the function neither validates the `docs/reqdoc/<request>/...` namespace nor verifies that an R2 object exists. `portal_submit_expense` then counts this row as supporting evidence, so a fabricated key satisfies the mandatory-document control without any uploaded document. Route attachment through the upload service or validate an independently issued/uploaded key before inserting the row. | `DOC-RECEIPT` | outdated | open (P0) |
| 48 | `PRRT_kwDOSUSj886UrXRz` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364591 | `db/portal-migrations/062-request-documents.sql`:331 | P1 | Gate recurring disbursements on supporting documents — When `expense_docs_required` is enabled, migration 062 only changes the interactive `portal_create_expense`/`portal_submit_expense` path. The service-role-only `portal_recurring_run` retained from migration 061 still inserts `direct_expense` requests, builds their chains, and changes them to `in_review` with no `portal_request_documents` row, so scheduled disbursements continue to bypass the new release-blocking evidence requirement. | `CDX3-RECURRING-GATE` | open-on-GitHub | fixed @8cd7890 |
| 49 | `PRRT_kwDOSUSj886UrXR2` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364597 | `db/portal-migrations/062-request-documents.sql`:387 | P1 | Add the draft upload flow before applying migration 062 — When 062 is applied with its default `expense_docs_required=1`, `portal_create_expense` now returns a draft with `needs_documents=true`, but `purchase-portal.html:5655-5661` still treats that response as a submitted request and contains no calls to `reqdoc`, `portal_attach_document`, or `portal_submit_expense`. Every successful UI submission therefore remains a draft with no approval chain, while the UI reports success and sends pending-approval notifications; the frontend must implement draft creation, upload/attach, and explicit submission before this migration can be deployed. | `DOC-UI (draft→submit)` | open-on-GitHub | fixed @b3d949f |
| 50 | `PRRT_kwDOSUSj886UrXR_` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364605 | `db/portal-migrations/062-request-documents.sql`:131 | P1 | Collect the manual-IBAN reason before invoking the RPC — For a bank disbursement entered without a beneficiary-master record, this new check requires `p_iban_reason`, but the existing form neither collects that reason nor adds `iban_manual_reason` to `p_details` (`purchase-portal.html:5644-5657`). Because the compatibility wrapper derives the argument solely from `p_details`, every manual bank-transfer request now fails after 062 is applied even though that input mode remains offered by the UI. | `OWN-IBAN-MANUAL` | open-on-GitHub | accepted-risk; reason UI/RPC implemented @b3d949f |
| 51 | `PRRT_kwDOSUSj886UrXSC` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364615 | `db/portal-migrations/062-request-documents.sql`:269 | P1 | Restrict document replacement to returned requests — For any request visible to its requester, an editor, or an admin, `portal_replace_document` performs no request-status check before deactivating the old evidence and inserting a replacement. Consequently the requester can replace supporting evidence while the request is under review or even after approval/payment, contradicting the migration's immutable-history invariant and changing the active dossier after decisions were made; require the parent request to be in the intended returned state. | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 52 | `PRRT_kwDOSUSj886UrXSH` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364621 | `db/portal-migrations/062-request-documents.sql` | P1 | Authorize payment-specific document attachments — When callers provide any non-null `p_payment_id`, this conditional skips both the requester/editor authorization and the draft/returned state restriction. Any authenticated user who can merely see `p_request_id` can therefore insert a document for any existing payment ID, including one belonging to another request, because the function also never verifies that the payment belongs to the supplied request or that the caller can manage payment evidence. | `CDX3-ATTACH-PAY` | outdated | fixed @8cd7890 |
| 53 | `PRRT_kwDOSUSj886UrXSJ` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364625 | `db/portal-migrations/062-request-documents.sql` | P1 | Preserve returned documents until guarded resubmission — For a returned direct-expense request, this condition permits physical deletion of the previously reviewed document. The existing `portal_resubmit_request` in migration 050 then moves the request straight back to `in_review` without checking `expense_docs_required`, so the requester can delete the last supporting document and resubmit with zero evidence. Returned requests should retain prior versions, and the resubmission RPC must enforce at least one active valid document. | `DOC-RESUBMIT-GATE (062)` | outdated | implemented (062, not applied) |
| 54 | `PRRT_kwDOSUSj886UrXSN` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364630 | `functions/api/portal-config.js` | P1 | Refuse requests without a verified deployment branch — If `CF_PAGES_BRANCH` is absent or unavailable as a runtime binding, `!!branch` is false and the function classifies the request as production. A preview with production-scoped Supabase values can then receive `{ok:true}` for the production project, bypassing the exact preview refusal this endpoint is meant to enforce. Fail closed when the deployment environment cannot be positively identified rather than treating a missing marker as production. | `CFG-ENV (verified branch)` | outdated | fixed @8cd7890 |
| 55 | `PRRT_kwDOSUSj886UrXSQ` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364634 | `functions/api/portal-config.js` | P2 | Parse the Supabase origin canonically — In a preview, a production URL such as `https://user@mwbjoysuybgbrvfrprex.supabase.co` still resolves to the production host, but this prefix regex returns `null`, so the production-ref check does not reject it and the frontend boots against production. Parse the URL with `new URL(...)`, reject credentials and unexpected paths/ports, and compare the canonical hostname to the production hostname. | `CFG-ENV (config URL parse)` | outdated | fixed @8cd7890 |
| 56 | `PRRT_kwDOSUSj886UrXSV` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364639 | `purchase-portal.html`:2573 | P1 | Preserve a boot path on GitHub Pages — The repository's `.github/workflows/pages.yml` publishes the repository root as a static GitHub Pages artifact and does not deploy Pages Functions, so `/api/portal-config` returns a static 404 there. Previously this page could initialize its direct Supabase client; after this change every GitHub Pages deployment deterministically enters the configuration-error screen and never boots. Either stop publishing this portal on that target or provide configuration through a mechanism available on static hosting. | `PAGES-DEPLOY` | open-on-GitHub | open |
| 57 | `PRRT_kwDOSUSj886UrXSX` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364643 | `functions/api/portal-config.js` | P1 | Move the supplier frontend onto environment-aware config — The new endpoint isolates only `purchase-portal.html`; `supplier-quote.html:121-122` still embeds the same production project URL and anon key. A staging preview's `portal-supplier-invite` generates a same-origin preview link, but opening that link makes the supplier page query production directly, so supplier-flow E2E still touches production and staging tokens fail or can collide with production data. Route every System-3 frontend, including the supplier page, through the environment-aware configuration. | `SUPPLIER-ENV` | outdated | open |
| 58 | `PRRT_kwDOSUSj886UrXSb` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364648 | `scripts/env-guard.mjs` | P2 | Parse guard URLs canonically before comparing hosts — For the documented E2E `--url` mode, `https://user@mwbjoysuybgbrvfrprex.supabase.co` is a valid URL whose host is the production project, but `refOf` fails the anchored regex, compares the entire lowercased string to `PROD_REF`, and exits successfully with `--confirm STAGING`. This makes the production guard bypassable by URL syntax; use `new URL(s).hostname` and require an exact canonical Supabase project hostname. | `CFG-ENV (env-guard)` | outdated | fixed @8cd7890 |
| 59 | `PRRT_kwDOSUSj886UrXSg` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364656 | `db/portal-migrations/062-request-documents.sql`:343 | P1 | Exclude unsubmitted drafts from committed budget — After 062 introduces persistent drafts, `portal_budget_committed` still includes every active `direct_expense` except cancelled/rejected/returned requests, and this submission check explicitly observes the current draft in the committed total. A user with `can_create` can therefore create abandoned drafts that reserve budget indefinitely—even without uploading or submitting—and eventually prevent legitimate expenses when `budget_enforce=1`; exclude unsubmitted drafts from commitment or provide an expiry/cancellation mechanism with an intentional reservation model. | `CDX3-BUDGET-DRAFT` | open-on-GitHub | fixed @8cd7890 |
| 60 | `PRRT_kwDOSUSj886UrXSj` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364659 | `functions/api/portal-doc.js`:34 | P2 | Authorize reqdoc uploads before writing to R2 — For `kind=reqdoc`, the upload endpoint only checks the caller's global `can_create` or `can_edit` permission; it never verifies that `request_id` exists, is a draft/returned request, is visible to the caller, or belongs to the requester. Any ordinary creator can consequently write unlimited 10 MB orphan objects under arbitrary request IDs even though the later attachment RPC rejects them, consuming storage without an associated record or cleanup path. | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 61 | `PRRT_kwDOSUSj886UrXSn` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364663 | `db/portal-migrations/062-request-documents.sql`:244 | P2 | Make removed document objects inaccessible — `portal_remove_document` deletes only the database row and does not remove or tombstone the corresponding R2 object. The GET endpoint authorizes a key solely from the request ID embedded in its path and never checks for an active `portal_request_documents` row, so anyone retaining the returned key can continue previewing a supposedly removed sensitive upload, and the object persists indefinitely in storage. | `DOC-API (reqdoc GET row-exists)` | open-on-GitHub | fixed @b43ae88 |
| 62 | `PRRT_kwDOSUSj886UrXSs` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364671 | `db/portal-migrations/062-request-documents.sql` | P1 | Enforce the payment-document setting — Migration 062 adds `payment_docs_required` as a configurable completeness control, but it does not redefine any payment creation, approval, or disbursement transition to read this value or require a payment-linked document. Setting it to `1` therefore has no effect, allowing procurement-linked payments to proceed without evidence while operators reasonably believe the new control is enabled; either implement the server-side transition check or omit the nonfunctional setting until the enforcing migration exists. | `PAY-DOCS-COMPLETE / S8-PAY-EVID` | outdated | open |
| 63 | `PRRT_kwDOSUSj886UrXSw` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364678 | `db/portal-migrations/062-request-documents.sql`:267 | P2 | Prevent replacement forks from inactive versions — `portal_replace_document` does not require `v_old.active=true`, so after replacing a document once the caller can invoke the RPC repeatedly on the same inactive original. Each call inserts another active row with the same `version` and `supersedes_id`, producing forked histories with multiple active successor documents instead of the claimed linear version chain; lock and reject inactive source versions or enforce uniqueness for each superseded version. | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 64 | `PRRT_kwDOSUSj886UrXSz` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364681 | `scripts/env-guard.mjs`:51 | P2 | Couple the environment guard to the protected command — The guard only validates arguments and exits; the staging plan then runs the migration or E2E command separately, and no workflow or wrapper binds that command to the checked target. A changed variable, copied command, or different CLI target after the successful check can therefore apply 062 to production despite the displayed guard success. Execute the protected operation from the guard/wrapper using the already validated canonical target, rather than treating a prior independent process as an enforcement boundary. | `S1-GUARD-COUPLE` | open-on-GitHub | open |
| 65 | `PRRT_kwDOSUSj886UrXS4` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364688 | `functions/api/portal-config.js`:67 | P1 | Reject privileged keys from the public config response — The endpoint trusts the variable name and returns whatever value is stored in `PORTAL_SUPABASE_ANON_KEY` without checking its JWT `role`/project claims or rejecting Supabase secret-key formats. If an operator accidentally pastes the service-role or another privileged key into this similarly named setting, `/api/portal-config` publishes it to every unauthenticated visitor despite the stated guarantee that service-role material is never returned; validate that the key is explicitly an anon/publishable key for the configured project before responding. | `CFG-ENV` | open-on-GitHub | fixed @8cd7890 |
| 66 | `PRRT_kwDOSUSj886UrXS_` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364697 | `functions/api/portal-config.js`:94 | P2 | Include required server bindings in boot readiness — The configuration endpoint returns `ok:true` after checking only the browser URL and anon key, even when `PORTAL_SUPABASE_SERVICE_ROLE_KEY` or `QUOTES_BUCKET` is absent. In that environment the portal boots and permits login, but the new mandatory supporting-document endpoint immediately returns 503, making it impossible to complete the draft-to-submit flow; include boolean readiness checks for the required server-side bindings and fail boot without exposing their values. | `CFG-ENV` | open-on-GitHub | fixed @8cd7890 |
| 67 | `PRRT_kwDOSUSj886UrXTD` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672364701 | `db/portal-migrations/062-request-documents.sql` | P2 | Use one authoritative document size limit — Migration 062 advertises `doc_max_bytes` and defaults it to 15 MiB, while the only upload endpoint calls `_file-guard.js`, whose hardcoded maximum is 10 MiB and never reads this setting. Files between 10 and 15 MiB are therefore rejected despite being permitted by the database configuration, and lowering the database value lets the endpoint store a now-disallowed object before attachment fails; enforce the same environment/database-derived limit before writing R2 and when inserting metadata. | `DOC-SIZE-LIMIT` | outdated | fixed (doc_max_bytes=10MiB) |
| 68 | `PRRT_kwDOSUSj886Ur6gD` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578481 | `db/portal-migrations/062-request-documents.sql`:384 | P1 | Wire mandatory documents into direct-expense submission — When `expense_docs_required` uses its new default of `1`, this path returns a draft with `needs_documents` rather than submitting it. In the System-3 direct-disbursement flow, `submitExpense()` still calls `portal_create_expense`, clears the form, reports success, and sends `submitted`/`pending` notifications (`purchase-portal.html:5655-5661`); a repo-wide search found no UI calls to `portal_attach_document`, `portal_submit_expense`, or `kind=reqdoc`. Consequently every direct-expense request is stranded as a draft while the UI maps `draft` to `in_review` and offers neither upload nor submit actions. Add the draft → classified multi-upload → review → submit flow, and notify approvers only after `portal_submit_expense` succeeds. | `DOC-UI (draft→submit)` | open-on-GitHub | fixed @b3d949f/ca5c7ba |
| 69 | `PRRT_kwDOSUSj886Ur6gG` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578487 | `db/portal-migrations/062-request-documents.sql`:129 | P1 | Collect the required reason for manual bank details — For a bank payment entered without selecting a master beneficiary, the current form collects only the IBAN and account name and sends no `iban_manual_reason` (`purchase-portal.html:5600-5657`). This new guard therefore rejects every submission through the still-visible “إدخال يدوي” route. Add a required, clearly explained reason field when manual entry is selected and include it in `p_details`, or remove manual entry until that UX is available. | `OWN-IBAN-MANUAL` | open-on-GitHub | accepted-risk; reason UI/RPC implemented @b3d949f |
| 70 | `PRRT_kwDOSUSj886Ur6gL` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578493 | `functions/api/portal-doc.js`:33 | P2 | Authorize reqdoc uploads against the target draft — Any active staff member with `can_create` or `can_edit` can POST a `reqdoc` for any syntactically valid request ID; the POST path never verifies that the request exists, is visible to the caller, is a draft/returned request, or is owned by the caller before writing to R2. The stricter `portal_attach_document` RPC runs only after the object is stored, so stale-state, permission, or network failures leave untracked files and retries accumulate duplicates; a caller can also deliberately upload under arbitrary IDs. Validate the target request before `QUOTES_BUCKET.put` and make upload-and-attach atomic or provide guaranteed cleanup. | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 71 | `PRRT_kwDOSUSj886Ur6gO` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578500 | `purchase-portal.html`:2571 | P2 | Gate the portal behind an accessible bootstrap state — When `/api/portal-config` is slow or stalls, this unbounded fetch leaves the pre-existing portal chrome visible and interactive even though `SB` is still `null`; no loading shell, timeout, retry, offline state, or input blocking is installed before awaiting it. On failure, the code replaces the body with operator-facing configuration text that has no `role="alert"`, focus target, recovery action, or support route. Start with an `aria-busy` blocking shell, add timeout/retry and user-facing offline messaging, then move focus to an announced fatal-state heading if configuration ultimately fails. | `BOOT-STATES / S10-STATES` | open-on-GitHub | open |
| 72 | `PRRT_kwDOSUSj886Ur6gU` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578508 | `db/portal-migrations/062-request-documents.sql`:268 | P1 | Restrict document replacement to returned requests — `portal_replace_document` is documented as the correction path for a returned request, but it checks only the caller's identity and never checks `v_req.status`. A requester, editor, or administrator can therefore invoke it while evidence is under approval, during payment, or after closure; the function deactivates the document used for the decision and installs a new active version. Enforce the intended `returned` state, or another explicitly bounded correction state, before changing the active dossier. | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 73 | `PRRT_kwDOSUSj886Ur6gY` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578515 | `db/portal-migrations/062-request-documents.sql`:178 | P1 | Validate payment ownership and role before attaching documents — When `p_payment_id` is non-null, this conditional skips both the requester/editor authorization and the request-state restriction. Any authenticated user who can merely view the supplied request can attach a document to a payment, and the function never verifies that the payment belongs to `p_request_id`, so unrelated payment evidence can be inserted into the request dossier. Resolve the payment under a row lock, require its `request_id` to match, and enforce the appropriate finance/disbursement role and payment state. | `CDX3-ATTACH-PAY` | open-on-GitHub | fixed @8cd7890 |
| 74 | `PRRT_kwDOSUSj886Ur6gh` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578525 | `db/portal-migrations/062-request-documents.sql` | P2 | Make document replacement resilient to duplicate requests — A double-click, network retry, or stale tab can call `portal_replace_document` twice with the same old document ID. This update has no `active` compare-and-set condition and the subsequent insert has no uniqueness or idempotency guard, so both calls can create active version-2 rows superseding the same document. Atomically claim only an active source row with `UPDATE ... WHERE id = p_doc_id AND active RETURNING`, and reject or return the existing replacement when that claim fails. | `CDX3-REPLACE` | outdated | fixed @8cd7890 |
| 75 | `PRRT_kwDOSUSj886Ur6gk` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578529 | `db/portal-migrations/062-request-documents.sql` | P1 | Revalidate documents when returned expenses are resubmitted — This function permits every document to be removed while a direct-expense request is `returned`, but migration 062 does not update the existing `portal_resubmit_request` RPC, which moves returned requests back to `in_review` without checking `expense_docs_required` or counting active documents. A requester can therefore remove all supporting evidence and resubmit through that existing RPC, bypassing the new mandatory-document invariant. Add the same active-document check to resubmission, or route returned direct expenses through a single document-aware submit RPC. | `DOC-RESUBMIT-GATE (062)` | outdated | implemented (062, not applied) |
| 76 | `PRRT_kwDOSUSj886Ur6go` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578535 | `db/portal-migrations/062-request-documents.sql` | P1 | Require a verified uploaded object before attaching metadata — `portal_attach_document` accepts any non-empty `p_storage_key`, and `portal_submit_expense` later treats the resulting active row as sufficient evidence without verifying that the object exists or belongs to `docs/reqdoc/<request_id>/`. A requester can therefore call the authenticated RPC with a fabricated key, satisfy the mandatory-document count, and submit a dossier whose preview fails for every approver. Perform attachment through the server endpoint after verifying the freshly uploaded R2 object, and bind the key, request ID, MIME type, size, and caller in one operation. | `DOC-RECEIPT` | outdated | open (P0) |
| 77 | `PRRT_kwDOSUSj886Ur6gt` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578541 | `db/portal-migrations/061-codex-round2-hardening.sql`:215 | P1 | Apply the document requirement to recurring expenses — After migration 062 enables `expense_docs_required` by default, `portal_recurring_run` still bypasses the new draft/upload/submit path and directly moves each generated direct expense to `in_review` without creating any `portal_request_documents` row. Recurring-expense approvers therefore receive requests that violate the evidence requirement enforced for manually created expenses. Either version approved standing documents on the recurring template and copy/link them into each occurrence, or leave each generated occurrence as a document-required draft/task until evidence is attached. | `CDX3-RECURRING-GATE` | open-on-GitHub | fixed @8cd7890 |
| 78 | `PRRT_kwDOSUSj886Ur6gx` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578548 | `db/portal-migrations/061-codex-round2-hardening.sql`:190 | P1 | Surface skipped recurring obligations to finance — When a recurring expense exceeds budget, the job writes an audit event with `request_id = NULL`, advances `next_run`, and creates no payable occurrence. The portal's audit screen is built only from events grouped under loaded requests (`purchase-portal.html:1867-1874,2791-2819`), so this event is not visible there, while the recurring panel simply shows the later next date; finance can miss an obligation with no exception queue or recovery action. Persist a skipped occurrence linked to the template and expose it as a blocked task with reason, amount, due date, owner, and retry/override workflow before advancing the schedule. | `RECUR-BLOCKED / S8-RECUR-BLOCK` | open-on-GitHub | open |
| 79 | `PRRT_kwDOSUSj886Ur6g2` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672578555 | `db/portal-migrations/062-request-documents.sql`:116 | P2 | Lock the direct-expense department to the user's profile — The backend now rejects any non-admin `p_department_id` that differs from the user's profile, but the direct-expense form still renders every department as an enabled required selector (`purchase-portal.html:5587-5590,5623`). A requester can complete the form using an apparently valid department and discover only after submission that it was forbidden; inactive departments are also included. For non-admins, preselect and lock the active profile department with explanatory text; for admins, filter inactive departments and retain the explicit selector. | `AUTHZ-EXPENSE-DEPT (060)` | open-on-GitHub | implemented @135f5af |
| 80 | `PRRT_kwDOSUSj886UsLtr` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684565 | `db/portal-migrations/062-request-documents.sql`:384 | P1 | Complete document upload before reporting expense submission — When `expense_docs_required` has its new default value of 1, this branch returns a draft with `needs_documents:true` instead of submitting it. The existing `submitExpense()` in `purchase-portal.html` ignores that flag, clears the form, displays “أُنشئ طلب الصرف”, emits submitted/pending notifications, and opens the detail screen; moreover, a repo-wide search finds no frontend call to `portal_attach_document` or `portal_submit_expense`. Consequently, every browser-created direct expense is left in `draft` with no UI path to attach the required evidence or enter approval, while the requester and notification recipients are told it was submitted. | `DOC-UI (draft→submit)` | open-on-GitHub | fixed @b3d949f |
| 81 | `PRRT_kwDOSUSj886UsLtv` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684571 | `db/portal-migrations/062-request-documents.sql`:129 | P1 | Collect the required reason for manually entered IBANs — For a bank expense entered without selecting a master beneficiary, this new check always rejects the current direct-expense form: `submitExpense()` sends only `iban` and `account_name`, does not collect `iban_manual_reason`, and the compatibility wrapper passes the absent value into `portal_create_expense_draft`. Thus the UI's explicitly supported “إدخال يدوي” path now fails with the mandatory-reason exception before even creating the draft; add the reason field and pass it through, or remove manual entry from the form. | `OWN-IBAN-MANUAL` | open-on-GitHub | accepted-risk; reason UI/RPC implemented @b3d949f |
| 82 | `PRRT_kwDOSUSj886UsLty` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684577 | `db/portal-migrations/062-request-documents.sql` | P1 | Authorize and validate payment document attachments — When an authenticated caller supplies any non-null `p_payment_id`, this condition skips the entire requester/editor/admin authorization and draft-state block. The security-definer RPC also never verifies that the payment belongs to `p_request_id`, so any user who can merely view one request can attach purported payment evidence to it under an unrelated, guessable payment ID, bypassing the intended disbursement-role workflow and corrupting the financial dossier. Validate the payment/request relationship and require the appropriate payment-stage permission before insertion. | `CDX3-ATTACH-PAY` | outdated | fixed @8cd7890 |
| 83 | `PRRT_kwDOSUSj886UsLt4` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684585 | `db/portal-migrations/062-request-documents.sql`:268 | P1 | Restrict document replacement to returned requests — The replacement RPC checks the caller but never checks `v_req.status`, despite being documented as the returned-request workflow. A requester, editor, or administrator can therefore invoke it after approval or closure, deactivate the evidence reviewed by approvers, and make a new file the active version in the dossier. Preserve the approved evidence set by requiring the request to be `returned` before the update/insert, as the surrounding workflow specifies. | `CDX3-REPLACE` | open-on-GitHub | fixed @8cd7890 |
| 84 | `PRRT_kwDOSUSj886UsLt7` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684588 | `db/portal-migrations/062-request-documents.sql`:331 | P1 | Apply the document gate to recurring direct expenses — Enabling `expense_docs_required` here does not cover the scheduled route: the still-active `portal_recurring_run()` in `061-codex-round2-hardening.sql` directly builds the approval chain and changes generated `direct_expense` rows to `in_review` without creating or checking any `portal_request_documents`. Whenever the scheduler processes a due template, it therefore bypasses the new mandatory-evidence invariant that manual expenses must satisfy. The recurring path must create a draft awaiting evidence or use an explicitly governed recurring-document policy before entering approval. | `CDX3-RECURRING-GATE` | open-on-GitHub | fixed @8cd7890 |
| 85 | `PRRT_kwDOSUSj886UsLuA` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684609 | `purchase-portal.html`:2582 | P2 | Allow recovery from transient configuration fetch failures — Any one-off network error, malformed gateway response, or temporary 5xx from `/api/portal-config` reaches this catch block and permanently replaces the application with a message that diagnoses the failure as an environment misconfiguration. There is no retry action or automatic retry, so the portal remains unusable after the service recovers until the user manually reloads the page. Preserve a recoverable shell and offer retry while reserving the administrator-oriented configuration guidance for confirmed configuration responses. | `BOOT-STATES / S10-STATES` | open-on-GitHub | open |
| 86 | `PRRT_kwDOSUSj886UsLuP` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684621 | `db/portal-migrations/062-request-documents.sql`:204 | P1 | Verify the stored object before satisfying the evidence gate — The attachment RPC accepts any nonempty caller-supplied `p_storage_key` and trusts the supplied MIME type, size, and checksum without proving that an R2 object exists or that its key belongs to this request. A requester can therefore call `portal_attach_document` with a fabricated key such as `docs/missing.pdf` and a supported MIME type, after which `portal_submit_expense` counts the row and admits the expense into approval even though no evidence can be opened; the added SQL test itself demonstrates this by treating nonexistent `docs/inv1.pdf` as valid. Bind attachment creation to the authenticated upload endpoint and validate the server-derived object metadata before counting the document. | `DOC-RECEIPT` | open-on-GitHub | open (P0) |
| 87 | `PRRT_kwDOSUSj886UsLuS` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684625 | `functions/api/portal-doc.js`:33 | P2 | Bind request-document uploads to an authorized draft — Adding `can_create` as sufficient permission for `reqdoc` exposes the R2 upload to every ordinary requester, but the endpoint only checks that `request_id` matches a regex; it never verifies that the request exists, is a draft, is visible to the caller, or belongs to that caller before persisting the object. Such a user can repeatedly upload 10 MB objects under arbitrary request IDs and omit the later RPC, leaving unlimited orphaned objects with no cleanup path. Verify ownership and draft state before `QUOTES_BUCKET.put`, and rate-limit or otherwise reclaim uploads that are not attached. | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 88 | `PRRT_kwDOSUSj886UsLuZ` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684636 | `db/portal-migrations/062-request-documents.sql` | P1 | Enforce the evidence gate when resubmitting returned expenses — This submission RPC accepts only `draft`, even though the same migration explicitly permits document attachment and replacement while a request is `returned`. The only existing route for a returned direct expense is therefore the older `portal_resubmit_request`, which resets approvals and moves it directly to `in_review` without checking `expense_docs_required`; after return, the requester can remove every document and invoke that RPC to re-enter approval with no evidence. Extend the gated submission path to returned direct expenses or add the same active-document validation to the generic resubmission RPC. | `DOC-RESUBMIT-GATE (062)` | outdated | implemented (062, not applied) |
| 89 | `PRRT_kwDOSUSj886UsLug` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3672684649 | `db/portal-migrations/062-request-documents.sql`:116 | P2 | Align the department picker with server-enforced ownership — For a non-admin requester, the backend now requires any supplied department to equal the department in the user's profile. The existing direct-expense form still requires a manual choice and populates the selector with every department, including inactive ones, so selecting any visible department other than the user's own produces a server error even though the UI presents it as valid. Default and lock the field to the profile department for non-admins, and filter inactive departments for administrators. | `AUTHZ-EXPENSE-DEPT (060) / DOC-UI dept-lock` | open-on-GitHub | implemented @135f5af/b3d949f |
| 90 | `PRRT_kwDOSUSj886UwyzH` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478925 | `db/portal-migrations/062-request-documents.sql` | P1 | Restore the disbursement phase when resubmitting — When `portal_payment_transition` returns a direct-expense payment, it leaves the request in phase `payment`; this returned-request path rebuilds a `disbursement` approval chain but updates only the status and sequence. The request therefore remains in phase `payment`, while `portal_pr_transition` and `portal_run_sla` infer the active cycle from the phase and can operate on the `need` cycle instead of the newly built disbursement chain. Set `phase = 'disbursement'` atomically when this function resubmits the request. | `CDX4-PHASE-CYCLE` | outdated | fixed @b43ae88 |
| 91 | `PRRT_kwDOSUSj886UwyzR` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478940 | `purchase-portal.html`:5876 | P1 | Let requesters correct returned expense details — When a direct expense is returned for an incorrect beneficiary, amount, method, IBAN, or purpose, the returned branch renders only the existing header and document center before offering resubmission. There is no direct-expense update form or corresponding update RPC, so the requester can replace evidence but must resend the unchanged financial data that caused the return. Add a controlled correction operation for the core expense fields before enabling resubmission. | `RET-EXPENSE-EDIT / S8-EXPENSE-EDIT` | open-on-GitHub | open |
| 92 | `PRRT_kwDOSUSj886UwyzY` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478950 | `db/portal-migrations/062-request-documents.sql` | P1 | Separate edit permission from submission authority — For any visible draft or returned direct expense, a procurement user with `can_edit` can call `portal_submit_expense` and start a new financial approval cycle even when they are not the requester. Because procurement jobs have broad visibility, this turns a content-edit privilege into authority to submit another employee's financial request without requester consent or an explicit delegation. Restrict submission to the owner/admin or introduce a distinct audited submit-on-behalf permission. | `CDX4-SUBMIT-AUTHZ` | outdated | fixed @b43ae88 |
| 93 | `PRRT_kwDOSUSj886Uwyzh` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478959 | `functions/api/portal-doc.js`:33 | P2 | Authorize reqdoc uploads against the target owner — A user with `can_create` and sector/all visibility can upload `reqdoc` objects for another request in `draft` or `returned` state because the pre-upload check validates only generic permission, visibility, and status. The subsequent `portal_attach_document` RPC rejects non-requesters without `can_edit`, but the R2 object has already been written, allowing repeated orphaned uploads and storage consumption. Apply the requester/`can_edit`/admin target authorization before `QUOTES_BUCKET.put`. | `DOC-API (reqdoc authz)` | open-on-GitHub | fixed @8cd7890/b43ae88 |
| 94 | `PRRT_kwDOSUSj886Uwyzp` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478967 | `db/portal-migrations/062-request-documents.sql`:329 | P1 | Verify uploaded objects before accepting document evidence — An authenticated requester can call `portal_attach_document` directly with any fabricated key matching `docs/reqdoc/<request_id>/...`; no R2 object, trusted upload receipt, size, or checksum is verified. `portal_submit_expense` then counts that database row as an active supporting document, so the mandatory-evidence gate can be bypassed without uploading a file. Create document rows only from the server upload endpoint after a successful R2 write, or require a server-verifiable upload token. | `DOC-RECEIPT` | open-on-GitHub | open (P0) |
| 95 | `PRRT_kwDOSUSj886Uwyzw` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478977 | `db/portal-migrations/062-request-documents.sql` | P1 | Revalidate master beneficiary data at submission — The draft copies the selected beneficiary's active state, name, account, and IBAN only when it is created, but submission may occur much later and performs no beneficiary lookup. If the beneficiary is disabled or its controlled IBAN changes while the request is a draft or returned, this path submits and ultimately pays using the stale snapshot, bypassing the master-data change control that recurring expenses already refresh. Lock and revalidate the linked beneficiary at submission, refreshing or rejecting changed bank data. | `CDX4-BENEF-REVAL` | outdated | fixed @b43ae88 |
| 96 | `PRRT_kwDOSUSj886Uwyz2` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478983 | `db/portal-migrations/062-request-documents.sql`:284 | P1 | Prevent requesters from replacing payment evidence — If a direct-expense payment has a document row and finance returns the request, the requester or any `can_edit` user can invoke `portal_replace_document` with that payment document's ID. The function checks only the parent request's `returned` status and then preserves `v_old.payment_id` in the replacement, allowing a requester to deactivate and replace evidence belonging to the financial payment record. Reject rows with a non-null `payment_id` here and provide a separately authorized, state-bounded finance correction path. | `CDX4-PAY-EVID` | open-on-GitHub | fixed @b43ae88 |
| 97 | `PRRT_kwDOSUSj886Uwyz8` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478991 | `db/portal-migrations/062-request-documents.sql` | P1 | Keep finance-view permission read-only — The seeded `fin_officer` role has only `can_see_finance` and is explicitly described as read-only, yet this branch treats that permission as authority to append documents to any visible payment. There is also no payment-state check, so the role can alter the dossier of approved, disbursed, voided, or closed payments. Require a dedicated document-management or disbursement permission and bound additions to the appropriate mutable payment state. | `SEC-FINANCE-READONLY` | outdated | open (Stage-2) |
| 98 | `PRRT_kwDOSUSj886Uwyz9` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674478994 | `purchase-portal.html` | P2 | Honor the document-enforcement rollback setting — When operators set `expense_docs_required=0` as the migration's documented fail-safe rollback, the database permits submission without evidence, but the new browser path still refuses whenever `nActive < 1` and renders the submit button disabled. Because the UI now always creates a draft instead of using the compatibility RPC that auto-submits when enforcement is off, disabling the setting cannot restore direct-expense processing. Read the setting in both the button state and submit handler. | `DOC-ROLLBACK-FLAG` | outdated | fixed (pa_expDocsRequired honors 0) |
| 99 | `PRRT_kwDOSUSj886Uwy0E` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674479000 | `db/portal-migrations/062-request-documents.sql` | P2 | Charge cross-year drafts to one consistent fiscal year — For a draft created before year-end and submitted afterward, this function checks the current year's budget because `v_year` comes from `now()`, but `portal_budget_committed` later assigns the active request according to `portal_requests.created_at`. The request therefore passes a current-year budget check and then disappears from that year's committed total, allowing subsequent requests to consume the same capacity while the expense is counted against the prior year. Use a persisted fiscal year or apply one consistent business date for both submission and committed-budget calculations. | `FISCAL-POLICY` | outdated | open |
| 100 | `PRRT_kwDOSUSj886Uwy0N` | https://github.com/511avolo-dot/MR/pull/74#discussion_r3674479010 | `purchase-portal.html` | P1 | Send only the cycle-aware approval notification — Every direct-expense submission launches both `request/submitted` and `disbursement/pending` notifications. The former calls `notifyPending`, loads the disbursement approval row without its cycle, and creates an approval token with the default `need` cycle; the latter then sends the correct disbursement token. With email enabled, approvers receive duplicate messages and the first action link fails with `no_pending` because no need-cycle stage exists. Notify the requester separately and send approvers only through the disbursement-aware channel. | `CDX4-NOTIFY-DUP` | outdated | fixed @b43ae88 |
| 101 | `PRRT_kwDOSUSj886Uwy0V` |  | `db/portal-migrations/062-request-documents.sql`:353 | P1 | Invalidate old approval tokens during resubmission — When a disbursement stage is returned through the portal, its previously emailed token can remain unused. This resubmission deletes and rebuilds the approval chain at the same cycle and sequence but does not invalidate portal_email_tokens, so the old link still matches the new pending stage and can approve the corrected request without reviewing the new evidence. | `CDX4-TOKENS` | open-on-GitHub | fixed @b43ae88; disbursement-cycle parity → Stage-2 |
| 102 | `PRRT_kwDOSUSj886Uwy0d` |  | `db/portal-migrations/062-request-documents.sql` | P2 | Make draft document removal concurrency-safe — Two clients can read the same document before either obtains the parent request lock; after the first call deletes it, the second call proceeds and this unqualified DELETE affects zero rows, yet the function still writes another successful document_removed audit event and returns ok. Use DELETE ... RETURNING or lock the document row. | `CDX3-REPLACE (remove path)` | outdated | fixed @8cd7890/b43ae88 |
