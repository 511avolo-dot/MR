# م3 — التصليب الأمنيّ التشغيليّ: تنفيذ ما يمكن + دليل المالك لما لا يُنفَّذ إلا بيده (2026-08-07)

> فوّضني المالك بـم3. هذا يوثّق **ما نفّذتُه فعلاً** (فحوص قراءة-فقط على التجريبية `vpfnycxzqziltsnzxbpb`) و**ما هو
> بيد المالك حصراً** (توگلات لوحة التحكّم/تدوير مفاتيح/إقرار) بدليل تشغيليّ مضغوط. **لا أختلق قدرة، ولا أُنفّذ حذفاً
> مدمِّراً بلا إقرار.** الإنتاج `mwbjoysuybgbrvfrprex` لم يُمَسّ؛ PR مسودّة.

## أ. مدقّق الأمان الحيّ (التجريبية) — قراءة فقط ✅
`get_advisors(security)` = **0 ERROR · 89 WARN · 7 INFO**. كلها **مقصودة/حميدة** مطابقةً للـdisposition الموثّق:
| البند | العدد | الحكم |
|---|---|---|
| `authenticated_security_definer_function_executable` | 86 | ✅ مقصود — تصميم البوابة: مستخدم مسجَّل يستدعي RPC (DEFINER) محروسة داخلياً برفض افتراضي + فصل مهام. |
| `anon_security_definer_function_executable` | 2 | ✅ مقصود — `portal_supplier_rfq` / `portal_supplier_submit`: واجهة المورّد الخارجيّ بلا حساب، يُصادَق برمز داخل الدالة. |
| `rls_enabled_no_policy` | 7 | ✅ مقصود — 2 مساعِد تجريبيّ (`_staging_*`) + 5 جداول خادميّة (`email_tokens`/`idempotency`/`invitations`/`outbox`/`supplier_tokens`): RLS مفعّل + بلا سياسة = رفض-الكل للعميل (نمط خادم-فقط). |
| `auth_leaked_password_protection` | 1 | ⚠️ **الوحيد القابل للإجراء** — مُطفأ. توگل لوحة Supabase Auth، **بلا أداة MCP** ⇒ إجراء المالك (م3.1). |

**الخلاصة:** لا ملاحظة أمنية قابلة للإجراء عبر الكود/MCP سوى حماية كلمات السرّ المسرّبة (توگل لوحة تحكّم).

## ب. تصنيف مخلّفات QA/R2 (العائق #2) — قراءة فقط ✅
تعداد التجريبية: **20 طلب · 23 مستخدم · 5 عروض · 4 دفعات · 1 مورد · 1 مستفيد.**
- **الطلبات (20/20 تجريبية):** المُقدِّمون `qa_*`/`stg_*`/`e2e_*`/`qa_p0d_*`؛ المشاريع «QA Project Alpha / QA End-to-End /
  QA DoA Boundary / QA XSS / صرف مباشر»؛ المبالغ قيم حدود اختبار (25000 · 25001 · 125000 · 125001 · 150000 · 100 · 111…).
- **المستخدمون (23/23 تجريبية):** 19 ببادئات `qa_/stg_/e2e_/demo_`؛ و4 «مشكوك» تبيّن أنهم اختبار أيضاً
  (`p0c_req` = جولة مراجعة P0C · `plp_admin/plp_other/plp_req` بأسماء عرض عامّة «مستخدم آخر»/«طالب عادي»).
- **النتيجة الحاسمة:** **صفر بيانات إنتاج حقيقية تسرّبت إلى التجريبية المشتركة.** كل المحتوى صناعيّ.
- **الأثر:** جوهر العائق #2 (تصنيف حقيقي-مقابل-تجريبي) **محسوم = الكل صناعيّ**. التجريبية بيئة تُرمى، فالتطهير
  **اختياريّ لا شرط إطلاق** (الإنتاج منفصل ونظيف). يبقى **إقرارك** الرسميّ على هذا التصنيف لإغلاق العائق.

## ج. دليل المالك التشغيليّ — بنود لا تُنفَّذ إلا بيدك (بلا أسرار هنا)
| # | البند | لماذا بيدك حصراً | الخطوة (تقريبيّة) | التحقّق |
|---|---|---|---|---|
| م3.1 | حماية كلمات السرّ المسرّبة | توگل Supabase Auth — لا أداة MCP | Supabase → Authentication → Policies → فعّل "Leaked password protection" | إعادة `get_advisors` ⇒ يختفي `auth_leaked_password_protection` |
| م3.2 | MFA/SSO لحسابات الإدارة/المالية | قرار سياسة + توگل لوحة | Supabase → Auth → MFA؛ فعّل TOTP وألزِم أدوار admin/finance | تسجيل دخول admin يطلب OTP |
| م3.3 | تدوير `service_role`/anon (نظام1/2 + مشروع الزميل) | مفاتيح سرّية — قيد مُلزِم: عمل المالك، ولا أتعامل مع الأسرار | Supabase → Settings → API → Reset؛ حدّث أسرار Cloudflare؛ Redeploy | نداءات الخادم تعمل بالمفتاح الجديد |
| م3.4 | حذف الحسابات الوهمية (قبل الإطلاق) | على **الإنتاج** (لا أصل له عبر الاتصال الحالي) — والتجريبية نحتاج حساباتها لـE2E | عند إعداد الإنتاج: احذف أي حساب اختبار قبل فتح النظام | 0 حساب اختبار على الإنتاج |
| م3.5 | تطهير مخلّفات التجريبية | **حذف مدمِّر** — يتطلّب إقرارك (وقد صُنِّف الكل صناعيّاً أعلاه) | أقِرّ «الكل صناعيّ»؛ إن رغبت أُطهّرها على التجريبية بأمر صريح | — |
| م3.6 | نظام 1: بريد التسجيل + تصليب المخزن | Cloudflare + مشروع نظام1 (اتصالي الآن على التجريبية) | أعِد `SUPABASE_SERVICE_ROLE_KEY` + Redeploy (تمّ إصلاح البريد PR #75)؛ طبّق `db/system1-storage-hardening.sql` م1 | `curl /api/notify` = `{ok:true}` |
| Gate2 | سرّ `STAGING_E2E_USERS` | سرّ GitHub — إجراؤك | GitHub → Settings → Secrets → أضِف JSON بـ4–6 أدوار تجريبية | أُشغّل الـworkflow وأثبّت النتيجة |

## د. ما بقي منّي (فور إقرارك/إجراءاتك)
- **إقرار م3.5** ⇒ (اختياريّ) أُطهّر التجريبية بأمر صريح.
- **سرّ E2E** ⇒ أُشغّل `authenticated-multirole-e2e` وأثبّت (Gate 2).
- **م5 (النقل):** أُجهّز سكربت دلتا الهجرات المرتّب وأتحقّق بعد تطبيقك.
