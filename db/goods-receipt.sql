-- ════════════════════════════════════════════════════════════════════════
--  وحدة استلام البضاعة (Goods Receipt) + الإغلاق — مجموعة الذيابي
-- ════════════════════════════════════════════════════════════════════════
--  تكمل دورة حياة أمر الشراء: تسجيل الكميات المستلمة فعلياً لكل بند، وإغلاق
--  الأمر تلقائياً عند اكتمال الاستلام (تسليم كامل) أو تسجيل تسليم جزئي.
--
--  التصميم (خفيف، بلا جدول جديد): الكمية المستلمة تُخزَّن داخل بنود الأمر
--  (items jsonb: {desc,qty,unit,price,received_qty})، وسجل عمليات الاستلام في
--  عمود receipts jsonb على أمر الشراء: [{by, at, note, lines:[{desc, qty}]}].
--
--  يُنفَّذ مرة واحدة في: Supabase → SQL Editor (آمن لإعادة التشغيل).
-- ════════════════════════════════════════════════════════════════════════

ALTER TABLE proc_purchase_orders
  ADD COLUMN IF NOT EXISTS receipts jsonb;

-- لا حاجة لتغيير RLS: السياسة القائمة (auth_all للمصادَق عليهم) تغطّي العمود.
-- الإغلاق يبقى عبر حالة «تسليم كامل» (PO_TERMINAL) لكنه الآن مبنيّ على استلام فعلي.

-- تحقّق (اختياري):
-- SELECT po_number, status,
--   jsonb_array_length(COALESCE(receipts,'[]'::jsonb)) AS receipt_events
-- FROM proc_purchase_orders ORDER BY updated_at DESC LIMIT 10;
